# 【叶宇单片机】—第69节 sizeof()运算符—【2026-04-04】-中文

## 字幕正文

### **[00:00]**
今天来看一下我们既熟悉又陌生的size of 它主要有三个特点 一个陷阱 我们先来看第一个特点 举一个例子 定义一个变量A它占用几个存储单元 是不是一个存储单元 一个存储单元是一个字节 定义一个变量B它占用了两个存储单元 定义了一个指针变量 占用三个存储单元 又定义一个指针变量 还是占用三个存储单元 在五一单片机当中 所有的指针变量都是占用三个存储单元 为什么呢 因为指针变量存储的是地址 地址的宽度是固定的 那么它申请的存储单元的数量就是固定的 同样我们可以使用size of来计算它占用了几个存储单元 size of后面放一个变量名 也可以使用size of后面放一个类型

### **[01:01]**
同样可以计算出来 这就是它的第一个特点 size of可以计算变量或者是类型 它占用了几个存储单元 我们来看第二个特点就是size of里面放了一个表达式 那么它的结果是多少 我们先来看一下类型提升 小于int类型的 首先要提升为int类型 然后类型等级低的要提升为类型等级高的 举一个例子来看一下表达式的类型 这里有两个表达式 一个是A加上3.14赋值给A 一个是A加上100赋值给A 那么在计算的时候 我们通常分为两个步骤 第一步是计算

### **[02:00]**
第二步是赋值 下面也是一样的 我们先来看第一步 计算A加上3.14 结果是8.14 A是啊sa char类型 3.14是double类型 那么类型不一致 这个时候就会进行一个隐式的类型转换 会将asser char类型转换成double类型 那么结果也是double类型运算后的结果 有大小有类型 这就是第一步 第二步是赋值 这个时候也会要求类型一致 那么一个是on snchar类型 一个是double类型 这个时候会将double类型隐式的转换成assa char类型 那么整个表达式最终的类型是on sutchar类型 A的结果就是八 我们再来看第二个表达式 A加上100 那么结果是108 A是ALSACHARA类型 100是int类型

### **[03:01]**
同样这里的类型不一致 就会进行隐式的类型转换 会将asser char类型转换成int类型 那么结果也是int类型 经过计算 这个结果有大小有类型 就是第一步 这里的第二步也是赋值 同样要求类型要一致 但是一个是int类型 一个是on satchar类型 那么会将int类型转换成assa char类型 再进行赋值 那么整个表达式最终的类型是on sound char类型 现在对于表达式的每一步的类型我们都清楚了 那么size of里面放一个表达式 它是什么类型呢 结果又是多少 我们同样举一个例子 size of a赋值给BA是a sa char类型的 那么结果就是size of a sa char类型是1size of a加上3.14

### **[04:01]**
这里是一个表达式 A是on sattra类型的 3.14是double类型的 那么这个表达式的结果就是double类型的 所以说结果是size of double是四 在五一单片机当中 double占用四个字节的存储空间 size of5A加上3.14复制给A 这里又是一个表达式 我们分为两步来确定它的类型 第一步是A加上3.14 它的类型是double类型的 将这个double类型赋值给AA是on sa char类型 那么最终的结果就是on sa char类型 所以说结果是size of assignature类型是一注意 size of遇到表达式的时候 它只会确定这个表达式的类型 它不会执行这个代码 也就是不会对A进行赋值

### **[05:01]**
size of a加上100赋值给A 这里又是一个表达式 分成两步 第一步是A加上100 它是int类型的 将int类型赋值给A A是on sutchar类型 所以说最终的结果是on sutchar类型 也就是size of a sa char类型 结果是一 那么第一个B的值就是一 第二个是四 第三个是一 第四个是一 注意size of遇到表达式的时候 它只会确定这个表达式的类型 它不会执行这个代码 也就是不会对A进行赋值 那么A的值始终是五 接着我们看一下第三个特点 就是size of5 它的执行过程是在编译阶段完成的 并进行替换 就将size of a替换成一 size of a加上3.14 这个整体替换成四

### **[06:00]**
下面也会进行整体的替换 也就是编译阶段完成size of的替换 被替换之后 这个表达式就会被丢掉 不会被执行 也就是这个表达式不会生成任何的代码 最后是一个陷阱 我们来举一个例子 我们说看见数组名 它的类型就是元素类型的指针 但是遇到size of 它代表的就是整个数组 也就是求整个数组占用了多少个存储单元 那么这里的结果就是四 接着再看我们比较熟悉的一个程序 就是求四个数的平均值 我们看一下main里调用这个函数数组名的类型是元素类型的指针

### **[07:05]**
在函数的入口参数有一个数组 它会退化成一个元素类型的指针 也就是它是一个假数组真指针 那么编译之后就是这个样子的 是一个真的指针 如果在函数内部求size of buffer 这个时候求到的实际是一个指针变量 它占用了几个存储单元 注意这里就是一个陷阱 最后总结一下 第一个特点就是size of可以求变量和类型 占用了几个存储单元 第二个特点就是science of里面可以放一个表达式 只确定类型 不执行代码 也就是不会对A进行赋值 第三个特点就是编译阶段完成的这些操作并进行替换 也就是确定类型之后就会把它进行替换

### **[08:02]**
那么表达式就不会被执行 最后还要一个陷阱 在函数的入口参数定义一个数组 它实际上退化成一个指针变量 也就是它是一个假数组 真指针 如果在函数内部求size of8for实际上是一个size of 里面是一个假的数组名 它计算的是指针变量占用的字节数 注意这里就是那个陷阱 我们要注意一下好 这节课到此结束 拜拜

