# 【叶宇单片机】—73  结构体数据的传输与还原—【2026-04-20】-中文

## 字幕正文

### **[00:00]**
这是一个单片机 它要给别的单片机或者是别的芯片发送数据 一般都是八位八位发送的 那么这八位八位的数据 是不是放在一个数组当中比较方便 但是我们使用的变量一般都是五花八门的 比如我们使用一个结构体变量 在结构体内部 它是由不同类型的成员组成的 那怎么实现八位八位来发送数据呢 我们可以把结构体里面的数据 存到数组当中来发送 首先定义一个结构体类型 然后定义一个结构体变量 它的初值是50H60H 那么在存储器当中为它分配存储空间 这就是结构体的存储空间 它的初值是50H60H 注意这里数组的元素个数是通过size of5 后面加上一个结构体名计算得到的结果就是四

### **[01:04]**
那么为它分配四个存储单元 再定义一个指针变量 注意指针变量要进行初始化 再为它分配一个存储空间 再定义一个结构体指针变量 再为它分配一个存储空间 现在变量都定义好了 我们想把结构体当中的数据复制到数组当中 我们看怎么实现写一个函数 定义一个局部变量 再取结构体的地址赋值给指针变量 这个指针变量是什么类型的 是不是on3TCHAR类型的指针 但是取结构体地址 这个整体它是什么类型的 是不是结构体类型的指针 那么两个类型不一致 没办法直接复制 这个时候使用类型的强制转换 就可以直接复制了 那么PU8存储的是结构体的地址 就是0080H 那么PU8指向的就是0080这个地址

### **[02:02]**
注意他现在只是存储了一个地址 其他的什么都没有做 换句话说 PU8这个变量的值是不是0080 那么PU80就是0080H 如果对这个存储空间进行解引用 是不是就找到0080这个存储空间 按照on sa char类型来解读 PU81就是0081 为什么加一是0081呢 这与他结引用之后的类型有关 因为它截引用之后是阿三的char类型 只占用一个存储单元 所以说PU81下一个存储单元 就是0081 那么对这个存储空间进行解引用 也是按照俺三的char类型来解读 PU82就是0082 对这个存储空间进行解引用 PO83就是0083 可以对这个存储空间进行解引用 PU84就是0084 可以对这个存储空间进行解引用

### **[03:01]**
但是这属于越界了 它超过数组的使用范围了 所以说我们不这样使用 但是它使用是合法合规的 编译器是不会报错的 这就是指针的灵活性 它获取到一个地址 想怎么解读就可以怎么解读 接着我们看一下如何实现结构体的数据 复制到这个数组当中 一个for循环语句 I的初值是零 I小于四 然后I加加 将PO8找到的数据依次复制到buffer里面 这一句是不是有一点难理解 我们来慢慢的看一下 当I等于零的时候 那么就是PU80换一种写法 就是PU80进行解引用 当I等于一的时候 是不是PU81换一种写法 就是PU81进行解引用 当二等于二的时候 就是PU8I换一种写法 就是PU8加I进行解引用

### **[04:01]**
前面是汇编的写法 后面是C语言的斜画 他们俩的效果实际上是一样的 都是找到这个存储单元 可以对这个存储单元进行读和写 那么依次就找到这几个存储单元 然后将它们复制到数组当中 这就实现了结构体的数据存到数组当中 实际上还是依赖的是指针的灵活性 我们来看一下指针的灵活性 它想获取谁的地址 就获取谁的地址 想怎么解读就怎么解读 比如这个存储空间本来是一个结构体类型的 现在它一个一个按照阿SACHA类型来进行读写 我们可以把结构体里面的数据 存到数组当中来发送 那么接收方接收数据一般也是存在数组当中的 那如何把这个数组当中的数据 再按照结构体类型来解读呢 结构体的数据存储在数组当中

### **[05:02]**
数组当中如何按照结构体的方式来解读呢 我们之前定义了一个结构体指针 把数组的地址给这个结构体指针 那么数组名它本身就代表了数组的地址 数组名是一个什么类型 是不是元素类型的指针 这个我们之前都有讲过 一个是ASSIGNALCHARA类型指针 一个是结构体类型的指针 两个类型不一致 没办法直接复制 所以说这里要加上一个类型的强制转换 那么这个结构体指针存储的就是数组的地址 数组的地址是多少 是不是0084H 那么找到0084这个存储空间 对这个存储空间进行解引用 就是把这个存储空间 当做结构体的类型来解读了 那么结构体指针可以调用 第一个成员就找到这个存储空间 可以对它进行读和写

### **[06:01]**
结构体指针也可以调用 第二个成员就找到这个存储空间 对它进行读和写 同样这里体现了指针的灵活性 本来它是一个数组类型 现在按照结构体的类型来解读 我们来看一下两种不同的解读方式 一个是变量 一个是指针 首先这个存储器存储的是不是都是数字 只是我们给它不同的解读方式 第一种是固定解读方式 也就是变量解读方式 比如我们定义了一些变量 然后这个存储空间就按照结构体的类型来解读 这个存储空间 就按照数组类型来解读这个存储空间 按照指针 这个存储空间也是按照指针类型来解读 这是固定的解读方式 就是变量定义好了就不会变了 还有一种是灵活的解读方式 就是指针可以获取到他的地址重新进行解读 但是不改变原来变量的类型

### **[07:03]**
就比如说指针获取到0084这个地址 可以按照on sa char类型来解读 也可以按照四个字节这个整体来解读 也可以按照八个字节这个整体来解读 怎么解读 在定义的时候注意一下指针类型就可以了 所以说存储器存的都是数字 只是我们赋予了它不同的意义 一个是固定的解读方式 就定义一些变量 一个是灵活的解读方式 就是通过指针变量可以获取到任何地址 然后对它进行重新解读 好这节课到此结束 拜拜

