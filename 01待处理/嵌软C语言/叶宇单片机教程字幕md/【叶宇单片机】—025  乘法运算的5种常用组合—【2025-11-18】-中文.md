# 【叶宇单片机】—025  乘法运算的5种常用组合—【2025-11-18】-中文

## 字幕正文

### **[00:02]**
大家好 今天学习的是第25节 乘法运算的五种常用组合 学习一下乘法运算 算术转换规则 它适用于加减乘除运算 我们来回忆一下算术转换的规则 也就是四个字 提升与对齐 提升与对齐的目的 就是将两个不同类型的操作速 统一转换为同一类型 同一类型 也就是公共类型 也就是运算结果的类型 也就是说 两个操作数的类型 和运算结果的类型都是一样的 算术转换规则是在我们写完程序之后 在编译阶段演示完成的 有的时候一不小心忽略了数据的类型 可能就会造成结果溢出 溢出也就是得到一个错误的结果 所以说在加减乘除运算过程当中 我们要注意类型的变换 这是乘法运算的语法格式

### **[01:02]**
这里都是乘法 也就是从左到右依次运算得到一个结果 把运算结果赋值给保存变量 也就是保存变量提供一个存储空间 来存储这个数据 我们就来看一下乘法运算的五种常用组合 第一种乘数都是常量 定义一个变量 我们来执行这条语句 首先我们看一下编译器是如何使用运算规则的 实物是int类型 三是int类型 两个数的类型一致就可以计算了 也就是15×3得到一个结果 结果是int类型 那么执行完15×3之后得到一个结果 有大小有类型 将这个结果赋值给A A提供的存储空间是八位的 这个时候就将D8位0XD存到A的空间当中

### **[02:03]**
那么A的值就是0XD 也就是45 第二种乘数 一个是常量 一个是变量 我们定义两个变量 然后来执行这条语句 先看一下编译器是怎么使用算术规则的 X的类型是on sa char d int家族要提升为int类型 十的类型是int 所以说现在两个数的类型是一致的 都是int类型 就可以进行计算了 那么X乘以十 X提供的乘数是15 也就是15×10 这是15×10 得到一个结果 结果是int类型 那么执行弯X乘以十之后得到一个结果 有大小有类型 将这个结果赋值给B B提供的存储空间是八位的 所以说将D8位0X96存到B的空间当中

### **[03:04]**
那么B的值就是0X96 也就是150 如果我们把X乘以十赋值给B 改成X乘以86 赋值给B会发生什么变化呢 我们来看一下X的类型是on sana chair 低于int家族 所以说提升为int类型 86是int类型 现在两个数的类型一致就可以计算了 X乘以86 也就是15×86 得到一个结果 结果的类型是int类型 那么执行完X乘以86就得到一个结果 有大小有类型 把它赋值给BB提供的存储空间是八位的 也就是将第八位0X0A存到B的空间当中 那么B的值就是十

### **[04:00]**
实际的结果应该是1290 与实际的结果相差很大 发生错误的原因就是 将int类型的结果存储到八位的空间当中 所以说高八位会被丢掉 导致结果出错 第三种乘数都是变量 定义三个变量 我们来执行这条语句 X的类型是on sa char类型 Y的类型是on sa char类型 都是低于int家族 两个都要提升为int类型 现在两个数的类型都是int类型就可以计算了 X乘以YX提供的乘数是15 Y提供的乘数是六 那么就是15×6得到一个结果 结果是int类型的 那么执行完X乘以Y得到一个结果 有大小有类型 将这个结果赋值给C C提供的存储空间是八位的

### **[05:02]**
那么将D8位存到C的空间当中 C空间当中的值就是0X5A也就是90 第四种乘数 一个是保存变量本身 一个是常量 我们定义一个变量来执行这两条语句 我们先来执行第一条语句 D是assa child类型 低于int家族 要提升为int类型 二是int类型 现在两个数的类型一致就可以计算了 D乘以二 也就是18×2 这是18×2 得到一个结果 结果的类型是int类型 那么执行完D乘以二之后得到一个结果 有大小有类型 将这个结果赋值给DD 提供的存储空间是八位的 那么执行完这条语句 就将D8位0X24存到D的空间当中

### **[06:01]**
那么D原来的值18就会被覆盖掉 它的新值就是0X24 也就是36 记住D的值是36 我们再来执行这条语句 D乘以七赋值给D 我们刚才说D空间值18已经被覆盖掉 它的新值是36 也就是367赋值给D 我们先分析一下类型 D的类型是on sa char 类型小于int家族 要提升为int类型 七是int类型 现在两个类型一致就可以计算了 也就是之前计算的结果36 367得到一个结果是int类型的 那么执行完D乘以七之后就得到一个结果 有大小有类型 将这个结果赋值给dd提供的存储空间是八位的 就将D8位0XFC存到D的空间当中

### **[07:00]**
也就是252 第五种重述 一个是保存变量本身 一个是变量定义三个变量 我们来执行这两条语句 先执行第一条语句 E乘以X赋值给EE的类型是arsa char类型 X类型是on sa char类型 都小于int家族 都要提升为int类型 现在两个数的类型都是int类型 就可以计算了 E乘以XE提供的乘数是二 X提供的乘数是15 那么就是2×15得到一个结果 结果是int类型 那么执行完E乘以X得到一个结果 有大小有类型赋值 给EE提供的存储空间是八位的 所以将D8位存到E的空间当中 那么I就会被覆盖掉 它的新值是0X1E也就是30

### **[08:00]**
刚才执行这条语句 E的新值是30 也就是将二覆盖掉 它的新值是30 我们现在来执行这条语句 先看一下类型的提升与对齐 E的类型是on signature Y的类型是on sa char 两个类型都是低于int家族 现在提升为int类型 两个类型一致 都是int就可以计算了 E乘以YE提供的乘数是30 Y提供的乘数是六 那么306得到一个结果是int类型的 那么执行完E乘以Y得到一个结果 有大小有类型 将这个结果赋值给E E提供的存储空间是八位的 那么就将D8位0XB4存到E的空间当中 那么执行完这条语句 30也就被覆盖掉了 它的新值是0XB4就是180

### **[09:01]**
现在我们写程序来练习一下 还是在我们的程序模板上进行练习 先定义一些变量 然后练习乘法运算的五种组合 再将最后的结果输出到电脑上进行查看 我们来看一下 我把变量的定义放在左边 只是为了方便查看 与编写程序没有关系 我们来看第一种乘数都是常量 15×3=45 将45赋值给AA的值就是45 第二种乘数一个是变量 一个是常量X乘以十赋值给BX的值是15 15×10 就是150 B的结果就是150 这里我省略了类型提升 但是是有这个步骤的 因为前面讲过了 这里就大概看一下 第三种乘数都是变量X乘以Y赋值 给CX的值是15 Y的值是65690 赋值给CC的值就是90

### **[10:02]**
第四种D乘以R赋值给DD是182 就是36 赋值给DD的值是36 那么D乘以七赋值给D 现在D的勋值是36 就是367赋值给D 那么36×7就是252 现在D的值是252 第五种E乘以X赋值给EE的值是二 X的值是15 那么2×15就是30 将30赋值给E E的值是30 下一条语句是E乘以Y赋值给E 这个时候E的值是30 30乘以YY是六 就是30×6=180 将180赋值给EE的值是180 然后将我们得到的ABCDE结果输出到电脑上来 查看一下 我把结果已经整理在这边了 等会直接核对一下就可以了

### **[11:01]**
现在把程序恢复正常 然后点击save2 点击rebuild0警告零错误 现在把程序下载到单片机当中 注意芯片型号 扫描串口 最低波特率2400 最高波特率9600 打开25节的程序 点击下载之后 记得要冷启动 第一个数是45 第二个数是150 第三个数是90 第四个数是252 第五个数是180 那么单片机输出的结果 和我们分析的结果是一样的

