# 【叶宇单片机】—043  逻辑运算符，与“&&”、或“__”、非“!”—【2025-12-21】-中文

## 字幕正文

### **[00:02]**
今天我们来看一下逻辑运算的与运算 或运算和非运算 是不是 之前我们学习过位运算的与运算和或运算 我们先来复习一下位运算的与运算 这里的与运算是针对二进制数的 每一位我们说与零的话 结果就是零 只有两个数都是一 结果才是一 比如12与上九 这是12的二进制 这是九的二进制与运算 就是二进制的每一位进行与运算 雨淋结果就是零 只有两位都是一结果才是一 这就是位运算当中的余运算 我们再来看一下位运算的货运算或运算 也是针对二进制的每一位进行或运算

### **[01:00]**
我们说或一结果是一 只有两个数都是零 结果才是零 比如52或上11 这是52的二进制 11的二进制或运算 就是二进制的每一位进行或运算 获一结果是一 只有两个数都是零的时候 结果才是零 这就是位运算当中的货运算 再来看一下位运算的安慰取反 也就是不管372 11 对一个数的二进制的每一位进行取反 一取反是零 零是一零 这就是位运算的安慰取反 我们刚才回忆了位运算的与运算 for运算按位取反 那么这些运算有什么相同的地方呢

### **[02:01]**
是不是它们的运算对象都是一个数据 并且是对数据当中的二进制的每一位进行计算 我们再来看一下什么是逻辑与运算 或运算和非运算 在学习逻辑运算之前 我们先理解一下什么是逻辑 逻辑就是追求真理过程当中的一种分析方法 一种什么样的分析方法呢 就是通过条件得出一个结论 这个结论有两个 一个是成立 一个就是不成立 我们来看一下亚里士多德逻辑学的三段论法 他说所有人都必有意思 这是条件一 然后苏格拉底是人 这是条件二 因此苏格拉底必有一死 这是一个结论 也就是通过两个条件得出一个结论 这就是逻辑分析的一个过程 通过条件得出一个结论 我们再来看一下 日常生活当中的一个逻辑分析过程

### **[03:02]**
如果今天下雨 我就带雨伞 通过今天是否下雨得出一个结论 就是我是否带雨伞 比如这里只有作业做完 并且天气好 我才能出去玩儿 这里有两个条件得出一个结论 而且这两个条件是并且的关系 比如这里只要是周末或者是放假 我就去看电影 那么也是有两个条件得出一个结论 这两个条件还是或者的关系 我们再来看一下 为什么人们会把逻辑的一个分析过程 带入到数学当中呢 我们说人与人之间的交流是通过文字来交流 机器与机器之间是通过数字来交流 他们都是编码的一种文字 能够描述一个分析过程 那我们能不能用数学来描述一个逻辑 分析过程呢 也就试图用数学来描述大脑的工作

### **[04:01]**
我们先来看一下 用数学来描述逻辑分析过程 比如作业做完这个事情 如果是真的 并且天气好 今天天气很好 也是真的 因为两个条件是并且的关系 那么两个条件都是真的 所以说结果也是真的 那就是我才能出去玩 如果说今天作业没有做完 但是今天天气是好的 因为这两个条件是并且的关系 那么有一个条件不满足 结果就不满足 那么结果就是我不能出去玩 所以说这里通过数学来描述一个逻辑过程 因为两个条件是并且的关系 也就是与的关系 逻辑与就是对两个条件进行判断 然后得出一个结果 条件有真的有假的结果 也有真的有假的 所以说对他们用数学来描述的话

### **[05:00]**
假的我们用零表示 真的用一表示 因为条件一和条件二之间是并且的关系 也就是与的关系 那么如果有一个条件不成立 结果就是不成立的 只有两个条件都成立的时候 结果才是成立的 这里的逻辑语 与我们那按位与运算过程是一样的 但是运算的对象是不一样的 按位语它针对的是数据 逻辑语 针对的是条件的结果 而且逻辑值只能是零和幺 我们再来看一下逻辑语的应用 一般也是出现在if的条件语句当中 第一个条件判断的结果与上 第二个条件判断的结果 与上第三个条件判断的结果 如果第一个条件是真的 第二个条件也是真的 第三个条件也是真的 那么结果才能是真的

### **[06:00]**
如果第一个条件是假的 那么后面其实那么第二个条件和第三个条件 其实也不用判断 结果肯定是假的 如果第一个条件是真的 第二个条件是假的 那第三个条件也不用判断 结果肯定是假的 我们来举一个例子 条件1A大于等于七时与上A小于等于80 如果A的值是90 我们来判断一下 90大于等于70是真的 所以说结果是真的 就是一九十小于等于80是假的 所以说结果是零 一与上零 结果是零 结果是零就是假的 所以说不会执行大括号的语句 会跳过大块的语句 接着执行括号外的语句 这里判断的是A大于70 小于80 也就是A在70和80之间 有同学会说能不能这样写 70小雨A小雨80

### **[07:05]**
这样作为一个if条件可以吗 我们来看一下它的运算过程是从左往右运算 它的运算过程是从左往右运算 也就是先算70小于A我们用括号括一下 表示他是先算的 然后再小于80 那么70小于A对这个运算式进行判断 要么是对的 要么是错的 那么它的结果要么是一 要么是零 无论是一还是零都小于80 所以说这个式子是恒为真的 永远都是真的 他就失去了条件判断的意义 所以说不能够这么写 我们再来看一下 把这个事件用数字来表示一下 只要是周末 如果今天不是周末或者是放假 假如今天放假了

### **[08:00]**
因为两个条件是或者的关系 有一个是真的 所以说结果就是真的 我就能去看电影 还是通过数学来描述一个逻辑分析过程 只是这里的两个条件是或者的关系逻辑 或就是对两个条件进行或者判断 就是有一个条件是真的就可以了 然后得出一个结果 那么货运算有一个条件是真的 结果就是真的 只有两个条件都是假的 结果才是假的 这里的逻辑或运算和位运算当中的或运算 它的过程是一 它的计算过程是一样的 但是运算的对象不一样 位运算的或针对的是一个数据 逻辑或针对的是条件的结果 而且这个条件的结果只能是逻辑值 要么是零 要么是一 我们看一下逻辑或的应用

### **[09:01]**
它也经常会出现在if的条件语句判断当中 也就是第一个条件判断结果或上 第二个条件判断结果或上第三个条件判断结果 我们说或一结果是一 如果条件一是真的 那么条件二和条件三都不需要判断了 结果肯定是真的 如果条件一是假的 那么会判断条件二 如果条件二是真的 那条件三也不需要判断了 结果就肯定是真的 举个例子 A小于七时或上A大于80 A的值 假如是一百一百小于70是假的 逻辑值就是零 100大于80是真的 逻辑值就是一 那么零或上一结果是一 就是真的 就会执行大括号里的语句 接着执行括号外的语句 注意一下 这里的货运算 旁边的两个条件都是用括号括起来的

### **[10:02]**
为什么用括号括起来呢 因为你看有一个或运算符大于运算符 小于运算符 它们运算符的优先顺序我们是不记得的 所以说用括号括起来 这样就知道谁先算谁后算 逻辑非就是条件只有一个 对这个条件的结果进行取反 要和安慰取反区分一下 安慰取反真的按位取反 针对的是一个数据的二进制的每一位进行取反 而逻辑非是针对条件的结果进行取反 这个条件的结果只能是零或者是幺 条件的结果只能是一个逻辑值 我们再来看一下罗辑与逻辑 或经常会出现的一个短路问题 那什么是短路问题 我们首先想到的就是电路当中那个短路

### **[11:00]**
但是我们写的是程序哪里来的短路呢 我们来看一下if的条件语句当中 有这样一条长长的语句 是一个连续的逻辑与运算 那么只有这里的所有条件都是真的 那么结果才是真的 只要有一个条件是假的 结果肯定是假的 对不对 所以说我们来判断一下 如果条件一是真的 那么就会判断条件二 如果条件二是真的 那么就会判断条件三 如果条件三是假的 那么后面的都不会去判断了 因为有一个是假的 结果肯定就是假的 如果条件一是假的 那么有了那么有了一个假的 后面的都不用去判断了 因为结果肯定是假的 这样能够提高程序的执行效率 如果我们把条件一 条件二 条件三放到一个电路当中

### **[12:00]**
假如说条件一是真的 条件二是假的 那么有一个假的后面都不需要去判断了 结果肯定就是假的 我们再来看一下这一条逻辑或语句 第一个条件或上第二个条件 或上第三个条件 后面等等等 或上第N个条件 我们说货运算有一个是真的 那么结果肯定是真的 比如我们判断条件一 比如我们判断条件一是假的 那么就会判断条件二 如果条件二是真的 那条件三就不需要判断了 因为结果肯定是真的 如果我们判断条件一是真的 那条件二也不需要判断了 条件三也不需要判断了 后面的也不需要判断了 那结果肯定是真的 有一个是真的 结果就是真的 这样可以提高程序的执行效率 比如把条件一 条件二和条件三放到一个电路当中

### **[13:01]**
条件一如果是真的 那么后面的条件二和条件三就不需要考虑了 结果肯定是真的 这就是罗居宇与罗辑 或判断过程当中的一种短路现象 这种现象我们要做到心中有数 因为他是会经常用到的 还是会经常遇到的 我们再来总结一下什么是按位与安慰 for和按位取反 它们的特点是什么呢 就是针对一个数 这个数二进制的每一位进行操作 而罗居宇罗居for和罗机飞 他们针对的是一个逻辑值 这个逻辑值要么是零 要么是幺 然后再注意下他们的写法 因为写错的话也会产生问题 而且这个问题的话是有的时候会发生 有的时候不会发生偶然的问题

### **[14:02]**
如果把逻辑语 写成了安慰语 那么这会引发错误 而且这个错误是有的时候会发生的 有的时候不会发生的 就有的时候两个运算符得出的结果是一样的 有的时候两个运算符得出的结果又不一样 那么不一样的时候就会出错

