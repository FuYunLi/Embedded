# 【叶宇单片机】—82  阻塞延时，控制LED闪烁—【2026-06-05】-中文

## 字幕正文

### **[00:00]**
这节课我们用阻塞延时来使led灯闪烁 上节课我们看了for循环语句 它就是一个延时程序 我们把它封装成一个延时函数 封装成一个delay的延时函数 用这个延时函数来使led灯闪烁 我们电路板上有两个l led灯模块 这是l led灯 它的正极接到电源的正极 它的负极接到单片机的引脚上 我们知道单片机引脚可以输出高低电平 它对应的就是零伏和五伏 如果输出低电平 也就是零伏 这时候led灯亮 如果输出高电平 也就是五伏 这个时候led灯是灭的 我们就来控制这个P0端口的第零位 然后使r led灯闪烁 这个时候我们写一个程序 先让l led灯亮 然后再让l led灯灭 这里的P0杠零 我们一看就知道他是P0端口的D0位引脚

### **[01:03]**
但是单片机它不认识这样的文字 它只认识引脚的地址 所以说我们要将我们起的这个别名和地址 关联起来 这就是0X80D零位的地址的表示方式 连480这个地址 我们乍一看也不知道它是什么意思 所以说也会给这个地址起一个别名 也就是P0端口 那么看名字就知道它的意思 这一部分它是放在一个头文件当中 所以说如果包含了这个头文件 就可以直接写P0就可以了 他也会间接的得到连S80这个地址 我们再来看一下这里2led灯的亮和灭 它一亮一灭 它的速度是非常快的 我们都没有反应过来 所以说在这里面要加上一个岩石 也就是晾一会灭一会儿 这个时候就需要写一个延时函数 就是刚才我们的for循环语句 现在看我们的程序写的差不多了

### **[02:02]**
那么编译器从上往下编译的时候 他第一次遇到delay这个函数 他不知道要不要为这个函数准备一个入口参数 或者说是准备几个入口参数 所以说我们在前面要加一个delay函数的声明 把它的相关信息提前先告诉编译器 那么编译器遇到delay函数的时候 才会正确的处理 这里的I灯亮一会 然后灭一会 是不是只闪烁了一次 如果我们让它一闪一闪的怎么办 是不是加一个Y有循环就可以了 这就是一个完整的led灯闪烁的程序 我们把它下载到单片机上来看一下 这是程序下载后的效果 而led灯是一闪一闪的 但是非常的快 改一下延时 让它闪的慢一点 我们来看一下 我们如果改延时的话 是不是改这个数字5000 它循环的次数是不是越多 延时的时间就越长

### **[03:01]**
但是我们是把这个5000改成1万还是2万 还是3万比较合适呢 我们不知道 因为我们不知道这个for循环语句 循环一次是多长时间 这个时候 我们就要对这个延时程序进行优化一下 先打开我们下载程序的这个软件 里面有一个功能叫做软件延时计算器 它有三个参数需要设置一下 一个是系统的频率 我们设为11.0592兆赫兹 因为我们板上的静电频率就是11.059 二兆赫兹 然后定时长度我们选择一毫秒 还有一个选择就是8051的指令集 我们选择SSTC89 它适用于STC89系列 也就是我们板子上这个SSTC89 C52单片机 这里就产生了一个一毫秒的延时函数 接着我们把这个程序复制过来看一下

### **[04:00]**
这里有一个nope 它是汇编指令的封装 所以说我们要加一个头文件 它是放在这个头文件里的 所以说要包含进来 loop是什么意思呢 我们来看一下 我们的晶振频率是11.0592兆赫兹 也就是一秒周期性变化11.0592兆次 那么这一个周期的时间就是一秒除以 11.0592兆 我们也叫做时钟周期 那么CPU就是在晶振的节拍下完成一条条指令的 我们这里大概是12个时钟周期 可以完成一个操作 我们也把它叫做机器周期 那么一个机器周期是多长时间 是不是一秒除以11.0592 兆赫兹乘以12 也就是1.085微秒 那么这里的nope它实际上是一条汇编指令

### **[05:00]**
它是一个机器周期 也就是执行这样一条指令 它需要1.085微秒 但是执行这条指令的时候 CPU什么都不做 那我们想为什么不直接写nope这个汇编指令呢 因为这是一个C语言文件 我们写的是C语言程序 那么在编译的时候 它只认识C语言的语法规则 他不认识诺普 所以说这个时候 我们要把这条汇编指令封装一下 然后告诉编译器 它是一条汇编指令 现在我们在这个一毫秒延时的基础上 写一个任意毫秒延时的函数 也就是它输入几毫秒就延时几毫秒 这里面我们肯定是用delay1毫秒作为基础的 那么输入几毫秒就延迟几毫秒 这里面放一个while 有循环语句就可以了 我们看一下这里的delay延时一毫秒 但是他每次在延时的时候 还有一个Y有循环判断 所以它的实际时间是不是比一毫秒大一点

### **[06:02]**
因为它有一个Y有循环判断嘛 所以我们一般说软件延时 是一个非精确的延时诶 定时器的延时相对来说比较精确一点 由于这里的while有循环判断 使时间加长了 那么我们可以去掉这里的NOP汇编指令 去掉这个NOP指令 去掉这个头文件 那么这个函数整体看起来就比较清爽一点 接着我们来修改一下刚才的id灯闪烁程序 我们想让这里的延时时间长一点 我们就写一个delay1000毫秒 也就是延时一秒 就是亮一秒 然后灭一秒 因为这里用到了delay毫秒 所以说写一个delay毫秒的程序 delay毫秒当中又用了delay1毫秒 那么再写一个delay1毫秒的程序

### **[07:00]**
这样我们看是不是大概写好了 但是编译器从上往下编译的时候 第一次遇到delay毫秒 他不认识这个函数 它不知道如何为它准备一个入口参数 是否需要为它准备一个入口参数 所以说这个时候我们要写一个函数声明 提前告诉编译器delay毫秒的相关信息 那么编译器在后面才能够正确的处理DL毫秒 这个函数 接着从上往下编译 我们又会遇到delay1毫秒 那有的同学就会想 为什么不对delay1毫秒做一个函数声明呢 因为delay1毫秒是先定义他后使用的 所以说对他的相关信息 编译器是知道的 这个时候就不需要提前声明了 这个时候再把这个程序下载到单片机看一下啊 那么我们来看一下我们今天的主题阻塞延时 还是刚才这个程序只保留主函数的部分

### **[08:04]**
首先main是我们程序的入口 从这里开始执行我们写的程序 一般是从上往下顺序执行程序 但是遇到DL1000毫秒 也就是在这里等待一秒之后 才会执行下面的程序 执行完下面的程序又遇到delay1000毫秒 又会在这里等待一秒 等待一秒之后再执行下面的程序 那这里等待完一秒之后再执行其他的程序 就属于一个阻塞延时 就好像一条马路装了两台红绿灯 只有等红灯时间过了之后 才可以继续往后直行 如果没有这个红绿灯 它就会从上往下依次直行 然后一路上都会比较顺畅 这就是这节课我们所说的阻塞岩石 然后下节课我们来学习非阻塞延时 好拜拜

