# 【叶宇单片机】—054  从全局变量和局部变量中感悟“栈”为何物—【2026-02-01】-中文

## 字幕正文

### **[00:01]**
今天我们来看一下 从全局变量和局部变量中感悟占为何物 也就是学习一下全局变量 局部变量 还有站的工作机制 首先看一下我们上节课学习的一个程序 上节课我们学习了函数声明 函数调用函数定义以及函数的执行顺序 首先main函数从上往下执行 遇到函数调用函数 执行完子函数之后 会返回到main当中 接着执行下面的语句 我们还是在这个程序当中学一下全局变量 这里定义了一个变量A 它可以在main函数当中使用 还可以在函数一当中使用 那我们看一下这个全局变量有什么特点 首先是定义了一个全局变量 在下面的函数当中都可以随意的使用

### **[01:01]**
然后是这个全局变量实际上定义在函数的外面 就是定义在所有函数的外面 接着定义一个全局变量 是不是要在ram存储空间当中申请一个存储空间 我们知道ROM存储空间存储的是程序和常量 ram存储空间存储的是变量 那么定义了这样一个变量 就会在里面申请一个存储空间 这个存储空间在商店之后只属于A这个变量 A拥有永久的居住权 永久的使用权 也就是相当于也就是相当于你买了一个房子 这个房子只属于你 你有永久的居住权和使用权 也就是这个存储空间它只属于A这个变量 它不会分配给别的变量来使用了 接着我们看全局变量 另一个特点就是定义这个全局变量 不仅仅在这个原文件当中可以使用

### **[02:01]**
在其他的点C源文件当中也可以使用 但是前提是必须要用EXTEN进行一个变量声明 extant是外部的 来自其他文件的 也就是告诉编译器这个变量的定义 在其他文件当中 你去其他文件去找它的一个原始定义 在这个文件当中 它只是进行一个声明 它不会重新的分配一个内存给它 只有声明了之后 才可以在这个文件当中 正常的使用这个全局变量 我们对全局变量进行一个简单的总结 首先是定义在所有的函数外面 接着是可以在整个工程文件当中都可以使用 但是必须要用ext进行变量声明 刚才我们把变量定义在所有函数的外面 我们叫做全局变量 如果把一个变量定义在函数的内部

### **[03:00]**
我们叫做局部变量 这个局部变量同样也会在ram当中 申请一个存储空间 但是这个存储空间更像是一个宾馆 或者说是客栈 宾馆和客栈 它里面的房间它不是属于某一个人的 它会在不同的时间段分配给不同的人来使用 B申请的这个存储空间也不是专属于B的 它会在不同的时间段分配给不同的变量来使用 局部变量占用的存储空间比较特别 我们叫做占区 我们先来看一下战区的工作机制 首先每一个存储单元都是有地址的 战区它实际上就是RHYM的存储单元 既然是存储单元 它可以存储数据 也可以读出数据 在这里存储数据 我们叫做压榨 读出数据 我们叫做出站 我们先看一下压榨过程

### **[04:00]**
就是来一个数据存一个数据 来一个数据存一个数据 也就是从地址低的 往地址高的开始存储数据 那么站工作过程还有一个说法叫做向上生长 观察一下这里什么向上生长了 是不是这里的地址由低到高向上生长了 接着我们看一下出站过程 出战过程他遵循的一个原则是后进的先出 也就是最后进的最先弹出去 就这样一个一个将数据弹出去 这就是战区的一个工作过程 我们只是简单的了解一下 然后我们将战区的工作过程和函数的调用过程 同步起来看一下 首先是main函数 从上往下执行语句 遇见函数调用函数 但是在调用函数之前 它会将一些重要的信息进行压榨

### **[05:02]**
首先是现场保护 将一些数据进行压榨 也就是将一些寄存器的数据进行压榨 比如main在执行的过程当中 是不是会用到一些寄存器 这个时候去调用子程序 子程序在执行的过程当中 可能也会用到这些寄存器 那么就会更改这些寄存器 所以说我们提前要将这些寄存器的数值 保存起来 也就是现场保护 接着我们将函数的返回地址压栈 因为执行完子程序之后 是不是要返回将这个返回地址压栈 接着去执行这个子程序 执行子程序的时候 这个子程序的内部是不是会定义一些变量 这个时候给这些局部变量分配了一些内存空间 就是在战区当中 然后执行子程序

### **[06:00]**
执行完子程序是不是要返回 返回的时候就涉及到一个出站的过程 首先会跳过变量的存储空间 为什么说是跳过 不是弹出 一般弹出的话是弹出到某个寄存器 或者是某一些存储空间 这些数据我们都还要使用的 但是这些局部变量申请的存储空间里面的数据 我们只在函数内部使用 出了函数我们就不用它了 所以说我们不将它弹出 而是跳过这一部分存储空间 接着会弹出返回地址 弹出返回地址之后 子程序就可以正常的返回了 接着弹出保护现场的数据 也就是恢复现场数据 那么main函数就可以继续往下执行语句了 如果main函数从上往下执行 遇到函数I同样会调用函数I 调用函数之前也会将一些重要的数据进行压榨

### **[07:03]**
比如现场保护的数据 函数的返回地址 接着调用函数二 那么函数二的内部可能会定义一些变量 也就是一些局部变量 为这些局部变量分配了一些内存空间 然后执行函数二 执行完函数二之后 是不是也要返回它的整个过程实际上是一样的 我们刚才看了站执行的过程和函数的调用过程 从这两个过程当中 我们可以得到一些重要的信息 比如函数内的变量出了函数可以使用吗 是不可以使用的 因为函数内的变量它是放在一些公共区域的 也就是函数一执行之后 这个公共区域可能就会分配给其他函数的内部 变量使用了 所以说函数内部的变量 出了函数之后就不可以使用了

### **[08:02]**
还有一个问题就是还有一个问题 就是在函数内部可以定义很大的一个变量吗 比如说定义一个很大的一个数组 这样也是不可以的 因为战区它是有限的 比如这里从60H到7FH 一共也就32个字节 如果你定义的变量太多 就容易爆炸 就会引发一些问题 还有一个问题就是main调用函数二 在函数二的执行过程当中可以调用函数三吗 在函数三的执行过程当中可以调用函数四吗 在函数四的执行过程当中可以调用函数五吗 也就是不可以太多这样的内嵌调用 为什么呢 因为每调用一个函数就要进行现场保护 就要将就要将这个函数的返回地址进行压榨 同时还要为这个函数分配一些变量的存储空间

### **[09:02]**
如果你嵌套太多这样的函数 每一个函数调用的时候都要将现场进行压榨 返回地址压榨 还要为这个函数内的局部变量分配一些占空间 嵌套太多的话就会容易暴占 所以说我们要注意一下 刚才我们说了三个问题 对于初学者主要了解一下 不要犯这样的错误就可以了 首先是函数内的变量在函数外不可以使用 然后在函数内不能定义太多太大的变量 如果定义一个很大的变量 可以定义成一个全局变量 最后一点就是在函数内不能嵌套太多层 这样也可能会引发爆炸的一个问题 接着我们对全局变量和局部变量 进行一个简单的总结 重点要记住的是局部变量定义在函数内部

### **[10:00]**
然后出了函数就不能使用了 就无效了 然后我们看一下在定义局部变量的时候 需要注意的一些问题 比如这里定义了一个局部变量 B 这里定义了个B1个C 但是中间还有一条语句是不可以这样定义的 要把两条定义放在最上面 中间不要有其他的可执行的语句 最后我们看一下全局变量和局部变量的优先级 什么意思呢 就是你定义全局变量的时候 是不是有一个变量名 你定义局部变量的时候也有一个变量名 如果这两个变量名重名了 在某一处 你使用这个变量名的时候 到底是全局变量还是使用的是局部变量 我们来看一下它的优先级 这里定义了一个局部变量A 一个全局变量A那么执行will a的时候

### **[11:04]**
输出的结果到底是二还是五 这里就要记住 在函数内部 局部变量的优先级高于全局变量的优先级 这里要记住这个规则 接着我们再看一个例子 下面写不完 我们写在右边 这里定义了一个全局变量 两个局部变量 先输出的是三 然后输出的是二 自己可以看一下 虽然说全局变量和局部变量重名 没有什么太大的问题 因为他们有一个优先级 有一个先后顺序嘛 但是我们尽量在定义的时候不要重名 因为机器可以将它们分开

### **[12:01]**
但是我们人如果一不小心将这个A看作那个A 可能就会发生一些错误 好这节课到此结束 拜拜

