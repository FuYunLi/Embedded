# 【叶宇单片机】—033  位运算的按位取反—【2025-12-05】-中文

## 字幕正文

### **[00:00]**
大家好 今天学习的是第33节位运算的按位取反 我们先来看一下什么是按位取反 比如对29进行按位取反 这是29的二进制 对它的每一位进行取反 一取反是零零 取反是1000111 这就是按位取反 对这里的每一位进行取反 我们之前学过e for运算 e for1对 结果也会进行取反 我们来看一下有什么区别 204E或0X0F这是204的二进制 这是0X0F的二进制 两个数进行异或异或一取反 比如第四位是四个一 那么E或一结果进行取反 零是一 零是一 一是零

### **[01:00]**
一是零 高四位是四个零 那么异或零结果是不变的 零是零 零是零 一是一 一是一 这里的E或一取反是可以选择某些位进行取反 而不是全部进行取反 我们之前学过按位与运算 按位或运算 按位异或运算 比如按位与运算某一位于零那一位 结果是零 某一位与一那一位的结果不变 比如按位或运算某一位或零那一位的结果不变 某一位或一那一位的结果是一 比如按位异或运算 某一位异或零那一位的结果不变 某一位亦或一那一位的结果取反 这里是对某一位或者是某一些位 可以选择性的操作 A按位取反 是对自己房间的数据 不管372 11全部进行取反操作

### **[02:02]**
那么取反后的结果是什么类型呢 我们要一个概念 就是每一个数都有自己的解读方式 要弄清楚这一点 就是按照什么类型来解读这个数据 之前学习过算术运算 比如加减乘除位运算的与运算或运算异或运算 它们的运算都需要两个操作数 首先要对每一个操作数进行类型的提升 然后再看一看两个操作数的类型是否相同 如果不相同的话 再进行类型的对齐 因为我们硬件要求两个操作数的类型 必须是一样的 那么对于按位取反操作 他只对自己空间的数据进行操作 也就是只有一个操作数 就是他自己 这个时候我们只需要对类型进行提升 就不需要类型对齐了 因为对齐是两个人的事情 这里他只有一个操作数 类型提升

### **[03:00]**
也就是类型小于int家族的时候 提升为int类型 首先提升为int类型 如果int类型装不下 再提升为unsigned int类型 这里的横线都是类型的一种缩写方式 将提升后的数据给运算器 运算器是按照提升后的类型来计算的 如果想要找一种对齐感 就可以理解成自己跟自己对齐 得到一个公共类型 然后运算器按照公共类型来解读 按位取反运算的练习 定义一个变量 执行这条语句 A取反赋值给A 首先还是要来看一下操作数的类型 A的类型是assignal char类型 提升为int类型 A是五 这是五的二进制 因为提升为int类型 那么前面八位补零 这个时候再进行取反运算得到一个结果 那么就是对A进行取反 得到一个结果

### **[04:01]**
有大小 有类型 将这个结果赋值给AA提供的存储空间是八位的 所以说将D8位存到A的空间当中 那么A的值就是FA 我们来写程序验证一下 还是在我们的单片机上来练习程序 定义一个变量A初值是五 然后对A进行取反 结果赋值给A 最后输出A的结果 查看一下 我们来看一下A的结果 A输出的结果是0XFA 这与我们分析的结果是一致的 如果我们想把结果这16位全都保存下来 怎么办呢 我们定义一个变量Y 然后再来执行这条语句 对A进行取反 然后将结果赋值给Y 对A进行取反的结果 我们已经知道有大小 有类型 将这个结果赋值给Y Y提供的存储空间是16位的

### **[05:01]**
那么这里的16位都能够存到Y的空间当中 那么Y的值就是f f FA 我们来写程序验证一下 现在程序写好了 重新定义一个变量 Y将A取反的结果赋值给Y 最后将Y的值输出 查看一下Y输出的结果是零 X f f f a 这与我们分析的结果是一致的 而按位取反是对自己数据进行取反 包括符号位 我们来看一下定义一些变量来执行这条语句 首先要确认一下类型 A的类型是SNH类型 要提升为int类型 A的值是五 这是副的二进制 提升为int类型

### **[06:00]**
前面八位补一 现在类型确定了 然后再进行取反运算 这是取反后得到的结果 那么对A进行取反之后得到一个结果 有大小有类型 将这个结果赋值给Y Y提供的存储空间是16位的 那么这16位全部存到Y的空间当中 Y的值就是0X0004 我们来写程序验证一下A的值是五 是三的差类型 然后输出Y的结果 查看一下Y的结果是四 这与我们分析的结果是一致的 当运算的时候 如果有两个操作数 首先是对每一个操作数进行类型的提升 然后再对两个操作数进行对齐 如果只有一个操作数 只需要对类型进行提升就可以了

