# 【叶宇单片机】—052  支撑程序框架的switch语句—【2026-01-26】-中文

## 字幕正文

### **[00:01]**
今天我们来看一下支撑程序框架的switch语句 首先看一下它的常见格式 switch后面是一个括号 里面放了一个变量 变量我们比较熟悉 我们会定义变量 接着是大括号里面放了一些与switch相关的语句 这么长的一条语句 我们要怎么去理解呢 主要是理解它的一个执行顺序 与前面我们学习的if语句 while语句等语句一样 主要是理解它的一个执行顺序 我们来看一下switch后面有一个变量 这个变量值不同 它的执行顺序就不同 比如这个变量值如果是零的话 它就会匹配到case0这个入口 从这里开始执行程序 执行完语句零之后

### **[01:00]**
遇到break就会直接跳出switch语句 接着执行括号外的语句 注意这里的语句零它只是一个代表 实际上在case和break之间 它能够放很多条语句 接着我们看一下 如果变量值是一 那么就会匹配到K41这个入口 执行完语句一之后 遇到break又会跳出switch语句 接着执行括号外的语句 那么如果变量值是二 就会匹配到case42这个入口 从这里开始执行程序 遇到break之后又会直接跳出 最后一条语句是default default是什么意思呢 是默认的意思 也就是当变量的值与所有的case都不匹配的时候 就会默认的从default这个入口开始执行程序 比如变量的值是35 我们应该从case35这个入口开始执行程序

### **[02:01]**
但是没有case35 也就是所有case都不满足的时候 就会从default这个入口开始执行程序 执行完语句三之后 来到break又会跳出switch语句 接着执行括号外的语句 这里需要注意的是 switch后面这个变量它一定要是一个变量 只有是变量才能够选择switch的不同的分支语句 这是第一个 第二个就是case后面的值要是一个常量 它不可以是一个变量 如果是一个变量就会报错 这里需要注意一下 这里还有break 想一想break是什么语句的出口 之前我们学习过循环语句 break还是循环语句的出口 就是直接跳出这个循环 或者是相当于是直接脱离了这个循环语句 这里的break也是遇到break 它会跳出这个switch语句

### **[03:01]**
接着执行switch 括号外的语句与这个switch没有什么关系了 我们再来整体的看一下switch这个语句 它是不是一个多选一的分支结构语句 那么之前我们还学习过什么多分支语句呢 if else if也是一个多分支语句 我们来看一看它们俩的相同点和不同点 如果条件零满足 会执行语句零 接着跳出if语句 执行下面的语句 否则我们就会判断条件一 如果条件一满足 会执行语句一 接着又会跳出if语句 执行下面的语句 否则我们会判断条件二 如果条件二满足 就会执行语句二 接着会跳出if语句 如果条件零 条件一条件二都不满足的话 那么我们会执行else里面的语句

### **[04:01]**
三接着会跳出执行下面的一句 在if语句当中 如果所有条件都不满足的话 就会执行else里面的程序 这与default是不是有点像 在switch语句当中 如果所有的case都不匹配的话 就会执行default里面的语句 我们来看一看这两个有什么区别 首先是else else之前我们学习过 它是可以省略的 如果出现的话只能出现一次 并且只能出现在最后 同样default它也是可以省略的 如果出现的话只能出现一次 但是不是必须放在最后规范里 建议放在最后 但是这不是必须的要求 所以else default没有什么太大的区别 接着我们整体的看一下switch语句和if语句 首先它们都是多选一的分支语句

### **[05:03]**
但是switch的条件判断比较单一 它就是一个变量值 就是通过一个变量值来选择不同的分支 if的条件判断他可以选择不同的条件 比如说可以对不同的变量进行判断 然后选择不同的分支 但是switch的表达更清楚明了 所以说根据不同的需求来选择不同的语句 能用switch的尽量用switch 接着来看switch语句的一些细节 比如break可以省略吗 实际上它是可以省略的 但是省略之后会触发kiss的贯穿 我们看看是一个什么样的情况 变量值如果是零 就会从case40这个入口开始执行程序 执行完语句零之后 遇到break就会直接跳出switch语句 如果把这个break省略掉

### **[06:00]**
那么就不会从这里跳出 执行完语句零之后没有遇到break 没有遇到一个跳转的指令 那么他就会接着继续往下执行 就会来到case1 执行case1里面的语句一 如果这里的break也把它删掉 还是会顺序的往下执行 来到case2执行语句二 这里他遇到了break 就会从这个break直接跳出 这就触发了case的贯穿 就是连续执行了好几个case语句 直到遇到break才会跳出 或者是从上到下执行 到最后都没有遇到break也会跳出 因为最后的大括号本来也表示switch语句的结束 刚才我们说case后面的break可以省略 那么default后面的break可以省略吗 我们之前说过default语句不必放在最后 那么它也可以放在中间

### **[07:01]**
如果放在中间会怎么样呢 我们来看一下 如果变量的值与所有的case都不匹配的话 就会来到默认的一个入口 就是default入口 开始执行它里面的程序 执行完语句三 遇到break就会直接跳出 那么如果把这里的break把它去掉 那么程序就不会从这里跳出了 执行完已聚餐之后 不会遇到break 不会发生跳转 那么它就会顺序的往下执行 执行case41里面的语句一 接着遇到break才会直接跳出 这里也会触发case贯穿 也就是从default执行到了case里面的语句 所以说省略break语句的时候要注意贯穿的问题 同样我们之前说过 还有一个什么可以省略 default也是可以省略的 但是最好不要省略 保持语句的完整性

### **[08:01]**
接着我们看一下case的顺序有要求吗 比如说必须要14012345 6789十等等等等 这样排下去吗 是可以不按照顺序的 比如这边打乱了他的顺序 比如这里kiss2K四一 然后K40 然后default default是建议放在最后的 接着我们看一下switch语句的嵌套 也就是外面是一个switch语句 里面还有一个switch语句 我们之前说过 kiss和break之间可以放很多条语句 比如可以放if语句 while有语句同样可以放switch语句 虽然switch不是顺序执行的语句 也就是不是规规矩矩的从上往下执行的语句 switch语句它中间可能会改变它的执行顺序

### **[09:00]**
但是每一个switch语句它只改变自己的执行顺序 记住自己的跳转地址 不会干扰到别人 所以外面的switch语句和内部的switch语句 它们之间是互不干扰的 不会有影响的 就像我们之前说的循环语句 它们也不是规规矩矩的从上往下顺序执行的 他们也会改变执行的顺序 但是他们也是可以嵌套的 因为他们会各自记住自己的跳转地址 也不会去打扰别人 如果对于跳转地址 这一段听不懂的话也没有关系 因为这是从C语言翻译成汇编指令 之后来分析的 不懂也没有关系 就记住虽然嵌套在一起 但是可以按照独立的语句来分析 就不会出错 我们来写一个程序试一试 还是在我们的单片机上来练习程序 定义两个变量K和A然后来执行

### **[10:00]**
我们第一条语句将零赋值给A 第二条语句将二赋值给K 第三条语句就是一个switch语句 switch的变量是K K的值是二 那么它的入口就是case2 从case2这里开始执行程序 执行的是A加加 那么A的初值是零 执行完A加加A的值是一 接着后面没有遇到break语句 没有遇到跳转语句就会顺序的执行case43 然后再A加加 那么A的值是二 接着又没有遇到break语句 就会顺序的往下执行 执行K44又是A加加 那么A的值是三 接着遇到break语句就会跳出这个switch 我们看一下 现在A值是三 跳出switch来到view a就是将A的结果输出查看一下

### **[11:02]**
那么A输出的结果是三 我们可以写程序试一试好 这节课到此结束 拜拜

