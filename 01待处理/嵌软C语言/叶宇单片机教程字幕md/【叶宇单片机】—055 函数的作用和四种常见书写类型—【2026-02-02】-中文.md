# 【叶宇单片机】—055 函数的作用和四种常见书写类型—【2026-02-02】-中文

## 字幕正文

### **[00:01]**
今天我们来看一下函数的作用 和四种常见的书写类型 先看一下函数的一般格式 首先是函数名 就是给函数起一个名字 这里的语句12345我们叫做函数题 也就是把实现相同功能的语句放在一起 我们叫做函数体 那么函数的主要作用就是 用于实现功能的模块化 接着函数的其他部分主要与函数题有关 实现这功能可能需要外界的一些数据 那么就通过参数列表传进来 同样实现这个功能 如果产生一些新的数据 就通过返回值类型 告知返回的是一个什么样类型的数据 外界直接接收就可以了 我们说实现这个功能的时候 可能需要外界的一些数据 那么也有可能不需要外界的数据

### **[01:00]**
那么参数列表就有可能是有参数 或者是没有参数 不需要参数 同样返回值可能会返回值 就是有返回值 可能不会返回值就没有返回值 那么函数就会有四种不同的书写方式 我们来看一下 这里有四个函数表示四种不同的书写类型 首先返回值类型是void代表没有返回值 没有返回值 参数列表里面的void代表没有参数 没有参数 我们把参数列表形象的叫做输入接口 把返回值类型叫做输出接口 那么第一个就是没有输入 也没有输出 第二个是有输入 但是没有输出 第三个没有输入 但是有输出 第四个是有输入 也有输出的一个函数 这四个不同类型书写方式的函数 实现的是同一个功能 就是对两个数求和

### **[02:01]**
我们分别来看一下 第一类就是没有输出 也没有输入的函数 首先对函数进行一个声明 接着定义一个全局变量Y用来存储相加的和 定义一个全局变量A初值是二 它是一个加数 定义一个全局变量B初值是三 它也是一个加数 接着是main函数 然后是sum函数的定义部分 main函数是我们程序的入口 从这里开始执行我们写的程序 首先是调用sum函数 sum函数因为没有输入 所以说输入的括号里就什么都不写 接着调用sum函数 就是实现A加B结果赋值给y sum 这个函数虽然没有输入 也没有输出 但是通过全局变量实现了输入和输出

### **[03:07]**
接着我们看第二类没有输出 但是有输入的函数 开始写函数声明 接着定义一个变量Y用来存储相加的结果 main函数sum函数的定义 首先从main这里执行程序 先调用sum函数 sum函数是有输入参数的 也就是二和三将I赋值给I 将三赋值给K 也就相当于之前我们学的赋值语句 将外界的数据赋值给接口变量 就可以在函数内部使用外界的数据了 I加上key的结果 想要输出 通过一个全局变量Y将结果输出 I和K我们形象的叫做输入接口

### **[04:02]**
实际上它专业的名字叫做形参 二和三专业的名字叫做实参 这种书写方式 通过全局变量实现函数内数据输出 它的输入接口是封装好的 我们来看第三类 有输出 没有输入的函数 sum的定义部分 定义一个变量S用来存储两个数的和 接着返回S的值 在main当中调用sum这个函数 sum这个函数是没有输入的 所以说输入这里面什么都不写 调用sum这个函数就会执行 sum这个函数执行完了之后 有一个return s就将S这个结果输出 那么外面就一个Y来接收这个结果

### **[05:01]**
sum这个函数是一个没有输入 但是有输出的函数 既然是有输出 那么必须通过return这条语句将数据输出 return这条语句除了输出数据之外 它还有一个作用 就是跳出这个函数 如果在函数内return s后面还有其他的语句 那么就不会执行了 直接从return s这里跳出函数了 至于return的作用 我们下节课再详细的说 这种书写方式 就是通过全局变量实现函数内数据的输入 比如这里通过全局变量输入了A和B 它的输出接口是封装好的 接着我们看第四类有输出 有输入的函数 在main里调用sum这个函数

### **[06:02]**
它是由两个输入的 也就是将二复制给I 将三赋值给K 将外界的数据赋值给接口变量 那么就能在函数内部使用外界的数据了 两个数相加的和赋值给T 接着return t也就将T的值输出 那么外面有Y来接收这个值 这种书写方式 它的输入和输出接口都已经封装好了 接着我们将这四种不同的书写方式 但是都是实现同一个功能 也就是求两个数之和写到一个程序里 这里带大家把程序过一下 如果想练习的话 可以参考一下 首先是四个函数的声明 然后定义一个变量 A用来接收第一个函数相加的结果 定义一个变量 B用来接收第二个函数相加的结果 定义一个变量C接收第三个定一个变量

### **[07:04]**
D接收第四个 其他的变量都是在不同的函数当中 需要使用的加数 第一类没有输出 也没有输入 第二类没有输出 有输入 第三类有输出 没有输入 第四类有输出 也有输入 接着来到main函数 调用这四个子函数 然后将四个函数的输出结果ABCD分别输出 查看一下好 这节课到此结束 拜拜

