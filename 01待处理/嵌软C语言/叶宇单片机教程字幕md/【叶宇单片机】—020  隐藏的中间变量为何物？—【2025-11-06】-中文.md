# 【叶宇单片机】—020  隐藏的中间变量为何物？—【2025-11-06】-中文

## 字幕正文

### **[00:00]**
大家好 今天学习的是第20节 隐藏中间变量为何物 隐藏的中间变量 也就是两个数的运算结果到底是什么 先说一个结论 在硬件层面只能处理同一类型的数据 也就是八位加八位 16位加16位 32位加上32位 它们的类型都是相同的 比如八位加上八位输出的结果也是八位的 16位加上16位输出的结果也是16位的 32位加上32位 输出的结果也是32位的 另一个硬件层面决定的 就是计算结果的类型与加数的类型是相同的 我们这里只是讨论一下 使大家稍微理解这个结论 从硬件的角度看 两个数相加得到一个结果 它的结果有类型 有大小 结果的类型与加速的类型是相同的 注意赋值它只提供一个空间

### **[01:03]**
将数据存到这个空间当中 不改变数据的类型 假如说这是一条语句 把它翻译成指令之后 加法指令是先计算的 最后才是赋值指令 所以赋值指令不影响运算结果的类型及大小 从硬件的角度看 只能处理同一类型的数据 从C语言的角度看 它可以处理不同类型的数据与变量 编译器需要通过一套规则 自动将不同类型转换为同一类型 再交给硬件执行 这就是算术转换规则的使用 这个转换规则是编译器自动执行的 我们是看不见的 算术转换规则就四个字 提升与对齐 提升就是类型提升 在做运算的时候 如果类型低于on sign a int或者是signer int类型

### **[02:00]**
编译器会自动提升为这两个类型 先提升为signer int类型 如果装不了 然后再转成unsign int类型 公共类型转换就是经过提升后 如果两个加速的类型还是不一样的 那么编译器就会自动将类型等级低的 转换成类型等级高的 那么两个加速的类型就相同了 就可以给硬件去计算了 这个相同的类型我们也叫做公共类型 也就是运算结果的类型 如果加速的类型等级是char类型 或者是short类型 它是低于int类型的 就要转为int类型 但是int类型里有两个 一个是unsigned int 一个是sign int 首先转换成sn int 因为它表示的数小一点 如果装不下的话 再转换成unsigned int 提升之后 如果加速的类型还是不相同 比如一个是unsign int类型

### **[03:02]**
一个是unsigner lg类型 unsigned int类型 就会再自动转换成UNSLG类型 向高等级类型对齐 如果两个加速是同一个等级的 一个是有符号的 一个是无符号的 那么就将有符号的转换成无符号的 我们看一下类型等级 首先是int家族 然后是我们熟悉的char家族 然后short short家族就是短整型 short int longer家族就是长整型 Long int 每个家族都包含两个类型 一个是无符号类型 一个是有符号类型 什么是有符号类型 什么是无符号类型 像这里的八位 它全部用来表示数字 就是无符号类型 有符号类型就是用最高位来表示符号 剩余位用来表示数值

### **[04:00]**
那么有符号类型的容量就相对小一点 无符号类型的容量就相当于大一点 类型的提升与对其规则我们已经了解了 但是有的时候加速是一个常数 那么常数是什么类型呢 如果常速没有后缀时 由最想能容纳其职的类型确定它有两个类型 一个是int类型 16位的 它的范围是负的3万多到正的3万多 还有一个lg类型是32位的 它的范围是负的2亿多到正的2亿多 我们来举一个例子 比如有一个3万 它的范围是在负的3万多到正的3万多 这个范围之内 所以说3万的类型就是int 有一个5万可以在这个范围之内 所以它的类型就是long类型 如果一个常数它有后缀 然后看一下后缀就知道是什么类型了

### **[05:02]**
现在无论加速是变量还是常量 它们的类型我们都清楚了 记住算术转换的规则 四个字 提升与对齐 然后我们来练习几个题目 练习一定义一个变量A执行这条语句 这是一条纯赋值语句 跟预算没有关系 赋值符号的左边提供一个空间来装右边的数据 装得下就装 装不下就把它丢掉 这里A提供的空间是八位的 所以这里16位的数据A只能装低八位 就是0X36 执行完这条语句 就将0X36放到A的空间当中 那么高把位就会被丢掉 练习题二 定义一个变量B现在来执行这条语句 我们来看一下编译器是怎么使用算术规则的 B是on signchar类型 它会提升为int类型 一它是int类型

### **[06:01]**
现在两个加速的类型是一样 就可以计算了 B本来是八位的 现在提升为16位 前面补零一 现在提升为16位 前面补零 它的计算结果是16位的 是int类型的 那么B加一之后就得到一个结果 就得到一个结果 这个结果有类型 有大小 B提供的空间只能存储八位的 那么只能装得下这里的低八位 执行完这条语句之后 就将低八位放到B空间当中 高八位就会被丢掉 有同学说这个和上节课好像讲的不一样 上节课也是先执行运算规则 不过执行运算规则之后又简化了指令 所以说上节课的过程是简化了的过程 这节课的过程是完整的 定义一个变量C执行这条语句 我们看一下编译器是怎么使用运算规则 C的类型是on sana char

### **[07:00]**
提升为int类型 二的类型是int 那么现在两个加速的类型是一样的 就可以计算了 C本来是八位的 现在是16位 前面补零二 现在是16位 前面补零得到的计算结果也是16位的 它的类型是int类型 也就是执行C加二之后得到一个结果 这个结果有大小有类型 将结果赋值给CC提供的空间只有八位的 所以执行完这条语句之后 只能将第八位存到C的空间当中 高八位就被丢掉了 练习四定一个变量D执行这条语句 D的类型是arsa int类型 五的类型是int类型 现在两个加速的类型不一样 因为硬件只能执行相同类型的数据 所以说在执行类型对齐 将int类型转换为unsigned int类型 现在两个加速的类型是一样的

### **[08:02]**
就可以计算了 D本来就是16位的五 现在提升为16位 前面补零得到的结果是16位的 结果是按三的int类型 还有一个进位位 因为结果是unsign int类型 结果只能装下16位 所以说近位位就会被丢掉 也就是执行D加五之后得到这样一个结果 有大小有类型 D加五之后 将值赋值给DD的空间是16位的 所以说执行完这条语句之后 将这里的16位放到D的空间 注意这里的进位早在赋值之前就已经丢掉了 练习题五定一个变量E执行这条语句 我们看一下编译器是怎么使用运算规则的 就是先提升再对齐 E的类型是unsigned long类型 五的类型是int类型

### **[09:01]**
这两个类型不一样 因为硬件只能执行相同类型的数据 所以要将int类型转化为unsign log类型 现在两个加速的类型一样就可以计算了 E本来就是32位的五 提升之后是32位的 前面补零运算结果是32位的 结果的类型是unsigned long类型 还有一位进位位 因为结果是on suna lg是32位的 所以说只能装得下 这里的32位近位就会被丢掉 也就是执行E加五之后得到一个结果 这个结果有大小 有类型 E加五之后 将结果赋值给EE有32位的空间 所以说执行完这条语句 就将这里的32位放到E的空间当中 注意这里的进位早在复职之前就已经丢掉了 练习题六

### **[10:00]**
定义一个变量A定义一个变量X1 定义一个变量Y1 我们来执行这条语句 我们来看一下编译器是怎么使用运算规则的 先提升再对齐 X1的类型是on signature Y1的类型是on3char都是小于int家族的 所以说需要提升为int类型 现在两个加速的类型 一样就可以计算了 X1本来为八位的 现在提升为16位 前面补零 Y1本来为八位的 现在提升为16位 前面补零得到一个结果是16位的 是int类型的 也就是X1加上Y1之后得到一个结果 这个结果有大小 有类型 将这个结果放到A的空间 A提供的是16位空间 所以执行完这条语句 就可以将这16位放到A的空间 练习题七定义一个变量 B定义一个变量X2

### **[11:01]**
定义一个变量Y2 执行这条语句 我们看一下编译器是怎么使用运算规则的 先提升后对齐 xi的类型是unsigned int类型 YI的类型是ASSIGNCHAR类型 on snchar类型小于int家族 所以说需要提升为int类型 现在两个类型不一样 需要对齐 将int类型提升为unsigned int类型 现在两个加速的类型一样了 都是unsigned int类型就可以计算了 xi xi本来就是16位的 YI本来是八位的 提成为16位 前面补零得到的结果是16位的 结果的类型是unsigned int类型 注意还有一个近位位 但是这里的结果是unsigna int类型 只能装得下16位 所以近位位就会被丢掉 也就是执行XI加上YI之后得到一个结果

### **[12:00]**
有大小有类型 SI加上YI之后 将结果赋值给BB 提供的存储空间是32位的 也就是执行这条语句之后 将这16位赋值到32位空间里 注意这里的进位位 早在赋值之前就已经被丢掉了 虽然说B空间里有剩余的空间 但是也存不到这个均位练习题八 定义一个变量C执行这条语句 这里5万的类型是lg 5万的类型是lg 两个加速类型是一样的 就可以计算了 这个是5万的16进制 然后提升为32位 前面补0+50000的16进制 提升为32位 前面补零得到的结果是32位的 是lg类型的 也就是执行完5万+50000之后得到一个结果 有大小有类型

### **[13:00]**
5万+50000赋值 给CC提供的存储空间是32位的 执行完这条语句之后 就可以将这32位放到C的空间当中 练习题九 定义一个变量 D定义一个变量 E定义一个变量X3来执行这条语句 我们看一下编译器是怎么使用运算规则的 先提升后对齐 X3是unsign int类型 3万是int类型 两个类型不一致 要对齐 像unsign int对齐 两个加速的类型都是unsign int类型 那么就可以计算了 X3本来就是16位的 5万+30000是16位的 得到的结果是16位的 结果的类型是assigna int类型 注意这里还有一个进位位 但是结果的类型是16位的 只能装得下 这里的16位金门卫就装不了 就丢掉了 那么执行X330000得到一个结果

### **[14:02]**
有大小有类型 执行X330000 将结果赋值给DD 提供的是32位的存储空间 所以说执行完这条语句之后 将这里的16位放到32位的空间里 复制到32位的空间里 注意这里的近卫位在赋值之前就已经被丢掉了 虽然说D的空间很大 但是也没办法存这个进位位练习题 第九题还有一条语句 就是执行这条语句 X350000赋值给E 我们看一下编译器是怎么使用运算规则的 先提升后对齐 X3的类型是unsigned int类型 one的类型是lg类型 那么像高等级的类型对齐 那么assa int类型就会变成lg类型 所以说两个加速的类型都是lg类型 现在两个加速的类型一样就可以计算了 X3本来是16位的5万

### **[15:01]**
现在提升为32位 前面补0+50000 这个是5万的16进制 也需要提升为32位 前面补零得到的结果是一个32位的类型 是long个类型的 那么X350000之后得到一个结果 有大小有类型 X350000之后 赋值给EE提供的存储空间是32位的 那么执行完这条语句之后 就将这32位复制到E的空间当中 这就是编译器执行的提升与对齐 对最后结果的影响 有人说我们的项目很复杂 担心自动提升规则不明确 那就使出我们的必杀技 为了避免出现意想不到的错误 在实际的项目当中 把所有参与运算的变量都预先转化为UNSIGN log类型的变量 再参与运算 在硬件层面也只能处理同一类型的数据

### **[16:02]**
避免了自动转换 还有就是当遇到有争议的问题的时候 与其参与争议越陷越深 还不如想办法及时抽身绕开争议 可能以后再回头看的话 这些问题就迎刃而解了 之前我们有一个练习题七 如何将练习题七的变量全部都改成on sallog呢 我们来试一下 定义一个变量F定义一个xi定一个YI 定义一个T定一个I 将零赋值给T就是将T的空间清零 将xi赋值给T 这就是一条纯赋值语句 没有计算 只要保证T能够装得下XI就可以了 将零赋值给二 还是一样 将I的空间清零 将Y2赋值给二 还是一条纯赋值语句 没有任何计算 只要保证I能装得下

### **[17:00]**
YI就可以执行T加上二赋值给F 这里的T是unsigned long类型 R是啊sa long类型 加速类型是相同的 就可以直接计算了 不会发生自动转换变量F也是啊 sign a long类型 那么复制变量也足够的大 足够的能够装下 这里就把之前的XI是assigna int类型 YI是arsa char类型 不同类型的变量进行相加 转成了同一种变量 也就是on sallong类型进行相加 将练习题第九题的变量都转换成assa log变量 首先是定义一个变量 G定义一个变量H定一个变量X3 定义一个变量T将零赋值给T将X3赋值给T 这里只是一条纯复制语句 保证T能够装得下X3就可以了 然后T加上3万赋值给G

### **[18:03]**
T的类型是on sallang类型 那么3万也会被转为on sallong类型 所以说两个加速的类型都是on salong变量 G也是on sa longer类型 能够足够装得下 那么下一条语句T加上5万 T的类型是on3的long类型 5万也会被转化为on3的long类型 两个加数都是on3的lg类型 变量H也是on3的lg类型 能够足够装得下 所以说核心的原则就是确保加速类型足够大 接着补充一些连加 我们知道加法运算它是从左往右计算的 那么我们就从左往右进行计算 先是加速一加上加速二 它肯定是遵循那个运算规则的 先提升后对齐 然后得到一个结果 它是有一个大小 有一个类型的

### **[19:00]**
然后拿这个结果再以加速三进行相加 它也遵循的是先提升再对齐 之后又有一个结果 这个结果还是有大小有类型的 然后再拿这个结果与加速 后面的加速相加 它也是遵循先提升后对齐 然后得到一个结果 结果还是有大小有类型 就这样一步步进行连加的 然后把最后的结果再赋值给这个保存变量 还是在我们的程序模板上进行练习 首先是定义一些变量 然后是讲解几个例子 这几个例子前面我们已经讲过了 最后就是把结果输出到电脑上看一下 现在来讲第一个例子 X1加上Y1赋值给AX 一的类型是阿三的char Y1的类型是阿三的char 两个加速需要提升为int类型

### **[20:00]**
结果也是int类型 那么X1加上Y1就是零 XF10X12等于零 X110 因为结果是int类型的 所以说0X110被完全的保存下来 那么把0X110赋值给A A的类型是unsigned int 所以A的值是0X110 第二个例子 xi加上Y2复制给b xi是an3的int类型 YI是按三的char类型 两个加速 经过类型的提升与对齐 最后的公共类型是unsigned int类型 结果也是unsigned int类型 xi加上Y2就是0XFFF10X12 得到的结果是0X10010 但是结果是16位的 所以说实际相加的结果 我们取第16位就是0X0010

### **[21:00]**
把0X0010复制给BB的类型是on sallog 空间足够大 完全能够装得下 所以B的值是0X0010 第三个例子 5万+50000 因为5万的类型是lg类型的 所以5万5万0S186A0 结果会被完全的保存下来 然后把0S186A零复制给C C的类型也是lg类型的 所以说C的结果是0X186A零 第四个例子X330000 I3是unsigned int类型 3万是int类型 经过提升两个加速的类型都是R3的int类型 结果也是assign int类型 那么5万+30000 结果是0S13880 那么因为结果是assign int类型是16位的 所以只能保存低16位 实际结果是0S3880

### **[22:00]**
那么将0X38880赋值给D D的类型是unsign long类型 所以说D的值是 所以说D的结果是0X3880 S350000赋值给EX 三的类型是A3的int类型 5Y是lg类型 最后经过提升与对齐 两个加速的类型都是lg类型 那么X3是5万 就是5万+50000 结果是0X186A零 它的类型是lg类型 所以说结果会被完全的保存 将0X186A零复制给E E的类型也是lg类型 所以说E的结果是0X186A零 对第二个例子进行改进 就是将加速都变成unsigned long类型 再进行相加 把零赋值给TT的类型是unsigna lg类型 把xi赋值给t xi是an3的int类型 然后赋值给t on3的lg类型 这是一条纯赋值语句

### **[23:01]**
没有计算 只要保证T足够容纳得下 就可以将零赋值给II的类型是ASSALLOG类型 将YI赋值给IYI是阿三的char类型 I阿三的lg类型 然后T加上R赋值给F T的类型是R3的lg类型 R的类型是阿三的lg类型 那么就避免了编译器的自动转换 两个结果相加 就是0XFFFE加上0X12等于零 X10010 因为结果是unca log类型 所以说0X10010会被完全的保存下来 将0X10010赋值给FF是啊sa long类型 所以说F的结果就是0X10010 对第四个例子进行改进 也就是把加速转换成unsigned long类型

### **[24:01]**
把零赋值给T 把X3赋值给TX3的类型是unsign int类型 然后复制给TT是阿三的long类型 T加上3万 T是ASSLG类型 然后3万是int类型 最后的公共类型是嗯SALLANG类型 所以说T加上3万就是5万 加上3万 结果是0S13880 那么结果是on sa long类型 所以说结果会被完全的保存下来 将0X13880复制给GG是R3的lg类型 所以说G的结果就是0X13880T 加上5万赋值给HT的类型是阿三的lg类型 5万的类型是long个类型 那么公共类型就是on sallang类型 T加上5万就是5万 加上5万等于0S186A零 结果的类型也是阿三的lg类型

### **[25:02]**
会被完全的保存下来 那么将0S186A零复制给HH 也是阿三的lg类型 所以H的值就是0S186A零 那么我们将上面的结果进行输出 输出的结果我已经放在后面 等会我们下载到单片机当中对一下就可以了 现在把程序恢复到正常状态 然后点击保存 点击编译 零警告零错误 现在下载程序芯片型号 扫描串口最低波特率2400 最高波特率9600 打开程序文件 找到我们这节课的代码 点击下载 第一个数16进制110 对的第二个数16进制是一零 第三个数16进制是186A0 第四个数16进制是3880 第五个输入16进制是186A0 第六个输入16进制是10010

### **[26:00]**
第七个数16进制是13880 第八个数16进制是186A0对的 这节课到此结束 拜拜

