# 【叶宇单片机】—019  加法运算的溢出—【2025-10-31】-中文

## 字幕正文

### **[00:01]**
大家好 今天学习的是第19节加法运算的溢出 理解什么是溢出 这是我们数学里的加法 有一位相加 两位相加 组成部分有加数进位 还有一个输出结果 如果我们想要设计一个简易的加法器 就要往这几方面靠 这是一个简易的八位加法器 操作过程是由控制器控制输入两个八位的加数 经过加法器的计算 输出一个八位的结果和一个进位位 五一系列的单片机运算器也是八位的 也只能处理八位的数据 我们用这个简易的加法器来做一做加法计算 先说好 我们只能计算八位的 比如这里两个数相加得到八个一 我们用加法器来算一下 输入两个八位的加数 经过加法器计算 输出一个八位的结果和一个进位位 八位的结果是0XFF

### **[01:01]**
我们将这八位的结果存到一个单元里去 那么下次的计算就不会影响到这个结果 进位位也存在进位位单元 虽然它们是分开存储的 但是计算的结果是正确的 我们来计算一个最大的八位 就是八个一 加上八个一 得到的结果是九位的 我们用加法器来计算一下 输入两个八位的加速 就是八个一和八个一经过加法器计算 输出一个八位的结果和一个进位位 八位的结果就是0XFE 我们将0XFE存在某个单元里 将近位位存在进位位上 虽然它们是分开存放的 但是放在一起也是酒味的 计算的结果是正确的 这说明无论什么样的八位数给这个加法器 它都能算对 现在我们来看看如何用这个八位加法器 计算八位16位 32位的数据 以及理解什么是溢出

### **[02:00]**
练习题一 我们说过存储器的单元都是由字节组织的 也就是每个存储单元都是八位的 定义一个变量A就是申请一个字节的空间 我们来执行这条语句 将0X8536赋值给A0X8536 它是16位的 A申请的空间只有八位 那怎么办呢 这个时候A只接收第八位 就是0X36 那么执行完这条语句之后 就将0X36复制到A的空间里 这里本来要存储16位的 但是实际上只存储了八位 所以说结果是错误的 这种错误就是由溢出导致的 溢出 就是指结果超过了当前数据类型表示的范围 导致结果出错 但是这种溢出在编译阶段就会被发现 就会被处理 还到不了加法器练习题二 定义一个变量B申请一个字节空间

### **[03:02]**
初始值是0XFF我们来执行这条语句 B加上一赋值给B 这个是一个比算的过程 两个八位数相加得到一个九位的结果 我们用加法器来算一下 输入两个八位的加速 0XFF和0X01经过加法器的计算 输出一个八位的结果和一个进位 计算的结果是正确的 一共是九位 我们将这里输出到八位 0X00存储在某个单元当中 那么执行完B加一之后 得到一个九位的结果 要存储在B当中 但是B的空间只有八位 怎么办呢 这个时候只会将0X00存到B的空间当中 那么进位位就没有空间给它存储 所以说实际是九位 结果只存储了八位 导致结果出错 这也是由溢出引起的 也就是结果超过了当前数据类型的表示范围

### **[04:02]**
导致了结果出错 练习题三 定义一个变量 C申请了一个字节的空间 初始值是0XFF来执行这条语句 C加上二赋值给C 这是一个比算的过程 两个八位数相加得到一个九位的结果 我们用加法器来算一下 输入两个八位的加速 也就是0XFF和0X02经过加法器的计算 输出一个八位的结果和一个进位位一合在一起 一共是九位的 计算的结果是正确的 我们将这八位 也就是0X01存储到某个单元当中 那么下次的加法运算就不会对它有影响了 执行完C加I我们得到一个九位的结果 要存储在C单元当中 C只有八位怎么办呢 这个时候我们只会将0X01 存到C的存储单元当中 那么进位位就没有空间给它存储了

### **[05:00]**
实际计算结果是九位 但是只存储了八位 所以结果是错误的 这也是由溢出导致的 练习四 定义一个变量D申请两个字节的空间 初始值是0XFFFE 我们来执行这条语句 将D加上五赋值给D 我们的加法器是八位的 只能计算八倍的加数 但是这里的D是16位的 怎么计算呢 我们将D的16位分成两个八位 将0X05也分成两个八位 然后分别计算 先计算第八位就是0XF10X05 先计算第八位的结果是 0X03和一个进位位一 我们用加法器来算一下 输入两个八位的加速 分别是0XF1和0X05 然后经过加法器计算 输出一个八位的结果和一个进位位 一输出的结果是正确的 我们将八位的结果就是0X03

### **[06:03]**
存储到某个单元当中 那么就不会影响到下次的计算 接着我们计算高八位 0XFF加上0X00 注意还要加上进位位一 所以说是0XFF加上0X00 然后再加上进位位一 手动算出来的结果是0X00和一个进位位一 我们用加法器来算一下 输入两个八位的加速 就是0XFF和0X00 还有一个进位位一 那么经过加法器计算 输出一个八位的结果和一个进位为一 输出的结果是正确的 我们把八位的结果存储在某个单元当中 也就是0X00存储在某个单元当中 那么就不会影响到下次的计算 执行完了D加五之后 保存了一个0X03 0x00 还有一个进位位一 一共是17位的结果 我们要赋值给D 但是D只有16位的

### **[07:01]**
怎么办呢 我们把0X03和0X00 存入到D的空间当中 那么进位位就没有存储它 这个时候结果是17位的 但是只保存了16位 所以结果是错误的 这也是由于溢出引起的练习题五定义一个变量 E申请了四个字节的空间 它的初始值是我们来执行这条语句 E加上五赋值给E 我们的加法器是八位的 只能计算八位的数据 但是E是32位的 怎么计算呢 我们将E分成四个八位的 将0X05也分成四个八位的 先计算D8位 也就是0XF10X05 0x f 1+0 x05 我们手动计算的结果就是 0X031个进位位 我们用加法器计算一下 输入两个八位的加速 0XF1和0X05

### **[08:00]**
经过加法器计算 输出一个八位的结果和一个进位位 输出的结果是九位的 是正确的 我们将八位的结果 0X03存储到某个单元当中 那么就不会影响到下次的计算 接着计算0XFF加上0X00 注意还有一个进位位 一也要一起相加 那么0XFF加上0X00加上进位为一 我们手动算的结果是0X00和一个进位为一 那么我们用加法器算一下 输入一个加速 0XFF输入一个加速0X00 还有一个进位位 经过加法器的计算 输出一个八位的结果和一个进位位 一输出的结果是正确的 我们将八位的0X00存储到某个单元当中 接着我们计算0XF加上0X001 这个是比算的结果 0X00和一个进位位一 我们用加法器相加

### **[09:01]**
输入0XFF和0X00和一个进位位 一输出的结果就是0X00和一个近位位 一输出的结果是正确的 我们将八位的0X00存储到某个单元当中 接着再来计算 0XFF加上0X00加上金元位一 这个是手工计算出来的 结果是0X00和进位为一 那么我们用加法器进行计算 就是输入0XFF和0X00 还有一个进位位 一 经过加法器计算 输出的结果是八个零和一个进位为一 结果是正确的 我们将八个零 也就是0X00存储到某个单元当中 那么执行完E加上五 得到的结果就是0X030X000X00 0X00和一个进位位一 这些数据我们都把它存储好了 存储在这里有一个进位位一 那么执行完E加五之后

### **[10:00]**
我们得到了一个33位的结果 要把它赋值给EE申请的空间只有32位 怎么办呢 这个时候我们会将这四个八位 存储到E的空间当中 进位位就没有存储 实际结果是33位的 但是你只存储了32位 所以结果还是错误的 这还是由于溢出引起了错误 我们从上到下计算了半天 结果都是错误的 怎么办呢 那就看下一节吧 这一节主要是理解什么是溢出就可以了 理解为什么会出错 才能避免这样的错误 现在写个程序验证一下这种错误 还是在我们的程序模板上进行练习 定义几个变量AABCB的初值是零 XFFC的初值是零 XFF定义两个变量 D和ED申请两个字节的空间 然后有一个初始值 E申请四个字节的空间 有一个初始值 然后将0X8536赋值给A

### **[11:02]**
将B加一赋值给B 将C加二赋值给C 将D加五赋值给D 将E加五赋值给E 这五个题目就是刚才我们所说的五个题目 这里就不讲了 不理解的话再看一下 然后我们输出ABCDE的结果 A的结果是0X36 B的结果是零 C的结果是一 D的结果是三 E的结果是三 然后把程序下载到单片机 验证一下这几个由于E出问题而得到的结果 保存编译零警告 零错误 第一个数16进制是36 第二个数是零 第三个数是一 第四个数是三 第五个数是三 那么单片机输出的结果 和我们分析的结果是一致的 都是由溢出而得到一个错误的结果 这节课到此结束 拜拜

