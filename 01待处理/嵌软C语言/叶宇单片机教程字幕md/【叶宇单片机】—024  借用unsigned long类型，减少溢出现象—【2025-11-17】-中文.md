# 【叶宇单片机】—024  借用unsigned long类型，减少溢出现象—【2025-11-17】-中文

## 字幕正文

### **[00:01]**
大家好 今天学习的是第24节 借用UNSLG类型的中间变量 可以减少溢出现象 先回忆一下算数转换的规则 也就是四个字 提升与对齐 在提升与对齐当中 如果是类型等级低的 转为类型等级高的 比如八位转为16位 如果是正数和零 前面就补零 如果是负数 前面就补一提升与对齐 还有数值 前面补零和补腰都是在编译阶段影视完成的 我们来看一下复数类型的提升 比如八位的有符号数一负二负三 将八位的提升为16位的 无论是提升为16位的signer int类型 就是有符号类型还是unsigned int类型 就是无符号类型 它前面都要是补一 但是对于同一串数据不同的类型 它的结果是不一样的

### **[01:00]**
比如这一串数据如果按照sn int类型就是有符号来解读的话 它是二 如果按照on sound int无符号的类型来解读的话 就是6534 同样将八位提升为32位 前面也是补的是一 总而言之 对于复数的提升 无论提升为有符号类型还是无符号类型 前面补的都是一 对于正数与零的类型提升 比如八位的无符号数123将它提升为16位 或者是提升为32位 前面补的都是零 我们举一个例子 定义两个变量B的初始值是二 负数是以补码的方式存储的 所以说存储的是零 XF1是二 现在来执行这条语句 我们看一下类型的提升与对齐 A的类型是USA Int B的类型是sand的

### **[02:01]**
CHR小于int家族 首先提升为int类型 B是二 提升为int类型 前面要补腰 int类型与unsigned int类型又不一致 所以说将int类型又提升为unsigned int类型 数据是不发生变化的 这时候int是16位的 按三个int也是16位的 不需要补零或者是补腰 所以说数据没有发生变化 如果把A改成on sallang类型呢 我们来看一下这条语句 A是on slg类型 B是signer char类型小于int家族 首先提升为int类型 因为B是二 所以说前面补腰int类型和UNSLG类型类型不一致 所以说将int提升为on sallob 提升为int类型 按照int类型解读 这个数据是二 那么再将二提升为on3的lg类型

### **[03:00]**
所以说前面还是补腰 所以说对于负数的提升与对齐 前面补的都是腰算术运算 有的时候还会出现在条件判断当中 比如这里条件的结果非零 就是真的 如果是真的 就会执行大括号里这些语句 如果是假的 就不执行这些语句 定义一个变量 C是二 定义一个变量E是三 我们知道负数在存储器当中是以补码的方式存储的 所以说三的补码是0XFFFD 我们先来大概判断一下这条语句是真的还是假的 C加上E大于零 C是二 E是三 2+-3=-1 一大于零 那么条件是假的 就不会执行大括号里的语句 真的是这样的吗 我们来分析一下 C加上EC是assign int类型

### **[04:01]**
E是sign int类型 要提升为啊sign int类型 因为三的int是16位的 按send int也是16位的 所以说16位提升为16位 不需要补零 也不需要补腰 那么E的值还是0XFFFD 现在C和E的类型都是unsigned int类型 那么C加上E就是0X02 加上0XFFFD得到的结果是0XFFFF类型是unsigned int类型 那么C加E得到一个结果 有大小有类型 按照这个类型来解读 ff ff ff f就是6535 C加E的结果就是65535是大于零的 所以说大括号里的语句是需要执行的

### **[05:00]**
所以这与我们刚开始判断的结果是截然不同的 如果在项目中遇到 就是一个很麻烦的问题 而且这个问题很难查找 因为它的类型变换是隐式完成的 我们知道算数转换规则是我们写完程序之后 在编译阶段影视完成的 那么这就要求我们要时时注意 可能一不留神没有注意到数据的类型 就会造成结果溢出的错误 或者是把有符号的数提升为无符号的数 也可能会使结果发生错误 那么怎么办呢 就回到了我们今天的主题 借用on slg类型的中间变量来减少错误 首先是避免在编译阶段影视类型的转换的问题 如何避免呢 如果我们在算术当中使用同一类型的话 就不会发生隐式类型的转换 还有就是要减少溢出 如何减少溢出呢 就是使类型表示的范围足够大

### **[06:02]**
所以说这个时候我们借用unsigner lg类型 例题三 我们定义三个变量来执行这条语句 在这个计算过程当中 三个变量三个类型在编译阶段就一定会发生隐式的类型转换 这个时候我们就要引入on send lg类型作为中间变量 左边是原来的程序 右边是我们修改后的程序 我们来看一下 我们本来定义一个变量A但是担心隐式类型转换 所以说重新定义一个变量 这就相当于重新造一个大房子 然后再把数据放进来 我们定义一个变量X我们担心算术转换过程当中会发生隐式类型转换 所以说又定义了一个unsigned long类型的变量 还是重新造一个大房子 然后再把数据放进来 最后定一个变量Y类型是on slg类型

### **[07:04]**
将T清零 因为T是32位的 里面的数据是不确定的 假如16位的数据赋值给32位 可能只覆盖了第16位的空间 刚刚16位可能没有处理 那么里面的数据就是不确定的 所以说要把T清零 将X赋值给T 也就是将X的数据195放到T的空间里 因为T的空间足够大 在运算的过程当中就不会发生溢出的现象 将S清0T减Y赋值给S 这个时候T是UNSLG类型 Y是UNSLG类型 S是on3的lg类型 那么执行这条语句的时候 就不会发生影视的类型转换 因为它们的类型都是一致的 都是on sa long类型 而且也足够大 on3的lg类型是零到42亿多

### **[08:00]**
那么溢出的可能性就比较小 将计算后的结果赋值给A 其实复制之前可以加一个条件 判断判断是否溢出了 就是是否超过A的范围了 我们知道算术转换规则很重要 但是毕竟是在编译阶段 影视完成的 有可能一不留神就会出错 所以说要借用on sa lang类型的中间变量 这也是实战项目经验的总结

