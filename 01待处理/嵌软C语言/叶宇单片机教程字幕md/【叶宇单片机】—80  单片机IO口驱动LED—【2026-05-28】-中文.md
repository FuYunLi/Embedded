# 【叶宇单片机】—80  单片机IO口驱动LED—【2026-05-28】-中文

## 字幕正文

### **[00:00]**
这节课我们讲一下单片机L口驱动led灯 从这节课开始 我们重点讲的是程序框架 所以说对五一单片机的原理部分 可能讲的比较少 首先led是一个发光二极管 这是我们常见的 它还有一种贴片封装 然后它是有正负极的 我们可以通过它的引脚长短 来分辨它的正极和负极 也可以通过内部的电极大小 来分辨它是正极还是负极 这是它的电路符号 它的正极接电源的正极 负极接负极才会有电流通过 否则是不会有电流通过的 那么我们来看一下开发板上的led是怎么连接的 这里有两个l led模块 一个是四个并排的led灯 一个是八个并排的led灯 这是led灯 它的正极接到电源的正极 它的负极接到单片机的引脚上 单片机引脚可以输出高低电平

### **[01:01]**
那么在我们这里 高电平一对应的是五伏低电平 零对应的是零伏 如果引脚输出低电平 也就是零伏 那么就可以点亮这个led灯 如果输出的是高电平 那么这个led灯就熄灭 这里还有一个限流电阻 那么现在的问题就是 如何控制单片机的引脚输出高低电平 这是单片机的最小系统 也就是单片机能工作的一个基本要求 刚才的r led灯分别接在这几个引脚上 那么我们发现这里的P0引脚有一点特别 就是他接了一个上拉电阻 因为我们想让P0引脚输出高低电平 它输出低电平的时候是可以正常输出的 但是它输出高电平的时候 它是一个浮空状态 是一个高阻态 也就是没办法正常输出高电平 所以说这里接一个上拉电阻 才可以正常的输出高电平 只有P0是这样的 其他的引脚不需要接上了

### **[02:01]**
然后我们想的是 单片机内部如何输出高低电平呢 我们来看一下单片机内部的一个结构图 这里的模块是不是比较多 但是我们去掉一些CPU的模块 那么剩余的一些模块就是我们需要关心的 比如这里有ram定时器串口 看门狗 还有输入输出接口 时钟控制单元 把它们整合一下 就是我们在学五一单片机的时候 经常看到的一个图 但是我们还是在这张图上来讲 这里有很多模块 各个模块之间是通过总线连接在一起的 总线上一直有数据在传送 他很忙碌 我们重点关注的是这个模块 假如总线上有数据传输给这个锁存器 我们也可以叫做寄存器 那么数据所存在这里

### **[03:00]**
它与总线就处于一个隔离的状态 也就是总线上 现在无论传什么数据对它都没有影响了 然后由于这里的驱动能力弱 就加了一个驱动器 也就是增强信号 增强信号之后再通过引脚将这些数据输出 也就是输出给外部设备来享用 比如led按键 显示器传感器等等 因为CPU它处理数据是非常快的 所以说我们把我们需要的数据锁存下来 通过锁存器锁存下来 然后传输给外部设备 因为外部一般都是一些慢设备 然后慢设备它慢慢的去享用 现在的问题就是 我们如何输出这一个字节的数据 首先是写一个main函数 这是我们程序的入口 然后附一个字节的数据给P0端口 也就是高四位是一 低四位是零 分别对应P0的八位 输出一的时候也就是输出五伏 那么led灯是灭的

### **[04:00]**
输出零的时候也就是输出零伏 这个时候led灯是亮的 接着写 while有循环 也就是让程序在这里空跑 也就是等待在这里 那么这里的P0是什么 P0我们知道它是一个寄存器 我们是往这个寄存器写数据的 但是对于单片机来说 他不知道P0是哪个寄存器 它只知道一个寄存器的地址 那么这里就有一个头文件 将寄存器的地址与P0关联起来 我们来看一下这个头文件 它的主要作用就是给地址起一个名字 比如我们把它叫做P0寄存器 但是单片机叫做0X80 也就是单片机通过地址来识别这个寄存器的 所以说这个图文件的作用 就是给地址起一个别名 方便我们来操作这个寄存器 而不是记所有寄存器的地址 如果记所有寄存器的地址 这样就很麻烦 在单片机的内部 它是通过总线给各个模块传送信息

### **[05:02]**
其实都是按照地址来操作的 然后我们把这个程序下载到单片机看一下 也就是低四位是亮的 高四位是灭的 刚才我们是一次输出一个字节 我们也可以一次输出一位 我们来看一下 首先还是写一个main程序 这是我们程序的入口 然后给P0的第七位附一个零 同样这里P0杠七也是位地址的一个别名 那么我们还要写一句 就是给位地址起一个别名 这里的P0是一个基地址 后面的符号是一个位选择符 也就是选择第七位 那么它的位地址就是0X87起的别名 叫做P0杠七 因为这里用到了P0 所以说还要加入这个头文件 将P0与寄存器的地址0X80再关联起来 接着写一个Y有循环语句

### **[06:00]**
然后让程序在这里跑空语句 也就是等待在这里 然后我们把这个程序下载到单片机上看一下 就是P0的第七位 亮其他位灭 最后我们看一下如何建立一个工程文件 编写这个程序 然后下到单片机当中

