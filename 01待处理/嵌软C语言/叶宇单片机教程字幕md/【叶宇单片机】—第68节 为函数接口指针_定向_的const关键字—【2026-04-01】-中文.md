# 【叶宇单片机】—第68节 为函数接口指针_定向_的const关键字—【2026-04-01】-中文

## 字幕正文

### **[00:01]**
今天我们来看一下CONST关键字 在函数接口的使用 上几节课 我们说在参数列表定义指针变量 那么在函数内进行解引用 就找到外面的一个存储空间 可以对这个存储空间进行读和写 就间接地实现了函数内数据的输入和输出 但是有的时候我们只想输入一个数据 不想输出一个数据怎么办呢 这个时候我们可以使用CONST来定向 首先我们来回忆一下CONST关键字 定义一个普通的变量 它会在ram当中申请一个存储空间 定义一个CONST修饰的变量 那么它也会在ram当中申请一个存储空间 那么这两个变量有什么区别呢 首先这个ram存储器它是可读可写的 那么通过A就可以读写这个存储空间

### **[01:01]**
通过B物理上也是可以读写这个存储空间的 但是他不给你写 因为加了CONST关键字 那么语化层面上它会有一个约束 就是不给你写 比如将100赋值给A那么A里面写的就是100 将100赋值给B 这个时候就会报错 那么这个时候就产生一个问题 怎么给B写数据 首先在定义的时候可以直接对它进行初始化 初始值是80 那么B存储的就是80 还可以使用指针变量 我们知道指针它很灵活 它可以获取别人的地址 然后间接的来访问这个存储空间 我们可以定义一个指针变量 我们取B的地址给他 那么取币的地址是什么类型呢

### **[02:00]**
那么取B的地址 这个类型是在原来B变量这个类型的基础上 加一个星号 我们再看一下指针变量P它是什么类型 那么这两个类型不一致 我们之前说过 指针在赋值的时候要求类型一致 这个时候怎么办 可以使用强制类型转换 那么这个时候P存储的就是B的地址 也就是0039指向的也是0039 接着我们就想到对P进行解引用 也就是打开这个存储空间 那么星P是什么类型的 新P是不是on satcha类型的 这个类型是可读可写的 那么我们将新P赋值给A 也就是读出里面的数据是80 然后存储给A 接着我们将66赋值给新P 也就是写入一个数据是66

### **[03:03]**
那么这样我们是不是也间接地实现了 对B进行写数据 但是在定义变量B的时候 它使用了CONST修饰 它本意上就是不给你写的 虽然说我们这里用指针间接的给他写了数据 但是也不建议这样做 这里只是为了引出另一个指针变量 我们来看一下 再定义一个指针变量 注意这里面有CONST修饰 那么取B的地址是CONSTASSIGNCHA类型的指针 P1也是这个类型的两个类型一致 就可以直接赋值了 那么PE1存储的也是0039 指向的也是0039 这个时候我们对PE1进行解引用 也可以打开这个存储空间 那么我们想一下星P1是什么类型的 就是const on sattr类型的

### **[04:01]**
它是一个只读类型的 那么通过新PE1对这个存储空间只能读不能写 比如可以读取新P1的值 赋值给A 但是不可以将60写入新P1 这样他就会报错 所以我们要注意一下 指针变量截一用之后的类型到底是可读可写的 还是只能读的 讲到现在是不是对指针变量感觉又有一点乱了 但是你只要记住指针变量在赋值的时候 类型一致就可以赋值了 也就是指针变量可以存储地址 但是存储地址不是它的目的 通过这个地址灵活地找到存储空间来使用 才是它的目的 其实最终也就是回到基本的使用方法 按照这样的思路来分析 就不会出错 首先指针变量要做的是不是存地址 存地址我们是会的

### **[05:00]**
只要类型一致就可以直接赋值了 接着是对它进行解引用 要确定解引用之后的类型是什么 是可读可写的还是只能读的 也就是回到了基本变量的使用方法 我们之前对指针变量进行赋值的时候 要求它的类型要一致 但是也有特别的 我们来看一下 这里定义了两个变量 那么取A的地址它是什么类型的 是不是在A类型的基础上加一个星号就可以了 那么我们再看一下P的类型 这两个类型是不是不一致 不可以直接复制 但是这里它会发生一个隐式的转换 也就是on schr指针类型 可以安全地转换成const on schr指针类型 前面的指针类型它解引用之后是可读可写的 哎后面解引用之后的类型是const on snchar类型

### **[06:02]**
它是只能读的 那么可读可写是兼容只能读的 这个时候前面的指针变量可以隐式的转换成 后面的指针变量是合法合规的 那么反过来可以吗 反过来它是不可以的 因为只读它变成了可读可写 他本来就违反了嗯 原来变量的本意 我们来举一个例子 定义一个CONST修饰的普通变量 再定义一个指针变量 那么取B的地址 它的类型是const on类型的指针 P1的类型是ASSIGNCHA类型的指针 两个类型不一致 这个时候可不可以发生隐式转换呢 是不可以的 也就是这两个指针变量反过来 就没办法进行隐式转换了 那么我们这样写会报错

### **[07:01]**
之前我们说指针变量在赋值的时候 要求类型一致 但是有的时候也可以隐式转换 它也是合法合规的 就刚才我们说的那一种 还有一种就是我们来看一下 定义一个普通变量 再定义一个指针变量 们说取A的地址 它的类型是阿SACHA类型的指针 与P的类型是不一致的 那么这个时候它也会发生影视的类型转换 也就是将ASSACHA类型的指针 影式的转换成void类型的指针 那么反过来可以吗 就是普通指针变量和void的指针变量 它是可以相互的隐式转换的 就是不需要类型一致 他自己会影视的转换 同样这里的影视转换它也是合法合规的 现在回到我们今天的主题 我们说有的时候我们只想传入一个数据

### **[08:01]**
而不想改变这个数据 这个时候可以使用CONST来定向 我们来看一下 举一个例子 还是之前的一个程序 定义一个数组 它有四个元素 再定义一个全局变量 用来存储这四个数据的平均值 接着写一个函数 接着定义一个局部变量平均值 它用来存储这四个数据的平均值 然后返回这个平均值 在main函数当中调用这个函数 它有一个输入是一个数组名 那我们说数组名是什么类型 是不是元素类型的指针 同样函数的输入接口也是一个数组 在编译的时候它会退化成元素类型的指针 编译之后也就是这个样子 那么它的类型就是const on sncha类型的指针

### **[09:03]**
你一分析诶 这两个类型不一致 怎么可以直接赋值呢 你要知道它这里是兼容的 这个时候可以直接进行复制 因为下面这个指针变量它解引用之后是可读 可写的 上面这个指针变量解引用之后是只读的 那么它是兼容的 所以说可以直接复制 但是反过来是不可以的 就是将上面的指针类型复制给下面的指针类型 这样会报错的 接着我们看一下变量的内存分配 有两个全局变量 两个局部变量在执行main函数之前 会为全局变量分配存储空间 假如上面是战区 下面是全局数据存储区 那么会为全局变量分配存储空间 接着往下执行程序 遇到函数调用函数 调用函数的时候有一个压榨过程

### **[10:02]**
首先是现场保护 然后是将函数的返回地址压榨 这两个过程我们暂时先忽略 然后为局部变量分配存储空间 存储空间分配好了之后 就可以执行函数 我们说将数组名赋值给Buff 数组名代表的又是数组的地址 那么buffer存储的就是0030 那么Buff加零就是0030是一个地址 Buff加1buff加28F加三都是一个地址 注意这里仅仅是一个地址 只有对它们进行解引用 才可以读写这个存储空间的数据 我们之前说的都是读写这个存储空间的数据 因为它解引用之后都是普通的变量类型 这个变量类型是可读可写的 但是这节课我们解引用之后的类型是CONST 修饰的变量类型 它是只能读的

### **[11:00]**
所以说兼应用之后的类型我们要注意一下 那么对buffer指针进行解引用 就是const on sntra类型 它是只读的 如果我们一不小心给它赋值 这个时候就会报错 那么使用CONST修饰函数 入口参数的指针变量有什么意义呢 首先是可以清晰地展现程序员的目的 只传入数据 不改变这个数据 也就是对指针变量进行解引用 只能读取这个存储空间的数据 不能写入数据 不能改变它的数据 还有一个好处就是确保数据的安全 如果他一不小心修改了就会报错 最后我们来总结一下 首先是回忆了一下CONST关键字 它修饰的变量物理上能读能写 但是语法上约束它只能读 然后是两个合法合规的隐式转换

### **[12:01]**
比如普通的指针变量 可以转换成CONST修饰的指针变量 但是反过来是不可以的 还有一个就是普通变量可以转换成Y的 指针变量 反过来也是可以的 最后就是我们今天的主题 定义CONST修饰的指针变量在函数内进行解引用 也可以找到外面的一个存储空间 但是只能读不能写 也就是定向了 只能传入数据 不能输出数据 好了 这节课到此结束 拜拜

