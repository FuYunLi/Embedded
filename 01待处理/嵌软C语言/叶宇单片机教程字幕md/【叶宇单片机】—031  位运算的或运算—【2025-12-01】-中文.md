# 【叶宇单片机】—031  位运算的或运算—【2025-12-01】-中文

## 字幕正文

### **[00:02]**
大家好 今天学习的是第31节位运算的 货运算位运算的或运算 还是遵循算术转换规则 四个字提升对齐提升已对齐 依据的是类型等级提升 已对齐之后将数据给运算器 运算器是按照公共类型来解读数据的 我们先来说一下什么是按位或运算 这是一个电池和一个灯泡连接成的一个电路 我们用这个电路来理解或运算零或上零 结果是零 相当于两个开关 打开灯泡是灭的状态 零或上一结果是一 相当于一个开关 打开一个开关 闭合灯泡是亮的 一或上零 结果是一 相当于一个开关闭合 一个开关打开灯泡还是亮的 一或上一结果是一

### **[01:00]**
相当于两个开关都是闭合的 灯泡是亮的 那么这就是按位或运算 我们来观察一下按位或运算 如果是或一的话 那么结果必然是一 我们来计算一个52或上11 假如52和11 我们都用八位来表示 那么每一位的全职就是1248 16 34 00:01:29,290 --> 00:01:30,090 34 00:01:30,090 --> 00:01:31,129 34 00:01:31,129 --> 00:01:34,129 那么五十二五十二里面有32 52-32 还有20 那么20里面有16 20-16 还有四 那么四也是一 那么就得到了52的二进制 每一位11就是8+2 然后加上一就得到11 那么52或上11有腰就是一 45 00:02:04,269 --> 00:02:07,390 那么结果就是3F

### **[02:09]**
二位或运算的应用是使某一位至邀 我们知道存储器是按照字节单元来组织的 每一个字节单元是八位 我们定义一个变量B初值是0X00 也就是八个零 然后来执行这条语句 B或上0X01赋值给B 这是B的值 这是0X01 那么B或上0X01 结果是0X01 也就是将最低位变成了一 执行完这条语句之后 B的最低为零就会被覆盖掉 它的新值是一 然后其他位不变 只改变了这一位 我们再来举一个例子 B的初值还是0X00 然后来执行这条语句 B或上0X80赋值给B 这是B的值或上0X80

### **[03:00]**
结果还是0X80 也就是将最高位变成了一 那么执行完这条语句之后 B的最高位就变成了一 其他位也不变 按位或运算的字或简写或运算 它有两个操作数 其中一个操作数是保存变量本身或上某个数 结果赋值给保存变量 它的简写方式就是保存变量自或简写符号 剩余某个数 我们举一个例子 定义一个变量C这是一个字或简写方式 你要想到它原来的方式就会计算了 也就是C或上五赋值给C 我们来写程序练习一下 或运算 还是在我们的单片机上来练习程序 定义一个变量A然后执行这条语句 88或上0X01赋值给A 最后将A的结果输出查看一下 我们来看88或上0X01结果是多少

### **[04:00]**
88或上0X01 88里面有64 88-64 还剩24 那么24里面有16 24-16=8 那么这就是88的二进制代码或上0X01 结果是1001101 那么88或上0X01结果是0X59 我们将程序下载一下 来看一下单片机的输出结果 单片机输出结果16进制是0X59 这与我们分析的一致 我们之前说 按位或运算也是遵循算术转换规则的 也就是提升与对齐 我们举一个例子来看一下 定义一些变量 然后执行这条语句 B的类型是on sign int类型 A的类型是on snchar类型 提升为int类型

### **[05:00]**
因为A是on3的char类型 那么0XFF就是255是一个正数 那么提升为int类型 前面八位补零 也就是零X00FF再提升为unsign int类型 还是零X00FF 那么B或上A也就是0X0 0FF或上0X0001 结果是零X00FF是assign int类型的 那么执行完B或上A得到一个结果 有大小有类型 将这个结果复制给C C提供的存储空间是16位的 那么能将这里的16位全部存到C的空间当中 那么C的值就是零X00FF我们来验证一下

### **[06:03]**
现在程序写好了 我们下载到单片机看一下 单片机输出的结果是0XFF 这与我们分析的结果一致 我们知道按位或运算是遵循算术转换规则的 那么我们又知道 算术转换规则会对类型进行提升与对齐 也就是发生隐式的类型转换 那么影视的类型转换会不会影响到结果呢 我们来验证一下 我们将这里的A定义为sign a char类型 然后再来执行这条语句 现在A定义为signer char类型 然后再来执行一下这条语句 B的类型是on signer int类型 A的类型是signer char类型 提升为int类型 因为A是3a char类型 那么这里的0XFF就是一 那么A提升为16位之后 前面八位就补的是一 也就是0XFFFFA再提升为on3的int类型 还是0XFFFF

### **[07:01]**
那么B或上A也就是0XFFFF或上0X0 151 00:07:08,470 --> 00:07:13,550 结果是0XFFFF是on signa int类型的 那么执行完B或上A得到一个结果 有大小有类型 将这个结果赋值给C C提供的存储空间是16位的 那么能将这16位数据全部存到C的空间 那么执行完这条语句之后 C的值就是0XFFFF 与之前的结果C的值是零 XFF结果完全不一样 那么我们来验证一下 将A的类型变成signature类型 然后输出C的结果 查看一下C的结果是0x ff ff ff f 这与我们分析的是一致的 这里当A是on sa char类型的时候 执行这条语句得到的结果是0XFF

### **[08:01]**
当把A变成三的char类型的时候 在执行这条语句得到的结果是零 XFFFF是一个完全不同的结果 这就是类型的隐式转换对结构的影响 所以说我们在做运算的时候 一般都要选同类型 同符号的数在一起进行计算 就能避免影视类型转换所带来的问题

