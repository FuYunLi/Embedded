# 【叶宇单片机】—static的重要作用—【2026-02-07】-中文

## 字幕正文

### **[00:00]**
今天我们来说一下static的重要作用 前面我们学习了全局变量 局部变量以及函数 那么在他们的前面加上STACTIC 它们的使用就会发生一些变化 我们来看一下 首先回忆一下全局变量 只有知道全局变量是怎么使用的 那么加了STACTIC才会知道它发生了哪些变化 这是一个全局变量 会向rhyme申请一个存储空间 这个存储空间在上电之后只属于A这个变量 所以A有永久的使用权 永久的居住权 同时全局变量不仅可以在当前的文件当中 随意的使用 在其他的点C源文件当中也可以使用 但是必须加一个extent进行变量声明 我们再来说一下函数 这是一个全局变量 这是一个函数 我们也叫做全局函数

### **[01:01]**
全局也就是在全部的文件当中都可以使用 那么这个函数不仅在当前的文件当中使用 在其他的点C源文件当中也可以使用 全局变量是加一个extend变量声明 全局函数也可以加一个extend 进行函数声明就可以使用了 但是这个extent不是必须的 可以省略的 但是这类声明是必须要声明的 只有声明之后 在下面才可以正常的使用函数 正常的使用变量 全局变量前面加上static 我们叫做静态全局变量 全局函数前面我们加上static 叫做静态全局函数 什么意思呢 就好像给孙悟空戴上了紧箍咒 由不得他任性 这里也就是在其他的文件当中 不可以随意使用了 也就是变量A和函数一专属于当前这个文件

### **[02:02]**
在其他的文件当中都不可以使用了 这就是加STACTIC产生的一个变化 我们来简单的总结一下全局变量 全局函数 他们加了STACTIC之后 就变成了静态全局变量 静态全局函数 本来全局变量就全部文件可以使用 全局函数也是全部文件可以使用 但是静态全局变量就只能在当前文件使用 静态全局函数也只能在当前文件使用 那有的同学会问什么时候使用静态 什么时候使用全局 一句话就是能用静态 先用静态后用全局 用了静态之后 首先比较有安全感 因为它只能在当前文件当中使用 然后是该用静态的 用静态 该用全局的 用全局 那么是不是一目了然的 可以知道哪些只是在当前文件当中使用

### **[03:02]**
哪些在文件外也可以使用 这样程序阅读起来比较清晰明了 一目了然 使用静态还解决一个问题 就是重名的问题 名字重复的一个问题 比如我们写一个项目的时候 如果它的功能比较多 就会有很多个点C源文件 比如这里有一个文件一 有一个文件二 那你程序写得太多的时候 保不准有一个变量名重名了 或者是一个函数名重名了 他们又都具有全局属性 那么使用的时候到底是使用哪一个呢 这样是不是会产生歧义 产生一个问题 如果给他加了一个STACTIC 给他加了一个STACTIC 那么这两个只能在当前的文件当中使用 在其他的文件当中就不可以使用了 就解决了一个重名的问题 同样文件二如果它们是在文件内使用的

### **[04:01]**
可以加一个STACTIC 可以加一个STACTIC 那么哪些是在文件内使用的 哪些是在文件外使用的 就一目了然了 刚才我们讲了全局函数 全局变量 现在我们来说一下局部变量 这是一个局部变量 同样会在ram当中申请一个存储空间 但是这个存储空间它不是专属于B的 它会在不同的时间段分配给不同的变量来使用 这个存储空间 就像是一个客栈 客栈在不同的时间段 这个房间会分配给不同的人来使用 这部分特殊的区域 我们叫做战区 我们来看一下函数的调用与占区的执行过程 也就是了解一下局部变量的使用过程 main从上往下执行 遇到函数调用函数 调用函数的时候 先将一些重要的信息进行压榨 比如现场保护的数据

### **[05:03]**
函数的返回地址 这些我们前几节课都学习过 这里的SP是什么意思呢 SP是一个寄存器 用来记录战区使用到哪一个房间了 也就是记录这个房间的房间号 比如现在SP存储的是0063 也就是使用到0063这个房间了 接着来调用函数一 函数一的内部如果定义了一些局部变量 就会为这些局部变量分配一个 临时的数据存储区 SP此时的值就是0067 就是使用到0067这个房间了 接着执行函数一执行函数一会返回 就涉及到一个出站的过程 那么SP会跳过局部变量存储区 也就释放了这一部分空间 接着会弹出返回地址

### **[06:02]**
接着弹出现场保护的数据 现在SP来到了战底 也就说明现在战区是空的 没有被使用了 那有的同学会问诶 战区真的没有数据吗 有数据只是一些无效数据 所以说我们也可以说战区现在是空的 接着main往下执行 会执行函数二执行函数 爱的过程也是一样的 先将一些重要的信息进行压榨 接着是返回地址压榨 执行函数二 如果函数二的内部定义一些局部变量 就会为这些局部变量分配一些存储空间 执行完函数二之后 就有一个返回返回 就涉及到出站过程 SP就会跳过局部变量存储区域 也就是说把这部分区域释放了 接着会弹出返回地址

### **[07:01]**
弹出现场保护的数据 SP又来到了站底 现在战区又是空的 那么main函数又会接着往下执行 战区除了存储现场数据 返回地址还是一个临时的数据存储区 战区它也像是一个公共区 可以重复利用 可以服务于不同的函数 现在我们对局部变量的使用有了一个了解 接着看它加STACTIC会产生哪些变化 首先全局变量加了STACTIC叫做静态全局变量 全局函数加了STACTIC叫做静态全局函数 它们产生的变化 就是在其他的文件当中不可以使用了 局部函数 加了STACTIC叫做静态局部变量 那么它产生了哪些变化 局部变量B它没有加STACTIC

### **[08:01]**
之前是在战区申请一个存储空间 也就相当于他现在住的是客栈 那么加了STACTIC之后 他就搬家了 搬到了全局变量存储区 也就相当于搬到了一个小别墅里面 这个别墅专属于他的 我们来举一个例子看一下 定义一个局部变量还是住在客栈里面 接着在main函数当中调用三次函数一 我们看一下B的值变化 第一次调用函数一给B分配的房间是一个客栈 B的初值是0B加加 那么B的值就是一 第一次B的值是一 第二次调用函数一分配的还是一个客栈 初值还是0B加加B的值还是一 第三次调用函数1B分配的还是一个客栈 初值是零 B加加B的值还是一

### **[09:03]**
还是文件一 我们只是做一点点改变 也就是局部变量B前面加一个static 其他的部分都是一样的 还是三次调用函数一 第一次调用函数一 B分配的是一个小别墅 初值是零 B加加NB的值是一 第一次调用之后 B的值是一 第二次调用函数一 那么B就不会给他重新的分配存储空间了 因为他已经有了一个小别墅 这个小别墅在上电之后只属于B 这个时候B加加B的值就是二 第二次调用B的值是二 接着第三次调用函数一 也不会为它重新分配一个存储空间 因为它有自己的专属小别墅

### **[10:00]**
接着是B加加 B加加之后B的值就是三 我们发现B搬到了小别墅之后 它里面的数据可以保持不变 也就保持最后一次使用的数值 不像是之前住在客栈里 他每次都会被重新的分配一个房间 所以说房间的数据没办法保存 也就没办法长时间的保存 我们看一下局部变量的特点 它使用的是战区临时数据存储区 同时多一份时间开销 什么意思呢 就是每次调用函数一的时候 就是每次调用函数一的时候 都要重新的给他分配一个存储空间 最后又将这个存储空间收回 那么就多一份时间开销 诶加了STACTIC之后 也就是搬到了别墅之后 不用每次给他分配一个存储空间了 那么就节省了一份时间开销

### **[11:01]**
最后我们再整体的总结一下全局变量 全局函数加了static之后变成了静态全局变量 静态全局函数 全局变量 全局函数本来是全部文件当中可以使用的 变成了静态之后 只能在当前的文件当中使用 局部变量 加了STACTIC之后变成静态局部变量 局部变量的特点是专属当前函数 接着存放在栈区的临时数据存储区 多一份时间开销 静态局部变量它也专属当前函数 他住在一个大别墅 他少了一份时间开销 因为不用每次调用的时候 都要重新给它分配一个存储空间 什么时候使用全局变量 全局函数什么时候使用静态全局变量 静态全局函数我们之前说过 先静态后全局

### **[12:00]**
那什么时候使用局部变量 什么时候使用静态局部变量 这个要看一个需求 看什么需求呢 首先是看数据需不需要保持上次执行的结果 也就是住在客栈里 他没办法长久地保存它的数据 如果住在小别墅里 它可以长期的保存它的数据 看看占不占用战区 如果定义一个很大的数组的话 那么它肯定是很占用战区的 这个时候加static 那么它定义的区域就是全局数据存储区 就不占用战区了 因为战区比较小 全局数据存储区比较大 接着看函数是不是被频繁调用 如果是频繁调用的话 使用STACTIC比较节省一点时间 它不用每次进函数都要重新分配存储空间 这样说下来 感觉局部变量不管37211都给它加上stack tic可数比较好

### **[13:00]**
但是也不是这样的 因为毕竟战区它也是有一个优势的 它是一个公共区 它可以重复的利用 也就是比较节约存储空间好 这节课到此结束 拜拜

