# 【叶宇单片机】—032  位运算的异或运算—【2025-12-03】-中文

## 字幕正文

### **[00:01]**
大家好 今天学习的是第32节位运算的异或运算 异或运算还是遵循算术转换规则 也就是四个字 提升对齐 提升与对齐依据的是类型等级 将提升与对齐之后的数据给运算器 运算器是按照公共类型来解读数据的 我们先说一下什么是按位异或运算 这是一个门电路 我们通过这个门电路来理解异或运算 这是一个或门 它的输入与输出结果符合或运算 这是一个与门它的输入与输出结果符合与运算 这是一个与非门 先将输入按照与运算得到一个结果 再将结果进行取反 就是与非门 我们来看一下零一或零得零 输入两个零 经过或门结果是零 经过与灰门结果是一

### **[01:00]**
零与上一结果是零零 亦或一得一 输入一个零 输入一个一经过或门是一 经过与灰门是一一与上一结果是一一 亦或零得一 输入一个一输入一个零 经过或门是一经过与非门是一 一与上一结果是111或一得零 输入一个一 输入一个一经过或门得一 经过与非门得零一与上零 结果还是零 看一下异或运算如何来记忆异或运算的结果 异或运算E就是不同 当两位的数不同的时候 结果是一两位的数不同的时候 结果是一 当两位数相同的时候 结果是零 当两位数相同的时候 结果是零异或运算 我们在记忆的时候 主要记住这个E字就是不同

### **[02:00]**
当不同的时候得到的结果就是一 相同的时候得到的结果就是零 之前我们学过按位与运算 按位或运算 现在我们来学习按位异或运算余运算 我们知道某一位与上零结果还是零 如果某一位与上一 那么结果是不变的 或运算或上零结果是不变的 或上一个零结果是不变的 或上一个一结果还是一或上一个一 结果还是一 异或运算 异或零 结果是不变的 亦或一是对结果进行取反 零一或一结果取反是一零异或一结果取反是一

### **[03:06]**
一亦或一结果取反是零 按位与运算的时候 我们一般使用的是与零结果是零按位或运算 我们一般使用的是或一结果是一按位异或运算 我们一般使用的是E或一结果是取反的 我们先来做42E或16 42里面有32 42-32 还有十十就是82E或16 得到的结果就是01011100 也就是3A 我们来看一下按位异或运算的应用 使某一位取反 也可以使某一些位取反 我们定义一个变量B初值是0XFF 也就是八个一来执行这条语句

### **[04:02]**
BE或0X01赋值给B 这是B的值 这是0X01 这是异或的结果 E或一也就是使最低位进行了取反 执行完这条语句之后 最低位变成了零 其他位不变 我们再来举一个例子 B的初值还是0XFF再来执行这条语句 BE或0X80赋值给B 这是B的值 这是0X80 这是异或的结果 亦或一 也就是对一进行取反 那么执行完这条语句之后 最高位变成了零 其他位保持不变 按位异或运算的字 亦或简写异或运算 它有两个运算数 其中一个运算数是保存变量 另一个是某个数相异或得到一个结果 赋值给保存变量 它的简写方式就是保存变量 亦或简写符号

### **[05:01]**
剩余某个数 我们来举一个例子 定义一个变量C这是E或的简写方式 你要想到它原来的方式就会计算了 也就是CE或五得到一个结果赋值给C 我们来写程序练习一下异 或还是在我们的单片机上来练习程序 定义一个变量A然后执行这条语句 也就是85E或0X0F结果赋值给A 然后再将A的结果输出到电脑上进行查看 我们先来算一下85 E或0X0F结果是多少 85E或零X零F85里面有64 85-64=21 有16 21-16=5 就是410X0F就是四个一 然后议会的结果是0101101

### **[06:02]**
也就是5A 然后我们把程序下载到单片机上 来看一下单片机的输出结果 单片机输出的结果也是0X5 A与我们分析的一致 我们之前说过 按位异或运算也是符合算术运算规则的 那么算术运算规则里面有一个隐式的类型转换 对异或的结果有没有影响呢 我们来看一下定义一些变量执行这条语句 B的类型是on signer int类型 A的类型是on sa chan类型 提升为int类型 因为A是R3的char类型 那么0XFF就是255是一个正数 提升为int类型 前面八位补零 也就是0X0 0f f 然后再提升为on3的int类型 还是零X00FF 执行BE或A 也就是零X00FFE或0X000F

### **[07:01]**
结果是0X00F零是unsign int类型的 那么执行完BE或A得到一个结果 有大小有类型 将这个结果复制给cc 提供的存储空间是16位的 那么将这16位都能放到C的空间当中 C的值就是0X00F零 我们来写程序验证一下 现在程序写好了 定义三个变量ABC 然后执行这条语句 AE或B赋值给C 最后将C的结果输出 查看一下C的输出结果是0XF0 这与我们分析的结果一致 将这里的A改成3ACHARA类型

### **[08:00]**
然后再来执行这条语句 A已经是signer char类型 然后再来执行这条语句 B的类型是unsigner int类型 A是sa char类型 然后提升为int类型 因为A是3a char类型 那么0XFF是一 那么A提升为int类型 前面八位就要补腰 也就是0XFFFF再提升为on3的int类型 A的值还是0XFFFF 那么BE或A也就是0XFFFFE或0X0 00F结果是0XFFF0是unsign int类型的 那么BE或A得到一个结果 有大小有类型 将这个结果赋值给C C提供的存储空间是16位的 那么能将这16位全部存到C的空间 我们来验证一下这个结果 将A的类型变成3a char类型

### **[09:01]**
然后输出C的结果 查看一下C的结果是0XFFF0 这与我们分析的是一致的 这里我们发现只是改变A的类型 然后执行这条语句 就得到两个完全不同的结果 这就是算术运算规则 在进行隐式类型转换的时候 对结果的影响 所以说我们在做运算的时候 一般都是使用同一类型 同一符号的数进行计算 这样能避免一些奇怪的问题

