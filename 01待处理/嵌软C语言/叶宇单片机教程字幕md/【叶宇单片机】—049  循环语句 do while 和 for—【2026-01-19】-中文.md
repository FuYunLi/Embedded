# 【叶宇单片机】—049  循环语句 do while 和 for—【2026-01-19】-中文

## 字幕正文

### **[00:00]**
今天我们来看一下do while和for循环语句 主要是看一下它们的执行顺序 首先是do while 有语句 do下面是一个大括号 里面放了一些语句 接着是while有条件判断 接着是括号外的其他语句 首先程序从上往下执行 第一次来到do语句 那么第一次来到do语句 这里没有什么条件限制 接着就会顺序的往下执行 就是执行一次大括号里的语句 来到why are 有条件判断 当条件是真的 那么就会循环执行一次大括号里的语句 接着又来到了条件判断 判断条件是真的 那么就会循环的执行大括号里的语句 这就是一个所谓的循环语句 当判断条件是假的 那么就会跳过大括号里的语句

### **[01:00]**
接着执行下面的 这是我自己平时的一个理解方式 我们再来看一下它的流程图 这是标准的理解方式 首先是程序从上往下执行 先执行一次大括号里的语句 接着来到条件判断 判断条件是真的还是假的 如果条件是真的 那么就执行大括号里的语句 接着又来到了条件判断 如果条件还是真的 那么就循环的执行大括号里的语句 如果条件是假的 那么就会执行下面的语句 两种理解方式 选择适合自己的就可以了 我们再来看一下while和do while的区别 上节课我们学习了while的语句 这是它的一般格式 首先还是条件判断 如果是真的 那么就执行大括号里的语句 接着又回到条件 对条件进行再次判断

### **[02:00]**
如果是真的 那么就一直循环的执行大括号里的语句 如果条件判断是假的 那么就跳过大括号里的语句 接着执行下面的语句 我们再把do while语句放在一起比较一下 这是他的一个执行顺序 我们观察一下蛙有语句和do while有语句 它们的相同点和不同点相同的地方 是不是都有条件判断 当条件是真的 那么就循环的执行大括号里的语句 当条件是假的 就会跳过大括号里的语句 这边也是当条件是真的 就会循环的执行大括号里的语句 当条件是假的 就会跳过大括号里的语句 这是他们相同的地方 再想一想他们不同的地方是什么 是不是第一次来到度语句的时候 他会先执行一次大括号里的语句 然后才是条件判断

### **[03:00]**
所以说do while 大括号里面的语句至少会被执行一次 这里要注意一下 还是在我们的单片机上来练习 首先定义两个变量 变量A初值是零 还有个变量二 然后来到main 这个是我们程序的入口 从这里开始执行我们写的第一条程序 也就是将三赋值给I 接着来到do语句 那么会执行一次大括号里的语句 A加一赋值给AA的初值是零 A的值就是一 接着是2-1 赋值给II的值是3-1 I的值是二 接着才来到value的条件 判断判断R的值 二的值 此时是二是真的 就会再次执行大括号里的语句 也就将A加一赋值给A 此时A的值是二 然后将I减一复制给I 那么此时R的值是一

### **[04:02]**
又来到了YU的判断 此时I的值是一还是真的 那么还会再执行大括号里的语句 再将A加一赋值给A 那么A的值是三 将2-1复制给二 那么二的值是零 再次来到while有条件判断 此时I的值是零 判断是假的 那么接着会执行下面的语句 也就是输出A的值和I的值 我们把程序下载到单片机 看一下A的值和I的值是不是三和零 那么第一个数是三 第二个数是零 那么它们输出的结果与我们验证的是一样的 接着我们再来看for循环语句 for语句后面是变量的初始化 变量的条件 判断变量的更新 这么长的一条语句 我们可能有一点不理解 但是变量这两个词我们是熟悉的 因为我们经常会定义一些变量

### **[05:02]**
然后下面是一个大括号 里面放了一些语句 一些需要循环执行的语句 接着是括号外的其他语句 我们先来看第一行 举一个例子 比如我们定义一个变量二 首先是变量的初始化 也就是将零赋值给二 那么二的初值就是零 接着是变量的条件判断判断RI小于三吗 也就判断这个条件是真的还是假的 接着是变量的更新 这里是二加加 也就是I的值增一就是给他一个新值 对第一行有了初步的了解之后 我们来看一下它的执行顺序 首先是执行一次变量的初始化 接着来到变量的条件判断 就是判断这个条件是真的还是假的 如果条件是真的 那么就执行大括号里的语句 接着来到变量更新

### **[06:00]**
就是给变量一个新值 就是给变量一个新值 然后再来到条件判断 再次对这个条件判断是真的还是假的 如果条件判断是真的 那么就一直在这里循环的执行 这也就是一个所谓的循环语句 如果条件判断是假的 那么就会跳过大括号里的语句 接着执行下面的语句 这个是一种理解方式 我们再来看一下标准化的理解方式 程序从上往下执行 执行一次变量的初始化 接着来到条件判断 判断这个条件是真的还是假的 如果条件是真的 那么就执行大括号里的语句 接着来到变量更新 就是给变量一个新的值 然后再次来到条件判断 如果条件判断是真的 那么就这样循环的执行 如果判断条件是假的 那么就会跳过大括号里的语句 接着执行下面的语句

### **[07:01]**
注意这里变量的初始化它只执行一次 我们来举一个例子 首先是对变量进行初始化 只执行一次 将零赋值给R2的初值就是零 接着来到条件判断 判断I小于三吗 是小于三呢 那么就会执行大括号里的语句 接着来到变量更新I加加 那么二的值就是一 接着又来到条件判断 判断I小于三吗 二还是小于三呢 那么会再次执行大括号里的语句 接着又来到变量更新 再次I加加 那么I的值是二 又来到条件判断R是小于三的 是成立的 那么又会执行一次大括号里的语句 接着又来到R加加 此时R的值就是三 然后再来到条件判断I小于三吗

### **[08:00]**
这个时候是不成立的 那么就会跳过大括号里的语句 接着执行下面的语句 那么我们来想一下这里的循环执行了几次 是不是I等于零的时候条件成立 执行了一次 I等于一的时候执行了一次 I等于二的时候执行了一次 I等于三的时候 就跳过大括号里的语句 也就是二等于012的时候执行了三次循环 我们再来看一下for语句 它实际上也可以用while语句来写 首先还是变量的初始化 将零赋值给I 那么I的初值就是零 接着来到Y有条件判断I小于三吗 R是小于三的 那么会执行大括号里的语句 接着来到变量的更新 I加加 那么I的值就是一 执行完这条语句之后

### **[09:01]**
又会回到条件判断 此时I的值是一小于三 又会执行一次大括号里的语句 接着又来到了变量更新 那么R的值是二 执行完这条语句之后 又来到了条件判断R是小于三 又会又会执行大括号里的语句 又来到了变量更新 此时R的值是三 又来到了条件判断 那么三是不小于三的 就会跳过大括号里的语句 接着执行下面的语句 所以说for语句 有的时候也可以用while语句来表示 但是我们一般还是使用for语句 因为它三个条件是放在一起的 比较清楚明了诶 while语句它们是分开的 可能有的时候一不注意就会犯错 我们来写程序练习一下 还是定义两个变量A和I 然后来执行我们第一条语句就是for语句

### **[10:00]**
首先是R的初值是零 接着是条件判断R小于三 那么会执行大括号里的语句 我们想一下 这里一共执行了几次 大括号里的语句 是不是I等于零条件成立等于一条件成立 等于二条件成立 那么I等于零 一二的时候执行了三次大括号里的语句 那么A的值是多少 那么第一次执行的时候 A的初值是002 第一次得到的结果是二 然后第二次在执行的时候是2+2 A的结果是四 那么第三次在执行的时候是四加I A的结果是六 那么A的值就是六 我们输出A的结果查看一下 我们看一下输出A的结果是六 与我们分析的是一致的 之前变量的初值我们设的是零 但是这个初值不是固定的 你可以根据自己的需要来设置的 这里我们将初值设为三

### **[11:01]**
然后判断的是I大于零吗 接着变量的更新是用I减减来更新的 所以说这三条语句不是固定的 你根据自己的需要能达到要求就可以了 然后它同样可以用while有语句来表示 接着我们再来看for语句的一些特殊的写法 这是它一个常规的格式 如果我们把两个大括号去掉 那么它是什么意思呢 首先是上面的for语句和语句一 它实际上是一条语句 因为一个分号代表一条语句结束吗 这样一条语句又是什么意思呢 我们来看一下 也就是相当于大括号里面只有一条语句一 然后其他的语句都放在括号外面 我们再来看另一种格式 还是在它的常规语句上

### **[12:00]**
我们变一下 首先是还是把两个大括号去掉 但是我们在for语句后面添加了一个分号 在这里添加了一个封号 那么for语句就是一条独立的语句 语句一也是一条独立的语句 那么这里的for语句它是什么意思呢 就相当于大括号里面有一条空语句 就是什么都不执行 什么都不做 接着是其他的语句放在括号外 像这些省略大括号的写法 我们还是比较常见的 所以说你见到的时候 你要知道他是一个什么意思就可以了 接着是for语句的变量 初始化变量的条件判断和变量的更新 变量的条件判断 除了大于小于 还有小于等于不等于或者是等于的条件 判断好这节课到此结束 拜拜

