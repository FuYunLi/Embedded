# 【叶宇单片机】—047  二维数组—【2026-01-07】-中文

## 字幕正文

### **[00:00]**
这节课我们来学习一下二维数组 在理解二维数组之前 我们先来认识一下倍数 认识倍数有利于理解二维数组 这里是一份苹果有三个 然后再来一份 那么它们是阿贝的关系 一共就有2×3个苹果 这里有一份2×3个苹果 再来一份 然后再来一份 一共有三份 那么就是三倍的关系 那么一共有3×2乘以三个苹果 这就是所谓的倍数 我们再来看一下二维数组 我们先来看一下上节课学习的一维数组 定义一个一维数组A和中括号结合 说明A是一个数组 这个数组有两个元素 每个元素都是on3的差类型的 也就是A是一个数组

### **[01:00]**
A有两个元素 每一个元素都是on3TCHAR类型的 那么说明A有两个on3TCHAR类型 那么在左边的存储器就可以申请两个on sound CHA类型的存储空间 我们再来想一下 我们使用数组的目的是什么 是不是批量的申请同一类型的存储空间 那么其实对于二维数组和三维数组 它们也是一样的 都是批量的申请同一类型的存储空间 我们现在来看一下二维数组 定义一个二维数组 B和中括号结合说明B是一个数组 而且这个数组有两个元素 每一个元素都是一个一维数组 也就是B是一个数组 B有两个元素 每一个元素都是一个一位数组 但是还是不理解它是什么意思

### **[02:01]**
没关系 我们先来看一下我们认识的一位数组 这个一维数组它有三个元素 每个元素都是on sncha类型的 那么在存储器当中就可以申请三个啊 3ACHARA类型的存储空间 这就是一个一维数组 然后我们再来看一下B它有两个元素 每个元素都是一个一维数组 也就是有两个一维数组 那么在存储器当中我们再申请一个一维数组 那么现在有两个一维数组 就完成了二维数组的存储空间的申请 那么我们如何来使用呢 还是先看一下一维数组A它有两个元素 我们可以通过A0找到第一个元素 通过A1找到第二个元素 同样B也有两个元素 我们可以通过B0找到第一个元素

### **[03:01]**
B1找到第二个元素 但是A0和A1 他找到的是on sa char类型的存储空间 它可以直接的使用 但是B0他找到的是三个 on3TCHA类型的存储空间 B1也是 那么我们说这里批量的申请了 on suncha类型的存储空间 那肯定是按照on3TCHA类型来使用的 不可能按照什么int或者是double 或者是其他的类型来使用的 对不对 那么如何找到这每一个on sa char 类型的存储空间呢 B0它有三个这样的存储空间 我们对每个存储空间进行编号 注意编号是从零开始的 80 00:03:46,889 --> 00:03:50,330 那么B00就可以找到第一个存储空间 B01可以找到第二个 B02可以找到第三个 B1也是这样处理的 将对每一个存储空间进行编号

### **[04:01]**
然后B10找到第四个存储空间 B11找到第五个存储空间 B12找到第六个存储空间 那么现在二维数组定义好了 然后存储空间也申请好了 对于每一个存储空间我们也可以找到了 那么怎么使用呢 我们先来看一下普通变量的使用方法 因为数组只是批量的申请同一类型的存储空间 那么在使用的时候还是回到类型本身来使用 就是像普通的变量一样来使用 定一个变量M申请一个字节的存储空间 定一个变量N申请一个字节的存储空间 我们可以将N赋值给M 也可以将M赋值给N 也就是通过他们的变量名 可以找到这个存储空间 对这个存储空间的数据可以读出 也可以写入新的数据 这就是普通变量的使用方法

### **[05:00]**
我们再来看一下一维数组A有两个元素 也就是可以申请两个啊 3D券类型的存储空间 可以通过A0找到第一个存储空间 A1找到第二个 那么找到on sa char类型的存储空间 就可以像普通变量一样使用 可以将M赋值给A0 也可以将A0赋值给M 也就是说 通过A0可以找到on sattra类型的存储空间 那么可以读出这个存储空间的数据 也可以写入数据 那么我们再来看一个二维数组B有两个元素 每个元素都是一个一维数组 一维数组是三个on sa char类型 那么就可以申请三个on sa char类型的存储空间 这是一个一维数组 B有两个一位数组 再申请一个一位数组 就有两个一位数组 那么如何找到ALSA差类型的存储空间呢

### **[06:01]**
可以通过下标来找到B00 找到第一个 这刚才我们已经讲过了 那么找到每一个存储空间 就可以像普通变量一样来使用 可以将M赋值给B00 也可以将B01的数据存储到M 也就是通过下标找到每一个存储空间 可以对这个存储空间的数据读出 也可以写入新的数据 就像定义普通的ARSACHA变量类型一样来使用 我们再来通过倍数 透彻的理解一下二维和一维的关系 这里有一份苹果三个 再来一份是EBAY的关系 那么一共有2×3个苹果 再定义一个二维数组 它有两个元素 每个元素都是一个一维数组 一维数组是三个on su的char类型 那么这就是一个一维数组 二维数组有两个

### **[07:01]**
我们再定义一个一维数组 这又是一个一维数组 那么一共有2×3个 那2×3个是什么意思呢 是不是两倍的一维数组 那一维数组又是由三个啊3a char类型组成的 也就是六个on snh类型 这就是二维和一维的关系 它们是一个倍数的关系 也就是有几个一维数组 我们再来看一下数组的越界定义 一个一维数组 它有两个啊 sa char类型的存储空间 它只有这两个存储空间 可以是随意使用的 下面的存储空间可能被分配给别人了 数组就不能使用了 那么我们可以通过A0找到第一个 通过A1找到第二个 那么下面的存储空间我们可以找到吗 可以的 可以通过AI找到下一个存储空间 这是合法的

### **[08:00]**
也可以对它进行赋值 比如说将A0赋值给A2 将A0的数据存到AI里面 那么AI原来的数据就会被覆盖掉 这个空间本来是分配给别人的 然后你这样私自的把他的数据给替换掉 是不是会造成一些问题 所以说我们不可以越界来使用 但是为什么AI这种写法是合法的 这个要学习完指针之后再说 因为它是和指针有关系的 暂时你就记住使用的时候不要越界 我们再来定义一个二维数组 它有两个元素 每个元素都是一个一维数组 一维数组是三个on scha类型的存储空间 这是一个一维数组 有两个一位数组 那么我们批量申请了这六个存储空间 只有这六个存储空间可以随意使用的 下面的存储空间就可能被编译器 分配给别的变量了 如果你使用它就属于越界了 我们有的六个空间 我们可以通过下标找到每一个存储空间

### **[09:03]**
那么数组外面的这个存储空间 我们也是可以找到的 这种写法也是合理的 而且也可以对它进行赋值 比如说将A0存到B13里面 那么就会把B13里面原来的数据给覆盖掉 但是我们说这个存储空间 可能分配给别的变量使用了 你这样赋值的话是会影响别的变量的数据的 是会造成一些问题的 所以说不要越界来使用B13这种写法 和对B13的赋值都是合法的 也就是编译器不会报错的 但是为什么不会报错呢 这要等指针之后再说 我们再来看一下二维数组占用了多少个字节 比如说有一个一维数组申请了两个存储空间 就是size of加上数组名 可以算出这个数组占用了多少个字节 注意单位是字节 比如在定义一个二维数组

### **[10:00]**
它有四个元素 每个元素都是一个一维数组 一维数组就是两个啊 三个char类型的存储空间 这是一个一维数组 他有四个一位数组 那么再申请两个 再申请两个 再申请两个 就是有四个一位数组 我们说计算它一共占用了多少个字节 可以通过size of5括号B来计算 那么想一想可不可以用它的倍数来计算一下 四个一维数组是不是四倍的一维数组 那一维数组又有两个阿sa char类型组成的 也就42个on sncha类型存储空间 那on center差类型又是一个字节的 也就4×2乘以一个字节 一共占用了八个字节的存储空间 我们稍微带一下三维数组 那么你对多维的数组就都理解了

### **[11:01]**
定义一个一维数组A和中括号结合 说明A是一个数组 A有两个元素 每个元素都是on summer char类型的 也就是有两个啊 sa char类型可以申请两个啊 sa char类型的存储空间 那么我们再定义一个三维数组 C和中括号结合 说明C是一个数组 这个数组有三个元素 每一个元素都是一个二维数组 C是一个数组 C有三个元素 每个元素都是一个二维数组 是不是就有三个二维数组 是不是还有一点不理解 那我们先从一维数组开始看起 一维是有三个on summer char类型 那么可以申请三个啊 三道恰类型存储空间 这是一维 我们再来看一下二维 二维是两个一维 那么再来一个一维

### **[12:03]**
再来一个一维数组 它就是二维 这就是一个二维数组申请的空间 三维是不是有三个二维数组 那我们再来三个这样的二维数组 先从一维的开始 一维 两个一维构成一个二维 这第二个 一个一维 两个一维 两个一维构成一个二维 这里是不是三个二维数组 就是我们这个变量申请了所有的存储空间 那么如何找到每一个存储空间呢 我们说批量的申请了ALSACHARA类型的存储空间 使用的时候也是按照阿SACHA类型来使用 不能按照其他的类型来使用 那么如何找到每一个啊 SACHA类型的存储空间呢 它其实是和艾维的寻找方法是一样的

### **[13:00]**
C有三个元素 我们先通过C0C1C二找到每一个元素 然后C有三个元素 每一个元素是一个二维数组 那么在对二维进行编号 每一个里面的二维进行编号 二维又是由一个一维组成 那么对一维里面的每一个进行编号 注意他们的编号都是从零开始的 这样就能找到每一个存储空间 C000找到第一个存储空间 那么就可以像普通变量一样来使用了 比如这个存储空间我们可以通过C012找到 我们再来通过倍数透彻地理解一下三维 二维一维之间的关系 自己也可以猜一下它们之间是什么样的关系 这里有一份苹果 再来一份

### **[14:00]**
它们俩是阿贝的关系 就是2×3个苹果 把爱成三个苹果 再整体的看成一份 然后有三份爱成三个苹果 那么一共是不是有3×2乘以三个苹果 我们再来看一下三维数组 我们先从一维的看起 一维是三个on sunchar类型 二维是两个意味 那么再来一个意味 两个一尾一共有2×3个 那么三维有三个二维 那么再来三个二维 这第二个 第三个阿伟 那么这里一共有多少个 一共也有3×2乘三个

### **[15:01]**
那么这里的3×2乘三是什么意思 是不是3×2乘三个 on scha类型的存储空间 注意这里的三维刚好是三个元素 二维刚好是二个元素 其实这个元素的个数你可以写十个八个都行 这里是刚好巧了的 然后我们说一下三维 二维和一维之间是一个什么样的关系呢 我们之前说二维实际上是一维的倍数 三维实际上就是二维的倍数 它们之间是一个倍数倍数的关系 我们再来整体的看一下二维数组的定义 首先看一下数据类型 就是我们要批量的申请同一类型的存储空间 这个类型有哪些呢 暂时我们可以使用这些基本的数据类型 所以说只是批量的申请这些类型使用的时候

### **[16:00]**
还是按照基本类型来使用 接着是数组名 我们说数组名的命名规则是ABC下划线123 也就是大小写字母和下划线可以开头 数字必须放在后面 接着就是二维的个数和一维的个数 注意二维实际上是一维数组的倍数 这样的一个关系 比如说我们想批量申请ASSAINT类型的存储空间 给一个变量名 然后他的关系是先申请三个嗯 三个int类型的存储空间 然后前面是二就是二倍的关系 这个就是他申请的所有的存储空间 如果是B33就是三倍的一位数组 那么这就是他申请的存储空间 如果是B43就是四倍的一维数组 这是他申请的存储空间 那么你看这个存储空间像什么

### **[17:02]**
是不是可以把一个表格的数据 放到这些存储空间当中 这就是二维数组经常使用的一个场合 我们总结一下 一维数组它批量的申请同一类型的存储空间 二维数组它是有规律的 批量的申请同一类型的存储空间 这里的规律就是倍数的关系 我们再来看一下它们的初始化 首先我们看一下对于一维数组的初始化 这里定义了一个一位数组C它有五个元素 然后对每个元素进行初始化 首先是一个大括号 大括号里放的是初始化的数据 注意这些数据要用逗号分开 比如十十一十二十三十四 注意它们之间用逗号分开 那么这里有五个数据 执行完这条语句之后 存储器当中存储的就是十十一十二十三十四

### **[18:02]**
再比如定义一个二位数组B2它有两个元素 每个元素是一个一位数组 首先它是一个数组 数组就要用大括号把每个元素括起来 那么它有两个元素 每一个元素都是一个一维数组 就是括号里有两个一位数组 就是括号里有两个一维数组 然后对每一个一维数组进行初始化 注意两个一维数组之间也要用逗号分开 初始化的时候 每一个数据也要用逗号分开 那么执行完这条语句之后 存储器存的就是二十二十一 393 00:18:44,259 --> 00:18:45,140 393 00:18:45,140 --> 00:18:46,990 二十四二十五 当然了 我们看这里蓝色的括号 实际上它也是可以省略的 那么省略之后可以把它分行写 也可以把它整理成一行 这样子来初始化

### **[19:00]**
我们再来说一下不完全初始化 什么是不完全初始化呢 就是不全部初始化 比如把后面两个去掉 这个时候编译器看到AC有五个元素 它只初始化了三个元素 后面两个元素编译器就会给它补零 所以说执行完这条语句之后 存储器当中的十三十四会被去掉 然后重新初始化的值是零 再比如二维数组 它也可以不完全初始化 那么编译器会把这个值补零 执行完这条语句之后 存储器当中会把这两个数据去掉 然后新值是零 这就是不完全初始化 还有一个问题可以省略元素的个数吗 比如C有五个元素 这里的五可以省略吗 可以的 因为编译器会数你大括号里有几个数据 它一个数有五个数据

### **[20:01]**
那么就会在这里面重新写上五 同样在二维数组当中 B有两个元素 这里的二可以省略吗 可以的 那么编译器也会去数哦 他一看有两个大括号 就知道两个元素 那么就会写二 那有同学会说这里的三可以把它省略吗 这是不可以的 因为因为B有两个元素 两个元素的组成部分 你给它省略了 因为B有两个元素 每个元素是一个一维数组 这个一维数组你可以是三 也可以是300 也可以是3000 它没那编译器就没有什么依据 所以说你没办法把它省略了 它依据什么来判断你一围有几个元素呢 可以是三个元素 也可以是30个元素 虽然说你这里一维只初始化三个数据 但是不是有一种不完全初始化吗

### **[21:00]**
他想哎你意为后面可能是不完全初始化 可能有30个元素 还有300个元素呢 它是没有什么依据的 所以说不可以省略

