# 【叶宇单片机】—83  累计主循环的“非阻塞延时”控制LED闪烁—【2026-06-11】-中文

## 字幕正文

### **[00:00]**
这节课我们来讲一下累计主循环的非阻塞延时 控制led灯闪烁 这个名字有一点长 先不管它 反正就是还是控制led灯闪烁 上一节课我们学习了阻塞延时 我们来简单的回忆一下程序 从main开始顺序的往下执行 遇到delay1秒 那么就在这里等待一秒之后再往下继续执行 又遇到delay1秒 那么还在这里等待一秒 等待一秒之后再继续往下执行 这里的一秒就是我们所说的一个阻塞延时 为什么我们一般不建议使用阻塞眼屎呢 一般我们会在这个主循环里放很多任务 如果每一个任务里面都放一个阻塞延时 那么整个程序就会给人一种比较卡顿的感觉 就是你点一个按钮 可能卡了一秒或者两秒才有一个反应 这里的阻塞岩石就像是红绿灯

### **[01:02]**
只有等这个红灯结束了才可以继续往下走 那么这里的等待时间到底是多长 时间才算是阻塞延迟呢 是一秒两秒还是十秒呢 根据一些项目经验 我们说 一毫秒是阻塞延时与非阻塞延时的一个分界线 就是大于一毫秒属于阻塞延时 我们尽量少用小雨 一毫秒属于非阻塞延时 我们可以适量的使用一下 但是这里的一毫秒它只是一个经验值 它不是标准答案 这节课我们就使用非阻塞延时来控制led灯闪烁 先写一个程序 我们控制P0端口的D7引脚上的rd灯来闪烁 然后写一个main函数 这里面我们放了一个switch条件分支 用来执行不同的任务 比如任务一是rd灯亮

### **[02:00]**
任务二是rd灯灭 定义一个全局变量 那么switch 会根据这个全局变量的值来执行不同的分支 如果它的值是零 就执行case0就是让l led灯亮 然后切换到下一个任务 如果它的值是一 就执行case1 就是让RD灯灭 然后再切换到任务零 这里的一亮一灭 是不是闪烁了一次 如果我们让l led灯一直闪烁怎么办 是不是外面再加一个Y有循环 然后就会一直循环的闪烁 这里有一个问题 就是这两个break执行break的时候 它跳到哪里呢 是跳出switch语句 还是跳出while有循环 注意switch里面的break跳出的是switch语句 这个r led灯在这个主循环里实现了闪烁 但是它闪烁的非常的快

### **[03:01]**
因为量和面之间就几条语句 它的时间非常的短 那我们想累计主循环的次数来实现延时 然后让r led灯闪烁的慢一点 我们看怎么办 首先定义一个全局变量 这个全局变量用来累计主循环的次数 如果这个次数大于5000 那么就是r led灯亮 接着切换到下一个任务 注意同时要把这个累计次数进行清零 那么K41是rd灯灭 它的延时程序也是一样的 还是累计主循环的次数 如果这个次数大于5000 那么rd灯就灭 先来看一下完整的程序 我们主要来讲一下这个主循环的部分 先看一下它的执行顺序 首先开始循环来到switch的条件 判断他的初值是零

### **[04:00]**
就执行case0 然后累计一次主循环的次数 接着来到if条件判断 这个时候条件是假的 就会跳过if语句来到break 刚才执行了几条语句 我们来记录一下 因为每一条语句都会消耗一点时间 那么就形成了一个时间延时 也就是一个时间累加的过程 从break再跳出switch语句 接着往下执行 下面没有什么语句 又来到了Y有条件判断的地方 开始新一次循环 然后就是switch条件判断它的值还是零 又来到case0 再次进行累加 来到if条件判断还是不成立的 那么又来到break 再把刚才执行的一些语句记录一下 它们累加起来就是一个时间延时

### **[05:01]**
从break再跳出switch语句 接着又来到why有条件判断的地方 又开始新一次的循环 那么一共执行多少次这样的循环呢 当累加到5000一次的时候 这个时候if条件判断就是真的就会执行它的 大括号里面的语句 首先将这个计数值清零 然后id灯亮 再切换到下一个任务 接着来到break 跳出switch语句 接着又来到while有循环判断处 开始新一次的循环 这一次switch的条件判断值是一 那么就会执行case1的任务 这个时候又累加进入K41的次数 累加到5000次之后才会让l led灯熄灭 然后这一段程序开始执行累加进入case1的次数

### **[06:02]**
也就是累加主循环的次数 他的操作和上面是一样的 这就是累计主循环的次数实现的延时 这个延时虽然说他走的很流畅 然后程序也不卡顿了 但是他还是有一个问题的 我们来看一下 我们把switch语句这部分封装成一个rd函数 实现功能模块化 就是它主要实现led灯闪烁 然后我们再重新写一下这个程序 在主循环里调用LD函数 我们看一下它的执行顺序 首先从main开始 然后来到while 有循环 接着是调用函数 就来到了switch条件判断数值是零

### **[07:01]**
那么执行case0进行累加 然后再条件判断条件不成立 就跳过大括号来到break 这个时候跳出switch语句 接着往下执行 然后就来到了Y有循环的部分 接着来到Y有条件判断 这个跟刚才是不是一样的 还是累计主循环的次数实现延时 但是如果我们变一下增加一个函数 那么它的执行顺序从Y又开始 会执行这个增加的函数 执行完这个函数之后 然后再执行LD函数 接着来到switch条件 判断初值是零 就执行case0 然后再进行累加 这里也是累加一次 虽然是累加一次 但是他的时间不一样了 因为这一次的时间 增加了执行完这个函数的时间 所以说它时间变长了

### **[08:02]**
如果想让时间变短 就调整这里的累加次数 如果再增加一个函数 那么Y有循环的时候 先执行完这一个函数 再执行这个函数 再执行id函数 然后来到switch条件 判断初值是零 就执行case0 再次进行累加一次 虽然还是累加一次 但是他的时间变长了 它加上了执行这个函数的时间 和执行这个函数的时间 那么i led灯闪烁就变慢了 如果想让rd灯闪烁快一点 就调整这个累加次数 所以说累计主循环的次数去实现延时 它虽然是程序嗯 执行起来比较流畅 但是它有一些问题 比如说当你任务增加的时候 这个延迟时间可能就不一样了

### **[09:07]**
好这节课到此结束 拜拜

