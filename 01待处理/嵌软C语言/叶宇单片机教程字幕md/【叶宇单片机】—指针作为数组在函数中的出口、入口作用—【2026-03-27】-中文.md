# 【叶宇单片机】—指针作为数组在函数中的出口、入口作用—【2026-03-27】-中文

## 字幕正文

### **[00:01]**
上一节课我们学习了数组作为函数的入口 66节讲的是数组作为函数的出口 67节讲的是数组作为函数的出口和入口 那么这三节的内容其实都是一样的 所以我们把66节 67节放在一起来讲 那么先来看一下函数的一般格式 函数内传递数据 它有自己的输入接口和输出接口 上节课我们说在参数列表定义一个数组 实际上它就退化成了一个指针变量 那么在函数内进行解引用 也就是在函数内部获得外部变量的一个地址 通过解引用可以实现读写这个存储空间的数据 也就间接地实现了函数内数据的输入和输出 其实这节课的重点就这几句话

### **[01:01]**
我们再来举一个例子 看一下如何通过数组实现函数内数据的输入和输出 定义一个数组 它有四个元素 分别是四八十六三十 然后我们写一个函数求这四个数据的一半 也就是248 15 再将这些数再存到这个数组当中 接着我们写一个函数 求这四个数据的一半 在main函数里调用这个函数 这个函数有一个输入没有输出 它输入的是一个数组名 我们说数组名的类型是什么 是不是元素类型的指针

### **[02:00]**
同样这里的入口参数是一个数组 它也会退化成元素类型的指针 接着我们看一下这个全局变量 这个局部变量它的内存分配 了解内存分配有利于理解数据的传送过程 在执行main函数之前 会为全局变量分配存储空间 我们把虚线上面大概的叫做占区 下面大概叫做全局数据存储区 那么为全局变量分配存储空间 接着执行程序遇到函数调用函数 调用函数有一个压榨过程 首先是现场保护 然后就是将函数的返回地址压栈 这两个过程我们先忽略 接着是为局部变量分配存储空间 存储空间分配好了之后 那么来执行函数 我们来看一下这里的Buff是不是将数组名复制给Buff

### **[03:00]**
数组名又代表的是数组的地址 那么buffer里存储的就是0030 也就是在函数内部获得了外部变量的一个地址 接着Buff加零得到的是一个地址 是0030 Buff加一也是一个地址 0031buff加二 Buff加三都是一个地址 注意它们仅仅是一个地址 只有对他们进行解引用才可以读写这个存储空间的数据 接着我们来看一下这个函数 buffer中括号零就等效于Buff加零减引用 这两种写法都是一样的 也就是一个是汇编语言的写法 一个是C语言的写法 但是在C语言当中都可以使用 也就是通过前面这种写法可以读写存储空间的数据 通过后面的这种写法也可以读写存储空间的数据 那么在函数当中

### **[04:00]**
这里就相当于是读出数据 读出的数据是四八十六三十 也就是间接的传递数据到函数内部 这里是赋值 相当于是输出数据 就是将结果再存储到这个数组当中 那么数组当中存储的值就是248 15 也就是在函数内获取到了外部变量的地址 就可以间接地对这个变量进行读和写操作 也就间接地实现了函数内数据的输入和输出 那么执行完函数就有一个返赔的过程 返赔就涉及到出站 首先是释放局部变量的存储空间 那么SP来到栈底 这个时候战区又是空的Buff 这个存储空间已经被释放了 那么Buff加三进行解引用也就没有什么意义了

### **[05:01]**
所以说也不可以这样使用了 接着执行下面的程序 我们说数组作为函数的入口参数 实际上也是退化成一个指针变量 那我们就看一下指针变量的本质 就是想获取谁的地址 就可以获取谁的地址 打开这个地址的存储空间 也就是可以对它进行读或者写 那么按照什么类型来读写呢 也是指针自己说的 算好 这节课就到此结束 拜拜

