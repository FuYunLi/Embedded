# 【叶宇单片机】—76  二维数组与指针—【2026-05-05】-中文

## 字幕正文

### **[00:00]**
今天我们来看一下二维数组与指针 也就是看一下它们俩之间是如何实现等效替换 我们在分析二维数组的时候 就好像剥洋葱一样 然后一层一层的往下分析 先来看一下变量的定义 取变量的地址它是什么类型的 比如定义一个变量取它的地址 它的类型就是在原来类型的基础上加一个星号 再定义一个变量取它的地址 它的类型还是在原来类型的基础上加一个星号 再定义一个变量取它的地址 它的类型还是原来类型的基础上加一个星号 再定义一个一维数组取它的地址 它的类型还是在原来类型的基础上加一个星号 再定义一个二维数组 取它的地址 它的类型还是在原来类型的基础上加一个星号 Now 我们发现这两个指针类型它有两个运算符

### **[01:03]**
一个是星号运算符 一个是中括号运算符 我们知道括号的运算符它的优先级是最高的 那么编译器在分析这个类型的时候 它就会把它当做一个数组 实际上它是一个 它是一个指针 所以说这个时候要给它们加上一个括号 那么它就是一个一维数组的指针 它是一个二维数组的指针 接着我们再对它们进行解引用 解引用之后的类型就是去掉这个星号 比如第一个是按照on scha类型来读写 这个存储空间的数据 第二个是anina int 第三个是double 第四个是一个数组类型 但是数组类型它有三个元素 有的时候我们不知道指向的是哪个元素 所以说它还有一种用法 就是退化成一个元素类型的指针

### **[02:00]**
同样这里也是一个数组类型 它也有三个元素 这里有三个元素 有的时候我们不知道它是使用的是哪个元素 所以说有的时候 它也会退化成一个元素类型的指针来使用 同样这里有两个运算符 一个是星号 一个是中括号 所以说要加上一个括号 这里对它们进行解引用 解引用之后也就是等效于变量名 对于数组名取地址 然后解引用它等效于数组名 也就是Buff 这个时候Buff它实际上是有两个类型可以使用的 一个是数组 一个是元素类型的指针 下面也是一样的 那么这个二维数组的数组名它也有两个类型 一个是数组 一个是元素类型的指针 我们来简单的总结一下取地址的类型 就是加上一个星号 解引用的类型就是去掉星号 如果去掉这个星号是一个数组类型 还要想到它可以退化成一个元素类型的指针

### **[03:01]**
来使用 那什么时候使用它的数组类型 什么时候使用它的指针类型 我们来看一下这里的buffer变量名 它有两个类型 这里的table它也有两个类型 那什么时候使用数组类型 一般取数组名的地址 它的类型就是在原来类型的基础上加一个星号 也就是一维数组的指针类型 还有一个就是science of 后面是一个数组名 它用来计算这个数组 一共占用了多少个存储单元 一般其他的时候就把buffer 当做元素类型的指针来使用 同样这里二维数组的数组名table它有两个类型 一般取地址是按照数组的类型来使用 也就是一个二维数组的指针 还有一个就是size of 后面是一个数组名 也就是把它当做一个数组来看待的 计算这个数组一共占用了多少个存储单元 那么其他的时候

### **[04:01]**
一般都是把它当作元素类型的指针来使用 如果对取Buff的地址进行解引用 那么它是什么类型 它是不是是一个数组 然后它也可以退化成一个元素类型的指针 也就是等效于数组名Buff 同样对取table的地址进行解引用 它是什么类型 解引用就是去掉星号之后 它是一个数组 但是它也可以退化成一个元素类型的指针 这里还是等效于table 也就是说我们在分析数组的时候 要先看到它的本质是一个数组 然后再想到它可以退化成一个元素类型的指针 来使用 接着我们看一下数组与指针之间的等效替换 首先定义一个一维数组 为它分配一个存储空间 我们可以找到它的每一个元素 同样这里的buffer它有两个类型 一个是数组 一个是元素类型的指针

### **[05:01]**
Buff0就等效于Buff加零解 引用buff1buff2都是一样的 那么这里的buffer它是什么类型 它是不是元素类型的指针 这个时候buffer加零减引用 它的类型就是on sound char类型 所以说可以把每一个存储空间 按照on3的CHA类型来读写 接着定义一个指针变量 将buffer赋值给这个指针变量 这个时候buffer是什么类型 我们说数组只有两种情况 是当作数组来使用 一个是取数组名的地址 一个是size of 后面加一个数组名 其他的时候都当作元素类型的指针来使用 这个时候Buff的类型就是阿SACHA类型的指针 两个类型一致就可以直接复制 那么P1存储的就是0080 H是八伏的地址 P1的类型是ARSACHA类型的指针 这个时候对它进行解引用

### **[06:00]**
比如P10 解引用就找到0080这个存储空间 按照on sound cha类型来读写 P11减应用 找到下一个存储单元 P12节应用 找到下一个存储单元 我们给它换一种写法 那么我们看一下左右两边的写法是不是等效的 我们来分析一下P1的解引用 P1的类型是ASSACHA类型的指针 那么对它们进行解 引用的类型就是ASSACHA类型 也就是对这个存储空间 是按照on snh类型来读写的 这个时候他们俩实际就实现了等效替换 因为buffer虽然是一个数组 但是这个时候它是当做一个元素类型的 指针来使用的 P1也是一个指针类型 它们俩的类型是一致的 就可以实现等效替换 我们再来看一下 二维数组与指针之间的等效替换 定义一个二维数组为它分配存储空间

### **[07:02]**
我们可以读写每一个存储单元 table它也有两个类型 一个是数组 一个是元素类型的指针 首先第一次对table进行解引用 那么这里的table是什么类型 是不是元素类型的指针 这个时候table加零解引用 table加一减引用 table加二减引用 对它解引用之后 也就是table中括号 它是什么类型 是不是去掉星号 它是一个数组 首先是一个数组类型 然后它可以退化成元素类型的指针来使用 它首先是一个数组类型 那么这里的table0 那么table0就找到一个一位数组 table1也找到一个一位数组 table2也找到一个一位数组 第一次解引用 我们把二维数组变成了一维数组 变成了一维数组还是没办法用的

### **[08:02]**
我们要回到基本的变量类型来使用 也就回到on3TCHAR类型来使用 接着对它进行解引用 如果不相信这里的table中括号它是一个数组类型 可以通过size of后面加一个数组名验证一下 我们对一维数组再次进行解引用 由于位置有限 我们就选这几个数进行解引用 首先table中括号00进行解引用 table中括号11进行解引用 table中括号21进行解引用 那么这里的table中括号它是什么类型呢 这个时候他是不是又是一个指针类型 对指针类型进行解引用 那么它最后的类型就是on s的char类型 也就是table中括号中括号的结果就是on satcha类型 所以说这里的table中括号零零 就找到一个存储空间 按照ASSACHA类型来读写 table中括号一一

### **[09:00]**
table中括号二一也是找到一个存储空间 按照on sound chan类型来读写 我们再来看一下这里的加法 table加零 table加一 table加二 table是一个指针类型 它是一个一维数组的指针 这个指针是两个字节 那么加一就相当于是加了两个字节 这里的加一是table中括号 然后加上零 加上一相当于加了一个字节 指针的加减法主要是与它的类型有关 如果这里不清楚的话 可以看一下前面的课程 接着我们定义一个指针变量 想实现与二维数组的等效替换 这里的指针变量也是一个一维数组的指针 它们俩的类型是一样的 为指针变量分配存储空间 它存储的是table的地址

### **[10:01]**
就是0080 首先第一次对PE1进行解引用 它实际上和table进行解引用是一样的 因为两个类型是一样的 那么P10解引用之后是什么类型 是不是在原来类型的基础上去掉一个星号 它首先是一个数组 然后可以退化成元素类型的指针 它首先是一个数组 也就首先是一个数组 然后可以退化成一个元素类型的指针来使用 那么这里的P1中括号它就是一个数组 接着对PE1中括号继续接引用 那么这里的PE1中括号现在是什么类型 它现在就不是一个数组类型 而是退化成元素类型的指针 那么对它进行截引用之后

### **[11:01]**
也就是P1中括号中括号就是on snh类型 也就是找到了这个存储空间 按照on signature类型来读写 我们把它补充完整 我们看一下它的左边和右边 是不是还是有一种等效替换的感觉 table虽然是一个二维数组 但是它在解引用的时候 是按照元素类型的指针来使用的 P1也是一个指针变量 它们俩类型一致 就可以实现等效替换 这里的地址相加的结果没有一点一点的算 就是如果有不理解的话 可以自己慢慢的算结果 然后慢慢的看一下每一步的类型 数组名它本质是一个数组 但是它也有指针的属性 如果某个指针变量与数组的指针类型是一致的 就可以实现等效替换 接着我们举一个例子 从三个二维表格中提取某一个表格的数据 比如这里有三个表格

### **[12:03]**
我们要提取某一个表格数据怎么提取 首先定义三个二维数组 Table1 table2和table3 分别来保存这三个表格的数据 然后再定义一个缓冲区 用来存储某一个表格的数据 接着为他们分配存储空间 这是table1的存储空间 我们先用三个存储单元来代替一下 这是table2 这是table3 这是缓冲区 我们都用三个字节来代替一下 如果我们想把table1的数据复制到缓冲区 这个时候我们写一个循环嵌套语句就可以了 这是内循环 这是外循环 如果我们想把table i复制到缓冲区 是不是还要写一个循环嵌套语句 如果想把table3复制到缓冲区

### **[13:00]**
还要写一个循环嵌套语句 虽然写了三次循环嵌套语句 但是每一次只是修改了变量名 其他大部分都是一样的 那么数组的变量名 我们可不可以用指针来替换一下 定义一个指针变量 这里的指针变量它是一个一维数组的指针 它和table1 table2和table3嗯退化成的指针类型是一样的 都是一个一维数组的指针 如果将table3的地址就是0090给这个指针 那么通过这个指针变量 是不是可以读写每一个存储单元的数据 就等效于table3 那么在写程序的时候 将table3的数据复制到八或 这里的table3就可以替换成P 如果将table i复制到Buff当中 这个时候可以将table i的地址0088给P

### **[14:01]**
这个时候PU 可以找到table i的每一个存储单元的数据 那么在写程序的时候 还是使用这个变量P就可以了 如果将table1复制给buffer 就将table1的地址0080H给这个指针变量 那么这个指针变量就可以读写 它的每一个存储单元的数据 在写程序的时候还是用P来代替table1就可以了 那么我们发现加入指针变量之后 这个循环嵌套语句只需要写一次就可以了 我们来整体的看一下这个程序 首先定义三个二维数组 存储三个表格数据 然后定义一个缓冲区 定义一个指针变量 然后在main函数当中来使用 定义一个临时变量 这个临时变量用来选择哪一个表格 比如现在值是一 就选择第一个表格 再写一个switch分支语句 如果值是一

### **[15:00]**
就执行case1 那么将table1的地址复制给P 接着break就是跳出switch语句 如果数值是二 就将table2的地址给P 接着break 如果是三 就将table3的地址复制给P 如果都不是 就default 然后return return就是退出这个函数 退出这个函数 这个时候我们应该输出一个错误信息 但是我这里用return直接代替了 现在想提取哪个表格的数据 就把这个表格的地址给到P 接着写一个循环嵌套语句 就可以将这个表格的数据复制到缓冲区当中 最后是一个死循环 也就是程序等待在这里 这就是我们今天的一整个程序 今天主要理解的就是一维数组和指针 二维数组和指针之间是如何进行等效替换的

