# 【叶宇单片机】—045  组合判断if  else if  else—【2025-12-24】-中文

## 字幕正文

### **[00:01]**
今天我们还是来学习一下if语句 If else If else 这里先不管它怎么用 先记住两个单词 if如果else 否则我们先来看一下if语句的基本格式 如果条件一是真的 那么就执行大块儿的语句一 接着执行语句why 如果条件音是假的 那么就会跳过大括号里的语句 接着执行语句 Why 这边是它的流程图 这个流程图只是为了方便我们去理解 没有什么其他的意思 程序从上往下执行执行的条件一 对条件进行判断 如果条件音是真的 就是yes 那么会执行语句一 接着执行语句Y 如果条件是假的 就是NO 那么就会跳过语句一 什么都不执行 直接执行英语句why

### **[01:00]**
当条件一不满足的时候 他是什么都不做的 如果我们想有一些操作或者是执行一些语句 怎么办呢 我们可以使用else 如果条件音是真的 那么就会执行大括号里的语句一 接着会跳过else在执行语句why 如果条件一是假的 也就是否则条件一不是真的就来到 否则这里执行它的大括号里的语句X 接着执行语句Y我们来看一下它的流程图 程序从上往下执行 然后来到条件一 对条件进行判断 如果条件一是真的 那么就执行语句一 接着执行语句Y如果条件是假的 那么就会执行语句X接着执行语句Y 这是一个基本的条件二 分支结构就是由一个条件它得到两个分支

### **[02:04]**
但是这两个分支的话我们只能二选一 然后我们看一下 这里只是对一个条件进行判断 如果我们有好几个条件怎么办呢 我们可以使用else if 如果条件音是真的 那么就执行大括号里的语句一 接着会跳过else if执行语句why 如果条件是假的 也就是否则条件一不是真的 那么就来判断条件二 如果条件是真的 那么就执行大块的语句 接着执行语句Y 如果条件爱是假的 那么就会跳过大括号里的语句 接着执行语句why 我们来看一下它的流程图 程序从上往下执行执行的条件一 对条件一进行判断

### **[03:01]**
如果条件一是真的 那么执行语句一接着执行语句Y 那么条件爱的语句无论是真的还是假的 都会被直接跳过 不会被执行到 如果条件一不满足 那么语句一就不会被执行 接着来判断条件二 如果条件二是真的 那么就会执行语句二 接着执行语句Y 如果条件不满足 那么会跳过语句二 什么都不执行 直接执行语句 Why 这里我想说的是条件已满足 执行语句一 如果条件下满足执行语句二 如果他们俩都不满足 就什么都不执行 它是一个多选一的结构 虽然是多选一 但是要注意的就是它有一个先后顺序 就是如果条件一满足了 那么就会执行语句一 无论条件案是真的还是假的

### **[04:02]**
都不会被执行 会被直接的跳过 如果我们再增加一个条件判断 我们再来看一下它的执行顺序其实都是一样的 如果条件是真的 那么就会执行大括里的语句一 接着跳过条件二的判断 条件三的判断执行语句Y如果条件一是假的 就是来到 否则否则条件一是假的 就来判断条件二 如果条件是真的 那么就执行大括号的语句 接着跳过下面的语句来执行语句 Why 如果条件二是假的 也就是否则条件二是假的 就会来判断条件三 如果条件三是真的 就执行大括号里的语句 接着执行下面的语句 如果条件三是假的 那么就会跳过大括号里的语句 接着执行下面的语句

### **[05:02]**
我们来看一下它的流程图 程序从上往下执行 执行到条件一 对条件一进行判断 如果条件音是真的 那么就会执行语句一 接着执行语句Y那后面的条件二和条件三 无论是真的还是假的 都会被直接的跳过 如果条件一不是真的 那么语句一就不会被执行 接着来判断条件二 如果条件二是真的 就会执行语句二 接着执行语句Y后面的条件三也会被跳过 如果条件二是假的 那么语句二就不会被执行 就会来判断条件三 如果条件三是真的 那么就会执行语句三 接着执行语句Y 如果条件三也是假的 那么就会跳过语句三 什么都不执行 直接来执行语句 Why 我想说的是

### **[06:00]**
这是一种多选一的结构 就是如果条件是真的执行语句一 如果条件二是真的执行语句二 如果条件三是真的执行语句三 如果条件一条件二 条件三都不是真的 那么什么都不是执行 虽然是多选一的结构 但是它有一个先后的顺序 就是先判断条件一 如果条件一是真的 那么就执行语句一后面的条件二和条件三 无论是真的还是假的 都会被直接的跳过 所以说我们在做项目的时候 判断条件的时候 要注意条件的先后顺序 当条件一是假的条件 二是假的条件 三是假的 他是什么都不执行的 如果我们想执行一些操作 或者是执行一些语句 怎么办呢 我们可以在后面加一个else 如果条件是真的 那么就执行大括号里的语句一

### **[07:00]**
接着跳过下面的if相关语句 执行语句why 如果条件一是假的 就是否则条件一不是真的 就来判断条件二 如果条件二是真的 那么执行它大括号的语句 接着跳过下面所有的语句 执行语句why 如果条件二是假的 就是否则条件二是假的 就来判断条件三 如果条件三是真的 就执行大括号里的语句 接着跳过下面的语句 执行语句Y 如果条件三也是假的 那么就否则条件三是假的 就来到else执行else大括号里的语句 接着执行下面的语句 我们来看一下它的程序流程 就是程序从上往下执行执行的条件一 对条件一进行判断 如果条件是真的 就执行语句一

### **[08:00]**
接着执行语句Y如果条件一是假的 那么语句一就不会被执行 然后来判断条件二 如果条件是真的 那么就会执行语句二 接着执行语句Y后面的条件三 满足与否都会被直接跳过 如果条件爱不是真的 那么语句爱就不会被执行 接着来判断条件三 如果条件三是真的 就执行语句三 接着执行语句Y 如果条件三也不是真的 那么语句三就不会被执行 接着来执行语句X 然后再来执行语句 Why 这里还是多选一的结构 如果条件一满足执行语句一 如果条件二满足执行语句二 如果条件三满足执行语句三 如果三个条件都不满足 然后执行语句X虽然是多选一 但是它有一个先后的顺序

### **[09:02]**
就是如果条件一满足 那么就执行语句一 后面的条件二和条件三 无论满足与否都会被跳过 所以说在做项目的时候 要注意下条件的先后顺序 现在if else if和else都讲完了 我们来看一下一些比较容易出错的地方 就是else if只能放在if的后面 else if只能放在if的后面 因为你把它放在前面 也没有这样的语法 所以说是错的 也可以放在最后面 第二条需要注意的就是else不能放在中间 比如这里的else if条件I的判断 把后面的去掉 我们来看一下 把ELS放在中间 如果条件一不满足 就来到else 然后再执行语句二 那下面的else if条件三需不需要执行呢

### **[10:03]**
我们来看一下它的流程图 程序从上往下执行执行的条件一 对条件进行判断 如果条件一是假的 那么就会直接执行语句二 那条件三是不会被执行的 之前条件三会被执行 因为它这里有一个分支 有一个台阶 现在他没有这个台阶 所以说条件三是不会被执行的 不会被执行 但是这一段语句放在这儿又不合适 所以说注意的就是else不能放在中间 else只能出现在最后 并且只能出现一次 就是放在最后也只能出现一次 如果条件一不满足条件 二不满足条件三不满足 那么就执行else里面的语句 X就相当于对if的条件分支进行一个封口 它后面没有其他的分支了 到此结束了

### **[11:00]**
还要注意的就是else它跟随最近的一个if 如果这里else if条件三的判断 我把else去掉 那么这个else就会跟随这个if进行条件判断 我们来看一下 如果把else丢掉一个 会有什么影响呢 这是它原来的语句 假如如果条件一是真的 那么就执行语句一 接着执行语句Y在流程图当中 就是如果条件一是真的 就执行语句一 接着执行语句Y这里只执行了两条语句 我们再看一下 丢掉一个else 这里if条件三相当于这里有两条if语句 我们先看第一个if语句 如果条件音是真的 那么就执行语句一 接着跳过下面的语句 来到了第二个if语句 然后来判断条件三 如果条件三是真的 就执行语句三

### **[12:01]**
如果条件三是假的 就执行语句X接着执行语句Y 我们再来看一下它的流程图 判断条件一 如果是真的执行语句一 接着来判断条件三 无论如果是真的执行语句三 如果是假的执行语句X 接着再执行语句Y之前我们只执行了两条语句 现在丢掉一个else 执行了语句一之后再来判断条件三 无论条件三是成立的还是不成立的 这里都会执行一条语句 然后再来执行一条语句 Why 所以说丢掉了一根else 他虽然是它虽然形成了一条新的if语句 而且这个新的if语句编译器也查不出来 也认为它是正确的 但是实际执行的效果是不一样的 所以说我们要注意一下

### **[13:01]**
最后总结一下 if只能出现在开头 else if出现在if之后 else只能出现在最后 并且只能出现一次 就是对条件分支进行一个封口 然后else跟随最近的if 就是本来他是跟随这个if的 如果这里的else丢掉 那么他就会跟随这个if进行判断 定义一个变量X初值是五 然后定义一些变量ABC初值是零 我们来看一下二选一的情况 如果X大于六 那么X的值是五 五不大于六 这个条件是不成立的 那么就会执行else将R赋值给A 我们再来看一下多选一判断 如果X大于六 那么X是不大于六的

### **[14:01]**
就来判断下一个条件七等于X吗 那么七也是不等于X的 因为X的值是五 就来到else将三赋值给B 我们再来看多选一或者是什么都不选 如果X大于六 这个条件是不成立的 就来判断七等于X吗 这个条件也是不成立的 那么就来判断下一个条件八等于X吗 这个条件也是不成立的 那么后面就没有其他的语句了 也就是所有条件都不成立的话 那么什么都不选 直接跳过这些语句 还有这里条件判断的时候 有些你是直接能判断它的真假的 比如说大于小 于是不是等于这些你能直接判断真假的嗯 有的时候是一个运算式嗯 或者是一个变量 这些都是得到的结果 都是一个具体的数值 我们之前说过是一个数值的话

### **[15:01]**
就是非零即真 这些数值如果不是零的话 就是真的 如果是零的话 就是假的

