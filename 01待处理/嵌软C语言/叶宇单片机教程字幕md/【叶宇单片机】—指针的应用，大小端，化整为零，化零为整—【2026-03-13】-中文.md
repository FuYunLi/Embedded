# 【叶宇单片机】—指针的应用，大小端，化整为零，化零为整—【2026-03-13】-中文

## 字幕正文

### **[00:01]**
今天我们来看一下指针的应用 感受一下指针的灵活性 比如大小端化整为零 化零为整 先看一下大小端 这里有一个数字1万 那么猜一猜哪边是大端 哪边是小端 左边是大端 右边是小端 这里有一个16进制数 同样左边是大端 右边是小端 在单片机当中也有大小段 这种说法 主要是指它存储的方式是从大端开始存储的 还是从小端开始存储的 我们来定义一个变量 看它的存储方式 同样申请一个存储空间 如果先存储的是12H 那么它就是从大端开始存储的这种方式 我们叫做大端存储方式 如果是从七八开始存储的 那么我们叫做小端存储方式 五一单片机采用的是大端存储方式

### **[01:00]**
其他的单片机可能采用的是小端存储方式 那么我们如何确定大小端呢 这个时候我们就可以利用指针的灵活性 比如我们可以先获取第一个存储单元的地址 然后按照啊sa char类型来解读 这个存储单元的数据 我们来具体的看一下 首先定义一个指针变量 对指针变量要进行初始化 再定义一个变量B 我们想获取第一个存储单元的地址 那么这个时候就可以取A的地址复制给P 取A的地址 它是什么类型的呢 它是嗯SUNDERLG类型的指针 AP的类型是on satchar类型的指针 两个类型不一致是不可以直接赋值的 这个时候可以用类型的强制转换 那么就可以直接赋值 那么P存储的就是0081

### **[02:00]**
H指向的也是0081 接着将星P赋值给B 也就是打开0081这个存储单元 按照什么类型来解读呢 那么我们看一下P定义时候的类型是on sa CHA类型的指针 那么这个存储单元 就按照on3的char类型来解读 如果B的值是12H 那么它就是大端的存储方式 如果B的值是78H 那么它就是小端的存储方式 接着我们看一下这个存储单元外面 他们的地址的变化是不是都是从低到高 那么我们对P加一进行解引用 就得到下一个存储单元的数据 如果是34H 它就是大端存储方式 如果是56H就是小端存储方式 接着也可以对P加二进行截应用 P加三进行截应用 这里有没有感受到指针的灵活性 就是它想获取谁的地址

### **[03:01]**
就像普通变量赋值一样简单 那么对这个地址怎么解读 也是他自己说了算 比如这里的A本来是on slg类型的 然后他按照on3的CHA类型来一个一个的解读 接着我们看一下化整为零什么意思呢 比如说我们有一个四个字节的数据 我们要把它发送出去 是不是八位八位发送呢 这个时候就需要化整为零 还是用变量A举一个例子 这个时候定义一个数组 它有四个元素 每一个元素都是on3的char类型的 就是将A拆成八位 分别存储在这个数组当中 我们先来使用移位的方式看一下 比如将A向右移24位 那么得到的就是0X12 因为数组元素是on3的char类型的 所以说只能存储第八位 那么第一个元素存储的是12H

### **[04:00]**
接着将A向右移16位 得到的就是0X1234 同样只能存储第八位 第二个元素得到的是34H将A向右移八位 得到的是0X123456 那么存储第八位得到的就是56H 最后将A向右移零位 得到的就是0X12345678 那么存储D8位就是78H 这里一个一个进行赋值 是不是有一点麻烦 那么我们可不可以整体进行赋值呢 但是一个变量是整数类型 一个变量是数组 没办法直接复制 这个时候我们可以借助指针 还是定义一个指针变量 我们想整体的将A赋值给数组 这个时候数组是零散的 是不是要将它整合一下

### **[05:01]**
这个时候取数组的地址给P 那么P存储的就是0085 指向的也是0085 将A赋值给新P 也就是对P进行解引用 也就是打开0085这个存储空间 按照什么类型来解读呢 我们看一下P定义的类型 是不是on sallog类型的指针 那么就按照UNSALLOG类型来解读 这个时候就可以通过新P 对这个存储空间进行读或者写 同样A是on3的lg类型的 那么就可以将A直接赋值给新P 这时候只需要一次赋值就实现了化整为零 那有没有发现指针的灵活性 本来他是一个数组 现在P获取到他的地址 就可以将它们整体的按照on3的lg类型来解读

### **[06:00]**
接着我们看一下化零为整之前 我们是不是化整为零 是为了一个一个发送数据 现在化零为整就是对接收的数据进行整合 还是刚才这两个变量 有一些零散的数据放在数组当中 我们想化零为整 先用移位的方式看一下 比如将第一个元素赋值给A 那么A得到的就是0X12 那么前面几位补零 得到的就是0X1200 然后A加上数组的第二个元素赋值给A 第二个元素是0X34 A的结果就是0X1234 得到的结果就是LINX123400 这个时候A再加上数组的第三个元素 得到的就是0X123456

### **[07:02]**
得到的就是0X12345600 这个时候A再加上数组的第四个元素 那么得到的就是0X12345678 A的结果就是0X12345678 这样一个一个赋值是不是比较麻烦 那我们想可不可以像刚才一样整体进行赋值 同样这个时候我们需要借助一下指针 再定义一个指针变量 我们想整体进行赋值 这个时候数组是不是零散的 那么我们就将它整合一下 将数组的地址赋值给PP存储的就是0085 这个时候将星P赋值给A星P 也就是打开0085这个存储空间 按照什么类型来解读呢 按照ASSALANG类型来解读 这个时候就可以通过新P 对这个存储空间进行读或者写

### **[08:01]**
也就像普通变量一样来使用它的存储空间 同样A也是阿三的lg类型的 这个时候就可以直接赋值 将新P赋值给A同样也是一次赋值 实现了化零为整 这里感受一下指针的灵活性 指针它想获取谁的地址 就可以获取谁的地址 就像普通变量赋值一样简单 那么打开这个地址的存储空间 按照什么类型来解读这个存储空间的数据呢 也是指针自己可以说了算呢 但是也不是瞎解读的 或者说是胡乱解读的 你在解读的时候 你要对这个数据要做到心中有数 你要知道你解读的是什么东西 好这节课到此结束

