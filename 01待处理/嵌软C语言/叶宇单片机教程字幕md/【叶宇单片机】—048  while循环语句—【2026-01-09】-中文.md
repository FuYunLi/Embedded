# 【叶宇单片机】—048  while循环语句—【2026-01-09】-中文

## 字幕正文

### **[00:01]**
今天我们来看一下while循环语句 主要是理解它的一个执行顺序 我们先来看一下它的常见格式 Why are you 后面是一个条件判断 接着是大括号里面放一些语句 接着是括号外的语句 首先是条件判断 这个条件判断和if里面的条件判断是一样的 也是有两点 一是对常量或者是变量进行判断 也就是对一个数据进行判断 只有当结果是零的时候才是假的 还有一点就是关系判断 也就是大于小于等于 这些Y有里面对这个条件进行判断 如果是真的 那么就执行大括号里的语句 接着不会执行下面的语句H返回到条件 再次对条件进行判断 如果条件是真的 那么还是执行大括号里的语句 接着再返回对条件进行判断

### **[01:01]**
如果条件是真的 那么就会一直循环的执行大括号里的语句 只有当条件是假的时候 才会跳过大括号的语句 接着执行括号外的语句 我们来看一下它的流程图 流程图只是为了方便我们去理解它的执行顺序 没有什么其他的意思 比如程序从上往下执行 执行到条件 对条件进行判断 如果条件是真的 那么就会执行大括号里的语句 也就是语句一到语句N 接着不会执行括号外的语句 而是返回到条件 对条件再次进行判断 如果条件是真的 那么就会一直在这里循环执行语句一到N 只有当条件是假的 才会执行括号外的语句 我们来看一下why有语句的一些特殊的写法 这是它正常的一个写法 如果我们把两个大括号去掉

### **[02:00]**
那么这个时候蛙有语句和语句一 实际上是一条语句 在C语言当中 一个分号表示一条语句结束 所以while有和语句一 它们实际上是一条语句 也就是while有下面的大括号里 只有一条语句一 接着是语句二和语句N加一 我们再来看另一种特殊的写法 这是它正常的一个写法 比如我们把两个大括号去掉 在Y有条件后面再加一个分号 一个分号代表一条语句结束 那么这就是一条独立的语句 这也是这也是这也是它们都是一条独立的语句 那么why you语句是什么意思呢 也就是相当于大括号里只有一条空语句 接着是语句一 语句二和语句N加一 就是说对他的条件进行判断 如果条件是真的 那么就会执行一条空语句

### **[03:01]**
这有什么意义呢 其实它也有它的使用场合 比如说对一个事件进行判断 如果这个事件满足 就会执行空语句 接着返回条件再次进行判断 如果满足就再次执行空语句 接着返回再进行条件判断 也就是说这个事件如果满足的话 就会一直在这里循环执行空语句 其他的语句 其他的程序就不会被执行了 这像不像程序就卡在这里了 专业的讲叫做阻塞 当然还有一种叫做非阻塞 非阻塞就是同样能实现条件判断 但是不会让程序卡在这里 当这个条件为假的时候 才会跳过空语句 执行下面的语句 我们来写程序看一下 还是在我们的程序模板上进行练习 首先定义三个变量 ab的初值是零 还有一个I

### **[04:02]**
main是程序的入口 从这里开始执行 我们第一条语句 将三复制给I 然后来到Y有语句里判断II的值是三 也就是条件是真的就会执行大括号里的语句 第一条语句是I减一复制给II的值是三 3-1=2 那么二的值就是二 第二条语句是A加一赋值给AA的初值是零 那么01是1A的值 就是一 接着再回去对条件进行判断 I的值现在是二还是真的执行大括号的语句 首先是I减一赋值给I 也就是结果是一 接着是A加一赋值给AA的结果是二 接着再返回去对条件进行判断 I现在的值是一还是真的 在执行大括号里的语句 2-1 也就是1-1

### **[05:00]**
结果是零 A加一 也就是I加一 结果是三 接着再返回去对条件进行判断 R的值是零 那么判断结果是假的 就会跳过大括号里的语句 接着执行下面的语句 那么执行这条Y有语句我们得到了R是零 A是三 接着执行下面的语句 将零赋值给I再来判断Y有里面的条件 零小于三是真的 接着执行大括号的语句 I加一复制给I I的值是零 0+1是一 I的值是一 然后是B加二赋值给BB的初值是零 0+2 B的值是二 接着再回到条件 对条件进行判断 R的值是一 一小于三是真的 那么执行大括的语句 2+1 也就是1+1 二的值是二 B加二 也就是2+2

### **[06:00]**
B的值是四 接着再返回去对条件进行判断 I的值是二二小于三是真的 那么执行大括号里的语句 2+1 也就是I加一 结果是三 B加上二 也就是4+2 结果是六 再返回去对条件进行判断 三小于三 这个是不成立的 那么就会跳过大括号里的语句 接着执行下面的语句 执行这条蛙有语句 我们得到R13 B16 接着将A的结果输出 B的结果输出 也就是A的值是三 B的值是六 我们来查看一下 第一个数是三 也就是A的值是三 第二个数是六 也就是B的值是六 与我们分析的结果是一致的 好这节课到此结束 拜拜

