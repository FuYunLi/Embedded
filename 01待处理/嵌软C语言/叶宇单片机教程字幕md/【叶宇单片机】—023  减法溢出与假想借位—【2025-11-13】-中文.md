# 【叶宇单片机】—023  减法溢出与假想借位—【2025-11-13】-中文

## 字幕正文

### **[00:01]**
大家好 今天讲的是第23节减法溢出与假想借位 什么是有符号数 什么是无符号数 假如这里的数据范围是八位的 就是从第零到第七 无符号数就是所有位都是用来表示数据的 通常变量类型的前缀是unsigned 有符号数就是最高位 用来表示符号位 最高位为零 就表示的是正数 最高位为一 表示的就是负数 剩余的位用来表示数据 通常变量类型的前缀是signed 比如这里的类型家族二进制的位数不同 有八位的 16位的 16位的 32位的 每个家族都有无符号类型 还有有符号类型 我们说负数的二进制表示方式 也就是说的是有符号类型

### **[01:00]**
假如有符号数据类型的范围是四位的 这里只是说假如最高位表示符号位 零表示的是正数 一表示的是负数 正数的二进制表示方式是比较好理解的 那么负数是怎么得到的呢 一是不是0-1就得到一 这里我们用0-1想得到一 但是零不够减的 我们向前借一位 然后后面还是四个零 再减去一就得到四个腰 表示的就是一 这里的小B表示这一串数是二进制的 我们再来看一下二 二就是0-2得到二是不是不够减呢 我们向前面借一位 然后还是四个0-2得到一个结果 这个结果就是二 其他的同理在理解复数的时候 用这种借位方式比较好理解

### **[02:02]**
这里我们说一下什么叫做补码 这里的四位二进制表示方式都是补码 零和正数的补码是它们的本身 负数的补码我们可以通过借位得到 那有人会问我们平时用的都是原码反码补码 它们之间是什么关系呢 举个例子 10000-2得到的是二的补码 100-2 100-0010 这样直接相减是不是有借位 但是单片机不擅长处理借位怎么办呢 我们将1000拆成四个一 然后再加上一 那么就是四个1-0010就没有借位了 实际这一步是取反 也就是原码取反之后是一个反码 得到的是反码 所以说不用减法

### **[03:00]**
也可以直接对每一位取反就可以了 得到的结果就是所谓的反马 反马再加上一就得到了补马 我们在计算的时候直接用原码取反 然后加一就得到了补码 这种计算方式比较快 但是理解的时候还是用借位来理解比较好 假如有符号数是八位的最高位 表示符号位 符号为零表示的是正数 符号为幺表示的是负数 正数我们很好理解 那么负数怎么得到呢 还是用0-1不够减 向前借一 后面零保持 然后再减去一个一 就得到八个一 这就是一 那么二的话也是0-2不够减 往前借一 后面的零保持 然后再减去二 得到二的补码 同样假如有符号的数据范围是16位的

### **[04:02]**
那么负数的补码还是16个零 减去一不够减的向前借一 然后再减去一 得到一个16个腰 也就是0x ff ff ff f 这就是一负二还是16个零 减去二不够减的 向前借一 后面的零保持 然后再减去二 得到的结果就是二的补满 理解复苏补码的时候 用这种借位方式理解比较好 计算的时候用原码取反加一就可以得到补码 这种计算方式比较快一点 我们平时写代码的时候用的是负数 这方面我们自己认识 但是经过编译器之后都会变成补码 也就是一种二进制的形式 那说明负数在单片机当中 一切的行为都是补码方式 比如计算存储都是补码方式 为什么没有减法器

### **[05:02]**
加法运算始终是从最右列向最左列进行计算 如果有进位的 就放到下一列 然后一起进行加法计算就可以了 但是减法运算没有进位 只有借位 比如253-170 十六三减去六 需要借位 5-7 需要借位 如何才能实现这个反逻辑呢 答案是并没有这么做 并没有减法器 而是用一个小技巧来让减法不涉及借位 这个小技巧就是将减法转换为加法 将被减数减去减数转化为被减数 加上减数的补码 让减法完全使用加法器 比如这里八位的加法器 原来输入的是两个加数 现在输入的是一个被减数和减数的补码 进行相加 无论是加速还是被减数 减数的补码实际上都是二进制 都是一个数 加法器才不管你是什么数呢

### **[06:01]**
给我我就相加 我们只是利用加法器能实现相加的这个特性 至于为什么通过这种方式能实现减法 我们放在例题后面来讲 对于想一探究竟的可以听听 在做例题之前 我们复习一下20节讲的算数转换规则 这个规则适用于加减乘除运算 还是四个字提升与对齐 然后运算结果的类型与加速的类型是相同的 这里是类型的等级 类型等级与二进制的位数有关 位数越多 它的等级越高 当类型等级低于int家族的都转为int家族的类型 那为什么都转为int家族呢 因为int类型与单片机的数据线的位数相关 16位的单片机16根数据线 int是16位的 一次可以处理16位数据

### **[07:00]**
32位单片机32根数据线 int是32位的 一次可以处理32位数据 所以都提升成int类型 就是单片机一次可以处理的数据类型 减法运算练习题 一定义一个变量执行这条语句 我们看一下编译器是怎么使用运算规则的 先提升后对齐 零是int类型 一是int类型 两个类型一样就可以计算了 0-1就是0+-1的补码 这里是零 这里是一的补码 得到一个结果和一个int类型 也就是0-1之后得到一个结果和一个类型 那我们看一下ff f ff f类型是int 那么结果是一 那么0-1=-1 这个减法运算是对的 但是将0-1赋值给AA提供的存储空间

### **[08:00]**
是八位的 那么执行完这条语句之后 就会将第八位FF存到A的空间当中 A是按照on sch来解读这个空间的数据的 那么ff f按照ANCACHA来解读 就是I55 那么这个时候看结果又是错的 但是错不是因为减法运算造成的错误 而是因为他的解读方式造成的错误 如果A按照sin的char来解读 那么FF就是一 还有一个需要注意的地方 就是这里的int int是有符号的 这里的an sa char是无符号的 那么在赋值的时候会不会改变符号 不会的 赋值就是把二进制复制进去 不会修改二进制的位置 至于怎么解读这个数据与赋值没有关系

### **[09:01]**
练习题二 定义一个变量执行这条语句 我们先看一下编译器是怎么使用运算规则的 先提升后对齐 二是int类型 五是int类型 两个类型一致就可以计算了 2-5就是I加上五的补码 这是二 这是五的补码 得到一个结果 结果是int类型 那么I减五之后得到一个结果 有大小有类型 FFFD是int类型的 那么结果就是三 2-5=-3 减法运算是正确的 我们将2-5赋值给BB提供的空间是八位的 执行完这条语句 将低八位存储到B的空间当中 那么B存储的值就是FDB 按照sin a chat解读这个数据

### **[10:01]**
那么FD就是三 这个时候解读的结果也是正确的 练习题三 定义一个变量执行这条语句 零是int类型 一是另int类型 两个类型一致就可以计算了 0-1就是零 加上一的补码 这是零 这是一的补码 得到一个结果 结果是int类型 那么0-1之后得到一个结果 有大小有类型 那么FFFF是int类型的结果就是一 那么0-1=-1 减法运算是正确的 我们将这个结果赋值给C提供的存储空间 是16位的 执行完这条语句 就可以将这16位存到C的空间当中 但是C是按照按SIGINT来解读的 就是无符号16位 那么FFFF就是6535 结果又是错误的

### **[11:00]**
但是这个错误不是因为减法运算而发生的错误 是因为解读方式哎发生了错误 如果C是按照send int 也就是16位有符号类型来解读 那么ff f ff f就是一 就是对的 练习题四 定义一个变量执行这条语句 二是int类型 for是int类型 那么I减五就是I加上五的补码 这是二 这是五的补码 得到一个结果 结果是int类型 那么2-5之后得到一个结果 有大小有类型 FFFD是int类型 那么这个值就是三 那么I减五之后得到的是三 那么减法运算是正确的 将这个值赋值给D D提供的存储空间是16位的 执行完这条语句就将这16位存到D的空间 因为D按照sign int就是16位

### **[12:02]**
有符号类型来解读的 所以说FFFD解读下来就是三 结果也是正确的 练习题五定义两个变量 执行这两条语句 我们来一条一条执行 先执行第一条语句 加减运算是从左往右依次运算 我们先运算1-6 一是int类型 六是int类型 两个类型一致就可以计算了 1-6就是1+-6的补码 这是一这是六的补码 得到一个结果 还有一个类型 那么1-6之后得到一个结果 有大小有类型 FFFB是int类型的 那么结果是五 说明减法运算是正确的 1-6之后得到的结果是五 五再加七 五是int类型

### **[13:00]**
七也是int类型 那么-5+7就是7+-5的补码 这个是五的补码 这个是七两个数相加得到一个结果 还有一个进位位 结果是int类型的 因为结果是int类型的 所以说只能存储这16位进位位就会被丢掉了 那么-5+7得到一个结果 有大小有类型 000I是int类型的 那么结果就是I运算的结果是对的 整体下来就是1-6加上七 执行完这条语句之后 得到的结果是二是int类型的 执行完这条语句之后 将结果赋值给EE提供的存储空间是八位的 只能存储低八位 就将0X02 就将0X02存到E的空间当中 那么E是按照an sa char来解读这个数据的

### **[14:02]**
所以说0I也就是二结果是正确的 我们来执行第二条语句 从左到右依次计算 先计算1+7 一是int类型 七是int类型 类型一致就可以计算了 1+7结果是八 是结果是int类型 那么1+7得到一个结果 有大小有类型 结果是八 然后八再减六 八是int类型的 六是int类型的类型一致就可以计算了 8-6就是8+-6的补码 这是八 这是六的补码 得到一个结果和一个进位位 结果的类型是int类型的 因为结果是int类型的 所以说只能存储这16位进位位就会被丢掉 那么8-6得到的结果有大小有类型 这个结果是二

### **[15:00]**
8-6就得到二 说明这个减法运算是正确的 执行完1+7减六 将这个结果赋值给F F提供的存储空间是八位的 所以说只能接收第八位 也就是0X02存储到这个空间当中 F是按照an sa cha来解读这个数据的 所以说0X02就是二 结果也是正确的 我们在减法运算的几个例题当中发现 把减法转换成加法 虽然说感觉不是符合逻辑 但是结果都是正确的 他最后出现错误 也只是因为不同的解读方式发生了错误 但是我们也对这种减法转换成加法存有疑虑 是为什么呢 还有就是为什么没有减法器 为什么要用补码 我们来解决这三个问题 为什么没有减法器 加法运算 每一列进行相加进位位进到下一列

### **[16:00]**
然后一起相加 减法运算没有进位 只有借位 比如I53-17 六 3-6 要借位 5-7要借位 如何才能实现这个反逻辑呢 答案是并没有这么做 并没有减法器 而是用一个小技巧来让减法不涉及借位 这个小技巧就是将减法转换成加法 也就是将被减数减去减数转换为被减数 加上减数的补码 让减法完全使用加法器 为什么这种方式是合理的呢 我们来讨论减法运算当中的几种情况 被减数和减数一个是正数 一个是负数 被减数和减数都是负数 253-176 392 00:16:49,230 --> 00:16:54,980 先加上0X10000-176 再减去0X10000 加上一个数

### **[17:00]**
再减去一个相同的数 这个式子是成立的 I53的16进制是0XFD0X10000 减去176 他得到的结果是0XFF50 然后再减去一个数 两个相加得到一个值 再减去一个数 最后得到这个结果 结果是77 整个运算过程都是正确的 符合数学逻辑的 如果这是一条C语言的运算 是不是遵循运算规则 也就是先提升后对齐 我们从这个角度看一看 负的176 负的176是int类型的 是16位的 那么它的补码就是0X10000 减去176 这一句就实现了被减数加上减数的补码 但是按照数学逻辑来讲 它还要减去一个0X10000

### **[18:00]**
才是正确的结果 但是我们在前面做减法运算的时候 只是将被减数加上减数的补码 并没有减掉一个什么值 为什么结果还是正确的呢 我们来看加法器是怎么做的 253-176 就是253+-百七十六的补码 得到一个结果 还有个结果类型 我们用加法器来运算 先运算第八位FD加上0X50 得到一个结果和一个进位位 我们将结果保存起来 将进位位放到下一列进行计算 再计算下一列零零加上FF再加上进位位 100FF近位位一 得到一个结果和一个进位位 我们将结果保存起来 这个是数学运算 这个是加法器运算 我们放在一起看一看 加法器运算的是这一步被减数加上减数的补码

### **[19:04]**
得到一个这个结果 这个加法器都可以做到 但是加法器没有减去这个值 我们知道I53是int类型 负的176是int类型 那么得到的这个结果就只能取这16位 进位就会被丢掉 所以说在加法器当中 近位位是自动丢掉的 虽然没有体现减去这样一个值 但是丢掉近位位就相当于减去它 所以说结果是正确的 我们再来总结一下 这一步实际上是被减数加上减数的补码 往上看 也就是这一个过程 我们发现被减数大于这个减速 得到的结果是正的 然后再加上这个值 那么加法器得到的这个结果 必然是一个溢出的状态 也就是相当于减掉了这个部分 所以说结果是正确的

### **[20:01]**
被减数大于减数的时候 当被减数加上减数的补码必然是会溢出的 这就是它的溢出部分 176-253 176+1个数 然后再减去253 再减去这个数 在数学逻辑上是没有问题的 176的16进制加上0S10000 减去253 得到了一个结果 再减去这个值 这两个数相加得到一个结果 然后再减去它最后得到一个-77 整个数学逻辑都是正确的 我们看这一部分就相当于是176加负的 253的补码 还要减去这样一个数 但是我们在前面做减法运算的时候 只是将被减数加上减数的补码 并没有减掉一个什么值 为什么结果还是正确的呢 我们来看加法器是怎么做的

### **[21:01]**
176-253 就是176加上负的253的补码 得到一个结果 还有一个类型通过加法器来计算 先计算第八位 也就是0SB00X03 得到一个结果和一个进位位 将结果进行保存 然后计算高八位 零X000XFF得到一个结果和一个进位位 将八位的结果进行保存 我们把数学计算的过程与加法器计算的过程 放在一起 看一看 加法器完成的过程是这一部分 然后得到一个结果 但是结果减去这个值在加法器当中并没有体现 这是为什么呢 这个值得到的结果是负的77照抄过来 也就是得到这个等式 那么0x ff fb3就等于0X1

### **[22:01]**
0000-77 这是什么呢 这是不是负的77的补码 负的77的补码就是0SFFB3 因为在单片机当中 复速直接是以补码的方式存储的 所以说这里就存储了它的补码 就没有进行减一个值 所以说这种情况也是正确的 我们来总结一下 被减数加上减数的补码 往上看的话就是完成了这个过程 因为被减数小于减数 那么被减数小于减数 那被减数减去减数得到一个负值 负值再加上这个值的话 那么计算的结果必然是小于这个值的 也就是不会溢出 也就是被减数小于减数 将被减数与减数的补码相加 必然不会溢出 同时这个结果也是一个补码 -2-5就是两个补码的相加

### **[23:02]**
但是结果要减去这两个数 才能够得到一个正确的结果 我们加法器是怎么做的呢 -2-5就是二的补码 加上五的补码 得到一个结果 还有一个近位位结果了 类型是int类型的 因为是int类型的 只能存储这16位 我们看一下这两个补码 经过加法器的运算 存储了一个结果 还有一个进位位 这个是数学计算的过程 这个是加法器计算的过程 放在一起看一下 加法运算可以完成的 就是这一部分得到了这个结果 也就是存储在这里 还有一个进位位 但是因为他们都是int类型的 这个近卫卫会被丢掉 也就是相当于减去这个值 得到了这个值 但是这个值减去这个值 但加法器并没有体现出来 为什么结果还是正确的呢 0XFFF9-0X10000等于七

### **[24:04]**
将它抄过来就是这个样子的 那么0XFF9就等于0X10000-7 七的补码就是0x ff ff f9 又因为负数直接是以补码的方式存储的 所以说结果只保存这16位是正确的 负的3I76I减去32760 这个是一个数学计算的过程 肯定是对的 这是一个机器计算的过程 负的3I76I减去SI760 也就是两个补码相加得到一个16位的结果 结果是int类型的 因为是int类型的 只能存储这16位 借位位就会被丢掉 经过加法器运算 也就存储了这16位和一个进位位 我们把这两种运算放到一起看一下

### **[25:00]**
加法运算完成的过程是这一步得到一个结果 因为负的32762和负的32760 都是int类型的 所以说只会存储这个16位近位就会被丢掉 就相当于这里减掉了一个值 得到一个这样的结果 那我们根据之前的经验 0X000E减去0X10000 得到一个负值 把它整理过来是这一行 那么0X000E是不是等于0X10000 减去一个值 是某个负值的补码呢 但是我们知道复数的符号位都是一诶 这里符号位是零 所以说结果是无效的 我们也知道负的32762是int类型 负的32760是int类型 而它的结果已经超过int类型 所以说输出的结果是错误的 我们刚才讨论了这几种情况

### **[26:02]**
将减法运算改成被减速加上减速的补码 都能够得到一个正确的结果 但是还是要遵循这个运算规则 提升与对齐 结果也是有类型有大小的 所以说如果运算的结果超过它的类型范围 也会得到一个错误的结果 有什么解决办法吗 也就是20节讲的必杀技 就是提供足大的空间来存储数据 这节课到此结束 拜拜

