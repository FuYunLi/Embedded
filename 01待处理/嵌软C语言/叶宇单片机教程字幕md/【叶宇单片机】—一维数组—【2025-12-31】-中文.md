# 【叶宇单片机】—一维数组—【2025-12-31】-中文

## 字幕正文

### **[00:01]**
这节课我们来看一下一维数组 首先想一下为什么会有一维数组 前面我们也学习了各种类型 那么对于大大小小的数据都可以存储 为什么还会有一维数组呢 比如这是一个串口接收的数据 都是16位的 我们需要把这16位的数据 一个一个的存储起来 我们可以定义一个变量存储 第一个数据 定义一个变量存储 第二个数据再定义一个变量存储第三个数据 那么下面还有许多数据需要存储 我们就需要定义很多个变量来存储这些数据 那么一个一个来定义这些变量 是不是很麻烦 这个时候就需要我们的数组 比如我需要存储的数据 需要on3的int类型 我需要三个这样的类型 然后再定义一个变量名

### **[01:01]**
这就是一个一维数组 当编译器看到这样的一维数组 就会在内存当中分配三个连续的啊 SIGINT类型的存储空间 那么就不需要像这样一个一个来申请了 这就是一维数组的意义 就是批量的申请同一类型的存储空间 现在一位数组写好了 三个存储空间也分配好了 那怎么使用呢 比如这里是三个基本的变量 A1A2A三 我们平时是将A2赋值给A1 或者是A1赋值给A2 直接使用他们的变量名 就可以读取数据或者是存储数据 也就是读出数据或者是存储数据 通过变量名就可以直接操作 那么可不可以使用数组名直接操作呢 我们来看一下 比如将A2赋值给BB有三个元素

### **[02:02]**
你要存储在哪个元素当中呢 或者是将B赋值给A2 同样B还是有三个元素 你要取哪个元素进行赋值呢 所以说数组名不可以这样直接的使用 我们再想一想 我们一开始使用数组的目的是什么 是不是批量的申请同一类型的存储空间 现在我们想使用数组 实际上是不是使用on sign int类型 也就是使用三个ASSAINT类型的存储空间 那么如何找到这三个on st.类型的存储空间呢 基本的变量我们可以通过A1A2 A3找到这个存储空间 对这个存储空间进行存储或者是读取 在数组当中 我们也想使用这三个assigned int类型的存储空间

### **[03:01]**
我们可以通过B中括号下标零 找到第一个存储空间 可以通过B中括号一找到第二个存储空间 可以通过B中括号二找到第三个存储空间 注意这里的三代表它有三个元素 也就是申请三个ASSAINT类型的存储空间 但是我们在找到第一个 on3的int类型的存储空间的时候 它的下标是从零开始的 然后依次是一和二 这里使用的时候要注意一下 所以说数组的最终使用 还是回归到基本的on st.类型来使用 它实际上和你直接定义三个 ASSAINT的使用方法是一样的 比如可以将A2赋值给B0 R的数据存储给B0 也可以将B0存储到A2 将B0的数据存储到A2 也就是通过B0

### **[04:00]**
B1B2可以对存储空间进行写 或者是读 这里的操作和你直接定义变量名进行操作 它实际上是一样的 还是那句话 我们使用数组 只是为了批量的申请同一类型的存储空间 最终使用的时候还是回到基本类型来使用 我们再来看一下数组的越界 比如B有三个元素 每一个元素都是ASSAINT类型的 那么编译器就会分配三个 ASSAINT类型的存储空间给你后面的存储空间 编译器可能就分配给别的变量使用了 那就不属于这个数组的了 也就是说这个B的数组只有这些存储空间 它可以随意的使用 如果你使用外面的存储空间就属于越界了 我们来看一下 比如这个B3 它会找到这个on sa int类型的存储空间

### **[05:00]**
然后我们将AI赋值给B3 它就会将AI的数据存到B3当中 就会把B3原来的数据给覆盖掉了 刚才我们说这个存储单元 可能可能被编译器分配给别的变量 你这样赋值的话 肯定会改变别的变量的值 可能会造成一些问题 所以说不可以这样使用 问题是这条语句它要是合法的 就是说你这样使用编译器不会报错 认为它是正确的 那为什么认为B3它是正确的呢 这个我们后面学到指针的时候再说 但是我们平时使用的时候要注意数组不能越界 因为那个地盘那个范围 可能可能给其他的变量使用了 我们再来计算一下一个数组 它占用了多少个字节 注意这里的单位是字节 我们之前说过 我们的存储器也是按照字节来组织的 那么一个小方格就是一个字节

### **[06:03]**
现在我们来计算一下数组占用多少个字节 比如这里B有三个元素 每一个元素是on3int类型的 按3a int类型用占用两个字节 有三个这样的元素一共就有六个字节 但是我们平时的时候不是这样算出来的 我们通过science of5来算 Size of5 你就把它当做一个加减乘除 那样的运算符来记就可以了 它可以计算一个变量或一个数组 它占用了多少个字节空间 比如size of b 那么它就能算出来B这个数组 它占用了六个字节的空间 再比如size of a1A一是一个变量 它就可以计算出这个变量 它占用了两个字节的存储空间 我们再来整体的看一下一维数组的格式和特点 首先是数据类型

### **[07:00]**
数组我们说就是批量申请同一类型的存储空间 那么你首先先确认一下 你需要的类型是什么样子的 然后是数组元素的个数 就是你需要申请几个这样的存储空间 接着是数组名 数组名就是给这个数组起一个名字 就像你定义变量的时候给变量起一个名字 你定义函数的时候也会给函数起一个名字 他们的命名规则都是ABC下划线 123什么意思呢 ABC下划线就是大小写 字母及下划线可以开头 123数字只能放在后面 比如这里定义一个数组 它是on st.类型的 我需要三个这样的类型存储空间 然后给这个数组起一个名字叫B 我们使用数组的目的 就是为了批量申请同一类型的存储空间

### **[08:02]**
就不需要一个一个去定义了 但是使用的时候 我们还是回归到基本类型来使用 我们再来看一下数组的初始化 首先是全部初始化 C有五个元素 每个元素都是on sound char类型 也就是批量申请五个一个字节的存储空间 然后对它们进行初始化 首先是五个数据 用逗号分开 177 00:08:34,048 --> 00:08:37,249 注意两边还有一个大括号 把它们扩在一起 表示它们是一个整体 是一个数组的 执行完这条语句之后 第一个存储空间存的就是零 接着是一 接着是二 接着是三 接着是四 再比如B有三个元素 每个元素都是ASSIGINT类型的 那么就批量申请了三个啊 三个int类型的存储空间初始化就是567

### **[09:04]**
注意数据用逗号隔开 旁边还有两个括号 表示它们是一个整体 是一个数组 那么执行完这条语句之后 第一个on sound int类型的存储空间存的是五 第二个存的是六 第三个存的是七 注意这里是on int类型的 那么每一个元素 它的空间都是按照assa int类型来使用的 也就是按照两个字节来使用 换句话说也就是B0B1B二 它们都是assign int类型的嗯 至于类型为什么是assign int类型 后面学完指针之后再说 现在你只需要知道如何找到每一个元素 然后如何使用就可以了 还有一种是不完全初始化 比如有五个元素 我只初始化了三前面三个元素 那么后面两个元素它默认的就是零

### **[10:02]**
执行完这条语句之后是012 后面两个默认的就是零 再比如B有三个元素 我只初始化了一个元素 后面两个默认的就是零 那么执行完这条语句之后就是500 还有不完全初始化一种特殊情况 就是你第一个就是有五个元素 你第一个初始化是零 那么后面四个他都默认的是零 那么执行完这条语句之后 每一个存储空间数据都是零 就相当于对数组进行了清零 同样B有三个元素 你只初始化了第一个元素 而且是零 那么后面两个元素它就默认是零 那么执行完这条语句之后 存储的都是零

### **[11:01]**
如果你不初始化的话 如果是B3 然后直接给一个封号 前面上三个int类型 如果你不初始化的话 那里面的数据原来是什么 它就不变 保持原来的数据嗯 那么就是一些未知的数据 这个时候你就要注意一下 可以省略数组元素个数吗 比如说C有几个元素 这个可以省略吗 可以的 只要你后面每一个元素 对它进行初始化就可以了 那么编译器它就会数呃 12345数有五个元素 那么它这里它就会写它给你自动写好 是五个元素 就会给你申请五个存储空间 比如这里B元素的个数他没写 那么编译器就会数5673个元素 三个元素 那么就会在这里写三 接着会给你批量的分配三个

### **[12:03]**
ASSIGINT类型的存储空间 我们刚才说有那种不完全初始化 如果你写的是不完全初始化 只有三个元素 那么它这里它就会数出来就三个 然后就会给你分配三个字节的存储空间 后面两个他不会分配给你了 他可能分配给别人了 同样B这里你只初始化了一个元素 那么编译器一数A就初始化了一个元素 这里写的就是一 那么就给你申请一个unsign int类型的存储空间 后面两个就不会分配给你了 他可能就分配给别人了 这节课是我们第一次讲异维数组 你主要学会如何使用就可以了 也就是学会定义异位数组 然后再找到每一个元素 注意它的下标是从零开始的就可以了 而且每个元素的类型都是assa int类型的 因为你申请了 就申请三个ASSAINT类型的存储空间嘛

### **[13:01]**
然后就可以对它进行赋值 或者是将里面的数据取出来 暂时只学会如何使用就可以了 至于其他的都需要等学会指针之后再说

