# 【叶宇单片机】—013  赋值语句的覆盖性—【2025-10-20】-中文

## 字幕正文

### **[00:00]**
大家好 我们今天讲的是第13节 赋值语句的覆盖性 在讲赋值语句的覆盖性之前 我们先明确一个点 后面不会乱赋值语句的左边变量 我们需要它们提供一个空间 用于存放数据 复制语句的右边 我们需要他们提供一个数据 这个数据就是用来放在这个存储空间的 那么也就会把原来的数据给覆盖掉 这就是赋值语句的覆盖性 那么这个数据可以来自于变量空间 也可以来自于一个纯数据 比如说是十 这一点如果有不理解的话 可以看一下上一节的内容 讲赋值语句的覆盖性 最常举的一个例子就是两个数交换 现在我们来看一下 第一条赋值语句是把B的值放到A的空间 B的值是五 放到A的空间A原来你的一就会被覆盖

### **[01:00]**
它的新值是五 那么执行完这一条语句 A的值变成了五 B的值保持不变 第二条A的值放在B的空间 A的值已经变成了五 然后放在B的空间 无论B之前是什么值都会被覆盖掉 他的新值是五 那么执行完这两条语句 A的值是五 B的值也是五 并没有实现ab交换 那么我们想要实现ab交换怎么办呢 就像家里有一瓶油 一瓶醋 想把这两个瓶子里的东西换一下 直接交换的话 原来的东西就会被覆盖掉 油不是油 醋不是醋 所以我们需要第三个瓶子作为中转站 这里的存储也需要第三个空间作为中转站 我们先定义一个变量T就新申请了一个空间 空间里的值 我们不用管它 第一条赋值语句就是取A的值放在T的空间

### **[02:01]**
A的值是一 放在T的空间 就是把T原来的值覆盖掉 它的新值是一 也就是把A的值做一个备份 第二条语句是取B的值放在A的空间 B的值是五 然后放到A的空间 把A的原来的一覆盖掉 它的新值就是五 那么就实现了B的值放在A的空间 第三条赋值语句是T的值放在B的空间 把T的值放在B的空间 那B原来的五就会被覆盖掉 它的新值就是一 也就是相当于把A的备份放在B的空间 就相当于是把A的值放在B的空间 就实现了ab的交换 现在我们来写程序测试一下 我们使用的还是上几节课介绍的C语言 程序的模板 从C语言学习区开始到C语言学习期结束

### **[03:01]**
这是我们可以修改的地方 现在我们定义三个变量 AB和T然后将A的值给T 就是将A的值做一个备份 此时T的值是一 将B的值给A 此时A的值是五 将T的值给B 此时B的值是一 那么这里就实现了ab的交换输出的结果 A是五 B是一 T是一 我们下载到单片机 看一下是不是芯片型号 扫描串口波特率4009600 打开程序文件 找到今天13节的机器码 点击下载 这里第一个数是五 第二个数是一 第三个数是一 和我们预期的是一样的好 这节课到此结束 拜拜

