# 【叶宇单片机】—044  小括号改变判断优先级—【2025-12-22】-中文

## 字幕正文

### **[00:01]**
这节课我们来学习括号改变优先级 之前我们学习过加减乘除运算 如果把加减乘除运算放到一个计算公式当中 那么我们知道先算乘除后算加减 这个优先级顺序我们能记得很清楚 后来我们又学习了位运算 如果在一个计算公式当中有加减乘除 还有位运算 位运算 比如与运算 还有for运算 假如加减乘除和位运算放到一个计算公式当中 它计算的过程也是有一个先后顺序的 就是谁先算谁后算 当时我们说不用记住这么多的优先级顺序 我们只需要记住加减乘除就可以了 其他的优先级顺序我们就交给括号 括号里的先算 括号外的后算 现在我们又学习了逻辑运算符

### **[01:00]**
如果一个计算公式当中有加减乘除运算 有位运算 还有逻辑运算 那么他们是谁先算谁后算 是有一个优先级顺序的 但是记住那么多优先级顺序很麻烦 所以我们还是只需要记住 加减乘除的优先级顺序就可以了 就是先算乘除后算加减 其他的优先级顺序我们都不需要记住 交给括号还是先算括号里的 然后再算括号外的 所以这节课我们看一下 括号在逻辑计算当中的使用 比如逻辑语的应用 这是一个条件 判断有三个运算符 云运算小雨 还有大于它们的运算 优先级顺序我们是不记得的 也就是先计算70与上A呢

### **[02:00]**
还是先计算A小于80呢 这个我们是不确定的 如果我们加上了括号 那么我们就可以确定谁先算谁后算A大于70 加上了括号 所以说他先算A小于80 也加上了括号 所以说在计算A小于八时 将它们俩得到的结果再进行与运算 这样我们就很清楚它们的运算先后顺序了 如果一个条件判断当中有逻辑或运算 还有逻辑与运算 他们的执行顺序如果是不同的话 那么得到的结果也是不一样的 我们来看一下 假如第一个条件是真的 第二个条件是真的 第三个条件是假的 我们将括号给第一个条件和第二个条件 那么第一个条件是真的 第二个条件也是真的 两个结果进行逻辑或结果也是真的 然后与上第三个条件 第三个条件是假的

### **[03:02]**
那么一与上零结果是零 当把第一个条件和第二个条件括起来的时候 表示他们是先算的 那么整体得到的结果是零 我们再来看一下 把第二个条件和第三个条件括起来 让他们先算会得到一个什么样的结果 假如第一个条件还是真的 第二个条件还是真的 第三个条件还是假的 就是保证他们的条件结果是一样的 我们看一下括号放在不同的位置 会不会改变最终的结果 这里第二个条件是真的 第三个条件是假的 那么两个结果一与的话 结果肯定是假的 然后再或上第一个条件 第一个条件是真的 那么一或上零结果是一 这里改变了括号的位置 得到的结果是一 前面改变括号的位置 得到的结果是零 那我们在逻辑运算的时候 放括号的时候要注意一下到底是谁先算谁后算

### **[04:03]**
这个先后顺序 如果错的话 结果可能也就不一样了 这里再补充一个括号的使用 叫做类型的强制转换 其实之前我们也学过 这里就大概过一下 假如我们对接收的数据进行处理 假如这个数据是来自某个传感器 然后传感器给我们发送的数据是16位的 无符号数据 我们就可以定义一个X 然后来接收值 假如是1000 然后来定义一个Y来接收值 假如是3000 那么我们想计算X乘以Y赋值 给AX是on sa int类型的 Y是on3的int类型的 两个类型一致就可以计算了 那么就是1000×3000得到300万 因为两个运算符都是unsign int类型的 那么我们说结果也是unsign int类型的

### **[05:00]**
又因为结果是按三的int类型的 只能保存16位 那么只能保存低16位 那么高八位就会被丢掉 X乘以Y 它得到的一个结果是16位的 有大小 有类型 将这个值赋值给AA 提供的存储空间是四个字节的 是on sallog类型的 那么就将这两个数据 存到四个字节的存储空间当中 虽然说这里的A空间足够大 能够存储这里三个字节的数据 但是高八位在存储之前就已经丢掉了 那么它只存储了第16位 得到的结果肯定是一个错误的结果 这个时候怎么办呢 我们之前说过 造一个大房子M然后将X数据放进去 造一个大房子N将Y的数据放进去 这里就是X赋值给M Y赋值给N 然后再计算M乘以N赋值给A

### **[06:03]**
这个是M这个是N得到的结果还是300万 结果是昂三的long类型的 因为M和N都是昂三的long类型的 所以说结果也是昂三的long类型的 能够保存四个字节的数据 那么这里三个字节的数据 就会被完整的保存下来 执行完M乘以N得到一个结果 有大小有类型 将这个结果赋值给A 那么A就可以完整地保存这三个字节的数据了 这个是我们一种方法 就是引入中间变量 就是重新定义一个 就是重新定义一个变量 然后来存储值 目的是把这个值的类型变大一点 使计算结果能够保存 第二种办法 我们使用的就是C语言类型的强制转换 X乘以Y 将X在运算的时候强制转换成R3的lg类型 Y在运算的时候强制传换成R3的lg类型

### **[07:01]**
那么这是X这是Y两个都是on3的lg类型的 也就是临时转换成 也就是临时的 把这两个数据转换成R3的lg类型的 那么得到的结果还是300万 因为两个运算数都是阿三的lg类型的 结果也是阿三的lg类型的 就能保存这三个字节的数据 那么在执行完这里的X乘以Y得到的结果 有大小有类型 再将这个结果赋值给A 那么A就能将这里的呃 三个字节全部都保存下来 这里的括号就是类型的强制转换 就是把计算的数据 在计算过程当中转换成一个等级高的类型嗯 转换成等级高的类型 就是为了保存它最后的结果 就不用像之前这样再重新定义一个变量 如果处理的数据多的话

### **[08:00]**
那就要定义很多变量 就是会有一点嗯 变量多了 看起来有点乱

