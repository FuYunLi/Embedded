# 【叶宇单片机】—结构体指针—【2026-04-16】-中文

## 字幕正文

### **[00:01]**
今天我们来看一下结构体指针 今天的类型定义还是基于32位单片机 这节课的主要内容是理解一下什么是间接寻址 什么是直接寻址 先定一个变量A会为它分配一个存储空间 再定义一个指针变量 P1也会为它分配一个存储空间 注意中间会空几个 因为为使P这个变量的存储空间的地址 处于一个对齐的状态 也就是CPU可以依次读取数据 不用拼接数据 再定义一个结构体变量 那么要先创建一个结构体类型 然后才可以定义一个结构体变量 先为asser char分配一个存储空间 中间空一格 再为short int分配一个存储空间 那么这就是一个结构体的存储空间 这是A的房间 这是P1的房间 这是X的房间

### **[01:01]**
他们都有一个什么样的特点 是不是每个人都有自己的房间 可以对这个房间进行写数据 也可以读数据 读写数据我们又是怎样实现的 是不是通过赋值语句来实现的 那么赋值语句有一个要求 需要类型一致 我们来看一下 对于基本的数据类型来定义一个变量的时候 对它进行读写 也要求类型一致 但是它会进行一个影视的类型转换 所以说不是必须要求类型一致 比如把16位的数据存储到八位的存储空间 这个时候类型虽然不一致 但是也可以赋值的 他会把16位的第八位自动截断 复制到八位的存储空间 所以说我们需要注意一下 它有一个隐式的类型转换 对于指针变量它也要求类型一致 但是有一个特别就是void类型的指针

### **[02:00]**
普通的指针变量和void类型的指针变量 它们俩之间可以双向的进行隐式的类型转换 也就是双向赋值都是可以的 它会进行一个影视的类型转换 对于结构体变量 它要求类型是必须一致的 因为不同类型的成员它组成了一个结构体 如果进行一个隐式的类型转换 那么是不是每一个成员都会进行一个 隐式的类型转换 这个时候就比较乱 所以说这个时候类型就必须一致 我们说对变量读写主要是通过赋值语句实现的 那么赋值语句有一个要求就是类型一致 那么我们就来看一下各个变量的类型 首先定义一个变量A取A的地址是什么类型 我们之前说过 取地址的类型 是不是就在原来类型的基础上加一个星号 对它进行解引用之后又是什么类型

### **[03:01]**
是不是就去掉这个信号 A也是on suncha类型 这个时候他们俩实际上是等效的 因为获取的是同一个地址 然后把这个地址指向的存储空间 都是按照on satcha类型来解读的 再定义一个结构体变量 取X的地址是什么类型 是不是在原来类型的基础上加一个星号 对它进行解引用又是什么类型 是不是去掉一个星号 这个时候X也是一个结构体类型 他们俩也是等效的 因为他们俩获取的是同一个地址 把这个地址指向的存储单元 都是按照结构体类型来解读的 我们再定义一个结构体类型的指针P2 然后为它分配一个存储空间 取A的地址是on sound chan类型的指针 这里的双斜杠是一个解释符号

### **[04:03]**
也就是编译的时候 双斜杠后面它不会编译 会自动的忽略掉 那么可以取A的地址赋值给P1 因为它们俩的类型是一致的 可以直接进行赋值 这个时候P1存储的就是A的地址是0080 H指向的也是0080 取X的地址是结构体类型的指针 这个时候可以取X的地址复制给PI 因为两个类型是一致的 可以直接复制 那么pi存储的就是X的地址 X的地址是0088H 那么pi指向的就是0088 注意这里P1和P2 它们仅仅存储了一个地址 其他的什么都没有做 我们现在知道只要指针的类型一致 就可以直接进行赋值 那么怎么来使用呢 也就是解引用

### **[05:01]**
先看一下新P1 P1存储的是A的地址是0080H 那么新P1就是找到这个存储空间 对这个存储空间进行读和写 那么按照什么类型读写呢 我们看一下P1的类型是on scha类型的指针 那么新P1就按照on scha类型 对这个存储空间进行读写 那么在程序当中 你写一个变量A 它实际上也是得到0080H这个地址 然后把这个地址指向的存储空间 按照on sound char类型来解读 也就是新P1是一种间接寻址方式 A是直接寻址方式 间接寻址 就是你的地址放在P1这个存储空间当中 你要到这里来取地址 得到的地址是0080H 直接寻址就是你在程序当中写一个变量A 那么在编译阶段 他直接可以获取地址0080H

### **[06:01]**
我们再来看一下新P2P2存储的是X的 地址是0088H 那么新pi就可以找到0088这个存储空间 对这个存储空间进行读和写 那么按照什么类型来读写呢 我们看一下pi的类型定义 它是一个结构体指针 那么新pi就是一个结构体类型 X也是结构体类型 它们俩也是等效的 一个是直接寻址 一个是间接寻址 介间寻址就是他的地址放在pi的存储空间当中 你要来这个存储空间找 就是0088H 直接寻址就是在程序当中写X这个变量 那么在编译的时候 它会自动的翻译成地址0088H 他们俩得到的都是相同的地址 然后把这个地址指向的存储空间 都是按照结构体类型来解读的 所以说他们俩是等效的 都是提供地址

### **[07:00]**
现在我们了解了间接寻址和直接寻址 再来看一下如何使用 把它们放在一个函数当中 将E百赋值给星P1 星P1找到的存储空间 也就是A的存储空间 存100 将60复制给X的第一个成员 存储的就是60 我们说X是一种直接寻址方式 那么我们也可以用pi这种间接寻址方式 来试一下 新pi间接获取X的地址 然后来调用第一个成员 将70赋值给第一个成员 这里有两个运算符 一个是星号解引用运算符 一个是点调用成员的运算符 点调用成员的运算符优先级比较高 那么相当于后面有一个括号 这样是不对的 所以说我们要在前面加上一个括号 这样才能得到X的地址 那么这个时候第一个纯元存储的就是70

### **[08:01]**
这里的新pi是间接寻址方式 找到这个存储空间也是按照结构体类型来解读 因为它定义的就是一个结构体类型的指针 也不是说所有的指针类型都可以的 我们也可以将A赋值给X的第二个成员 那么它就存储的是100 A是八位的 X的第二个成员是16位的 但是他们是基本的数据类型 会进行一个影视的类型转换 所以说这个时候对类型一致 没有那么严格的要求 同样这里的X是一个直接寻址方式 我们也可以用一个间接寻址方式 星P2 注意这里要加上一个括号 我们看一下每次用指针来寻值的时候 是不是都要加上一个括号 如果忘记了就会出错 这个时候为了避免丢括号出现错误 我们就改了一下它的书写方式 注意这里仅仅改了书写方式 没有什么其他的意义

### **[09:00]**
最后我们来总结一下 定义一个结构体变量 定义一个结构体指针存储的是X的地址 X的类型是结构体类型 我们说它是一种直接寻址方式 得到的地址是0088H星pi 它的类型也是结构体类型 它是一种间接寻址方式 得到的地址也是0088H 所以说他们两个类型一致 可以直接进行替换 X调用 它的成员也就可以写成新P2调用它的成员 因为这里需要每次都要记得加括号 所以说我们换了一种书写方式 其实所有的指针变量都是一个间接的寻址方式 所有的变量名放在程序当中 都是一个直接寻址方式 好这节课到此结束 拜拜

