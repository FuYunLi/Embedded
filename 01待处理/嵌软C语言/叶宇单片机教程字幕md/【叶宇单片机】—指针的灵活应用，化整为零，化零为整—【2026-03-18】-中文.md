# 【叶宇单片机】—指针的灵活应用，化整为零，化零为整—【2026-03-18】-中文

## 字幕正文

### **[00:00]**
上节课我们说如果发送一个数据的话 要把这个数据拆成八位八位的 然后存放在数组当中来发送 但是我们有的时候发送不是一个数据 可能是好几个数据 这节课我们就来学习 将几个变量拆成八位八位的 然后存放在数组当中 定义两个变量 分别把它们拆成八位八位的 然后存放在一个数组当中 为他们分配各自的存储空间 因为我这里的存储空间位置有限 那么我就简单的举两个变量 然后把它们拆成八位八位的 如果拆解多个变量也是一样的方法 我们想将变量A和变量B存放在数组当中 那么我们知道是没办法直接赋值的 这个时候我们可以将数组拆成两个变量 分别来存放数组A和数组B 那么借助什么可以将数组拆成两个变量呢

### **[01:04]**
同样还是借助指针变量 这里我们定义两个指针 之前我们取数组的地址 都是通过数组名来取他的地址 这节课我们通过数组的元素也可以取它的地址 我们取元素一的地址赋值给P1 因为数组的元素是on sa char类型的 那么取它的地址就是on scha类型的指针 那么与P1的指针类型是一致的 就可以直接赋值 那么PE1存储的是0084 指向的也是0084 这个时候将A赋值给新P1 也就是通过新P1 可以对这个存储空间进行读或者写 同样按照on sound char类型来解读 这个存储空间的数据

### **[02:00]**
A也是on cha类型 所以说可以直接进行赋值 接着我们取元素I的地址赋值给P2 那么这里要加上一个类型的强制转换 因为pr的类型是unsigned int类型的指针 只有类型一致了才可以直接赋值 那么P2存储的就是0085 指向的也是0085 这个时候将B复制给新P2 也就是通过芯片I 可以读写这个存储空间的数据 就像普通变量A和普通变量B1样 它可以读写它自己的存储空间的数据 这里的星P2也可以读写他自己空间的数据 然后按照unsent int类型来解读 B也是ASSAINT类型的 那么可以直接赋值 这个时候就通过指针将数组拆成两个变量 实现了直接赋值 也就实现了化整为零

### **[03:03]**
接着我们再反过来看 刚才我们是将多个变量拆成八位 八位的发送出去 那如果我们接收一串 比如接收到的数据放在数组当中 那么我们要将这些数据整合一下 我们看一下P1里面存储的是0084 也就是数组第一个元素的地址 那么新P1可以打开这个存储空间 按照on center char类型来解读 那么pi存储的是0085 也就指向的是第二个元素 那么新pi可以打开这个存储空间 按照UNSIGINT类型来解读 这个时候就把数组拆分成两个普通的变量 因为我们通过新P1和新P2 可以对它的存储空间进行读和写 就和普通的变量使用方法是一样的了

### **[04:03]**
这个时候就可以实现直接赋值 也就是将新P1赋值给A 那么A存储的就是01H 将新P2赋值给BB 存储的就是0203H 那么我们发现这个操作是不是跟刚才是相反的 因为我们把这个数组 通过指针拆成了两个普通的变量 这个时候化零为整 直接赋值就可以了 再次感受一下指针的灵活性 指针它想获取谁的地址 就可以获取谁的地址 就像普通变量赋值一样简单 因为他有自己的存储空间 他可以对自己的存储空间进行读或者写 但是它存储空间存储的是一个地址数据 打开这个地址的存储空间 按照什么类型来解读这个存储空间的数据

### **[05:02]**
也是指针自己说的 算好这节课到此结束 Byebye

