# 【叶宇单片机】—027  整数求商—【2025-11-22】-中文

## 字幕正文

### **[00:01]**
大家好 今天学习的是第27节 整除求商 这节课分成三个内容 一是什么叫做除法 二是整除求商的运算 三是加减乘除运算的总结 加减乘除这一段东西有点多 并且容易出错 不过听完这节课就算轻舟已过万重山 后面就能轻松一段时间了 而且讲这么多 不是真的让你去用 而是为了避免犯错 知道什么时候会出现错误 真正使用的时候 用UNSLG作为中间变量去计算 具体看24节 接下来讲的除法运算还是用到算数转换规则 算术转换规则就四个字 提升对齐 目的是将两个不同类型的操作数 转换为同一类型 符合硬件的需求 在提升与对齐过程当中

### **[01:01]**
正数与零前面补零 负数前面补腰 目的是使结果不变 类型的提升与对齐 要遵循类型等级 现在我们把类型等级补充完整 最后两个高等级是float类型和double类型 比如int家族遇到float类型 就要转换成float类型 再比如遇到常数1.0 3.14 负的0.5等等一些小数常数 那么这个常数的类型就是double类型 今天的主题什么叫做除法9÷4商是2.2 59÷4商等于二 余数是一 那么如何通过C语言实现这两个计算呢 第一个商是小数 那么谁有小数浮点类型

### **[02:00]**
所以说A定义为float类型 A除以四赋值给AA是float类型 四是int类型 提升为float类型 那么A除以四的结果也是float类型 就可以保存小数部分了 将2.25赋值给AA也是float类型 就可以把小数完整的存储下来 赋值语句的主要目的就是存储第二个商势 二余数是一两个都是整数 那么肯定不能用浮点类型 只能用整数类型 所以说B定义为int类型 B除以四赋值给BB是int类型 四是int类型 两个类型 一致就可以计算了 B除以四结果是2.25 但是结果是int类型 所以说结果是二 那么商就是二 我们把这种除法运算 也就是运算数都是整形的时候叫做整除求商

### **[03:03]**
同样余数是整数 对于余数的计算 被除数与除数不变 只是中间的符号变成了取余符号 B是int类型 四是int类型 两个类型一致就可以计算了 B除以四 取余是一 我们把这种取余数的计算方式 也就是运算数都是整形的时候叫做整除求余 这节课我们学习整除求商 下节课学习整除求余 所谓的整除就是运算数都是整数类型 没有浮点类型 浮点类型是可以参与除法计算的 但是我们说的整除计算 说的是整数类型参与的计算 这是我们整除求商的格式 从左到右依次计算 先计算前两个数还是相对类型的提升与对齐 然后得到一个结果 有大小有类型

### **[04:01]**
将这个结果作为一个被除数与后面的除数相除 还是相对类型进行提升与对齐 得到一个结果 有大小有类型 将这个被除数与后面的除数进行相除 还是先对类型提升与对齐 得到一个结果 有大小有类型 这就是整除求商的联除过程 还是两个数 两个数操作的练习 一整除求商时除数为零的情况是什么呢 D1些变量来执行这两条语句 我们看见算术计算首先要做的事情 就是类型的提升与对齐 先来看第一条语句 23是int类型 Y是ASSIGNCHAR类型 提升为int类型 两个类型一致就可以计算了 23÷0结果是多少呢 我不知道 那么我们来看下一条语句 23和零都是int类型

### **[05:02]**
两个结果一致就可以计算了 23÷0结果是多少呢 我也不知道 因为在数学当中除数为零是没有意义的 那么在单片机当中除速为零会发生什么事情呢 我们直接写程序测试一下 还是在我们的单片机上练习程序 定义一些变量 执行这两条语句 最后将结果输出查看一下 我们点击saw 点击rebuild 发现有一个警告 警告的内容是第14行除数为零 我们找到第14行除数为零 这一句是不合理的 我们把它去掉 接着再点击CWW 点击rebuild 零警告零错误 把程序塞到单片机看一下 第一个数是255 那么23除以YY是零 23÷0=255 这个结果很奇怪

### **[06:01]**
整除求商 整除求余 在C标准中除以零是一个未定义的错误 未定义的错误就是不知道会发生什么样的错误 最有可能的情况就是程序崩溃 也就是跑飞或者是单片机复位怎么办呢 在做除法运算的时候 我们先将除数判断一下 判断它是否为零 只有当除数不为零的时候才执行这条除法语句 如果当除数为零 就不执行除法语句 那么就能避免一些意想不到的问题 练习四整除求商时被除数小于余数 定义一个变量 执行这条语句 七是int类型 十是int类型 两个类型 一致就可以计算了 7÷10结果实际上是0.7 但是结果是int类型 所以说结果是零 那么执行完7÷10得到一个结果

### **[07:02]**
有大小有类型 将这个结果赋值给C C提供的存储空间是八位的 所以说将D8位0X00存到C的空间当中 那么C的值就是零 练习三整除求商时被除数等于除数 定义一个变量 执行这条语句 十是int类型 两个类型一致就可以计算了 10÷10 结果是一是int类型 那么执行完10÷10之后得到一个结果 有大小有类型 将这个结果赋值给dd提供的存储空间是八位的 就将D8位0X01存到D的空间 那么D的值就是一 练习四整除求商时被除数大于除数 定义一个变量 执行这条语句 十和四都是int类型

### **[08:00]**
两个类型一致就可以计算了 10÷4实际结果是2.5 但是结果是int类型 那么结果就是二 执行完10÷4得到一个结果 有大小有类型 将这个结果复制给E E提供的存储空间是八位的 就将低八位存到E的空间当中 那么E的值就是二 我们写程序来测试一下这几个结果 但是后面还有重要内容 不要这个时候就跑了 第一种情况是被除数小于除数 我们把它下载到单片机看一下 结果结果是零与我们分析的一致 第二种情况是被除数等于除数 我们把它下载到单片机看一下 结果结果是一与我们分析的一致 第三个是被除数大于除数 也就是10÷4 我们把它下载到单片机看一下

### **[09:01]**
结果结果是二与我们分析的一致 我们再来看一下整除求商时 比较容易出错的情况 练习五 整除求商被除数是正数 除数是负数 定义一些变量执行这条语句 N是on sa child类型提升为int类型 X是signature A类型是四 存储的是0XFC提升为int类型 那么X的值就是零 XFFFC从八位提升为16位 前面补腰 那么现在两个类型一致就可以计算了 N除以XN是十 X是四 那么10÷-4 实际的结果是负的2.5 但是结果是int类型 所以说结果去掉小数部分是二

### **[10:03]**
那么执行完N除以X得到的结果有大小 有类型 将这个结果赋值给M M提供的存储空间是八位的 所以说将D8位存到M的空间 计算的结果 如果按照int来解读的话 FE是二 但是M是啊SATRA类型 如果把存储在M里的 FE按照on3TCHAR类型来解读的话 就是254 所以说M的值是254 我们来写程序验证一下 N是十 X是四 那么N除以X复制给M 我们看一下单片机的输出结果 单片机的输出结果是254 16进制是F1 与我们这里分析的是一致的

### **[11:00]**
注意看我们这里的除数是按照int类型来解读的 所以说是四 如果我们把同样一个除数按照on sent int来解读 那么会发生什么样的变化呢 我们就把这里的N定义为unsigned int类型 将N定义为assigned int类型 然后来执行这条语句 N是UNSIGINT类型 X是sa char类型是四 存储的是0XFC 现在X提升为int类型 前面补一也就是0x f f f c int类型 再提升为unsigned int类型 X的结果还是0XFFFC 那么N除以X也就是十除以FFFC 但是ff ff fc是unsign int类型

### **[12:01]**
按照on3的int类型来解读的话 就是6532 那么10÷6532就是0.00015 因为我们的计算结果是unsign int类型 所以说它的结果是零 那么执行完N除以X计算的结果有大小 有类型 将这个结果赋值给mm提供的存储空间是八位的 这时候M的值就是零 将零存到M的空间 我们写程序来测试一下 将N变成on set的int类型 然后然后输出M的结果 查看一下输出的结果是零 与我们分析的一致 有些同学可能会想 你之前的除数是四 这里怎么就是65532呢 我们说过这里的类型是UNSIGINT类型

### **[13:02]**
按照无符号类型来解读的 所以说是65532 这个是单片机当中负责加减乘除运算的运算器 当你把两个数提交给运算器 运算器还要知道是怎么来解读这两个数 按照浮点类型来解读吗 还是按照有符号类型来解读 还是按照无符号类型来解读 比如这里0XFFFF运算器会问这是一吗 你就要告诉他公共类型是on signa int类型 就说这两个数的类型都是unsign int类型 那么运算器说好的 他就知道怎么去解读这两个数据了 0x f f ff f他解读的结果就是255 如果你告诉他是int类型 它解读的结果就是一 所以说还回到这里 提升与对齐之后得到两个数

### **[14:01]**
那么这两个数给运算器 您还要告告诉它它是按照什么方式来解读的 按照不同的方式解读它的结果是不一样的 练习六整除求商时被除数是负数 除数是正数 我们定义一些变量 执行这条语句 B除以C赋值给AB是三的int类型是十 存储的就是0XFFF6 B是int类型 C是on sattra类型 提升为int类型 两个类型一致就可以计算了 0XFFF6也就是-104 结果是负的2.5 因为预算结果是int类型 所以说将小数部分去掉 结果是二 那么执行完B除以C得到的结果有大小 有类型 将这个结果赋值给A

### **[15:01]**
A提供的存储空间是八位的 所以说将FE存到A的空间当中 那么F1我们按照int类型来解读的话 就是二存到这里还是F1 A是按照on signature类型来解读FE的 那么A的值就是254 我们来写程序验证一下被除数是十 除数是四 那么我们输出它的结果来看一下 结果是254是F1 与我们分析的是一致的 同样这里的0XFFF6 按照int类型来解读是十 如果我们想让它按照on sent int来解读 会怎么样呢 那么将其中的一个除数C的类型变成on sigma int类型 我们再来分析一下这条语句

### **[16:00]**
B是三 a int类型是十 存储的是0XFFF6 那么B将int类型提升为on sa int类型 C的类型是unsign int类型 两个类型 一致就可以计算了 那么B除以四 我们知道这里的0XFFF6 是按照unsign int来解读的 所以说它的值是6526÷4 结果是16381.5 因为计算的结果是unsign int类型 所以说取整数部分16381 也就是零X3FFD执行完B除以C之后 得到一个结果 有大小有类型 将这个结果赋值给A A提供的存储空间是八位的 所以说将0XFD存到A的空间当中

### **[17:03]**
那么高八位就装不下 就会被丢掉 那么得到一个错误的结果 存储的FD也就是253 我们来写程序验证一下 将C变成unsign int类型 我们输出A的结果 查看一下输出的结果是I53FD 这与我们分析的是一致的 如果我们想把16位的结果都存储下来 怎么办呢 那么就把A定义为unsign int类型 我们来写程序验证一下 A定义为unsigned int类型 输出结果 查看一下输出的结果是1638 16进制是零X3FFD 这与我们分析的结果是一致的 在加减乘除的时候 对于负数提升之后的类型要注意

### **[18:02]**
因为其解读值的结果 就是根据提升之后的类型来解读的 加减乘除运算的总结提升 对齐提升与对齐遵循的是类型 等级 提升与对齐之后的数据提交给单片机的运算器 那么单片机的运算器就会问这个数据是一吗 就是如何来解读这个数据呢 那么你就要告诉他这个数据是什么类型的 那么运算器就根据不同的类型来解读 这个数据运算之后的结果的类型也要注意 这决定我们如何对这个结果进行解读 我们讲这个运算规则不是真的让你去用 而是为了避免出错 真正使用的时候 用on sa lang作为中间变量去计算 具体看24节练习时 整除球场的字除简写

### **[19:02]**
被除数是保存变量本身除以一个除数 赋值给保存变量 它的简写方式就是保存变量 除法的简写符号是一体的 中间不要分开 剩余一个除数 定义一个变量 这个是整除求商的简写方式 你要想到它原来的方式就会计算了 也就是G除以五赋值给G 先进行类型的提升与对齐 然后再进行计算 就可以得到一个正确的结果 练习11整除求商有没有自除一的特殊写法 我们知道之前有字加一的特殊写法 那么有没有字除一的特殊写法 这个式子就相当于G除以一赋值给G 这个式子也相当于是H除以一赋值给H除以一 得到的结果还是它本身 所以说这样写没有什么意义 就没有这种写法好 这节课到此结束

### **[20:00]**
拜拜

