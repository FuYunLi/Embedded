# 【叶宇单片机】—035  位运算的右移—【2025-12-08】-中文

## 字幕正文

### **[00:00]**
大家好 今天学习的是第35节微运算的右移 这与上节课学习的位运算的左移 只有一点点的区别 我们来看一下 首先是用于运算的格式 将被余数向右移N位 结果复制给保存变量 比如有一个八位数0X0F向右移一位 也就是整体向右移一位 右边移出去的就会被丢掉 左边空出来一位是补零吗 如果向右移两位 也就是整体向右移两位 右边移出去的就会被丢掉 左边空出来的两位是补零吗 我们来想一下 在类型的提升与对齐的时候 如果对一个八位数进行提升 提升为16位 如果这个八位数是正数与零 那么前面补的就是零 前面补的就是零 如果这个八位数是负数 前面补的就是一

### **[01:00]**
那么前面补的就是一 我们说这里做的目的就是使这个数大小不变 同样在右移的时候 如果是一个无符号数 这前面补的就是零 如果是一个有符号数 那么我们就要来看一下这个符号位 符号位如果是零 那么前面补的就是零 符号位如果是一 那么前面补的就是一 这样做的目的是使数值的大小不变 我们再来看一下这两个运算数的类型 右移运算有两个运算数 一个是被移数 一个是N 我们来看一看它的类型 之前我们学习过算术运算 也就是加减乘除位运算与运算或运算异或运算 还有左移 它们都是两个操作数 现在的右移也是两个操作数

### **[02:00]**
也是遵循类型的提升与对齐 提升与对齐的时候 正数与零 前面补的是零 负数 前面补的是一 目的是使数值大小不变 对于右移前面空出来的位 如果这个数是一个正数与零 前面补的也是零 如果是一个负数 前面补的是一 目的是使数值大小不变 类型的提升与对齐 依据的是类型等级将提升 已对齐之后的数据给运算器 运算器是按照公共类型来解读数据 右移的移位位数N有的时候是一个常数 那么常数是什么类型呢 没有后缀时有最小能容纳旗帜的类型确定 首先是提升为int类型 如果int类型装不下 再提升为lg类型 有后缀时有后缀直接指定其类型 练习题一定义一个变量来执行这条语句

### **[03:01]**
在运算之前先进行类型的提升与对齐 C向右移八位 C是3a char类型提升为int类型 八是int类型 两个类型一致就可以计算了 C是0X8 一是负的129 这是它的二进制提升为int类型 因为是一个负数 所以说前面补的是一 这个时候就可以进行右移运算了 右移八位就是整体向右移八位 那么前面空出来的八位补什么呢 我们说这是一个负数 负的129 对负数进行右移 前面补的是一移出去的八位就会被丢掉 那么C向右移八位 得到一个结果 有大小有类型 这个类型是int类型的 将这个结果赋值给C C提供的存储空间是八位的 那么就将0XFF

### **[04:01]**
也就是D8位存到C的空间当中 需要注意的第一点就是右移后 符号位是有迹可循的 如果是零与正数 前面补的是零 复数前面补的是一 目的是保证数据不变 练习题二 定义一个变量来执行这条语句 我们发现M是sign int类型的 是16位的 这里向右移16位移位运算 无论是向左移还是向右移 它的移位位数必须小于被移除的位宽 否则会触发一些未定义的行为 还有就是一位位数不能够是一个负数 也会触发未定义的行为 那么右移它的移位位数的要求和左移是一样的 比如int家族 它的位宽是16位的 正常移位位数是0~15

### **[05:00]**
如果你移位位数是16 十七十八 他就会触发未定义的行为 这里没有char家族和short家族 是因为在运算之前要先进行类型的提升与对齐 小于int家族的都会被提升为int家族 所以说在运算的时候 至少都是int类型的 对char类型一位八位是合法的 因为它会提升为int类型 int类型是16位的 还有一点就是移位运算 无论是左移还是右移 它的移位位数必须是非负整数 也就是零和正整数右移的总结符号位 它是有迹可循的 一位位数的违法 如果超过了被移数的位宽或者是一个负数 它会触发未定义的行为 这个时候我们要改成零或者是正整数 右移的应用 1÷2 举一个例子240 这是它的八位二进制

### **[06:01]**
这是它的每一位的权 如果向右移一位 就是整体向右移一位 右边移出去的被丢掉 左边空出来的补上零 因为它是一个正数 所以补零 那么我们看这一位的一 它的全是二的七次方向右移一位 它的全是二的六次方 这一位的全是二的六次方 向右移位 它的全是二的五次方 那么我们向右移位 它的每一位的权实际上都除以了一个二 所以说结果也是240除以二一百二 我们向右移两位 也就整体向右移两位 右边移出去的就被丢掉 左边空出来的补两个零 因为它是一个正数 我们看这一位的全是二的七次方 向右移两位变成了二的五次方 这一位的全是二的六次方 向右移两位变成了I的四次方 说明每一位的全都除以了四

### **[07:00]**
也就是240÷4得到60 我们再来举一个例子 有一个数14 这是他的二进制 每一位向右移一位 也就是整体向右移一位 右边移出去的被丢掉 左边补上零 因为它是一个正数 同样这一位的全是二的三次方 向右移一位变成了二的二次方 这一位的全是二的二次方 向右移一位变成了二的一次方 那么每一位的全都除以了二 所以说结果也是14÷2得到七 那么向右移两位 也就是整体向右移两位 右边移出去的被丢掉 左边空出来的补上零 这一位的全是二的三次方 现在变成了二的一次方 这一位的全是爱的二次方 现在变成了二的零次方 这样我们看每一位的权也是除以了四

### **[08:01]**
但是结果并不是14÷4 因为他把这里的一这个有效位 它把它给去掉了 那么结果就不符合除以四的规律了 所以说不要迷恋右1÷2 它有的时候也会出错 右移的应用二 不同数据类型之间的分解 定义一些变量 然后来执行这两条语句 先来执行第一条语句 C是赋值给LC是unsign int类型是16位的 L是on3的char类型是八位的 那么只能将D8位0X34存到L的空间当中 那么执行完这条语句之后 L空间的值是0X34 再来执行第二条语句 C向右移八位运算之前 先进行类型的提升与对齐 C是assa int类型 八是int类型 然后提升为unsign int类型 现在两个类型一致就可以计算了

### **[09:02]**
C向右移八位 C是零 X1234向右移八位 前面八位补的是零 因为它是一个正数 右边移出去的八位就把它去掉 那么C向右移八位得到一个结果 有大小有类型 将这个结果复制给H H提供的存储空间是八位的 那么就将D8位0X12存到H的空间当中 那么执行完这条语句之后 H的值就是0X12 就实现了对C0X1234的分解 高八位存到H当中 低八位存到L当中 由于运算的简写 保存变量向右移N位 结果复制给保存变量 它的简写方式就是保存变量右移 简写符号还有一个N我们定义一个变量 这是右移的简写

### **[10:01]**
你要想到它原来的计算方式 就会计算了F向右移两位 然后将结果赋值给F右移的总结 首先它的符号位是有迹可循的 零和正数 前面补零 负数 前面补一移位的位数违法超过被移数的位宽 或者是一个负数 就会触发未定义的行为 我们要把它改成零或者是正整数 还有右移除以二 不要太迷恋它 因为它有的时候也会出错 我们之前学习过左云运算 对于一位的位数 他们俩的要求是一样的 然后也不要太迷恋左移乘以爱他 有的时候也会出错的 左移和右移的主要区别是在于它的符号位 左移的时候 符号位的改变会触发未定义的行为 不同的编译器它有不同的处理方式 但是在Q当中保存最后的结果

### **[11:00]**
也就是把符号位当做普通数来处理 然后一起向左移 最后的结果是什么 Q就保存什么 但是还是建议被移数优先使用无符号数 无符号数就没有那个符号位 就不会触发未定义的行为 它的结果也是确定的

