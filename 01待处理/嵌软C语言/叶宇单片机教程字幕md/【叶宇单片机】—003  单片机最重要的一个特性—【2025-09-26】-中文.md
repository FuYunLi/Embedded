# 【叶宇单片机】—003  单片机最重要的一个特性—【2025-09-26】-中文

## 字幕正文

### **[00:02]**
大家好 今天学习的是第三节 单片机最重要的一个特性 这一节我们还是先去了解单片机 对单片机有一个感性的认识 道生一一生二 二生三 三生万物 道德经认为 世间万物缤纷多彩 都源于一个东西 这个一的东西就是道 电子世界也存在一这个一繁衍出手机电脑 电视机 机器人等丰富多彩的电子世界 这个一就是单片机最重要的一个特性 单片机最重要的一个特性是什么呢 首先是将程序下载到内存 这个是输入引脚 单片机通过输入引脚 可以识别引脚上的高低电平信号 这个是输出引脚 单片机可以输出不同时间长度的高低电平信号 那么这里就有五个关键词 就是程序下载到内存

### **[01:04]**
然后通过引脚识别出高低电平信号 也可以输出不同时间长度的高低电平信号 我们就来分别讲一下这五个点 可以用三种语言来写单片机的程序 分别是C语言 汇编语言和机器语言 我们举一个例子来简单的看一下这三种语言 例如计算207+9 用C语言来表示的话 比较清楚明了 一眼就能看出计算的是207+9 用汇编语言来表示的话 如果不知道符号的意思 还要去查一下指令集手册 这里就简单的说一下 move的意思就是将207存入到R2寄存器 ADD的意思就是相加 将R寄存器里的207和九相加 结果再存到R寄存器里面

### **[02:00]**
用机器语言来表示的话 就是一串一串的零和一数据串 这个是单片机的存储器 只有机器语言才可以直接的下载到存储器当中 C语言和汇编语言 没有办法直接的下载到存储器 如果C语言和汇编语言想要下载到存储器 那么就要经过编译器翻译 翻译成机器语言 然后再下载到存储器 既然单片机可以用C语言来写 汇编语言来写机器语言来写 为什么现在主流的语言是C语言呢 我们来看一下 最开始写的是机器语言 然后通过在纸带上打孔来表示零和一 进而来保存程序 再读入计算机 此时相当于绳结记事阶段 后来人们发现单片机不同的功能有不同的编号 而这些编号基本上是固定的

### **[03:00]**
比如这里专业来讲的话叫做操作码 人们就想能不能用符号来代替固定的操作码呢 这个时候就衍生出了汇编语言 有一段时间我们就是用汇编语言来写程序的 因为这比机器语言有了一点可读性 有的时候呢自己也可以充当编译器 把汇编语言再翻译成机器语言 这样有利于了解程序在单片机内部的执行本质 为什么汇编语言也被取代了呢 这是一段汇编语言 就算知道每个助记符的意思 如果想要读懂这样一篇汇编语言 要进行一行一行的翻译 才能够懂得它整体表达意思 这个阶段就相当于是文言文阶段 所以说汇编语言的可读性比较差 而且汇编语言就是符号化的机器语言 所以汇编语言比较依赖于硬件

### **[04:02]**
可移植性也是非常差 再后来人们就发明了C语言 用自然语言代替了硬件指令 用函数模块化来开发程序 更符合人类的表达逻辑 这个时候相当于进入了白话文阶段 C语言程序就非常方便移植编辑 阅读和传播 现在单片机开发的主流语言就是C语言 所以C语言是必修课 C语言是白话文 简单易懂 汇编语言是选修 汇编语言是文言文 需要你去翻译 当然汇编语言也有它的应用场合 我们现在偶尔还能看见 因为汇编语言的翻译效率比较高 往往一句汇编语言对应一句机器语言 而一句C语言有可能对应好几句机器语言 所以说汇编语言的效率高一些 现在我们来讲内存mp3大家都熟悉吧

### **[05:01]**
就是把不同的歌曲下载到mp3里 就能够听到不同的音乐 单片机有的时候就像mp3 不同的程序下载到单片机里面 单片机就可以去做不同的事情 那么程序下载到单片机的什么地方呢 就是内存 内存是由ROM和ram组成的 ROM和ram都能够装数据 也就是他们俩都能装东西 但是各有不同 这里补充一点 就是存储器由一个一个的单元组成的 ROM和ram都是每一个存储单元都是按字节组织的 也就是八位 这里还回到刚才我们就是说的 两种存储器都能装东西 但是各有不同 先来看一看ROM和ram里存储的内容是什么 那么它们的不同之处自然也就明白了

### **[06:00]**
ROM里存放的是指令代码固定不变的常量数据 那既然里面存放的是指令代码 单片机上电工作的时候就会取指令 执行指令 取指令执行指令 所以呢ROM一般是不可以更改的 那么断电的时候存储的内容也不丢失 可以长期保存ROM的容量 它一般是与程序代码的容量相匹配的 我们刚才说ROM是不可以更改的 如果想要改它就要重新下载 但是下载的次数是有限的 flash型的话通常是10万次 但是也有一些特殊的话 可能只有一次 那么现在我们来看一下ram ram存储的内容是全局变量和局部变量 那么在单片机商店工作的时候 可能会被随时的修改 修改的次数是没有限制的 但是在断电的时候 它存储的内容也会立即消失

### **[07:02]**
RHYM的容量通常也就是与全局变量 局部变量的容量相匹配的 既然说到ROM和ram里面要装东西 那么是谁把我们写的程序装进去的呢 不管是用C语言还是汇编语言所写的程序代码 自然包含了指令代码 常量数据 全局变量 局部变量 那么谁在幕后帮我们进行分类存储 是谁把一个程序代码一分为二 分别让他们装到ROM和ram里呢 是编译器和下载器 编译器不仅能够帮助我们 把C语言或者汇编语言翻译成机器语言 还帮我们分好了类 分好了存储的地址和位置 那么下载器就根据这些信息 把它们下载到存储器当中 这些工作一般不需要我们干预 它们是自动完成的 这一节讲的是广角

### **[08:00]**
这幅图是SSTC89C五I单片机的最小系统 所谓最小系统就是让单片机运行起来 它可以不做其他事 但是它可以正常的运行 STC89CY单片机两侧有很多的引脚 这些引脚大概可以分为四类 电源复位 晶振和IO口 那么我们先讲一下电源 电源顾名思义就是给单片机供电 单片机有两种常用的供电电压 一般是3.3伏或者是五伏 有的单片机两种电压都是兼容的 再来讲一下OO管脚 这类管角我们以后要常用单片机 通过它们来获取输入的信息 或者是输出控制外部设备 还有就是复位和晶振 这两个我们来讲一下 这里是复位引脚 这边是复位电路 单片机上电后 需要外部电路给它一个瞬时的高电平

### **[09:02]**
或者是低电平的复位信号 才能够启动工作 复位电路通常是由电容电阻组成的 也有一些系统用专门的复位芯片来实现 为什么会有复位电路呢 那我们说单片机一上电 就开始从存储器里取代码执行功能 取代码执行功能 执行功能肯定要用到各种寄存器 那么在执行功能之前 就要把寄存器的值都清除干净 所以有复位电路 让所有的一切寄存器都清除干净 从头开始 第四类管角是晶振 任何单片机想要工作就必须有晶振 人类有心脏砰砰砰跳 那么心脏对人类来说很重要 单片机有晶振砰砰砰跳 那晶振对单片机来说也很重要 但是看了不少视频 都停留在晶振与频率与时间的关系上

### **[10:01]**
就没有一个立体的认识 晶振到底是什么东西呢 最近看了穿越计算机的迷雾 这本书讲到了这一点 我们来简单的说一下 这里是存储器 这里是按键 按键按键 通过操作这四个按键 可以实现从存储器里取出一个数据 那么从存储器里取出一个数据 要分为几个步骤来完成 第一步就是KAC按键是零 就是不按KR按键是一 就是按下KDRKDR按键是零 不按KRD按键是零不按 那么第一步就完成了 第二步是不按不按按按 那么第二步完成了 第三步是按不按不按不按 那么第三步完成了 到此一个数据就会被读出来

### **[11:02]**
接着再读第二个数据 第三个数据就重复上面的工作就可以了 但是这是不是有一点麻烦 现在我们重新设计一下这个逻辑电路 看这里还是实现这三个步骤 但是是通过移位寄存器和移码器来实现的 什么是移位寄存器呢 就是这里的按键按一下移位寄存器当中的数据 就移动一下 移码器呢就是根据这边的数据 然后以码出我们想要的数据 那我们来看一下是怎么实现的 首先这里的按键按一下移位寄存器 得到一个数据是100 通过已码器已码得到的数据是零 1000100就是我们需要的第一步数据 那么第一步就完成了 接着是第二步 按键按一下移位寄存器 又得到一个数据是010 经过译码器进行译码

### **[12:01]**
得到的数据是0011 这就是第二步了 那么第二步就完成了 接着是第三步 按键又按一下移位寄存器 又得到一个数据是001 经过乙码器乙码得到的数据是1000 那么就是我们的第三步 第三步也完成了 至此一个数据就被读出来了 再读第二个数据 第三个数据就重复上面的操作 那么这个K这个按键就要一直备案备案 对不对 我们就把这个位置的按键换成了晶振 那么这里就可以通过晶振来实现这三个步骤 将一个数据读出来 所以从程序的角度来看 单片机执行程序指令是按一个节拍 一个节拍来执行的 所以说把晶振比喻成单片机的心脏 还是非常形象的 现在也有很多单片机把晶振集成到它的内部 所以说这类单片机就不用外接晶振也是可以的

### **[13:04]**
现在我们讲的是电瓶 单片机可以通过输入引脚检测到高低电平 当电压低于或者等于0.8伏的时候 程序里读到的就是零数字量 当电压高于或者等于2.4伏的时候 程序里读到的就是一数字量 这里是输出引脚 当输出零数字量的时候 实际上输出的是零伏 当输出一数质的量的时候 实际上输出的是3.3伏 一般输出的高电平等于它的供电电压值 往往是3.3伏或者是五伏 这一节我们讲的是时间 时间是单片机程序必不可少的元素 驱动发光二极管闪烁需要时间 工业自动化的某些延时需要时间 单片机的时间主要来源于两方面 首先是指令时间

### **[14:00]**
之前我们说过 单片机执行程序指令是按照一个节拍 一个节拍来执行的 那每执行一条指令都要消耗一些时间 那么执行指令的数量不同 就可以获得不同时间的长度 举个例子来说 这里是一个延时函数 这是延时函数的调用 给它一个值是1000 那么这里就是1000 这里就是1000 那for指令就会执行1000次 假如在调用的时候 我们给它一个值是500 那么这里就是500 这里也是500 那么for指令就会执行500次 那么for指令执行1000次和执行500次 获得的时间长度肯定是不一样的 第二方面 时间主要来源于单片机内部自带的定时器 先举一个例子 设置定时器每20ms产生一次中断 现在要获取十秒钟的时间

### **[15:00]**
这个对于初学者来说可能比较难 但是也不是不好理解 我们来看一下吧 不理解也行 因为后面都会详细的讲到 这里是一个定时计数器 我们想要实现定时的功能 它前面有三个通道 334 00:15:23,450 --> 00:15:25,610 我们想要实现定时的功能 就要选系统时钟 因为它滴答滴答的时间是一致的 然后沿着红色的线将相关的寄存器都配置好 使这个红色的线是畅通的 那么我们来看看它是怎么工作的 这里是一个系统时钟 它滴答滴答的频率是12兆 这里说除以12 那就除以12 得到的频率是一兆 那么它滴答滴答一次的时间是多长时间呢 我们就用一秒除以一兆 得到一微秒每赫兹 所以说滴答一次就是一微秒

### **[16:01]**
这个一微秒就来到了这个定时计数器 定时计数器这时候说啊 我有时钟了 我可以来寄宿了 我要记什么数呢 题目当中说每20ms产生一次中断 那么20ms等于2万微秒 那么计数器就要记2万次才可以得到2万微秒 也就是20ms 这个时候你又跟计数器说 你帮我计数2万微秒 技术好了 再告诉我 那么计数器就是去计数了 这个功能它可以做到的 计数到2万微秒之后呢 计数器就通过这个TF0告诉你 它将TF0至一说2万微秒 到了2万微秒 到了这个时候你就去中段里累计一次 这个TF0至一 接着你再去告诉定时器 再去计2万微秒 记完2万微秒 它又将TF0至一

### **[17:01]**
这个时候你又累计了一次 就这样我们累计了500次之后呢 500×20毫秒就得到了我们想要的十秒 好了 这节课到此结束 拜拜

