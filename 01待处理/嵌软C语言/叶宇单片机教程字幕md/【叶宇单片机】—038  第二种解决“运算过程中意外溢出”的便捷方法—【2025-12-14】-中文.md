# 【叶宇单片机】—038  第二种解决“运算过程中意外溢出”的便捷方法—【2025-12-14】-中文

## 字幕正文

### **[00:00]**
大家好 今天我们来学习第38节之前 在做加减乘除运算的时候 有的时候结果会溢出 今天我们来介绍第二种解决办法 也会把第一种解决办法带一下 定义一些变量来执行这条语句 X乘以Y赋值给AX的类型是unsign int类型 Y的类型是on3的int类型 两个类型一致就可以计算了 X是1000 Y是3000 那么1000×3000 结果是300万 是on sigma int类型的 因为是on3的int类型的 只能存储16位的数据 所以说会保存低16位 高八位就会被丢掉 那么执行完X乘以Y就得到一个结果 有大小有类型 将这个结果赋值给A A提供的存储空间是四个字节的 那么就将这里的两个字节存到A的空间当中

### **[01:02]**
本来是三个字节的 结果只存储了两个字节 那么结果肯定是错误的 我们来写程序验证一下 还在我们的单片机上来练习程序 定义一些变量 执行这条语句X乘以Y赋值给A 最后将A的结果输出 查看一下A的结果是5万零八百八十十六 进制是0XC6C零 这与我们分析的结果是一致的 这就是运算过程当中结果的溢出 那么怎么解决呢 第一种办法是引入中间变量 这是原来的程序 我们对它进行修改一下 定义一个变量M是on3的lg类型的 就是为X里面的数据申请一个更大的房间 定义一个变量N还是on sallog类型的 为Y空间里的数据申请一个更大的房间 将X赋值给M

### **[02:01]**
就是将X里面的数据存到M的空间当中 将Y复制给N 就是将Y里面的数据存到N的空间当中 然后来执行M乘以N赋值给A M的类型是on3的lg类型 N是on3的lg类型 两个类型一致就可以计算了 这是M这是NM存储的是1000 这是1000的16进制是一个正数 前面八位补的是零 N存储的是3000 这是3000的16进制是一个正数 前面八位补的是零 那么M乘以N得到300万 结果是on sallonger类型的 因为结果是on sallonger类型的 可以保存四个字节 所以说这里的结果会被完整的保存下来 那么执行完M乘以N赋值给A

### **[03:01]**
A也是阿SALANG类型的 那么就能将这里的结果完整地保存下来 第二种办法 使用C语言类型的强制转换 将X强制转换成unsigned long类型 将Y强制转换成on sallonger类型 我们知道 只有赋值语句才会改变存储空间的数据 其他时候的计算 都会把数据放在一个临时的存储区 不会改变原始的数据 那么X原来的值是1000 这是1000 强制转换成on3的long类型 前面八位补的是零 这就是临时存储区的X的值 Y是3000 这是3000 强制转换成on3的long类型 前面八位补的是零 这就是Y在临时存储区的值 那么X乘以Y得到300万 结果是on3的long类型的

### **[04:00]**
因为两个运算数都是unsa lg类型的 所以说结果也是unsigna lg类型的 因为是arsa longer类型的 可以保存四个字节 所以说这里的数据会被完整的保存下来 然后再赋值给AA也是unsigned long类型的 所以说会把这里的数据 完整地保存到A的空间里 那么我们来写程序验证一下 将X和Y强制转换成on3的long类型 然后输出A的结果 查看一下A的结果是300万是零 XIDC6C零 这与我们分析的结果是一致的 我们来看一下这两种方法 第一种办法引入中间的变量 也就是先把房子造好 然后再把数据放进去 第二种办法使用类型的强制转换 就是在运算的时候临时给他造一个大房子

### **[05:04]**
我们刚才在使用类型强制转换的时候 也有类型的变化 就是其他类型的变化 其实跟这里面都差不多 正数与零前面补的也是零 负数前面补的是一 也遵循类型等级 他们的目的其实都是一样的 将等级低的类型转换成等级高的类型 正数与零前面补的是零 不改变数值的大小 这个我们很好理解 负数的提升 前面补的是一 比如这里八位的三 你要把它提升为on signa int类型是16位的 那么不管372 11 前面八位补的是一 然后保证是一个16位的三 也就是保证数值不变 至于你用unsigned int来解读 得到一个不同的结果 那就是你的事情了 人家本来是一个负数 你非得用正数的方式去解读 当然会得到一个不同的结果

### **[06:01]**
正常情况下将提升与对齐之后的数据给运算器 运算器是不会把你的数据算错的 这一点你要放心 但是为什么有的时候会得到一个奇怪的结果 不是你想要的结果 那是因为你跟运算器解释的时候解释错了 比如你本来想算一个一 然后给他的是0XFF也是一 这个是没有问题的 但是你一不小心给它的类型是on sa int类型 那么运算器就会按照unsigned int类型 去解读这个数据 那么解读出来的结果就是65535 所以说有的时候是因为你的一个错误的解释 才得到一个错误的结果

