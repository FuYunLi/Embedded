# 【叶宇单片机】—034  位运算的左移—【2025-12-07】-中文

## 字幕正文

### **[00:01]**
大家好 今天学习的是第34节位运算的左移 这节课主要有123455个知识点 但是实际的内容也不多 一是左翼运算 这个比较好理解 二是类型的提升与对齐 这个我们说了好多遍 这里只是再次的使用 三是未定义的行为 这个我们需要特别注意一下 四是左右运算的应用 也就是在哪些场合使用 五是总结 其实最后对左运算的总结也就是三句话 所以说内容不多 我们先来看一下什么是左翼运算 这是左翼运算的语法格式 被移数 然后左移N位 结果赋值给保存变量 比如这里有一个八位数0X0F我们左移一位 也就是整体向左移一位 左边移出去了就丢掉 右边空出一位就补一个零

### **[01:00]**
那么左移两位 也就是整体向左移两位 左边移出去的就丢掉了 右边空出来两位就补两个零 N等于一 左移一位就补一个零 N等于二 左移两位就补两个零 N等于N 左移N位就补N个零 这就是左移运算 接下来我们再来看一下运算数的类型 这里有两个运算数 被移数和N来看一看它们的类型 之前学习过算术运算 加减乘除位运算的与运算或运算异或运算 它们都是两个操作数 首先对每一个操作数进行类型的提升 然后再看一看两个操作数类型是否相同 如果不相同的话 再进行类型的对齐 因为硬件要求两个操作数的类型必须是一样的 左移运算也是两个操作数 所以说也符合类型的提升与对齐

### **[02:02]**
类型的提升与对齐 依据的是类型的等级提升与对齐 之后将数据给运算器 运算器是按照公共类型来解读数据的 左云运算有两个操作数 其中一个操作数是一位的位数 N有的时候它是常数 那么常数是什么类型呢 之前我们也说过 它没有后缀时 由最小能容纳其值的类型来确定 也就是先是int类型 如果int类型装不下 再改为lg类型 如果常数有后缀时 由后缀直接指定类型 我们来看练习题 一定义一个变量 执行这条语句运算之前 先进行类型的提升与对齐 C是on3的char类型提升为int类型 八是int类型 两个类型一致就可以计算了 C的值是0X81是一个正数

### **[03:00]**
这是0X81的二进制提升为int类型 是一个正数 所以说前面补零 那么C向左移八位 也就是整体向左移八位 右边空余的八位补零 也就是这里八位补零 左边移除了的八位要去掉 那么执行完C向左移八位 得到一个结果 有大小有类型 将这个结果复制给cc提供的存储空间是八位的 所以说只能将这里的第八位 0X00存到C的空间当中 我们来写程序验证一下 现在程序写好了 还是在我们的单片机上来练习 程序 定义一个变量 C初值是0X81 然后将C的值向左移八位 结果赋值给C 最后输出C的结果 我们看一下C的结果是零 这与我们分析的结果是一致的 如果我们想把这16位都保存下来

### **[04:01]**
怎么办呢 我们定义一个变量Y在执行这条语句 C向左移八位 我们知道它得到一个结果 有大小 有类型 将这个结果赋值给Y Y提供的存储空间是16位的 那么能将这里的16位全部存到Y的空间当中 我们来写程序验证一下 现在的程序写好了 重新定义一个变量Y将C的值左移八位 然后赋值给Y 最后输出Y查看一下Y的结果是0X8100 这与我们分析的结果是一致的 我们注意看一下 这里的数原来是int类型的 它的符号位是零 是一个正数 经过左移之后还是一个int类型的 但是这里的符号位是一是一个负数 这有什么影响呢 这会触发一个未定义的行为 这就是第一个需要注意的未定义的行为

### **[05:00]**
左移后改变了符号位 被触发未定义的行为 正常情况我们是左移 符号位不会被特殊的对待 就是所有二进制一起左移 但是移动后如果符号位发生了改变 比如零变成了一 或者是一变成了零 就会触发未定义的行为 对于未定义的行为 不同的编译器会有不同的处理方式 Q当中对这种行为的处理方式 就是保存左移后的结果 就是所有的二进制位一起左移 最后的结果是什么 就保存什么 符号位不会被特殊的对待 但是还是建议被移术优先使用无符号数 无符号数它就没有那个符号位 就不会触发未定义的行为 它的结果就是确定的 练习题二 定义一个变量 执行这条语句运算之前 要先进行类型的提升与对齐

### **[06:00]**
M是sign int类型 简写就是int类型 16是int类型 两个类型一致就可以计算了 这是M的16位 整体向左移16位 那么空余的16位补零 这里有没有注意到 就是把所有的数据都移出去了 这种情况也会触发未定义的行为 这里就是第二个需要注意的 未定义的行为就是左移 它的移位位数是违法的 什么是违法的呢 比如这里的int家族 它的位宽是16位的 正常的移位位数是0~15 如果你向左移16位 17位 它就是违法的 会触发一些未定义的行为 比如LANG家族 他的位宽是32位的 正常的一位位数是0~31 如果你向左移32位或者是33位 它就会触发一些未定义的行为 注意这里有一个错误

### **[07:01]**
我们想一下 就是char家族和short家族 我们说在运算之前先进行类型的提升与对齐 类型的提升就是小于int家族都提升为int类型 那么左翼运算的时候 至少都是在int家族基础上进行运算的 所以说左移八位是合法的 不要使用非法的移位位数 它会触发一些未定义的行为 还有一种就是一位位数 有的时候会写成一个负数 它也会触发未定义的行为 就是硬件都没有这个功能 都不知道怎么去处理 左移移位位数违法 还有一种情况就是它是一个负数 比如一 所以说我们就要求在移位运算 无论是左移还是右移 一位的位数必须是一个非负整数 也就是零或者是正整数才可以左移 未定义行为的总结

### **[08:01]**
首先是符号位的改变 会触发一些未定义的行为 但是在Q当中保存的是最后的结果 也就是二进制的每一位向左移动 结果是什么 Q就保存什么 还有一点就是移位位数的违法 超过位宽或者是一个负数 这个时候也会触发一些未定义的行为 我们要把它改成零或者是正整数 左移的应用1×2 举一个例子 有一个八位数0X0F这里是它每一位的全 如果我们左移一位 也就是整体向左移一位 左边移出来的就会被丢掉 右边空出来一位 我们就补上零 那么我们看一下这个一 它的全是二的零次方 左移一位之后 它的全变成了二的一次方 这个一它的全是二的一次方 左移一位之后 它的全变成了二的二次方

### **[09:01]**
就是左移一位 每一位的全都乘以了二 那么整体就乘以了一个二 左移两位 左边移出去的两位会被丢掉 右边空出来的两位会补零 我们来看一下这一个一本来是二的零次方 现在变成了二的I次方 这个一本来是二的一次方 现在变成了二的三次方 就是左移了两位 它的每一位的全都乘以了四 那么整体就乘以了一个四 再来举一个例子 有一个数192 这是二进制的 每一位向左移一位 右边空出来一位补零 左边移出去的 我们就把它丢掉 得到的结果是128 左移一位 理论上应该是结果乘以二 但是这里的实际结果并不是这样子的

### **[10:00]**
我们再来看一下左移两位 右边空出来了两位 我们补零 左边移出去的两位 我们把它去掉 得到的结果是零 我们理论上是左移两位 结果是乘以个四 但是他实际得到的结果却是零 不要迷恋左移乘以二 他有的时候也会出错 左移的应用二不同数据类型之间的合并 定义一些变量 然后来执行这三条语句 先来执行第一条语句 H复制给CH的类型是on sa char 类型是八位的 C是on3的 int类型是16位的 H的值是0X12是八位的 赋值给C占用了里面八位的空间 那么还有八位的空间没有使用 怎么办呢 之前说过赋值的时候有剩余的空间 正数的补零 负数的补一 所以说这里的高八位

### **[11:01]**
我们补0C的结果就是0X0012 执行第二条语句 在运算之前先进行类型的提升与对齐 C的类型是on sa int类型 八是int类型 然后提升为on sa int类型 现在两个数的类型都是on sa int类型 C向左移八位 C的值是0X0012 这是它的二进制 整体向左移八位 剩余的八位补零 那么剩余的八位补零 移出去的八位就被丢掉了 C向左移八位 得到一个结果 有大小有类型 将这个结果赋值给C C提供的存储空间是16位的 那么将这里的16位全部存到C的空间 C的值就是0X1200

### **[12:03]**
这一步的目的 主要是把0X12移到C的高把位上面 再来执行第三条语句 C加上L赋值给C运算之前 先进行类型的提升与对齐 set的类型是arsa int类型 L是arsa char类型 提升为int类型 L的值是0X34是一个正数 所以说前面八位补零也就是0X0034 那么C加上LC的值是0X1200 也就是0X12000X34 结果是0X1234是on sign int类型 那么执行完C加上L得到一个结果 有大小 有类型 将这个结果复制给C C提供的存储空间是16位的 那么就将这16位全部存到C的空间当中

### **[13:01]**
那么C的值就是0X1234 就实现了这里的数据合并 我们来写程序验证一下 现在程序写好了 定义三个变量HL和C然后来执行这三条语句 也就是刚才我们所讲的 最后将set的结果输出 查看一下C的结果是0X1234 这与我们分析的结果是一致的 这里我们要注意一下 大部分的时候 只有赋值语句才会改变存储单元的数据 其他的时候都不会改变 比如这里的左移运算 加法运算 都是把数据放在一个临时的存储区来计算的 不会改变数据的原始值 左移运算的应用三聚焦在某个变量的某个位 之前 学习过按位与运算 按位或运算 按位异或运算都是改变某一位或者是某一些位

### **[14:02]**
有选择性地进行改变 我们用左移运算也可以改变某一位 或者是某一些位有选择性的改变 比如或运算的或一是1B或上0X01 赋值给B0X01 这是它的二进制 也可以用左移运算 一向左移零位来表示 所以说0X01也可以写成一向左移零位 B或上0X02赋值给B0X02 它的二进制也可以写成一向左移一位 所以说连S02也可以写成一向左移一位 B或上0X04复制给B0X04 这是它的二进制 也可以写成一向左移两位 那么0X04也可以写成一向左移两位 这样来表达的话比较直观一些

### **[15:01]**
以下就可以看出一移到了哪一位上 同样与运算与零是零 B与上0XFE赋值给BLINUXFE 这是它的二进制 对它进行取反得到一个结果 这个结果可以用一向左移零位来表示 也就是一向左邻一位得到这个二进制 将这个二进制进行取反就得到了0XFE 所以说0XFE可以用一向左移零位 然后再进行取反来表示B与上0XFD 赋值给B0XFD 这是它的二进制 对二进制进行取反得到一个结果 这个结果可以用一向左移一位来表示 也就是一向左移一位 得到一个二进制 对二进制进行取反就得到了0XFD

### **[16:03]**
那么0XFD就可以用一向左移一位 然后再进行取反来表示 B与上0XFB赋值给B连XFB 它的二进制对它进行取反 得到一个二进制 这个二进制可以用一向左移两位来表示 也就是一向左移两位 得到一个二进制 这个二进制再进行取反的话就得到了0XFB 那么0XFB就可以用一向左移两位 再进行取反来表示 同样用这种方式来表示比较直观一些 一下就可以看到零移到了哪一位上 左移运算的左移简写 左移运算它有两个操作数 其中一个操作数是保存变量 然后左移N位赋值给保存变量 它的简写方式就是保存变量 左移简写符号剩余一个N我们来举一个例子

### **[17:04]**
定义一个变量F左移简写方式 你要想到它原来的方式就会计算了 F向左移两位 然后结果赋值给F左移运算的总结 也就是三个点 首先是符号位的改变 它会触发一些未定义的行为 在Q当中会保存最后的结果 但是还是建议被移数优先使用无符号数 无符号数就没有符号位 就不会触发未定义的行为 他的结果就是确定的 第二点是一位位数违法 也就是超过位宽或者是一个负数 这个时候我们就要把它改成零或者是正整数 第三点就是左移乘以二 不要迷恋左移乘以二 它有的时候也会出错

