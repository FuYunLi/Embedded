# 【叶宇单片机】—84  中断与中断函数—【2026-06-17】-中文

## 字幕正文

### **[00:02]**
今天我们来理解中断系统当中的几个概念 首先是中断 打一个比方 比如我现在正在上课 我讲一句话就是执行一条指令 讲一句话就是执行一条指令 这个时候有人举手提问 是不是打断了我 我要去响应 我怎么响应呢 是不是要先把我这句话说完 我不能讲半截的就不讲了 也就是执行完当前这一条指令 然后下一句话就是下一条指令就不执行了 转而去为你服务 为你服务的这一段程序 我们叫做中断服务 子程序也叫做中断函数 服务完了是不是返回到我刚才中断的地方 然后接着往下讲课 这么一个过程就叫做中断 接着我们看一下单片机当中的中断 是如何响应的 首先从主程序也就从main开始依次往下执行程序

### **[01:05]**
执行程序的时候突然有中断请求 那么怎么响应呢 首先单片机会把当前这一条指令执行完 那么下一条指令就不执行了 就停在这里 这里我们叫做断点 这里所说的指令是指C语言 翻译成汇编指令的那个指令 不是说C语言这一条语句 接着我们看一下这个断点 这个断点的地方是不是有地址 这个程序从上往下执行的过程当中 可能还使用了一些通用寄存器 我们是不是要把这个断点处的地址 还有一些通用寄存器的数据进行保存 也就是保存现场数据 第三步就是获取中断函数的地址 有了这个地址之后 我们才可以跳转这个地址指向的存储单元 接着往下执行中断服务子程序

### **[02:02]**
那么第四步就是执行中断函数 执行完中断函数是不是要返回 返回它就恢复一个现场数据 而且他当时保存了断点的地址 所以说能够正确地返回到这个断点处 接着往下执行程序 这就是一个中断的响应过程 它的整个过程都是自动完成的 我们大概了解一下就可以了 虽然说它是自动完成的 但是我们需要去写中断函数 先看一下中断嵌套 还是从主程序开始依次往下执行程序 这个时候有一个中断请求一 就是有一个中断言进行请求 就会打断现在正在执行的程序 转而去执行中断服务子程序一 那么在执行中断服务子程序一的时候 有一个更高优先权的中断请求二

### **[03:03]**
那么就允许终止现在正在执行的中断服务程序 转到另一个中断服务程序二 继续执行中断服务程序二 执行完了之后 它会返回到这个断点 接着执行中断服务程序 一执行完中断服务程序一了之后 又会返回到之前的断点 接着往下执行主程序 这就是一个中断嵌套 我们暂时了解一下就可以了 这里有两个中断请求 对它的响应过程是自动完成的 但是我们需要写这里的中断服务程序一 和中断服务程序二 我们来看一下中断函数的格式 首先给这个中断函数起一个名字 然后这个函数没有输出 也没有输入 接着在大括号里写中断服务程序的代码 这样看起来是不是和普通的函数没有什么区别

### **[04:02]**
但是别忘了后面写一个interrupt中断号 我们用这个格式来写几个中断函数 前面这部分我们可以理解 它就像普通的函数一样 后面这部分特别是这个中断号 他是什么意思呢 我们来看一下中断号 也就是中断源的一个数字名称 要解释它是什么意思 我们还需要理解一下中断向量的地址 中断向量就是中断服务程序的入口地址 这里有一段存储空间 我们看他是如何分配的 从03H开始分配给中段号 零从0BH开始分配给中段号 一从13H开始分配给中段号 二 从1BH开始分配给中断号 三分配给他 这些存储单元是干嘛用的呢

### **[05:00]**
我们看一下 这里是不是有几个中断源 外部中断零定时器 零中断 外部中断一定时器一中断串口中断 我们在写程序的时候 要分别为这些中断写一个中断服务子程序 比如为他们分别写了中断服务子程序 那么在编译的时候 编译器看到interrupt0 就把这个函数的地址存储在中段号零 对应的存储单元 看到interrupt1 就把这个函数的地址存储在中段号一 对应的存储单元 看到interrupt2 就把这个函数的地址存储在中段号二 对应的存储单元 也就是把我们写的中断函数的地址 分别写到对应的存储单元当中 这个时候如果来了一个中断 比如说外部中断零

### **[06:01]**
那么单片机就会到中段号零对应的存储单元 去取这个函数的中断函数的地址 有了中断函数的地址就会就可以执行中断函数 刚才我们说中断的响应过程 第三步就是获取中断函数的地址 有了地址之后才会去执行中断函数 我们来整体的看一下它的执行过程 首先是从主程序 也就是main开始依次往下执行程序 这个时候有一个中断请求 比如说外部中断零请求 单片机 这个时候他是可以区分 到底是哪一个中断源请求 那么他知道是外部中断零请求就会到零 就会到中段号零对应的存储单元 去取一个中断函数的地址 有了中断函数的地址 就可以跳转到这个地址指向的存储单元 接着往下执行函数 也就是执行中断服务子程序

### **[07:03]**
执行完中断服务子程序就会返回到断点处 接着往下执行主程序 那么今天就简单的介绍一下这几个概念 首先是中断 然后是中断响应的过程 中断函数以及中断向量好 这节课到此结束 拜拜

