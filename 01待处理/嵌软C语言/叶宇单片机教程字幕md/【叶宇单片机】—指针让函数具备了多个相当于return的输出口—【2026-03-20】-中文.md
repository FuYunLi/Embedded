# 【叶宇单片机】—指针让函数具备了多个相当于return的输出口—【2026-03-20】-中文

## 字幕正文

### **[00:00]**
这节课我们再次来看一下指针的灵活应用 就是指针可以使函数拥有多个输出口 我们先来看一下函数的格式 函数就是将实现同一功能的语句放在一起 实现功能模块化 我们知道函数内如果需要外面的数据 就通过参数列表传进来 如果产生新的数据需要输出 就通过返回值输出 那我们观察一下它的输出和输入接口 有什么特点 它的输出是不是只有一个返回值类型 也就是最多输出一个变量 再看一下它的输入 它的输入是一个参数列表 重点是列表 那么输入接口可以放很多个变量 举一个除法函数的例子 定义一个商 这是一个全局变量 定义一个除法函数

### **[01:01]**
它有两个输入 一个是被除数 一个是除数 定义一个局部变量商 然后对两个数进行求商 接着返回值是商 然后我们在main里调用这个函数 它输入两个数 一个是十 一个是五 输出一个熵给全局变量 这个时候我们通过函数的输出接口 只能输出一个变量 如果我们想输出两个变量呢 输出一个商 再输出一个余数怎么办呢 我们在解决这个问题之前 先来看一下这些变量 毕竟我们要通过变量来传输数据 首先是一个全局变量 然后函数的输入接口变量我们也叫做局部变量 接着内部还有一个标准的局部变量 那么我们看一下 这些变量在内存当中是如何分配的 执行我们写的程序的时候

### **[02:01]**
是从main函数开始执行的 但是在执行main函数之前 要为我们的全局变量分配一些存储空间 从中间虚线部分 我们大概的将上面叫做战区 下面叫做全局数据区 那么为全局变量分配一个存储空间 接着从main里往下执行除法函数 通过函数名调用除法函数 那么在调用函数的时候就有一个压榨过程 压榨的过程首先会对现场保护 将一些数据压榨 然后是函数的返回地址压榨 这两个过程我们暂时先忽略 我们来看一下它的局部变量 那么会将局部变量进行压榨 首先是一个被除数 然后是一个除数 接着是商 也就是为局部变量分配临时的数据存储区

### **[03:00]**
接着执行函数 再来看一下刚才的问题 就是多个输出的问题 刚才我们只输出了一个商 现在我们想输出一个余数 首先定义一个全局变量余数 再定义一个局部变量 然后对两个数进行求余 最后将这个余数赋值给全局变量 当然也可以直接复制给全局变量 那么我们说在函数内使用了一个全局变量 能够实现数据的输出 但是也同时破坏了函数的模块化和独立性 对不对 那这个时候我们怎么办呢 这个时候我们是不是多了两个变量 一个全局变量 一个局部变量 我们再来看一下它的内存分配 我们为全局变量分配一个内存 为局部变量分配一个内存 我们想将这个余数存到全局变量区的余数里面

### **[04:03]**
没办法直接复制 那么我们可以怎么办呢 想一想 是不是可以借助指针 比如我们在输入接口定义一个指针变量 这个指针变量如果存储的是JU8余数的地址 也就是0031 那么对这个指针变量解引用 是不是可以找到这个存储空间 然后可以对这个存储空间进行读写 在输入接口定义一个指针变量 那么就不需要这个局部变量对两个数求余 直接赋值给指针变量指向的存储空间 接着后面这个也不需要了 我们把它们写的更紧凑一些 在函数调用的时候 它的输入接口多一个数据就是取地址

### **[05:02]**
也就是在函数输入接口定义一个指针变量 可以读写函数外面的数据 实现函数内数据的输出 我们再来看一下 这个指针变量存储的是0031 对这个指针变量进行解引用 就找到了0031 这个存储空间 可以通过新辟于对这个存储空间进行读或者写 也就可以实现函数内数据的输出 那么执行完函数之后就会返回 返回就有一个出站的过程 出站的过程就是释放这些存储空间 然后SP又指向栈底 也就是这时候战区是空的了 如果下面再执行其他的函数 又可以重复的使用这个战区 所以说战区就相当于是一个公共区域

### **[06:00]**
可以重复使用 那么就节省了存储空间 接着我们看一下全局变量 这里面的星辟鱼 它的变量本体已经消失了 所以说这里也就没有什么实用的意义了 不可以这样使用了 我们说指针变量它想获取谁的地址 就可以获取谁的地址 就像普通变量赋值一样 简单打开它存储的地址的空间 按照什么类型来解读这个空间的数据 也是指针自己说的算 接着我们回到今天的主题 就是指针让函数具有多个输出口 想一下是如何实现的呢 函数内部的数据返回只有一个返回值类型 只能返回一个变量 如果我们想输出多个变量怎么办呢

### **[07:00]**
可以利用参数列表 在参数列表里定义多个指针变量 那么在函数内部对这些指针变量进行解引用 就可以读写外部的存储空间 实现数据的输出 那有的同学会想在函数内部 直接给全局变量赋值 也可以实现数据的输出 但是这样破坏了函数的模块化和独立性 我们通过输入和输出接口来传输数据 保证了函数的模块化好 这节课到此结束 拜拜

