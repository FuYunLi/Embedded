# 【叶宇单片机】—012  变量的定义和赋值—【2025-10-19】-中文

## 字幕正文

### **[00:01]**
大家好 今天学习的是第12节变量的定义和赋值 变量的定义就是将数据存在ram当中 我们先来看看ROM程序存储器 ROM存储着程序和常量数据 单片机复位之后就从里面取指令 执行指令 运行期间是不可以修改的 数据存储器ram存放着可以更改的数据 比如是全局变量 局部变量可以随时进行修改 这一节我们学习如何定义变量 并且给变量赋值用的都是ram空间 我们知道存储器的每一个单元 都是按字节组织的 也就是八位可以存储 从八个0~8个一换算成十进制的无符号数 就是0~255 那么两个单元的组合 就可以从16个0~16个一 换算成十进制的无符号

### **[01:00]**
就是0~6535 那么我们有的时候可能存放的数据是一 有的时候是100 有的时候是1万 有的时候是一个亿 这个就是我们存放数据的一个需求 接着我们看一看C语言是怎么设计的 那么我们存放的数据可能是一一百一万一个亿 它们所占用的存储单元的数量肯定是不同的 所以C语言就设计了几种类型 分别存放不同大小的数据 以节省空间 在五一单片机当中 一个存储单元定义的类型是unsigned char 可表示的数字范围是0~255 两个存储单元定义的类型是unsigned int 可表示的数字范围是0~655 三五 四个存储单元定义的类型是unsign a log 可表示的数字范围是零到42亿九千 496万7295

### **[02:01]**
这几个类型 就是向单片机申请不同大小的存储空间 申请之后 单片机可以通过地址来处理这些空间的数据 存储在这些空间的数据 我们要抓住两个关键点 一个是要知道存储单元的数量是一个 两个还是四个 一个是要知道地址 才能正确地处理里面的数据 这两个关键点缺一不可 单片机 它是不是知道这些空间的地址 和占用存储单元的数量 但是我们只是通过这几个类型申请了 至于分配的位置是不知道的 既然我们申请了空间 肯定是要用的 那怎么办呢 单片机擅长记住地址 和存储单元的数量 这些都是一些数字量 我们记起来肯定是麻烦的 所以说我们在申请空间的时候 就定义了一个变量名

### **[03:02]**
这个变量名就关联了存储单元的数量和地址 也算是我们申请空间之后得到的一个反馈 我们只需要记住变量名 就可以操作存储单元当中的数据了 那么一个完整的变量定义 比如是定义一个变量a on sign chan a 这里面的分号表示一条语句的结束 on signal char是用于申请空间的大小 A是用于绑定存储单元数量和地址的信息 我们在使用的时候就可以 直接用变量名来操作就可以了 至于它的存储空间和地址是后台自动处理的 变量名的命名规则是ABC加下划线 然后123 这是什么意思呢 可以以大写字母和小写字母或者是下划线开头 但是数字的话要放在后面 数字不可以开头 因为在C语言后期是要编译成汇编的

### **[04:02]**
在汇编当中数字开头表示的是数字量 所以说我们变量名不能用数字开头 那么我们看一看定义的这些变量哪些是合法的 哪些是不合法的呢 这个是不合法的 因为它不能数字开头 这个是用了关键字也是不可以的 这个中间是有空格的 也是不可以的 这个是对的字母 这个是字母开头也是对的 这个下划线开头也是对的 然后讲一下变量的使用 我们定义了一些变量 比如阿三的CHARA 阿三那恰M 阿三那恰N 也就是在rhyme当中分别申请了一些空间 这些空间的数据可以随时的修改读取 那么怎么使用呢 我们用一个赋值符号来使用 也就是这里的等于号 什么是负值呢

### **[05:01]**
比如这一条语句分号还是表示一条语句的结束 那这个赋值语句是什么意思呢 把M空间的数据赋值给A空间 也就是将M空间的数据复制复制到A空间 那么A空间的原来的数据就被覆盖了 那M空间里的数据是保持不变的 第二句赋值语句的意思就是 将N空间里的数据复制到M空间里 然后M空间里原来的数据就被覆盖了 N是保持不变的 我们从这里发现了什么 同一个变量 它在左边和在右边处理的方式是不一样的 在赋值符号的左边需要的是一个空间 这个空间用于存放数据 在赋值符号的右边需要的是一个数据

### **[06:02]**
这个数据可以来自某个变量的空间 也可以是纯数字 比如这里就是将十复制给mm里面就变成了十 那么有的同学会问 一个变量放在左边和放在右边 放在左边用的是空间 放在右边用的是空间里的数据 这个是谁来操作的呢 这个是编译器 它们编译我们C语言的时候自行操作的 我们不需要管 我们只需要记住左边它需要的是一个存储空间 右边需要的是一个数字量 这就是定义好的变量进行赋值 在五一单片机当中 一个存储单元定义的类型是UNSIGNACHA 两个存储单元定义的类型是unsign int 四个存储单元定义的类型是UNSIGNALANG 但是在不同的单片机当中 不一定是这样规定的

### **[07:00]**
C语言标准规定 unsign a chart所占用的字节数要小于等于 unsigned int占用的字节数要小于等于on sign lang 占用的次结束 在不同的单片机当中 assign a chair它的类型是全兼容的 就是不同的单片机当中是一样的 无门槛的 unsigned int类型是优先随CPU位宽适配 UNSLG类型是依赖编译器的长整数支持 unsign int类型占用的字节数是随CPU位宽适配的 八位的单片机比较特别 它通常是两个字节 16位的单片机 通常为两个字节 16位与CPU位宽一致 32位单片机通常为四个字节 32位与CPU位宽一致 那么我们现在就来写一个程序 定义一个变量 然后给变量赋值 这个是11节课的程序 我们把它复制一下

### **[08:00]**
改成12节的程序 这是一个练习C语言程序的模板 打开之后 我们只需要修改C语言学习区域 开始到C语言学习区域结束 其他的地方不修改不动 首先我们来定义四个变量 ABCD定义的类型都是ASSIGNATURE类型 现在定义好了 变量ABCD 就等于说是向ram空间 分别申请一个字节的存储单元 现在我们将D存储单元给它一个初值是九

### **[09:04]**
B给它赋一个值是3B存储单元的数据 就是3C给它赋一个值是B 就是将B存储单元的值把它复制到C空间 然后我们将ABCD输出看一下 程序编写好了 定义四个变量 然后分别把它们的值输出看一下 Save all rebuild 零警告零错误 现在我们把它下载到单片机当中 我们现在来下载一下我们的程序 芯片型号 扫描串口波特率24009600

### **[10:01]**
打开程序文件 找到我们12节的程序 找到我们12节的程序 找到机器码 点击下载 这个时候输出了四个数 第一个数是零 第二个数是三 第三个数是三 第四个数是九 我们看一下程序列 对不对 第一个数字是零 第二个数字是三 第三个数字是三 第四个数字是九 第一个数字是零 因为我们没有给A重新赋值 那A空间里的数据就是它原始的数据 原始的数据是零 它显示的就是零 第二个数据B我们给它赋值是三 所以说它输出的结果是三 第三个数C我们将B的值赋到C空间里

### **[11:00]**
所以C的值是三 第四个数D是九 因为我们在定义的时候就给它赋了一个值九 所以说程序执行的结果符合我们的预期 是没有问题的 好这节课拜拜

