# 【叶宇单片机】—第70节  “万能数组”的结构体—【2026-04-09】-中文

## 字幕正文

### **[00:01]**
今天我们来讲一下结构体主要分成三个部分 一是定义 二是使用 三是内存对齐 需要注意一下 就是讲到内存对齐的时候 它里面的类型DE义都是基于32位单片机 首先来看一下结构体的基本格式 STRUCT是一个关键字 后面加上一个结构体的名字 它的命名规则和普通变量的命名规则 都是一样的 就是ABC下划线123 也就是大小写字母和下划线 可以开头数字放在后面 然后大括号里可以定义一些成员变量 可以定义多个不同类型的成员 最后就是末尾要加一个封号 按照这个格式写出来的目的是什么呢 就是定义一个新的类型 我们来看一下它是如何使用的 它的使用过程和造一个机器的外壳是差不多的

### **[01:03]**
首先是开模具 然后生产外壳 使用外壳 比如assa int就是我们已有的一个模具 然后我们再为结构体开一个模具 STRUCT是一个结构体 关键字 后面是一个结构体的名字 名字可以随便起 接着大括号里放一些不同类型的成员变量 现在模具开好了 然后就可以批量的生产 定义不同的变量 比如用unsigned int定义一个变量 用这个新的类型定义一个变量 这是我们已有的一个类型 这是我们新定义的一个类型 只有在定义变量的时候 才会为他们分配存储空间 也就是开模具的时候并不会分配任何存储空间 最后就是在函数当中进行使用

### **[02:03]**
结构体的使用 也就是通过结构体的变量名加一个点 来调用不同的成员 最终也就是回到了基本变量的使用方法 我们来看一下 我们从汇编的角度来理解一下 结构体是如何调用每一个成员的 首先在定义的时候会为他们分配存储空间 注意这里有一个存储单元是空的 这是第一个成员 第二个成员 第三个成员他们各自的存储空间 我们使用汇编当中的基地址和偏移地址 来理解它的成员调用 首先基地址就是变量的起始地址是0080H 通过变量名可以获取基地址 那有的同学一想到地址哦 这个变量难道是指针类型呢

### **[03:01]**
我们说变量名它本身就有地址属性 就是通过变量名可以获取它的地址 但是它不是一个指针类型 它还是一个结构体类型 接着我们看一下它的偏移地址 第一个成员它的偏移地址是零 那么基地址加上偏移地址就是0080H 找到这个存储单元 按照on3的CHA类型来解读 第二个成员 他的偏移地址是二 那么基地址加上偏移地址就是0082H 找到这个存储单元 按照short int来解读第三个成员 他的偏移地址是四 那么基地址加上偏移地址是0084H 找到这个存储单元 按照on sa int类型来解读 就是结构体变量 它虽然是一个结构体变量 但是使用的时候还是回到基本变量来使用的

### **[04:01]**
除了在使用的时候可以对它进行赋值 在定义的时候也可以直接赋值 比如对一个普通变量可以直接赋值 对一个结构体变量也可以赋值 也就是一个大括号里面放一些数据 对每一个成员进行赋值 或者是对部分成员进行赋值 没有赋值的成员默认就是零 这里和数组有一点像 数组也是大括号里面对每个成员进行赋值 没有赋值的成员默认也是零 结构体 它还可以选择某一个成员进行赋值 比如选择第二个成员进行复制 第三个成员进行赋值 那么第一个成员没有复制 它的值就是零 刚才我们说只有在定义的时候 会它们分配存储空间 这里有一个存储空间是空的 第一个成员 第二个成员

### **[05:00]**
第三个成员各自的存储空间 如果这些变量是一个普通的变量 他们也是这样分配存储空间的 为什么 我们来看一下 我们讲这里的内存分配 都是以32位单片机为基础的 不讲八位单片机 因为八位单片机 它的内存分配就是从上往下依次排的 所以说这里我们讲32位单片机 假如这是一个三叉二位单片机 它有32根数据线 假如这是一个存储器 同样引出32根数据线 那么这32根数据线 是如何分配到每一个存储单元的 比如我们这里有四组 每一组是八根数据线 总共是32根数据线 它是如何分配到每一个存储单元的 我们化繁为简 大概就是这样依次排下去的

### **[06:01]**
当然不会有这么多根数据线 它们还是共用关系 就是实际上还是只有一组32根数据线 比如我们为一个变量分配四个字节的存储空间 它的地址是00H这是一种对齐的存储方式 什么意思呢 就是要想获取这四个字节的数据 通过32根数据线依次就可以获取 再为一个变量分配四个字节的存储空间 它的地址是04H还是对齐的 还是如果要获取它的数据 也是通过32根数据线一次获取的 注意这里的数据线它们是共用的关系 也就是只有一组三叉根数据线 也就是根据地址不相同 传输的数据不相同 但是线还是32根数据线

### **[07:00]**
我们把它们都画出来 只是为了方便理解 既然这里有对齐的存储方式 那么就有不对齐的存储方式 我们来看一下分配四个字节的存储空间 它的地址是01H就是不对齐的方式 那么要获取这四个字节的数据 它需要读两次 首先从上面32根数据线传输一次数据 再从下面32根数据线再传输一次数据 那么将两次获得的数据进行拼接 就得到我们想要的四个字节的数据 这里是不是比较麻烦 首先要获取两次 然后还要对数据进行拼接 所以说编译器在分配内存的时候 它们都是按照对齐的方式来分配的 比如这里四个字节按照对齐的方式来分配

### **[08:00]**
它的地址就是四的倍数 接着我们再来看一下两个字节的对齐方式 这种是对齐的 那么它会从上面16根线一次传输数据 再分配两个字节的存储空间 这种方式也是对齐的 它的地址是二的倍数 那么它会从下面16根线一次传输16位数据 我们再来看一种没有对齐的方式 它的地址是五不是二的倍数 它会从上面16根线传输一次数据 再从下面是六根线传输一次数据 最后将两次获得的数据进行拼接 就得到我们想要的16位数据

### **[09:00]**
那有的同学会说 不是一次可以传输32位数据吗 就算一次传输32位数据 它中间也要进行一次拼接 对于两个字节的存储空间 它的对齐地址是二的倍数 接着为八个字节分配一个存储空间 这种方式也是对齐的 它只需要读取两次就可以了 如果不是对齐的方式 它可能需要读取三次再进行数据拼接 我们看一下普通变量为他们分配存储空间 一般编译器也是按照对齐的方式来 为它们分配存储空间呢 注意中间有三个空的

### **[10:02]**
如果把它们放到一个结构体当中 那么这个结构体占用的存储单元 那么下面还要占用两个存储单元 才是这个结构体统共占用的存储单元 一共12个存储单元 那为什么呢 为什么明明每个数据都对齐了 下面还要占用两个存储单元呢 这主要是与数组有关系 比如在一个存储器当中为数组分配存储单元 这是第一个元素 第二个元素是不是紧跟在他的后面 第三个元素也是紧跟在后面的 那这与结构体有什么关系呢 如果定义一个结构体数组 它有两个元素 这是第一个元素的存储空间 然后再来看一下第二个元素的存储空间

### **[11:00]**
是紧跟着上面的 我们看它的地址是连续的 但是有没有发现 这个时候第二个元素里面所有的成员 他还是处于一个对齐的状态 如果第一个元素 如果第一个元素下面没有空出两个存储单元 那么第二个元素是不是紧跟着后面 那么第二个元素里面的成员 可能就不是对齐的一个状态了 不对齐 那么单片机在读取数据的时候 是不是可能要读取两次 可能还要对数据进行拼接 这样效率是不是很低 所以说结构体的内存分配 除了每个成员需要对齐之外 好考虑到数组 如果它是一个数组变量 那么在数组当中每一个成员还是要对齐的 所以说结构体的内存对齐 除了每一个成员需要对齐 方便单片机一次读取 不拼接数据 还要CD到数组内部的数据也需要对齐

### **[12:03]**
当然了 我们说这里的内存对齐只是让你了解一下 不会让你真的去算啊 这个结构体到底占用了多少个存储单元 因为我们有size of 可以计算size of后面放一个变量名 或者是类型名就可以计算它占用了多少个字节 3SO后面放一个结构体变量名 或者是结构体类型 就可以计算结构体占用了多少个字节 最后我们来总结一下 首先要对结构体进行开模 也就是确定一个新的类型 然后再用这个类型定义一个变量就可以使用了 只有在定义变量的时候才会分配存储空间 注意这里有一个存储单元是空的 而且这个结构体只分配了八个字节的存储空间 把第二个成员和第三个成员的顺序换一下

### **[13:01]**
那么他申请的存储空间就是12个字节 为什么呢 因为你除了保证每个成员对齐之外 还要保证在数组当中 每一个成员也需要对齐 所以说结构体成员的顺序换一下 它可能申请的存储空间的数量就不一样的

