# 【叶宇单片机】—指针变量的基础知识—【2026-03-04】-中文

## 字幕正文

### **[00:00]**
今天我们来学习一下指针变量的基础知识 指针变量它也是一个变量 它只是比普通变量多了一把钥匙 这把钥匙可以打开存储空间 普通变量指针变量它们都是变量 既然是变量 我们先来看一下变量是怎么使用的 首先是变量的定义 变量一般存储在ram存储空间 它的每一个存储单元是八位的 而且是可读可写的 接着我们来定义一个变量A 它会在ram当中申请一个存储空间 怎么知道占用的是一个存储单元 可以通过size of a来计算 然后再定义一个变量 申请了两个存储空间 可以通过cs o b来计算结果是二 再定义一个变量 这是一个指针变量 占用三个存储单元 可以通过size of pe1来计算结果是三

### **[01:04]**
再定义一个变量 这个变量还是指针变量 又申请了三个存储空间 可以通过size of pr来计算结果还是三 无论是普通变量还是指针变量 它们都有自己的存储空间 可以在存储空间进行写数据和读数据 那么指针变量 它存储空间存储的数据是什么数据呢 它存储的是地址数据 我们来看一下 先来简单的了解一下地址线 假如这是STC89C52 假如它有三根地址线 那么每一根地址线要么输出零 要么输出幺 就会有九种组合 就能识别到九个ram存储空间

### **[02:02]**
但是实际上它有16根地址线 它的地址就是从四个0~4个F 可以识别出这么多存储空间 那么我们的指针就是用来存储这些地址数据 这个时候有一个问题 就是这个地址是16位的 需要几个存储单元来存储 它的地址是不是需要两个就够了 但是为什么刚才我们申请的时候 申请的是三个存储单元呢 因为这16根地址线 除了给ram存储空间分配地址之外 还会给ROM存储空间分配地址 那我们发现这两个存储空间它的地址是相同的 这种结构叫做half结构 也就是程序和数据是分离的 这个时候指针如果指向的是零一地址 那么它识别的存储单元到底是ram当中的 还是ROM当中的 我们没办法确定

### **[03:01]**
所以说指针变量申请了三个字节的存储空间 两个字节用于存储地址 还有一个字节用来识别到底是ram存储空间 还是ROM存储空间 指针变量它存储的就是地址数据只是五幺 比较特别 它多了一个字节用来标识它的存储类型 在其他的单片机当中 一般都不会多出来这一个字节 它一般都是纯纯的嗯 单纯的存储地址数据 比如这是一个STM32F103 它有32根地址线 那么它的地址是从八个0~8个F 可以识别这么多个存储空间 其中一部分分配给ROME 一部分分配给rhyme 那么他们的地址就不会有重复的了 这种结构我们叫做冯诺依曼结构 也就是统一寻址 那么指针用来存储地址数据

### **[04:00]**
那么请问在这个单片机当中 指针变量它占用了几个存储单元 是不是四个存储单元 483 12 然后存储32位地址 所以说指针的本质是存储地址数据 下面是不是应该讲指针怎么使用了 但是先别着急 因为指针变量它就是一个变量 我们先来看一下变量是如何使用的 这里先解决一个问题 就是变量A在定义的时候 它的类型用于申请存储空间 也就申请了一个存储单元 变量B在定义的时候 它的类型也是用来申请存储空间 就是申请了两个存储单元 为什么PE1变量他申请了三个存储空间 pi变量它也申请了三个存储空间 按照我们说的类型不一样 它申请的存储空间不一样

### **[05:01]**
那为什么这里的指针类型不一样 但是他申请的存储单元数量却是一样的呢 还是刚才说的指针变量 这个存储单元它用来存储的是地址数据 地址线是固定的 那么地址的宽度就是固定的 所占用的存储空间就是固定的 所以说指针变量它申请的存储空间都是一样的 主要就是用来存储地址数据 那既然这里的类型不适用于 确定存储空间的数量 那它是干嘛用的呢 我们等一会再说 接着回到我们刚才说的普通变量 指针变量都是变量 来看一下变量是如何使用的 首先是对变量进行写数据 这里定义了四个变量 跟刚才是一样的 那么他们会在ram里申请各自的存储空间 对于普通变量A和B写数据

### **[06:01]**
我们是会的 比如说将十写入A里面 那么A存储空间的数据就是十 既然指针变量也是变量 那可不可以将十写入PE1 将100写入P2 但是这样写会报错 为什么呢 其实原因我们是知道的 想一想就是赋值操作的时候类型要一致 这里的十和100它默认的都是int类型的 所以说将十赋值给AA是on sa cha类型的 所以说会隐式地将实转换成ASSAACHA类型 将100赋值给BB是unsign int类型的 所以说也会影式的 将100转换成ASSIGINT类型的 但是在指针里你将十复制给P1

### **[07:01]**
它不会的隐式转换 它会报错 因为指针变量它存储的是地址数据 所以说存储地址数据的时候 我们要谨慎 怎么说呢 他就是犹豫报错 就是犹豫不隐式初始化 他让你自己来看一看 所以说我们写的时候 要将时通过强制类型转换转换成指针类型 那么与P1的类型就一致了 就可以直接赋值了 这里100也是也强制转换成on sa int指针类型 那么它与pr的类型就一致了 就可以直接赋值了 这个时候编译就不会出错了 所以普通变量在赋值的时候可以省掉 它会隐式地进行类型转换 但是指针在进行赋值的时候 你要保持类型一致 不然的话它就会报错 接着我们再来看一下

### **[08:00]**
对变量进行读 普通变量的话 读数据就将A放在赋值符号的右边 那么B存储空间存储的就是十 接着我想将P1赋值给P2 可以吗 也是不可以直接复制的 因为它们的类型不一致 这个时候通过类型强制转换 将P1转换成assummer int类型的指针 就可以复制给P2了 那么P2存储的就是十 从刚才对变量读写 我们发现指针变量也是变量 普通变量它有自己的存储空间 给这个存储空间起一个名字叫做A 然后按照on sa char类型来解读 这个存储空间的数据定义 一个指针变量也会分配三个存储空间 这个存储空间的名字叫做P1 也就是按照on signal char

### **[09:00]**
指针类型来解读这个存储空间的数据 无论是普通变量还是指针变量 它们都有自己的存储空间 可以对这个存储空间进行读进行写 到现在我们也没有发现指针变量有什么特别的 那么我们就来看一下指针的特别之处 首先还是定义这四个变量存储在ram当中 现在我们给每一个存储单元贴上门牌号 然后给这四个变量分配存储空间 之前我们是这样写数据到这个存储空间当中的 但是一般我们不这么使用 特别是指针 我们一般不是直接把一个地址 数据这样直接的存进去 因为C语言很少有直接跟地址打交道的 所以说一般我们是取变量的地址 比如这里取A的地址赋值给P1取A的地址

### **[10:01]**
这个整体它是on summer char指针类型的 它和PE的类型是一致的 所以说可以直接赋值A的地址是多少 A的地址是0080 所以说P1存储的是0080 我们也可以取B的地址放到pi当中 取B的地址整体是ASSAINT指针类型的 pr也是ASSAINT指针类型的 那么可以直接的赋值B的地址是0081H 那么P2存储的就是0081H 注意这里P1指向的是0080H地址 注意这里它仅仅指向的是这个地址 pi指向的是0081地址 注意它们仅仅保存的是地址 指向的是地址 好像没有什么用 我们指向这个地址主要是干嘛用呢

### **[11:00]**
主要是想使用这个存储空间的数据 对不对 这个时候你给PE11把钥匙 也就是P1前面带一个新号 那么给P11把钥匙就可以打开这个存储空间 打开这个存储空间之后 打开这个存储空间之后 按照什么类型来解读这个存储空间的数据呢 按照on sara char类型 来解读这个存储空间的数据 这个时候的星P1等效于A 也就是可以放在赋值符号的左边 也可以放在赋值符号的右边 就和A的使用是一样的了 同样给pi1把钥匙 也就是pi前面加一个小星星 就可以打开这个存储空间 打开这个存储空间之后 按照什么类型来解读这个存储空间呢 按照啊sa int类型来解读这个空间

### **[12:00]**
那么新PI就等效于B也就是新P2 它可以像B1样使用 主要是可以放在赋值符号的左边 也可以放在赋值符号的右边 也就是说可以通过新pi来读取 这个存储空间的数据 也可以也可以写入这个空间的数据 这种指针的使用方法 专业来讲叫做解引用 解引用的目的就是回到基本类型来使用 我们现在想一想指针变量的特别之处是什么 也就是指针变量的另一种使用方法是什么 是不是它多了一个新号 这个星号就相当于是一把钥匙 可以打开这个存储空间 这就是指针比较灵活的地方 也就是指针它有自己的存储单元 存储单元可以存储任何地址 然后通过新号可以打开这个地址的存储空间 我们再来整体的看一下

### **[13:00]**
定义一个变量A会申请一个存储空间 存储空间的名字叫做A按照usa char类型来解读 这个存储空间可以通过A来读写 定义一个指针变量 这个空间的名字叫做PE1 按照on summer char指针类型来解读 也就通过PE1可以读写这个存储空间 如果给P前面加上一个新号 也就相当于给P1把钥匙 它就可以打开这个存储空间 也就打开这个地址指向的存储空间 那么如何解读这个存储空间呢 按照on satcha类型来解读 它不是按照asser int或者是double类型来解读 它是按照啊SANACHA类型来解读 通过新P1这个整体可以读写这个存储空间 也就是回到了基本变量类型来使用

### **[14:01]**
之前我们留了一个问题 就是这里的指针类型到底是干嘛用的呢 这个时候我们应该有一点理解了吧 假如我们把这里的on center char类型的指针 变成usa int类型的指针 那么P1在使用的时候 也是按照on sigint类型的指针来使用的 那么对PE1进行解引用 也是按照unser int类型来解引用的 也就是打开这个存储空间 把这个存储空间按照啊SUMINT类型来解读啊 sa int类型占用的是两个存储空间 那么这里就将两个连续的存储空间 按照unsign int类型来解读 这个时候你应该明白指针类型的意义了

### **[15:01]**
接着我们看一下类型的变化 定义一个指针类型P1 那么P1它是一个指针类型的截引用之后 它是on satcha类型的 定义一个变量A取A的地址 这个整体它是on summer charge指针类型的 接着对它进行解引用 那么就是on sa chara类型 也就相当于A是on sa chara类型 这里的语符号是取地址运算符 就是你如果想获得某个变量的地址 那么给他前面加一个语符号 就取这个变量的地址 这里的新符号在定义的时候 表示它是一个指针类型 星符号 在使用的时候表示要打开这个地址的存储单元 并且使用这个存储单元指针的使用 我们了解差不多了 再补充一些细节

### **[16:01]**
比如再回到变量的身份 然后看一看加减乘除运算 首先说一下指针只有加减运算 没有乘除运算 为什么呢 我们来看一下 定义一个变量A定义一个指针变量 取A的地址赋值给P1 这是A的存储空间 这是P1的存储空间 它存储的是A的地址 A的地址是0085 那么P1就指向0085这个地址 注意它仅仅指向的是一个地址 如果P1-1就指向上一个地址 减一就指向上一个地址 减一还是指向上一个地址回到原来的地方 如果P11指向下一个地址 P11指向下一个地址 这里的加减主要是用于数组 因为数组它是连续申请同一类型的存储空间

### **[17:04]**
那么你减一就指向上一个元素 加一就指向下一个元素 因为我们大概对这一个存储空间有一个了解 如果你用乘除来计算的话 如果你用乘除来计算的话 那乘除的结果就不知道指向哪里了 而且指向那里的数据到底是干嘛用的 也不知道 所以说我们一般只用加减 不用乘除 接着我们来举一个例子 看这里的加一到底是什么 定义一个变量M定义一个变量N 定义一个指针类型的变量P 那么给他们分配各自的存储空间 接着全部的地址赋值给PM的地址是0083 那么P存储的就是0083 P指向的也是0083 接着是P加一赋值给P 那么P现在指向的是00831

### **[18:01]**
是不是0084 如果是0084 那我们来解引用一下 也就是打开这个地址的存储空间 按照assa int类型来解读 那你看一下这个紫色的这个空间 按照assa int类型来解读 它是不是有一点奇怪 因为它一部分是M里面的 一部分是N里面的 它解出来的数据又是on sa int类型的 是不是很奇怪 所以说这样的分析是不对的 我们从头再来 现在P指向的还是0083 那么P加一实际指向的是0085 这个时候我们再进行解引用的话 然后这个存储空间就按照arsa int类型来解读了 也就是原来NN是啊sa int类型 也就是使用N原来的存储空间

### **[19:01]**
这里的加一主要是与它解引用之后的类型有关 如果再加一 然后P指向的就是0087 要跳过一个啊sa int类型的存储空间 这种加一的用法主要还是用于数组当中 因为它加一正好指向的是下一个元素 这里M和N它的存储单元可能不是连续的 我这里只是举一个例子 说明什么是加一下节课再讲数组与指针 这里你只要明白它加一 与它的解引用类型有关系就可以了 再来说一下什么是空指针 也就是没有类型的指针 定义一个变量X 定义一个指针变量P 那么给他们分配的存储空间 将1000赋值给星P 然后取X的地址赋值给P这里我们要注意一下

### **[20:03]**
将1000赋值给新P 这个时候P里面有没有存放地址 他没有明确的存放地址 但是它里面肯定是有数据的一个存储器 一个存储器单元里面要么是零 要么是一 无非是无效数据 无效数据就是我们也不知道它里面是什么数据 这个时候它指向一个无效数据 就是地址 我们也不清楚到底是什么地址 那么对它进行解引用 就是打开这个地址的存储空间 按照assa int类型来解读 然后将1000存到这个存储空间当中 这个时候你会被感觉到一点害怕 就是他把存储单元 不知道什么位置的数据给改成了1000 那么就会产生一些未知的问题 而且发生这种错误 他是不会报警的 因为你操作都是合法的 这种指针我们叫做野指针

### **[21:01]**
就是它指向一个未知的位置 指向一个旷野 这个时候怎么办呢 既然P里面存储的是一个无效数据 那么我们能不能对它进行初始化 我们看一下如何对它进行初始化 这里和刚才都是一样 只是改了两个存储单元的地址 接着我们看一下对P进行初始化 将零复制给P 不可以直接这样复制 要加一个类型的强制转换 那么P里面存储的就是零 P指向的就是零这个存储空间 然后对这个存储空间进行解引用 接着将1000存到这个存储空间当中 这种写法就会报警了 因为这两个存储空间它是不可以用的 那这个时候出现报警 你就会来检查 将1000赋值给新p or p里面存储的地址

### **[22:03]**
不是我想要的一个地址 就是我没给P1个地址 我就开始直接使用了 这样就会报错 P里面的地址是零 它只是为了防止出现野指针的错误 然后初始化的时候初始成零 但是它不是真正的 你想使用的地址不是真正的一个有效地址 其实我们是想这样的 取X的地址复制给P然后将1000存储到新P 那么P保存的是X的地址 星P就是打开这个地址 也就打开X存储单元存储1000 这个时候又会觉得这样初始化有点太长了 每次要写有点太麻烦了 那么就可以使用宏定义 初始化的时候直接使用红名就可以了 那么在编译的时候 它会自动的替换成

### **[23:02]**
这样宏定义还有一个问题 就是它只能针对啊sa int类型的指针 如果换成啊SACHARA类型的指针又不可以用了 类型不一致了 这个时候怎么办 我们可以写一个wide wider类型的指针 也就是空指针 没有类型的指针 也就是万能指针 因为它可以隐式地转换成其他指针类型 比如这里P在初始化之后 就会隐式的转换成unsign int类型的指针 void的星它是一个万能指针 它可以影射的转换成其他类型的指针 首先指针变量是变量吗 它是一个变量 普通变量它有自己的存储空间 空间名字是A 按照assa char类型来解读这个存储空间 指针变量他也有自己的存储空间

### **[24:00]**
存储空间的名字叫做P1 按照ASSACHARA指针类型来解读这个存储空间 也就对存储空间可读可写 但是读写的时候要注意注意什么呢 类型一致 指针变量它除了是变量 它还有另一种使用方法是什么呢 是不是他还有一把钥匙 可以打开这个地址的存储空间 比如这里的P1 给他一把钥匙 它就会打开这个存储空间 然后把这个存储空间 按照啊3ACHA类型来解读 接着看一下指针变量的使用细节 也就是要对它进行一个初始化 避免野指针的出现 接着在定义变量的时候 对指针变量进行初始化 那么它就会隐式的转换成 如果我们在后面使用的时候 没有明确的给指针变量P一一个有效的地址

### **[25:04]**
如果一不小心直接这样使用 将1000复制给星P 那么就会报警 就会提醒我们过来检查好 这节课到此结束 拜拜

