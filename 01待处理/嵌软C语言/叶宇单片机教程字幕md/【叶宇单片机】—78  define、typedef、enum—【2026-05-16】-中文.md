# 【叶宇单片机】—78  define、typedef、enum—【2026-05-16】-中文

## 字幕正文

### **[00:00]**
今天我们来看一下define type define和ENEM 也就是宏定义类型 定义以及枚举类型 虽然都可以实现一键替换 但是萝卜白菜各有所爱 有的人挑食 有的人就不挑食 先来看一下define 它的格式就是给要替换的文本起一个红名 比如将3.1415926 起一个名字叫做pie 比如将这个表达式也起一个名字 实际上它是一个红函数 那么在程序当中 我们只需要使用它的红名就可以了 宏定义它可以简化文本的书写 实现文本一键替换 它最主要的功能就是实现一键替换 一键替换的规则就是无脑替换 就是什么都不要想 也就是照原样直接搬过来就可以了 这里要替换的是文本 也就是无论是数字字符串 表达式还是红函数等等 都可以进行替换 也就是他不挑食 什么都可以

### **[01:00]**
比如定义一个pi 然后我们在后面写程序 写程序忽然想求圆的面积 那么圆的面积就是派I的平方 定义一个变量存储半径 然后再定义一个变量存储面积 那么面积就是派R的平方 这里使用到了一个宏定义 那么在预处理阶段就进行无脑替换 也就是什么都不要想 直接搬过来就可以了 其他部分保持不变 将pi替换成3.14159I6就可以了 这就是define的一键替换 首先它是不挑食的 什么都可以替换 然后在替换的时候 它是无脑替换的 就是什么都不要想 直接搬过来就可以了 接着我们看一下type define的格式 就是为原有的类型起一个别名 比如为assummer int类型起一个别名 这样看起来是不是书写比较简单了 但是它的主要功能还是实现类型的一键替换

### **[02:01]**
注意这里他比较挑食了 他只要类型为什么要对类型实现一键替换呢 我们来看一下 这是C语言的基本类型 按照类型的等级由低到高来划分 首先我们来看一下C语言对这些类型的规定 首先是char类型必须占用一个字节 short类型 int类型 long类型 它们的大小是小于等于的关系 就是没有固定它们的大小 float和double类型大多数是四个字节和八个字节 由于类型的大小没有固定 那么在不同的单片机当中 类型的大小可能是不同的 比如这是八位机 各个类型占用的字节数 在32位单片机当中 int类型占用的是四个字节 那么这里就不一样了 比如我们写一个八位单片机的程序 然后定义了一些变量 这些变量都是unsign int类型的

### **[03:00]**
也就是16位 如果把这一段程序移动到32位单片机上面 那么在32位单片机里面 on sigma int类型是不是32位的 这个时候我们还想保持原来的16位怎么办 是不是要一个一个进行修改 我们再来看一下type define 定义类型实现一键替换 这里为usa int类型起一个别名U16 那么我们在写程序的时候 定义几个变量移动到32位单片机上面 那么ASSAINT类型是不是32位的 就不是16位的 这个时候我们就要进行修改 一键替换就可以了 只需要修改这一个地方就可以了 这里就是type define实现的类型的一键替换 我们再来补充一个点 看一下类型定义的特点 比如定义两个变量用逗号分开 那么这两个变量是什么类型的 A是阿三的int类型的 B也是ASSAINT类型的 就是这两个变量是同一个类型的

### **[04:01]**
那么还有一个比较特别的地方 比如在定义两个变量用逗号分开 那么这里的P1P2是什么类型的 P1是指针类型 P2就是整数类型 那有同学可能觉得有一点奇怪 怎么跟刚才不一样呢 那么这就是一个规则 记住就好 所以我们在定义指针变量的时候 他的星号要放在哪里呢 是放在类型的旁边 还是放在变量名的旁边 一般我们在定义指针变量的时候 它的新号要放在变量名的旁边 这样就能实时提醒我们这样一个规则 就是它只定义了一个指针变量 而不是两个指针变量 不要把这个星号放在类型的旁边 因为我们会误认为这两个变量都是指针变量 他们两个都可以实现一键替换 而且define它是不挑食的 什么都可以替换

### **[05:00]**
对于类型也可以实现一键替换 type define它就比较挑食 它只认类型 比如type define可以为指针类型起一个别名 实现一键替换 define也可以为类型起一个别名 实现一键替换 那么既然都可以实现类型的一键替换 为什么还要有type define呢 我们来看一下 比如这里定义了一个类型 然后我们写程序 写程序 用这个类型定义两个变量 写完程序之后 我们来到预处理阶段 也就是进行无脑替换 这个时候P1是什么类型 P2是什么类型 P1是不是一个指针类型 P2是普通的整数类型 接着我们来看type define 定义一个类型 再用这个类型定义两个变量 那么这里的P1是什么类型 P2是什么类型 P1是一个指针类型 P2也是一个指针类型 虽然都可以实现一键替换 但是type define定义的别名它有类型属性

### **[06:04]**
而define它只是进行一个无脑替换 他什么都不会想 只是照搬过来 虽然都可以实现类型的一键替换 但是我们给类型取别名的时候 还是使用type define 因为这个名称它会有类型属性 Type define 它除了实现类型的一键替换 它还可以简化类型的书写 我们来看一下它是如何简化类型的书写 比如这里定义了一个结构体类型 我们用这新的类型定义两个变量 那它的类型名是不是很长 烦死了 这个时候我们可以使用type define 为这个类型起一个别名 那么这个时候就可以这样写 是不是简化了类型的书写 这里的一大圈我们还可以合并在一起来写 也就是为这个类型起一个别名 首先是type define关键字

### **[07:02]**
然后为什么类型起一个别名呢 为这个类型新的类型起一个别名 就可以用这个别名进行定义了 下面都是一样的 接着我们看一下枚举类型 它主要是与常量有关系 这里列出了一些常量 这里的常量可以直接写在程序当中 也可以用define进行定义 也可以使用枚举常量 但是使用枚举常量的时候 不能出现小数和字符串 枚举常量 这个常量它只是一个整数 然后我们来看一下枚举类型 首先是一些基本的数据类型 然后是我们自己定义的新的类型 比如数组结构体枚举 还有共用体 它们都有一个什么样的特点 用这个类型可以定义变量 定义变量之后 就在存储器当中会为它分配存储空间

### **[08:00]**
它们和define和type define是不一样的 这两个它是不占用存储单元的 我们来看一下枚举的格式 首先看一下结构体类型的格式 因为它们两个是差不多的 首先都是一个关键字 后面起一个名字 接着是定义一些成员 这样就定义了一个新的类型 用这新的类型就可以去定义变量了 枚举类型也是一个关键字 加上枚举名 接着定义一些成员 注意这些成员它并不是用数据类型来定义的 那么这里就形成了一个新的类型 用这新的类型就可以定义一个变量 等一会我们看一下它的成员特点 就是记住它的成员就是一个int类型 也就是一个整数 可以是负整数 零和正整数 举一个例子 先生成一个枚举类型

### **[09:00]**
首先是一个关键字 然后后面是一起一个名字 接着是定义成员 星期一星期二 星期三 星期四 星期五 星期六 星期天的缩写 这个时候就形成了一个新的类型 用这新的类型定义一个变量 就像我们用基本类型定义一个变量是一样的 定义变量之后就会为它分配存储空间 给这个变量赋一个值 那么存储的就是零 为什么是零呢 枚举类型会自动为它的成员分配值 从零开始 然后依次加一往下 我们也可以修改它的值 比如第一个值修改成25 这个时候它的值就会发生变化 第一个值就是25 接着还是加一 那么存储的就是25 中间成员的值 我们也是可以修改的 那么这个时候的值就会发生改变 就是从负的99开始

### **[10:00]**
下面还是加一枚举的成员 它的本质是int类型 但是它只能读 我们也叫做枚举常量 枚举常量除了给枚举变量赋值之外 它还可以像普通常量一样来使用 注意这里有两个概念 一个是枚举常量 一个是枚举变量 都可以用来使用 我们再来看一下define和枚举类型的区别 首先枚举它可以定义一个新的类型 用这新的类型就可以定义一个变量来使用 它还可以使用它的枚举常量 也就是在程序当中我们可以直接使用它的常量 也可以实现常量的一键替换 说到一键替换 那define它是不挑食的 什么都可以替换 常量也是可以替换的 而且他们俩定义的常量是99%通用的 那为什么还会有枚举常量呢 我们来举一个例子 看它们两个区别 用type define为枚举类型起一个别名

### **[11:03]**
在main当中来使用定义一个枚举类型 然后为这个枚举类型赋一个值 接着是switch语句 根据这个值执行不同的分支 这里我们应该能够看懂 就是根据不同的值执行不同的分支 如果都没有的话 就执行default 那么这就是我们一个完整的程序 但是我们还是来看枚举类型 这里定义了几个常量 也可以使用define进行定义 它们的效果都是一样的 但是我们如果多定义了几个模式 是不是这样依次往下排 我们刚开始这样计划很清晰 但是如果中间我们再插入几个模式 想把它放到一起

### **[12:00]**
这个时候它的值如果按顺序应该是三 那么后面的值是不是都要修改 这样很麻烦 那么我们就改成九 这样是没有问题 但是它的顺序就会有一些混乱 比如再插入一个 这个时候它的值我们就定义为十 如果再插入一些 这个顺序是不是就会很乱 我们每次插入的时候还得找哪个数字 用了哪个数字没有用 现在到哪里了 是不是很麻烦 这个时候就体现了枚举类型的优势 就是如果有新添加 它的数值是自动的 依次往下排的 这个时候就不容易出乱 不容易出错 define它是不挑食的 它可以实现常量的一键替换 枚举 也可以使用枚举常量 它们俩是99%通用的 但是如果想使用一组有关联的整数常量 就用枚举

### **[13:00]**
如果是光杆司令 就建议使用define来定义常量 最后我们来总结一下 先看这三个格式 他们三个都可以实现一键替换 但是萝卜白菜各有所爱 比如define 它要替换的是文本 也就是它是不挑食的 什么都可以 它不define 它只要类型呃 枚举常量 它要的是整数常量 最好是一组相关的整数 然后用它定义放在一起好了 这节课到此结束 拜拜

