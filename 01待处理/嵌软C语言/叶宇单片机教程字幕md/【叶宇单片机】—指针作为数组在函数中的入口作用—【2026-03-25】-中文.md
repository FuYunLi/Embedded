# 【叶宇单片机】—指针作为数组在函数中的入口作用—【2026-03-25】-中文

## 字幕正文

### **[00:00]**
今天我们来看一下指针数组函数它们之间的关系 先来看一下函数的一般格式 函数主要是将实现同一功能的语句放在一起 实现功能模块化 如果实现这个功能 需要函数Y的数据就通过参数列表传进来 如果产生新的数据 就通过返回值类型返回上一节课我们说过 在参数列表也可以定义多个指针变量 在函数内对指针变量进行解引用 也就是找到函数外的一个存储空间 对这个存储空间可读可写 如果读出一个数据 就相当于间接地传递一个数据到函数内 如果在函数内对它进行赋值 就相当于间接地传出一个数据 也就是通过指针变量可以实现函数内数据的输入和输出 接着我们看一下数组函数指针它们之间的关系

### **[01:03]**
定义一个全局变量数组这个数组有四个元素 定义一个全局变量 这个变量用来存储这四个数据的平均值 接着定义一个函数 这个函数有一个输入参数 就是一个数组 这个数组同样有四个元素 在函数内定义一个变量 用来存储这四个数据的平均值 接着返回平均值 在main函数里调用这个函数 首先有一个输入是数组名 有一个输出给全局变量 那我们看一下这个数组有四个元素 每个元素都是on3的char类型的这个入口参数 它也有四个元素 每个元素都是on sa char类型的 这样来看它们的类型是不是一致 就可以直接赋值了 就是我们会不会这样想 全局变量它是一个数组 然后将这个数组当中的每一个元素赋值给局部变量

### **[02:04]**
这个数组当中实际上不是这样子的 我们说数组名它的类型是元素类型的指针 那么这里的数组名它的类型就是on satcha类型的指针 这时候我们看入口参数 它又是一个数组 这个时候类型就不匹配了 实际上在编译阶段 它也会退化成一个元素类型的指针 编译之后就是这个样子的 那么它的类型也是一个元素类型的指针 所以说两个类型一致就可以直接赋值 接着我们来看一下这些变量的存储空间分配 然后来了解它的数据传递过程 首先是两个全局变量 两个局部变量在执行main函数之前 要为全局变量分配存储空间 我们将虚线上面大概分为战区 下面是全局数据存储区

### **[03:00]**
为全局变量分配存储空间 接着往下执行程序 遇到函数调用函数调用函数的时候有一个压战过程 压榨过程首先是现场保护 将一些现场数据进行压榨 接着是函数的返回地址压榨 这两个过程我们暂时先忽略 然后是将局部变量压榨 也就是为局部变量分配一些存储空间 现在存储空间分配好了 那么就执行函数 我们想一想这个buffer指针变量存储的是不是数组的地址 因为数组名就代表数组的地址嘛 也就是将数组的地址0030赋值给八负 那么Buff加零是0030是一个地址 buffer加一是0031也是一个地址 Buff加2buff加三都是一个地址值 注意它们仅仅是地址

### **[04:00]**
只有对它们进行解引用 才可以找到这个存储空间 对这个存储空间进行读或者写 接着我们来看一下程序对这四个数进行求平均值 这里的buffer中括号零也就相当于buffer加零进行解引用 buffer中括号一也就相当于Buff加一进行解引用 它们只是两种不同的写法 实际上是一样的 也就是通过前面的写法可以读写这个存储空间的数据 通过后面的写法也可以读写这个存储空间的数据 使用是一样的 只是不同的写法 接着我们来看一下 通过Buff可以找到这四个存储空间 那么通过什么可以找到这个寺呢 这个四是我们大脑中记忆的 是我们手动写进去的 但是我们说函数是实现功能模块化 就是通过函数的输入和输出接口就可以实现功能

### **[05:02]**
不需要在函数内部再进行修修改改 所以说在函数内要修改数组元素的个数是不太好的 那么我们在接口处再定义一个变量 这个变量用来存储函数的元素个数 那么在调用函数的时候 在输入接口要写一个数组元素的个数 因为数组实际上退化成一个指针变量 这个指针变量并不知道这个数组有多少个元素 所以说我们一般在入口处再定义一个变量 用来存储数组元素的个数 那么执行完函数就会返回 返回就涉及到一个出站的过程 就会释放这些存储空间 SP来到栈底 那么这个时候战区就是空的 接着执行下面的函数 注意看一下这里的buffer加零解引用 buffer加一解引用

### **[06:01]**
我们说buffer这个变量已经没有了 已经释放了 那么这样使用就没有什么意义了 所以说不可以这样使用 在函数的入口处定义一个数组 实际上也是退化成一个指针变量 那么我们来看一下指针变量的本质 就是他想获取谁的地址 就可以获取谁的地址 对于这个地址的存储空间如何解读 也是指针变量自己说了算 回到我们今天的主题 在参数列表定义数组实际上也是退化成一个指针变量 那么在函数内部进行解引用 也就找到了函数外的一个存储空间 读取这个存储空间的数据 相当于输入一个数据到函数内部 所以说它是一个入口

### **[07:00]**
如果对这个存储空间进行赋值 就相当于函数内输出一个数据 下一节课我们来看一下它的输出实际上都是一样的 这节课到此结束 拜拜

