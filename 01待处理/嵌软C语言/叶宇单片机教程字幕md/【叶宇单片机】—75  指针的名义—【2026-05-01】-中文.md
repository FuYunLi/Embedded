# 【叶宇单片机】—75  指针的名义—【2026-05-01】-中文

## 字幕正文

### **[00:00]**
今天我们来看一下指针的灵活性 就是他想获取谁的地址 就可以获取谁的地址 想怎么解读就怎么解读 我们来举一个例子 从一个二维表格中提取某一行数据 比如第一行数据 第二行数据 现在我们想提取第三行数据怎么办呢 我们来看一下 首先定义一个二维数组来存储这些数据 然后我们想获取第三行数据 那么再定义一个数组来存这个数据 这个时候来为他们分配一下存储空间 这是二维数组的存储空间 如何获取每一个元素呢 是不是从table00开始 那么它的每个元素是什么类型 是不是on sa char类型 接着我们再为Buff分配存储空间

### **[01:02]**
我们想把第三行数据存储到这个数组当中 但是我们知道数组之间是没办法直接赋值的 这个时候怎么办 我们想一想指针的灵活性 是不是想获取谁的地址 就可以获取到谁的地址 这里我们想获取第三行数据的地址 那么这个数据是什么类型的 是不是on sa char类型的 那么就定义一个on sa char类型的指针 接着为他分配一个存储空间 然后在main函数当中来获取第三行的地址 也就是取table i0的地址给P 那么P存储的就是0084对P进行解引用 也就是找到这个存储单元 按照on satcha类型来解引用 那么星P就等效于P加零解引用 P加一解引用就会找到下一个存储单元 这里的P加零解引用

### **[02:01]**
还有一种写法就是P0P加一节应用 就是P1 也就是都可以实现对这个存储单元的读和写 接着我们看一下缓冲区Buff 那么找到第一个元素就是Buff0 找到第二个元素是buff1 这个时候他们两个之间就可以直接复制了 那么我们写一个for循环语句 它只需要循环两次就可以了 将pi赋值给Buff i 那么这个时候就提取到了第三行数据 这里我们就感受到了指针的灵活性 它想获取谁的地址 就可以获取谁的地址 想怎么解读就可以怎么解读好 这节课到此结束

