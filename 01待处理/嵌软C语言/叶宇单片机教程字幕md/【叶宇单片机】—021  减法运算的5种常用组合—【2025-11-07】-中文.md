# 【叶宇单片机】—021  减法运算的5种常用组合—【2025-11-07】-中文

## 字幕正文

### **[00:01]**
大家好 今天学习的是第21节 减法运算的五种常用组合 也就是学习一下减法运算 这个是减法运算的语法格式 减法运算是从左往右依次运算 然后得到一个运算结果 把运算结果复制给保存变量 保存变量提供一个存储空间 就把这个结果放到这个存储空间 我们就来看一下减法运算的五种常用组合 第一种减速是常量 定义一个变量A申请一个字节的空间 我们来执行这条语句 15-3=12 我们将12这个值赋值给A 也就是A提供一个存储空间 将12存到这个存储空间当中 执行完这条语句之后 A空间的值就是12 第二种减速 一个是常量 一个是变量 定义一个变量B申请一个字节的空间

### **[01:03]**
定义一个变量X申请一个字节的空间 初始值是15 我们来执行这条语句 X减去十赋值给B先进行减法运算 X减去10X提供一个减数是15 那么15-10就是五 将五赋值给BB提供的就是一个存储空间 将五存到这个存储空间当中 所以说执行完这条语句之后 B空间里的值就是五 我们来说一下什么是变量 什么是常量 这里用类型定义的be由类型DE的X就是变量 它们最大的特点就是 可以在ram空间当中申请自己的房间 并且里面的数据可以随时改变 所以说叫做变量 什么是常量 这里的十就是一个常量

### **[02:00]**
这个十也是C语言这条语句的一部分 当把这条语句翻译成一个机器指令之后 我们暂时先不讨论机器指令是什么 反正就是一些0101组合的数据 翻译成机器指令之后 这里的十是没有分开处理的 它也是机器指令的一部分 我们要知道机器指令它是存储在ROM当中的 所以说常量它也就是存储在ROM当中的 是固定不变的 除非重新下载程序 第三种减速都是变量 定义一个变量C申请一个字节的空间 定义一个变量X申请一个字节的空间 初始值是15 定义一个变量Y申请一个字节的空间 初始值是六 我们来执行这条语句 X减去Y赋值给C先做减法运算 X减去YX和Y都要提供一个减数 X提供的减数是15

### **[03:01]**
Y提供的减数是六 那么15-6=9 我们将九赋值给cc提供的是一个存储空间 就将九存到这个空间当中 所以说执行完这条语句之后 C空间里的值就是九 第四种减速 一个是保存变量本身 一个是常量 我们定义一个变量D初始值是18 来执行这条语句 D减去二赋值给D 我们先来执行减法运算 D减去二 那么D提供的减速是18 18-2=16 将16复制给dd提供一个存储空间 将16放到这个存储空间当中 所以说执行完这条语句之后 D原来的数18就会被覆盖掉 他的薪值是16 刚才我们执行完这条语句 D的值是16 现在我们来执行这条语句

### **[04:00]**
D减去七赋值给DD的新值是16 16-7=9 我们将九赋值给D 那么执行完这条语句之后 D里面的值16就会被覆盖掉 他的新值就是九 第五种减速 一个是保存变量本身 一个是变量 第一个变量E初始值是28 第一个变量X初始值是15 定义一个变量Y初始值是六 我们来执行这条语句 E减去X赋值给E先执行减法运算 E减去XE是28 X是15 那么E减去X等于13 将13赋值给EE原来的28就会被覆盖掉 它的新值是13 刚才我们执行这条语句 得到E减去X赋值给EE的新值是13

### **[05:01]**
我们再来执行下一条语句 E减Y赋值给EE的值是13 Y的值是六 那么E减Y就等于七 将七赋值给E 那么E原来空间里的13就会被覆盖掉 他的心值是七 我们再来编写一个程序 验证上面的五个减法例子 还是在我们的C语言模板上进行练习 定义一些变量 然后是执行五个减法的例子 最后将执行的结果输出到电脑端进行查看 我们现在把变量的定义放在左边的窗口 是为了方便查看 我们来看第一个例子 减速都是常数 15-3=12 将12复制给AA的值就是12 第二个例子减速一个是变量 一个是常数 X减去十赋值给BX的值是15

### **[06:03]**
那么15-10就等于五 将五赋值给BB的值就是五 第三个例子减速都是变量 X减去Y赋值给CX的值是15 Y的值是六 那么X减去Y等于九 将九赋值给CC的值就是九 第四个例子减速一个是保存变量本身 一个是常量 D减去R赋值给DD是18 那么D减去二就是16 16赋值给DD的值是16 然后再执行下一条语句 D减去七赋值给D 现在D里面的值是16 16-7=9 将九赋值给D 那么现在的D的值就是九 第五个例子减速一个是保存变量本身 一个是变量 我们来看第一条语句

### **[07:00]**
E减去X赋值给EE的值是28 X的值是15 那么E减去X就等于13 将13赋值给E 此时E里的值是13 执行第二条语句 E减去Y赋值给EE的值现在是13 Y的值是六 那么13-6就是七 将七赋值给E 此时E的值是七 然后将ABCDE这几个值输出 方便我们查看A的值是12 B的值是五 C的值是九 D的值是九 E的值是七 我们要注意E原来的值是13 已经被覆盖掉了 它的新值是七 现在我们把程序恢复到正常状态 然后点击save 点击rebuild 零警告零错误 我们把它下载到单片机来看一下 现在我们来下载程序 注意芯片型号 扫描串口最低波特率2400 最高波特率9600 打开这节课的程序文件

### **[08:02]**
点击下载 第一个数是12 第二个数是五 第三个数是九 第四个数是九 第五个数是七 通过验证 说明 单片机的输出结果和我们分析的是一样的好 这节课到此结束 拜拜

