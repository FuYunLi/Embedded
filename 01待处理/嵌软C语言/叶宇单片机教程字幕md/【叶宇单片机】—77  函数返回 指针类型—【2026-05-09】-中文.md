# 【叶宇单片机】—77  函数返回 指针类型—【2026-05-09】-中文

## 字幕正文

### **[00:00]**
今天我们来看一下函数返回指针类型 指针类型它存储的就是一个地址 也就是缓回一个地址 但是也不是所有的地址都可以返回的 绝对不可以返回普通局部变量的地址 先来回忆一下函数的格式 函数就是将实现相同功能的语句放在一起 如果实现这个功能 需要函数外的数据就通过参数列表传进来 如果产生新的数据 就通过返回值类型返回 之前我们说过 在参数列表处可以定义指针变量 那么在函数内进行解引用 就会间接的找到外面一个存储单元 对这个存储单元进行读和写 就间接地实现了函数内数据的输入和输出 如果把返回值类型定义成一个指针类型 指针类型存储的就是一个地址 也就是返回一个地址

### **[01:00]**
我们来看一下哪些地址可以返回 哪些地址不可以返回 首先定义一个函数用来获取字符串的地址 这个函数没有输入 但是有一个输出是指针类型 也就是输出一个地址 定义一个数组用来存储字符串 然后返回这个数组的地址 接着在main函数当中来调用 定义一个指针变量 将函数的返回地址给这个指针变量 现在我们得到了这个字符串的地址 那是不是可以输出这个字符串呢 我们来看一下这个程序的执行过程 首先它有两个局部变量 main函数是程序的入口 但是在执行程序之前 要先为变量分配一下存储空间 假如上面是战区 下面是全局数据存储区 首先这里没有全局变量 那么就开始执行main函数 main也是一个函数

### **[02:00]**
调用函数就有一个压榨的过程 首先是现场保护的数据进行压榨 函数的返回地址进行压榨 但是这两个过程我们暂时先忽略 接着为局部变量分配临时的数据存储区 然后执行程序遇到函数调用函数 调用子函数也有一个压榨的过程 首先是现场保护数据 压榨函数的返回地址 压榨这两个过程我们暂时先忽略 接着是为局部变量分配临时的数据存储区 局部变量存储的是hello world 那么最后为什么还有一个反斜杠零字符呢 因为他在处理字符的时候 他一直往后处理 往后处理 那么到哪一个字符就结束了呢 这里是一个结束标志 是一个结束符 就是它在存储数据的时候自动加进去的存储器

### **[03:00]**
旁边的SP寄存器 用来记录这个战区使用到哪里了 接着我们来看一下这个子程序 这个子程序里面只有一条有效的语句 就是返回数组的地址 返回这个地址给指针变量 P这个数组的地址是多少 是不是0072 那么P存储的就是0072 那么执行完函数之后就涉及到一个出站的过程 那么就会释放这个变量的存储空间 SP指针向下 也就是回到了没有调用这个子函数之前的位置 这个时候子函数返回 那么数组的存储空间是不是已经被销毁了 但是我们P里面还存储了他的地址0072H 那么在使用这个地址也就没有什么意义了 我们来看一下 接着往下执行程序 我们看一下下面的程序是什么 这是子函数在main里调用这个子函数

### **[04:03]**
这个时候我们获取了字符串的地址 我们想打印这个字符串 我们想一下 我们还能打印出hello world这几个字吗 子程序执行完了之后 hello world这个存储空间是不是已经被销毁了 但是我们保留了他的地址 接着往下执行程序 想通过这个地址来打印字符串 先调用这个子函数 调用函数的时候总归会有一个压榨过程 那么存储的是什么 我们暂时就不深究 这个打印子函数的具体内容是什么 我们也不知道 但是肯定是要使用指针变量P的 想打印这个字符串 Hello world 但是实际上hello world的存储空间已经被销毁 接着又分配给别人来使用呢 那么打印出来的肯定是一些乱码 那么执行完这个程序之后又会返回返回 就涉及到一个出站的过程

### **[05:02]**
这个时候SP又回到了调用子函数之前的位置 接着再往下执行程序 我们来总结一下 就是函数返回指针类型 但是绝对不可以返回普通局部变量的地址 因为函数结束之后 它的局部变量就会被销毁 指针就会变成野指针 如果我们就想返回这个局部变量的地址 怎么办呢 还是这个函数 还是这个局部变量 我们在它的前面加上一个static 那么它的存储位置就会发生变化 它就不是存储在战区了 它是存在全局数据区 它是一个静态变量 接着在main里来调用 还是一样的 我们把这个字符串的地址给P 接着想打印这个字符串 这个时候可以完整的打印出hello world了吗 这个时候就可以了 我们来看一下它的执行过程

### **[06:02]**
首先这里有一个静态局部变量 还有一个局部变量 在执行main函数之前 会变量分配存储空间 这里有一个静态局部变量 就会分配在全局数据区 存储的是hello world 那么来调用main函数也有一个压榨过程 会为它的局部变量分配临时的数据存储区 接着往下执行程序 遇到函数调用函数 调用子函数也有一个压榨过程 这里就没有局部变量了 子函数里面只有一条有效语句 就是返回数组的地址 返回数组的地址给指针变量P 这个时候数组的地址是0030H 那么P存储的就是0030函数 执行完之后就会返回返回 就涉及到一个出站的过程

### **[07:00]**
这里也没有局部变量需要出站呢 接着执行下面的函数 就是打印这一串字符串就可以正确打印了 因为我们存储的地址是一个有效的地址 而不是一个已经被销毁的变量的地址 我们来简单的总结一下 首先是绝对不可以返回普通局部变量的地址 因为函数结束 局部变量也就会被销毁 可以返回加了STACTIC修饰的局部变量的地址 因为它的存储空间发生了变化 就算函数结束 这个变量也不会被销毁 我们再来举一个比较常见的例子 就是返回一个输入的地址 定义一个全局变量 再定义一个函数 用来获取数组的地址 这个函数有一个输入 有一个输出函数内对输入的字符串进行处理 就是拼接字符 拼接之后这个字符串就变成了hello world

### **[08:03]**
你好零一 如果具体不懂的话也没有关系 我们说在参数列表 也就是输入接口 定义一个指针变量 在函数内进行解引用 就会找到外面的存储空间 对外面存储空间进行修改 这个时候他就找到外面的存储空间 把外面的存储空间数据拼接成hello world 你好零一 这一句如果不懂的话也没有关系 只是嗯写一个实现功能 在这里就是想使函数有用一点 接着返回这个指针变量在main函数当中来调用 同样定义一个指针变量 调用这个函数 将函数的返回地址给P它要有一个输入 输入的是一个数组的地址 这个时候我们得到了字符串的地址 还想打印这个字符串 可以正确打印吗 是可以的 我们来看一下

### **[09:01]**
首先这里有一个全局变量 一个局部变量 还有一个局部变量 在执行main函数之前 会为变量分配存储空间 首先这里有一个全局变量 Hello world 接着执行main函数 执行函数就有一个压榨过程 为局部变量分配临时的数据存储区 接着遇到函数调用函数 调用子函数就有一个压榨过程 会为局部变量分配临时的数据存储区 调用这个子函数 将Buff的地址传进来 那八伏的地址是0030H 那么这个局部变量存储的就是0030H 存储空间分配好了之后 接着执行此函数 子函数里面的字符拼接 主要是使这个函数看起来更有用一点

### **[10:00]**
我们暂时先不管它 主要看一下return这条语句就是返回一个指针变量 也就是返回一个地址给P 那么P存储的就是0030H 我们看一下这个数组的地址 他先是传入到函数的内部 又从函数内传出 它始终是一个有效的地址 执行完子函数之后就会返回 就涉及到一个出站的过程 那么就会释放它的局部变量的存储空间 也就是执行完函数 它已经把数据返回出来了 接着我们执行下面的函数 就是想打印这个字符串 这个时候就可以正确的打印字符串了 因为字符串的地址是一个有效的地址 最后我们来总结一下 首先是不可以返回普通局部变量的地址 然后可以返回加勒 STACTIC修饰的局部变量的地址

### **[11:00]**
因为它的存储位置发生了变化 也可以返回传入的地址 其实后面两条都不用记 只需要记住这一条 不可以返回普通局部变量的地址 因为普通局部变量的存储空间 它就是一个公共区域 它不是独属于这一个变量的 不是它专属的 也就是调用函数的时候给它分配一个存储空间 执行完函数就会收回这个存储空间 接着就会把这个存储空间分配给别人来使用 就好像是宾馆 然后你今天住了 他分配给你 你走了之后 他又会把这个房间分配给别人 所以说它是一个公共区域 好这节课到此结束 拜拜

