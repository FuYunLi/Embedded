# 【叶宇单片机】—指针与数组—【2026-03-10】-中文

## 字幕正文

### **[00:00]**
今天我们来看一下指针与数组 上节课我们学习了指针 我们说指针它有一个特殊的技能 就是他有一把钥匙 这把钥匙可以打开存储空间 我们先来简单的回忆一下上节课的内容 首先定义一个变量 申请一个存储空间 可以通过A对这个存储空间进行读写 定义一个指针变量 所有的指针变量固定分配三个字节的存储单元 可以通过P对这个存储单元进行读写 那么给P1把钥匙 它也可以打开一个存储空间 也就是通过星P可以对这个单元进行读写 那我们想一想 P和星P之间有什么关系 新P打开房间的门牌号是不是P给他的 那么打开这个房间取什么东西呢 是不是也是P给他的

### **[01:01]**
所以说新P它是完全依赖于P的 只有P需要跟外界打交道 也就是说只有P它需要跟外界打交道 他获取的是地址是谁的地址呢 暂时我们一般获取的都是变量的地址 但是P对地址有一个要求 就是类型一致 那么变量的地址是什么类型呢 我们来看一下 首先是变量的定义 取变量的地址 最后看一下地址的类型 定义一个变量A取它的地址 它的类型是在原来类型的基础上加一个新号 定义一个变量B取它的地址 它的类型还是在原来的类型基础上加一个星号 同样定义一个变量C取它的地址 它的类型还是加一个星号

### **[02:01]**
那我们观察一下地址的类型 是不是在原来类型的基础上加一个星号 就是地址的类型 接着对它们进行解引用 解引用的类型主要与指针类型有关 也就是指针类型 去掉星号就是解引用之后的类型 那么它们结应用之后也就分别等效于A 等效于B等效于C 再来看一下数组地址的类型 定义一个数组取他的地址 根据我们前面的分析 地址类型 是不是在原来类型的基础上加一个星号 就是地址类型 这是没有问题的 但是这里有一个优先级问题 就是中括号的优先级它是最高的 那么在分析这个类型的时候

### **[03:00]**
他会把它分析成一个数组 这个时候怎么办呢 我们可以在星号两边加一个括号 那么圆括号和中括号 它们的优先级都是最高的 这个时候就会从左往右运算 从左往右分析 那么第一个分析就是指针 认为它是一个指针类型 然后这个指针指向的是一个数组 接着看一下数组的第一个元素取它的地址 还是在原来类型的基础上加一个星号 数组的第二个元素取他的地址 还是在原来类型的基础上加一个星号 第三个元素也是一样的 那我们再观察一下这里的地址类型 也就是在原来类型的基础上加一个星号 就得到了地址类型 如果对它们进行解引用

### **[04:00]**
按照我们前面说的 去掉星号就是解引用之后的类型 这里基本上都是对的 但是有一个特别 这个我们等会再说 我们先来简单的总结一下 取地址的类型就是加上星号 解引用的类型就是去掉星号 但是有一个特殊的解引用 我们来看一下 定义一个变量A取地址加一个星号截引用 去掉一个星号 也就是相当于A这两个是一样的 定义一个数组取地址加上一个星号解引用 按理来说是去掉一个星号 但是去掉一个星号 它是一个数组 这个数组有三个元素 你到底使用的是哪个元素呢 这里是不是没有什么实际的意义 所以说它的类型是元素类型的指针

### **[05:03]**
那么数组名的类型就是元素类型的指针 它们两个是等效的 这是一种分析方法 我们再来看第二种分析方法 变量A它是on sa int类型的数组名 我们一般认为它是一个数组类型的 同样这里的数组类型没有什么实际的意义 所以说数组名的类型是元素类型的指针 大多数的场合下数组名都是这样处理的 但是有一个特别就是size of后面加一个数组名 这个时候它计算的又是整个数组 它占用了多少个存储单元 这个数组它有三个元素 每一个元素都是on sa int类型的 所以说size of的结果是六 如果size of 如果size of把它当做一个指针的话 我们知道所有的指针都是占用三个存储单元

### **[06:02]**
这里的结果就应该是三 但是实际上不是这样子的 所以说这里要注意一下 取地址的类型是加星号 解引用的类型是去掉星号 如果去掉星号之后是一个数组 那么就改为数组元素类型的指针 这里就是我们需要遵守的一个规则 然后记住它就可以了 还有一个需要注意的地方就是在函数里 这里定义一个数组 声明一个函数 这个数组的类型和函数的入口参数 类型是一样的 如果在main里调用这个函数 我们知道array类型是ASSAINT类型的指针 但是入口参数它是一个数组 两个类型不一致 怎么可以直接使用呢

### **[07:00]**
实际上编译之后 这个类型也是一个UNSIGINT类型的指针 也就是这个样子的 这个数组在编译之后 会变成数组元素类型的指针 然后在function这个函数里 都是按照这个指针来使用的 我们要注意一下 然后是取地址的类型 是指针类型 定义一个指针变量 它也是一个指针类型 那么这两个指针类型有什么区别呢 我们来看一下 我们先来看一个常量赋值 他们申请各自的存储空间 我们可以用常量对它们进行赋值 那么可不可以反过来将A赋值给十 或者是将B赋值给E百 或者是将P1赋值给十 将pr复制给100

### **[08:02]**
这样可以吗 这样是不可以的 不可以对数字量进行赋值 接着我们来看一下数字量地址 定义一个数组 为它分配存储空间 定义一个指针 我们说数组名的类型是unsign int类型的指针 指向的是第一个元素 P的类型也是on sound int类型的指针 那么它们两个都是指针有什么区别呢 是不是P它有自己的存储空间 但是array这个指针它没有自己的存储空间 那么我们将array复制给P array指向的是0081 P存储的就是0081 那么P指向的也是0081 这条语句经过编译之后 就是一个数字量 0081赋值给P 那可不可以反过来呢

### **[09:00]**
就是将P赋值给array 编译之后 就是将P赋值给一个数字量 这样是不可以的 我们刚才说数组名是元素类型的指针 完整一点来说就是它实际指向的是第一个元素 接着我们再来看一个数字量地址 就是取地址 它实际上得到的都是一个数字量 定义一个变量A定义一个指针变量取A的地址 它是unsign int类型的指针 指向的是0082 P也是on send int类型的指针 这两个指针有什么区别呢 还是跟刚才一样 P它有自己的存储空间 ha地址 它虽然是指针类型 但是它没有自己的存储空间 取A地址赋值给PP存储的就是0082

### **[10:01]**
指向的也是0082 编译之后就是将一个数字量0082赋值给P 那么可不可以反过来 这样编译之后就将P赋值给一个数字量 我们说不可以对数字量进行赋值 如果实在想在这个0082存储空间 存储一些数据 可以对它进行解引用 就是给他一把钥匙 打开这个存储空间 截引用之后是什么类型 是不是去掉星 就是ASSIGINT类型 那么解引用之后是unsign int类型 但是P是on sa int类型的指针 两个类型不一致 就不可以这样直接复制 但是可以给它赋值一个十二十三十 这样的数字量是可以的 再来看一下地址 自加上节课我们学习过指针的加减法

### **[11:03]**
定义一个数组 定义一个指针变量 数组名指向的是第一个元素 这个数组名的类型就是unsigned int类型的指针 这与P的类型是一致的 所以说可以直接赋值 那么P存储的就是0081 指向的就是0081 这个时候P加零复制给P 那么P的值不变 将八赋值给新P 也就是找到0081这个存储空间存一个八 接着P加一赋值给P 那么P指向的就是0083 为什么指向的是0083 可以看一下上节课我们讲过 那么P里面存储的就是0083 接着将九赋值给星P 找到0083这个存储空间存一个九

### **[12:00]**
接着P加一再复制给P 那么P指向的是0085 P存储的也就是0085 将十赋值给新P 那么找到0085这个存储空间存一个十 如果这个时候P再加一指向的就是0087 那么也可以找到0087 这个存储空间进行赋值 但这个时候数组越界了 我们要注意一下这里的地址 自家它改变了P存储空间的数据 因为我们对P进行了重新赋值 这里的P加一可以写成P加加 还是使用这两个变量 我们来看一下地址偏移 首先将酒进行赋值 P加一指向的是0083 对这个存储空间进行解引用 也就是打开这个存储空间存一个九 我们注意一下这里的P它还是0081

### **[13:01]**
没有改变 因为我们没有对它进行赋值 接着将十进行赋值 P加二指向的是0085 因为P的值是0081 加一是0083 再加一就是0085 那么对这个存储空间进行解引用 存一个十 注意这个时候P的值还是0081 因为我们没有对它进行重新赋值 所以说地址偏移它没有改变P存储空间的数据 所谓地址偏移 也就是在一个不变的地址上 比如说这里0081 在0081的基础上进行往下移动 或者是往上移动 就是地址偏移 还是用这个例子 我们来看一种等效的写法 这里的P它是一个指针类型 array也是一个指针类型

### **[14:01]**
这两个指针的区别就是有没有自己的存储空间 这个影响到负值 但是不影响到数学计算 array指向的也是0081 那么这里P加一减引用也可以写成array 加一减引用 P加二节引用也可以写成array加二节引用 也就是对数组的两个元素进行赋值 同样可以写成数组形式 也就是加上中括号 同样指针也可以写成数组的形式 这就是地址偏移的等效写法 只不过中括号这种写法 是汇编语言当中会使用的 A圆括号前面加一个星号进行解引用 这是C语言当中的这两种写法 它们的效果都是一样的 都是在一个基础的地址上进行偏移

### **[15:02]**
来找到一个存储空间 最后我们来举一个例子 定义数组一 它有三个元素定义数组二 它也有三个元素定义数组三 定义一个数组缓冲区 再定义一个变量 初值是二 定义一个R类型的变量 这里的GU8表示它是一个全局变量 U8类型的 接着我们来看一个switch语句 假如这个switch语句在一个函数当中 如果变量是一 那么就会执行case1 也就是将数组一复制到缓冲区当中 然后break跳出switch语句 如果变量是二 就执行下面的语句 也就是将数组二复制到缓冲区

### **[16:02]**
接着break跳出switch语句 如果变量是三 就将数组三复制到缓冲区 然后break跳出switch语句 这里我们有没有发现for语句他重复了三次 有没有什么办法能够改善一下呢 我们可以定义一个指针变量 是on center char类型的指针变量 注意一下这里的数组名它是一个什么样的类型 是不是也是on summer charge指针类型 那么数组名和指针的类型是一致的 接着我们来看一下switch语句 第一个for循环是将数组一复制到缓冲区 我们可以将数组一的名字复制给P 第二个循环是将数组I复制到缓冲区 我们可以将数组I的名字给P

### **[17:02]**
第三个询盘是数组三复制到缓冲区 我们可以将数组三的名字复制给P 那么P就可以得到数组的地址 这个时候再写一个for循环 将P指向的数组 它们里面的数据在复制到缓冲区当中 只需要一个for循环就可以了好了 这节课到此结束 拜拜

