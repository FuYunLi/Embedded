# 【叶宇单片机】—026  连乘、自乘、自乘简写、溢出—【2025-11-20】-中文

## 字幕正文

### **[00:02]**
大家好 今天学习的是第26节 连乘自成自成简写乘法 溢出乘法的计算过程还是用到算数转换规则 所以学习的内容感觉差不多 但是最后补充了一个知识点 就是赋值 希望大家能听到 最后我们来回忆一下算术转换规则 也就是四个字 提升对齐 提升与对齐的目的 就是将两个不同类型的操作数转换为同一类型 还有就是在提升与对齐过程当中 如果遇到正数与零 前面补零 负数前面补压 这些操作都是在写完程序之后 在编译阶段演示完成的 这是我们乘法的语法格式 从左到右依次计算 先计算前两个数 还是先对类型提升与对齐 然后计算得到一个结果 这个结果有大小有类型

### **[01:02]**
再将这个结果作为一个乘数 与后面的乘数进行相乘 还是先要对类型进行提升与对齐 得到一个结果 这个结果有大小有类型 将这个结果再作为一个乘数 与后面的乘数进行相乘 还是先提升与对齐得到一个结果 这个结果有大小有类型 这就是连乘的过程 是两个数 两个数进行操作的 对于加减乘除运算都是两个操作数 因为硬件要求八位加八位 16位加16位 32位乘以32位 也就是硬件要求两个操作数的类型要一致 所以说在计算之前要进行类型的提升与对齐 对于加减乘除运算的连加连减 连乘也是换成两个数 两个数计算的 我们先来学习连乘

### **[02:01]**
之前我们学习过两个数的相乘 现在我们学习连乘 这与我们日常的惩罚有一些不同 因为乘数可能来自于某个变量或者是某个常量 我们来看连城的练习 定一些变量 也就是申请一些存储空间来执行这三条语句 我们先来执行第一条语句 25乘以三赋值给A先计算25 二是int类型 五是int类型 两个数类型一致就可以计算了 25结果是十是int类型 那么用1030的类型是int类型 三的类型是int类型 两个类型一致就可以计算了 10×3结果是30 是int类型 I乘以53

### **[03:01]**
得到的结果是30 是int类型 将它赋值给A 我们注意连乘运算 最后的赋值是看最后一个结果的大小和类型 所以说将30赋值给AA 提供的存储空间是八位的 就将D8位存到A的空间当中 A的值是30 我们来执行第二条语句 K乘以X乘以Y赋值给B先计算K乘以X K是on schild类型 X是on schil类型 都小于int家族 所以说K提升为int类型 X提升为int类型 两个类型一致就可以计算了 K是二 X是三 那么23结果是六是int类型 再用六乘以Y6是int类型 Y是ASSIGNATURER类型

### **[04:02]**
提升为int类型 两个类型一致就可以计算了 六乘以YY的值是六 也就是6×6 结果是36是int类型 那么K乘以X乘以Y得到一个结果是36 是int类型的 我们将这个结果赋值给B B提供的存储空间是八位的 就将D8位存到B的空间当中 B的值就是36 这里要注意的就是 连乘运算的负值 是看最后一个结果的大小和类型 我们来看第三条语句 X乘以五乘以Y赋值给C先来计算X乘以五 X是on suna char类型 提升为int类型 五是int类型 两个类型一致就可以计算了 X是335 得到的结果是15是int类型 那么再用15乘以Y实物是int类型

### **[05:03]**
Y是啊SNHR类型 那么提升为int类型 现在两个数的类型一致就可以计算了 15乘以YY是六 那么就是15×6 结果是90 是int类型 那么执行完S乘以五乘以Y 得到一个结果是90 是int类型 将它赋值给CC提供的存储空间是八位的 所以说将D8位存到C的空间当中 那么C的值就是90 现在我们再看自成与自成的简写 第一种方式 一个乘数是保存变量本身 然后乘一个乘数复制给保存变量 它的简写方式就是保存变量乘法简写的符号 注意这个符号是一体的 中间不要分开 然后右边就是一个乘数 这就是它的简写方式

### **[06:01]**
第二种方式就是一个乘数 是保存变量本身乘以乘数 一乘以乘数I 然后等等等乘以乘数N 也就是这里的乘数比较多 然后将结果复制给保存变量 一般我们会把后面的乘数用括号括起来 这样把它们看作一个整体 那么它的简写方式就是保存变量 乘法的简写符号 右边就是括号里的内容 当然这个括号可以去掉的 这就是多个乘数的简写方式 我们来看自成与自成简写的练习 定义一些变量 我们来执行这三条语句 先来执行第一条语句 这是乘法的简写方式 你要想到它原来的书写方式就会计算了 计算的过程还是先类型的提升与对齐 D的值是556

### **[07:02]**
就是30 将30赋值给D 那么执行完这条语句之后 D空间里的值就是30 注意这里只是两种不同的书写方式 它们经过编译器翻译后的指令 实际上都是一样的 我们来执行第二条语句 这是乘法的简写方式 你要想到它原来的书写方式就会计算了 计算的过程还是先对类型提升与对齐 E乘以X赋值给E E的值是五 X的值是353 等于15赋值给E那么E存储的就是15 注意这里只是两种不同的书写方式 它们经过编译器翻译后的指令 实际上都是一样的 我们来执行第三条语句 还是乘法的简写方式 你要想到它原来的书写方式就会计算了 先计算括号里的还是先对类型的提升与对齐

### **[08:04]**
二乘以YY是六二乘以 Y是十二十二乘以KK是222是24 179 00:08:16,910 --> 00:08:19,850 再乘以FF的值是五 那么得到的结果就是九十九十是int类型的 再将90赋值给FF提供的存储空间是八位的 所以说执行完这条语句 F里的值是90 有没有自成一的特殊写法 因为我们之前学过自减一的写法 比如这里变量G减减 就相当于是G减一赋值给G减减 H就相当于是H减一赋值给H 那么乘法有没有自成一呢 比如变量G乘乘相当于是G乘以一赋值

### **[09:00]**
给G乘乘H相当于是H乘以一赋值给H 我们知道一个数乘以一还是它本身没有变化 所以说这样就没有什么意义 也就没有这种写法 现在来认识一下乘法的溢出定义一些变量 我们来执行这条语句 M乘以N赋值给AM的类型是insa chair N的类型是on sa chan 小于int家族都要提升为int类型 现在两个类型一致就可以计算了 M是30 N是十 那么30×10结果是300 是int类型 执行完M乘以N得到一个结果 有大小有类型 将这个结果赋值给A A提供的存储空间是八位的 那么将第八位0X2C存到A的空间当中 那么高八位0X01就装不了

### **[10:00]**
就会被丢掉 那么结果肯定是错误的 怎么办呢 那就在DNA的时候 申请的房间大一些就行了 比如这里把A定义成on st.类型 也就是申请两个字节的空间 就能存储这里两个字节的数据了 那么执行完M乘以N将结果赋值给A 也就是把这两个字节存到A的空间当中 那么A存储的就是300 结果就没有出错 如果把A定义成on slg类型 也就是申请四个字节的空间 我们看看结果是什么样子的 先执行M乘以N得到一个结果 有大小有类型 将这个结果赋值给A 这个结果是两个字节的 然后存储到A的空间当中 它会占用A空间的两个字节 0XIC0X零一 那么还有两个房间没有使用

### **[11:01]**
怎么办呢 我们来看一下这个结果 结果是300 是int类型的 也就是一个正数 对于正数与零 如果给他们分配的房间很大 有剩余的房间就补零 那么剩余的两个房间就全是零 也就是0X000X零零 那么四个房间都装满数据了 如果我们把N定义为sin的char类型 也就是一个有符号类型 将十赋值给N 我们再来执行这条语句 M是on sa char类型 需要提升为int类型 N是sign的char类型 需要提升为int类型 现在两个类型一致就可以计算了 M乘以N也就是30-10 30×-10 结果是-300是int类型 那么执行完M乘以N得到一个结果

### **[12:00]**
有大小 有类型 将它赋值给AA提供的存储空间是四个字节 那么这两个字节的数据就可以放在A的空间了 第一个字节存的是D4 第二个字节存的是FE 那么还剩两个房间没有使用 怎么办呢 我们来看一下这个结果 结果是-300是int类型的 是一个负数 对于负数而言 给他们分配的房间如果很大 有剩余的房间就补一 那么剩余的两个房间全补一 也就是0XFF0XFF 那么四个房间的数据现在都补齐了 这就引出今天我们补充的一个知识点 也就是大家留言的一个问题 赋值语句涉及类型转换吗 涉及但是把那么多的类型转换总结成两句话

### **[13:01]**
就是赋值不改变二进制的位置 如果在存储的时候有剩余的空间 正数与零前面补零 负数前面补一 我们来想一下负值的主要目的是什么 是不是把数据存储起来 那么既然负责存储数据 就不能更改数据 所以说在正数与零的前面补零 不改变原来的值 这个很好理解 那么在复数前面补腰 实际上也没有改变原来的值 比如这里有一个计算结果是两个字节是int类型 赋值给一个字节的空间 那么就取D8位存到这个空间当中 如果将这个结果赋值给两个字节的空间 那么就将这里的16位都存到这个空间当中 如果将这个结果赋值给一个四个字节的空间

### **[14:00]**
那么将这个16位存储到这个空间当中 前面两个字节会被用掉 那么还剩两个空房间怎么办呢 我们就要来分析这个结果了 这个结果是F1D4 也就是-300 是int类型的 那么结果是一个负数 那么剩余的两个房间就要全部补一 也就是0XFF0XFF 我们前面在算数转换规则里面也提到过 如果这个数是正数与零 那么前面就补零 负数 前面就补一 比如八位的正数转为16位 无论是转成有符号类型还是无符号类型 前面都补零 八位正数转换成32位 无论是转成32位的 有符号类型还是无符号类型 前面补的都是零

### **[15:00]**
比如八位的负数转成16位 无论是转成有符号类型还是无符号类型 前面补的都是一八位的负数转成32位 无论是转成32位的 有符号类型还是无符号类型 前面补的都是一 在算术转换规则当中 正数与零 前面补零 负数前面补一 它们的目的是不改变数据原来的值 这与赋值语句的目的是一样的 至于这样一个数 你按照什么方式来解读 比如你按照有符号方式来解读它是二 按照无符号方式来解读它是6534 这个是对一个数据的解读方式 那就是另一个事情了 我想说的是 零前面补零是不改变原来的数的负数 前面补腰实际上也没有改变原来的数 它们的目的就是不改变原来的数

### **[16:00]**
最后说一下小数的负值 比如将1.23赋值给A 先不管A是什么类型 先看小数的赋值 只需要两步 你就会了 第一步把小数点去掉 第二步把整数部分转换成long类型 那么就相当于我们去计算 得到一个结果是lg类型的 那么再对这个结果进行赋值 你就知道是怎么做的了 下面我们来写程序练习一下 还是在我们的程序模板上进行练习 先定义一些变量 然后把几个例子写一下 最后将输出的结果发到电脑上进行查看 我们将变量的定义放在左边窗口 只是为了方便查看 这与程序的编写也没有关系 这里的例题刚才都已经讲过了 然后我这里就简单的过一下第一个知识点

### **[17:02]**
连乘25乘以三赋值给A 那么A的结果就是30K乘以X乘以Y赋值 给BK的值是二 X的值是三 Y的值是六 那么I乘36结果是36 B的值就是36 X乘以五乘以Y赋值给CX的值是三 Y的值是六 那么3×5乘以六等于90 赋值给CC的值就是90 这里面计算之前 肯定是要先进行类型的提升与对齐的 但是刚才都已经讲过了嘛 所以说这里就过一下第二个知识点 自成的简写D自成简写符号 然后右边是一个值六 你要想到他原来的书写方式就会计算了 D乘以六赋值给DD的值是五 5×6 30赋值给DD的值就是30

### **[18:02]**
E乘法简写符号 然后剩一个值X你要想到它原来的书写方式 就会计算了 E乘以X赋值给EE的值是五 X的值是三 那么5×3是15 E的值就是15 F乘法的简写符号 I乘以Y乘以K想到他原来的书写方式 先计算括号里的I乘以Y乘以KY的值是六 K的值是二 那么I乘以62就是24 397 00:18:39,038 --> 00:18:41,650 再乘以FF的值是五 S乘以五就是120 将120复制给FF的值就是120 最后将ABCDEF的值输出 他们的结果我已经整理在这边了 等会对一下就可以了 现在我们把程序恢复到正常状态 然后点击save

### **[19:00]**
点击rebuild 零警告零错误 现在来下载程序 打开第26节的程序 点击下载之后记得冷启动 第一个数30 第二个数36 第三个数90 第四个数30 第五个数15 第六个数120 那么单片机的输出结果与我们分析的是一致的 这节课到此结束 拜拜

