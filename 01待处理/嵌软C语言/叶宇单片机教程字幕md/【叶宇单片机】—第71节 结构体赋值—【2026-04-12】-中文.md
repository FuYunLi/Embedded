# 【叶宇单片机】—第71节 结构体赋值—【2026-04-12】-中文

## 字幕正文

### **[00:01]**
今天来看一下结构体的赋值 今天的类型DE还是基于32位单片机 比如定义了几个普通的变量 把它们放到结构底当中 就定义了一个新的类型 那我们说使用结构体的意义是什么 是不是把一组相关的数据打包成一个整体 方便一起管理 一起传递数据 这是一个新的类型 比如ASSUTINT是我们已知的一个老的类型 一个类型只有定义变量之后才可以正常的使用 定义几个变量 在编译阶段会为它们分配存储空间 注意他们的地址都处于对齐的一个状态 对齐的目的主要方便单片机一次读取数据 不用拼接数据 注意中间有一个是空的 如果把这些变量放在一个结构体当中

### **[01:02]**
就定义了一个新的类型 用这新的类型来定义一个变量 那么也会为它分配一个存储空间 注意下面还要占用两个存储空间 但是没有使用 这个主要是与数组有关 我们说数组的第一个元素和第二个元素之间 是紧密相连的 那么如果定义一个结构体数组 那么要保证结构体数组当中的每一个元素 还是对齐的一个状态 如果不了解的话 可以看一下上节课的内容 接着我们在函数当中使用这个变量 使用这个变量 就是通过结构体名来调用不同的成员 调用第一个成员按照on sa cha类型来解读调用 第二个成员按照short int类型来解读调用 第三个成员按照insa int类型来解读 也就是还是回到了基本变量的使用结构题

### **[02:05]**
只是把一组相关的数据放在一起使用的时候 还是回到基本变量来使用 比如定义两个基本变量 然后在函数当中就可以使用这两个基本变量 也就是可以对它读和写数据就是可读可写 我们也想定义两个结构体变量 但是我们要先把它的类型写出来 就是定义一个新的类型 这是我们定义的一个新的类型 那么同样在函数当中可以使用这个变量 可以将100赋值给X的第一个成员 可以将X的第一个成员复制给Y的第一个成员 可以将X的第一个成员 再复制给Y的第二个成员 基本变量有自己的存储空间 可读可写

### **[03:00]**
结构体的成员也有自己的存储空间 也可以对它进行读和写 如果对X变量进行初始化 第一个成员是100 第二个成员是1000 那么将X赋值给Y 也就是结构体变量可以整体的赋值 那么Y的值就是100和1000 我们通过老的类型定义变量 它有自己的存储空间 可以对这个存储空间进行读和写 我们用新的类型进行定义变量 它也有自己的存储空间 也可以对自己的存储空间进行读和写 但是这里需要注意的是 就是要类型一致 因为它没有隐私的类型转换 我们知道普通的整数类型 它们在赋值的时候会有一个隐式的转换 像是结构体类型 你在赋值的时候必须类型一致 不然的话就会报错 因为它不会进行一个隐式的类型转换

### **[04:03]**
简单的总结一下结构体 就是把相关的数据打包成一个整体 在使用的时候 调用成员就是按照基本的变量类型来使用 使用的时候还可以使用这个结构体 整体也是按照基本变量来使用 因为无论是成员还是结构体 整体还是一个普通的变量 他们都有自己的存储空间 可以对这个存储空间进行读和写 所以说它们的使用方法也是一样的 既然结构体变量可以整体的进行赋值 我们来看一下它的使用 这里有一个数组 把它放在结构体当中 就定义了一个新的类型 用新的类型定义一个变量 对它进行初始化 也就是对数组元素进行初始化 注意数组元素的初始化是怎么初始化的 是不是一个大括号里面放数组的元素

### **[05:03]**
解构体的初始化 它的两边也有大括号 所以说这里要注意一下 接着再定义一个变量 注意它们的类型是一样的 然后在函数当中调用 将X赋值给Y 我们知道结构体可以整体的赋值 那么是不是相当于将数组整体的进行赋值了 因为我们知道正常的数组就是正常 你定义一个数组变量 它不可以整体进行复制 所以说这里实现了数组整体赋值 可不可以这样来复制X点数组名和Y点数组名 他得到的是什么 是不是相当于得到我们普通的一个数组名 这样写是不对的 我们说数组名代表的是地址 地址是一个常数 因为变量的内存分配是在什么时候进行分配的 是不是在编译阶段为变量分配内存

### **[06:01]**
那么变量的地址就固定了 程序运行的时候 它的地址也是固定的 不可以更改的 所以说数组的地址就是一个常数 但是可以调用数组的元素进行复制 还是回到数组本身的使用方法来使用 接着我们再来看一个整体复制 这里定义了两个变量 一个普通变量 一个指针变量在ram当中为他们分配存储空间 如果把它们放在结构体当中 就定义了一个新的类型 用这新的类型来定义一个变量 也会为它分配一个存储空间 注意中间有三个是空的 再定义一个普通变量分配存储空间 再定义一个结构体变量 并进行初始化 第一个成员是20 第二个成员是取取temp的地址 那么它存储的就是tap的地址 指向的也是temp 接着在函数当中来使用

### **[07:02]**
首先将Y的值赋值给X 他们俩的类型一致就可以直接复制 那么X的第一个成员就是20 第二个成员存储的是他们的地址 那么X的第二个成员指向的也是temp 就是不要想太多 你就把它当成一个普通的指针变量来理解 就可以了 他还是回到一个基本的变量来使用 就是一个基本的指针变量来使用 那么对这个变量进行解引用 存100 temple里存的就是100 对Y里的指针变量进行解引用 存的是80 那么tap里存的就是80 这里需要注意一下 就是两个人拿到了同一个地址 而且这两个人都有权利打开这个存储空间 进行修改数据 所以说这里需要注意一下 也就是无论是结构体成员还是结构体整体

### **[08:00]**
它都有自己的存储空间 可以对这个存储空间进行读和写 就像普通变量 它有自己的存储空间 可以进行读和写 但是按照整体来读 写的时候注意要类型一致 因为它不会进行一个隐式的类型转换 好这节课到此结束 拜拜

