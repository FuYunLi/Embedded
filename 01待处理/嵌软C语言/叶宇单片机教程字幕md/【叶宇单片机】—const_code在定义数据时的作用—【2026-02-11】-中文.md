# 【叶宇单片机】—const_code在定义数据时的作用—【2026-02-11】-中文

## 字幕正文

### **[00:00]**
今天我们来看一下CONST和code的使用 code修饰的变量只能读 它是五幺专属关键字 也就只能在五一单片机当中使用 其它单片机不可以用 CONST修饰的变量也是只能读 但是它是C标准关键字 也就在所有的单片机当中都可以使用 这里的只能读只能读 我们是不是想到ROM存储器只能读 ram存储器能读能写 这就是我们今天故事的一个背景 先看一下 全局变量会在RYM当中申请一个存储空间 局部变量会在ram当中申请一个存储空间 那么这两个变量能读也能写在它们的前面 加上code就会强制的在ROM当中分配一个存储空间 在局部变量前面加上一个code 也是强制的在ROM当中分配一个存储空间 本来这两个变量分配的存储空间是在ram当中

### **[01:03]**
给他们加了code之后 会被强制的分布在ROM存储器当中 也就节省了ram存储空间 因为ram的资源比较稀缺 ERROM往往是ram的几十倍或者是几百倍 所以说当你使用多个大数组的时候 可以把它定义在ROME当中 既然这两个变量分配在ROME当中 那么这两个变量也就只能读物理意义上的 只能读不能写 就是物理上只有读这个功能 没有写这个功能 所以这里将零写到A的存储空间是非法的 A加加也是非法的 就是也不可以改写里面的数据 B加加也是非法的 我们对code进行一个简单的总结 code它是五幺专属关键字 它修饰的变量是强制的存储在ROM当中的 那么这个变量就是物理意义上的 只能读不能写

### **[02:00]**
这样做的终极目标是节省ram存储空间 我们举一个例子 就是用code定义一些只能读不能写的数据 这里用code定义一个浮点类型的变量 这个变量名有一点长 它的意思是温度的上限值是35度 同样再定义一个温度的下限值是十度 这两个上下限值 一般在程序的运行期间就不会修改了 我们写一个函数用来判断温度的范围 套一个输入是输入一个温度值 如果这个温度值大于温度的上限 那么它就执行一些操作 如果这个温度小于温度的下限 那么它就执行一些操作 否则的话温度是在正常范围之内 那么它就执行一些操作定义 这样的变量还有一个好处就是一键修改 这里一键修改指的是我们在写程序的时候 比如说修改温度的上限修改到36度

### **[03:03]**
只需要把35改成36就可以了 然后其他的地方就不用修改了 就直接使用变量名就可以了 只能读指的是这个程序下载到单片机内部之后 在运行期间它是只能读不可以改的 上节课我们学习过static 如果在这个全局变量的前面加static static是限制它的作用域 也就是这个全局变量只能在当前文件使用 就不可以在其他的文件使用了 而code限制的是它的读和写功能 比如这里只能读 不能写 每一个修饰词的作用要分开理解 不要搞混了 接着再举一个例子是查表 也就是一个表格的数据 我只读不对它进行修改 我们来看一下定义一个数组 它有12个元素 每个元素代表的是这个月有几天 比如1月份31天 2月份28天

### **[04:01]**
3月份31天 这里用code修饰证明这个表格只能读不能写 接着我们写一个函数 这个函数有一个输入是月份 就是输入一个月份 然后有一个输出就是这个月有多少天 定一个局部变量天数 首先来判断一下月份是否合法 如果不合法的话 直接return0 从这里退出 那么函数会直接从这里返回 下面的语句就不会执行了 返回值是零表示的是非法 也就月份非法 如果月份是合法的 就不会执行return0 接着执行下面的语句 计算出天数 返回天数 这里的数组下标是月份减一 我们来看一下数组的下标是从零开始的 A月份是从一开始的

### **[05:00]**
月份减一就得到一个正确的下标 就得到了一个正确的天数 我们再来看一下CONST 在全局变量的前面加上CONST 它还是在ram当中申请一个存储空间 在局部变量前面加上一个CONST 它还是在ram当中申请一个存储空间 也就是CONST不会改变它的存储空间 那么rhyme它是能读能写的 CONST说它只能读 只是一个语法上的约束 它只能读 既然语法上约定这两个变量只能读 那么就不能将零写到这个存储空间 也不可以A加加改变这个存储空间的数据 也不可以B加加改变这个存储空间的数据 我们再次来总结一下code只能读 它是指的是物理意义上的 只能读不能写 CONST只能读指的是语法上面的约束 它只能读 实际上物理上它是能读能写的

### **[06:03]**
但是不让你写 那有人就会想 既然物理上能读能写 那我们能不能通过一些方法跳过语法检查 然后实现对这个数据进行写呢 这样也是可以的 比如这里定义一个指针变量 我们现在还没有学习指针 这里只是大概的了解一下 指针变量用于存储地址 比如这里存储A的地址 那么我们可以通过A的地址找到这个存储单元 给这个存储单元存一个数是20 这样是合法的 硬件是满足的 语法检查也是通过的 但是不建议这么写 因为这个变量它已经有CONST修饰 就是它只能读 我们约定了这个变量它只能读 你就不要偷偷的去修改了 不要破坏这个约定 好这节课到此结束 拜拜

