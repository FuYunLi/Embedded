# 【叶宇单片机】—051  for 和 while 循环嵌套—【2026-01-24】-中文

## 字幕正文

### **[00:00]**
今天我们来看一下for和while 有循环语句的嵌套 首先简单的来看一下循环嵌套 正常情况程序是从上往下一条一条的执行指令 当遇到了循环指令 就会循环的执行某一段程序 在这一段程序之内又放了一个循环 那么这就是循环嵌套 我们来看一下while ue语句的循环嵌套 这是while有语句的一般格式 先理解一个概念 就是每一条语句它实际上都是有地址的 可以通过这个地址来找到这个语句 比如说我们把这里的语句翻译成机器指令之后 会存储在存储器当中 那么存储器都有地址 通过这个地址反过来就可以找到这条语句 我们首先先有这样一个地址的概念

### **[01:00]**
接着我们来看一下它的执行顺序 如果条件满足的话 那么就从上往下执行每一条语句 来到大括号 这里 这个大括号在我们C语言当中 可能看着没有什么作用 实际上它翻译成汇编语言之后 这里会记录一个返回地址 那么它就可以跳到条件判断的地方 继续判断条件 如果条件不满足 那么这里也会记录一个跳出地址 那么就会跳过大括号的语句 接着执行下面的 虽然C语言没有体现一个返回地址和跳出地址 但是这样的循环语句翻译成汇编指令之后 它确确实实是存在这样的地址的理解 这个地址有利于理解循环嵌套的执行顺序 我们来看一下循环嵌套 首先外面是一个循环语句

### **[02:01]**
里面又有一个循环语句 我们看一下它的执行顺序 如果条件满足 那么就会从上往下执行语句来到循环语句 再次对条件进行判断 如果是满足的 那么就执行大括号里面的语句 来到下面这个大括号 这里翻译成汇编指令之后 它会记录返回的地址 也就是返回这个条件判断出的地址 接着去判断这个条件的真假 如果条件是假的 就会跳过大括号里面的语句 那么这里会记录他一个跳出的地址 就是跳出大括号里面的语句 接着执行下面的语句 执行下面的语句 我们又来到了大括号 这个大括号它翻译成汇编指令之后 它还是记录一个返回地址 就是记录他返回这个条件处的那个地址 如果这个条件不满足的话

### **[03:00]**
他也会跳出大括号 那么这里也会记录一个跳出的地址 就是记录他跳到哪里去 接着从这里执行下面的语句 我们观察一下这一对 还有这一对地址 它实际上是各管各的 也就是内部的循环 它想实现跳转 它有它自己的返回地址和跳出地址外部的循环 他想实现它的跳转 它有它自己的返回地址和跳出地址 内部循环和外部循环在跳转的时候 它们是互不干扰的 各管各的 既然在跳转的时候没有相互牵扯 那么剩下的都是一些需要顺序执行的语句 那么更不会相互干扰 也就是外部循环和内部循环 它实际上是两个独立的循环语句 你按照独立的循环语句来分析就不会出错

### **[04:01]**
同样continue和break与循环语句的关系 也是按照独立的循环语句来分析就可以了 就不会出错 我们先看一个程序 首先定义四个变量 ABI和K初值都是零 接着执行我们的循环语句 它是一个大循环 内部还有一个循环 也就是循环嵌套 我们先看一下大循环 大循环的判断条件是R小于二 二的初值是零 那么这个大循环会执行几次 大括号里面的语句 也就是R等于零的时候 I等于一的时候 条件都是成立的 那么会执行两次大括号里面的语句 那么小循环它在大括号内 这个小循环它整体会被执行两次 那么我们再看一下小循环

### **[05:01]**
他自己他自己的判断条件是K小于三 K的初值是零 那么K等于零等于一等于二 条件都是成立的 也就是小循环会执行三次 那么加上大循环会执行两次 小循环 整体是不是小循环内部会被执行六次 那么A的值是多少 B的值是多少 可以写程序验证一下 输出A的值是二 输出B的值实际是六 我们再来看一下for循环语句的嵌套 这是它的一般格式 这是它的执行顺序 同样这里从上往下执行到大括号的时候 它也会记录一个返回地址 如果条件不满足的时候 它跳出大括号语句 它也会记录一个跳出的地址 也就是这个for语句 它会记录自己的返回地址在哪里

### **[06:01]**
跳出地址在哪里 才能够正确的执行 我们再来看一下它的循环嵌套 这是一个for语句循环 内部又放了一个for语句循环 形成循环嵌套内部的循环实现跳转 它会记住他自己的一个返回地址 一个跳出地址 外部的循环实现跳转 他也会记住他自己的一个返回地址 和一个跳出地址 那么内部的循环和外部的循环 在跳转的时候是互不干扰的 剩下的语句都是一些不会发生跳转的语句 都是顺序执行的语句 更不会发生互相干扰 所以说内部的循环它是一个独立的循环语句 外部的循环也是一个独立的循环语句 你按照独立的循环语句来分析 就不会出错 如果你不理解

### **[07:00]**
这里的跳出地址和返回地址也没有关系 因为这个地址它是在汇编语言当中体现出来的 在C语言当中它体现不出来 如果你不理解没有关系 你只要记住它虽然是循环嵌套 但是他的执行顺序 还是按照独立的循环语句来执行的就可以了 先看一个循环嵌套的应用 就是延时定义一个变量 K这里是一个for循环语句 也就相当于for语句 大括号内是一条空语句 那么K等于零 K小于200是成立的 会执行大括号里的语句 接着回到变量更新 接着又回到条件判断 那么它会执行几次 这样的循环是不是会执行200次 这样的循环执行一次循环都会消耗一点时间

### **[08:00]**
那么循环执行了200次就会达到延时的功能 我们说for语句只有条件不满足的时候 才会跳出大括号里面的语句 接着执行下面的语句 也就是只有把这200次循环执行完 才会执行下面的语句 这样的岩石实际上是一个阻塞的岩石 就是在延时的过程当中 他一直在这里转呀转转呀转 他别的指令他不会去执行 就阻塞在这里了 所以说它是一个阻塞延时 如果我们的延时时间比较长 一条for语句达不到这样的延迟时间 怎么办呢 那我们可以再定义一个变量 然后再写一条for语句 也就是写一个外循环 这里的R是零条件 判断是R小于500 也就是会循环执行500次 大括号里面的语句 也就是括号内的for语句

### **[09:02]**
也就是相当于这个for语句 它整体会被执行500次 那么它内部的这个小循环就会被循环的执行 500×200次 那我想问的是 现在的延迟时间是之前这个for语句的500倍吗 实际上不是的 因为你每次执行的时候要进行条件判断 变量更新 这些语句它也会被循环执行了500次 它也会占用一些时间 for语句可以起到延时的作用 但是它的时间不是一个精确的时间 你想获得一个精确的时间 就需要用定时器来定时 我们再来看一下循环嵌套的应用 就是对二维数组进行初始化 比如有一个二维数组AI3对它进行初始化 用一个循环嵌套来对它进行初始化

### **[10:02]**
首先外循环的数值是零 条件判断是I小于二是成立的 那么执行大括号里面的语句 那么这条语句就是A0KA0K 我们看一下K的值 K的初值是零 条件判断是K小于三 那么K值的变化就是012 那么也就是将零赋值给A00 将零赋值给A01 将零赋值给A02 执行完内部循环之后 又回到这里的变量更新 那么I的值就是一一小于二条件成立 接着执行大括号里面的语句 也就是A1K那么K的范围是012 可以将零赋值给A100复制给A11 零赋值给A12 执行完内部循环之后 又回到变量更新

### **[11:00]**
二等于二 二是不小于二的条件不成立 那么就不会执行大括号里面的语句了 就会跳出大括号里面的语句 接着执行下面的语句 总结就是一句话循环嵌套 实际上还是按照独立的循环语句来处理 就不会出错 这节课到此结束 拜拜

