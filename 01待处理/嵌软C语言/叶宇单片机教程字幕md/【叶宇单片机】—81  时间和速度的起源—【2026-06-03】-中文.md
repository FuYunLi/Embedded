# 【叶宇单片机】—81  时间和速度的起源—【2026-06-03】-中文

## 字幕正文

### **[00:01]**
今天我们来看一下时间与速度 主要是理解一下程序是如何产生延时的 首先来看一下我们熟悉的一个for循环语句 先看一下它的执行顺序 首先是条件初始化 然后来到条件判断 如果是真的 那么就执行循环体 接着来到条件更新 更新之后又来到条件判断 如果是真的 那么就一直在这里循环的执行 从而达到一个延时的效果 如果判断是假的 那么就会跳出这个循环 那这个延时效果到底是怎么产生的呢 我们来看一下它的原理 首先这个for循环语句可以翻译成汇编指令 每一条汇编指令都对应着一个机器码 这里我们暂时就不展示了 C冒号后面是一个地址 也就是这条指令在ROM当中存储的位置 这一段汇编可以分成三个部分

### **[01:02]**
首先是条件初始化 然后是条件判断 接着应该是进入循环体 但是这里循环体是空的 所以说没有又来到条件更新 当I等于零的时候 它就会从上往下依次执行这些指令 这里有I加加 那么I的值就是一 接着又会跳到条件判断处 从这里接着往下执行指令 执行完了之后 R的值是二 接着又会跳到条件判断处 从这里往下执行 执行完了之后 I的值是三 接着又会跳到条件判断处 从这里往下执行 这一次判断他的条件是不成立的 就会跳出这个循环语句 现在for循环语句产生的延时就变成了 执行每一条汇编指令需要多长时间 我们来看一下执行每条指令需要多长时间

### **[02:00]**
首先了解一下频率与周期的概念 这里有一个周期性的变化 时间是一秒 频率是一秒之内完成一次周期性变化的次数 注意是次数 那么这里有几次是不是三次 那么它的频率就是3HZ 周期就是完成一次周期性变化的时间 那么这里的一个周期是多长时间 是不是一秒除以3HZ 注意一下频率与周期的概念是完全不一样的 我们来看一下单片机的频率与周期 我们电路板晶振的频率是11.0592兆赫兹 假如这里是晶振的频率 也就是一秒内周期性变化11.0592兆次 那么这里的一个周期是多长时间呢 是不是一秒除以11.0592兆赫兹 我们把它叫做时钟周期

### **[03:03]**
那么晶振与指令之间有什么关系呢 CPU是在晶振的节拍下完成一条条的指令的 如果进站的频率越快 那么CPU完成一条指令也就越快 也就CPU完成一个独立的操作 大概需要12个时钟周期 我们也叫做机器周期 那么这里的一个机器周期是多长时间 是不是一秒除以11.0592兆赫兹 然后乘以12 那么一个机器周期就约等于1.085微秒 然后我们看一下这里的汇编指令 每一条汇编指令执行的时间都可以用机器周期来表示 这样我们就可以知道执行每一条汇编指令大概需要多长时间

### **[04:08]**
for语句的延时时间 也就是执行这些指令一共花费了多长时间 这个时候我们是可以算出来的 也就是从这里开始 从这里结束 一共执行了多长时间 就是它的延时时间大概是55微秒 这就是for语句产生岩石的背后的原理 我们把这些原理去掉 回归到for语句本身 就大概可以理解是就可以大概理解循环语句是如何产生延时的 这节课主要理解的是程序是如何产生延时的 好到此结束 拜拜

