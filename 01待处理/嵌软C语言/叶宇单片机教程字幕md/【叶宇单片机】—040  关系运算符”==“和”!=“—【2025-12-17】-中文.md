# 【叶宇单片机】—040  关系运算符”==“和”!=“—【2025-12-17】-中文

## 字幕正文

### **[00:01]**
大家好 今天我们来看一下两个关系运算符 一个是等于 一个是不等于等于就是数学当中的等语 数学当中的关系运算符 还有大于号 小于号 但是在计算机当中 等于是用两个等于符号表示的 因为一个等于符号 在计算机当中表示的是赋值的意思 还有不等于符号 这个是数学当中的不等于符号 在计算机当中 用一个感叹号和一个等于号来表示 不等于 那么这两个运算符我们会在哪些场合使用呢 我们来看一下上一节课我们说了if语句的格式 也就是如果条件是满足的 就会执行大括号里的语句 接着执行括号外的语句 如果条件是不满足的 那么就会跳过大括号里的语句 接着执行括号外的语句

### **[01:00]**
这个if语句的执行顺序我们已经非常熟悉了 主要是这里的条件判断 条件判断一般有两种 一个是数值 一个是关系的判断 数值的判断我们说非零就是真的 非零就是真的 如果是零 我们就判断它是假的 关系判断 比如说等于关系判断大于小于 还有不等于关系的判断 对这些关系的判断和数学里判断是一样的 就是这个运算式是否成立 如果成立 就是对的 如果不成立 就是错的 对证的条件判断它的结果是真的假的 或者是对的和错的 用数学语言来表示的话 就是用零和一来表示 也就是逻辑值零和幺 它和那个加减乘除运算不一样 加减乘除运算会得到一个算数值

### **[02:00]**
跟这里的逻辑值是不一样的 我们要注意一下 我们来看一下 等于和不等于在if条件语句当中常用的格式 也就是比较两个数是相等还是不相等 这个这两个数可以是变量 也可以是常量 不等于也是一样的 它的数可以是变量 也可以是常量 我们来举一个例子 比如定义一个变量A是五 比较五和A的值是否相等 因为A的值是五 所以说这个式子成立的 它就会执行大括号里的语句 接着执行括号外的语句 这里比较十和A是否相等 那么十和A是不相等的 所以说会跳过大括号里的语句 接着执行括号外的语句 我们看一下比较两个数不相等 定一个变量B是零 六不等于BB的值是零

### **[03:02]**
那么六当然不等于B 这个式子是成立的 就会执行大括号里的语句 接着执行括号外的语句 这里判断零不等于BB的值是零和零是相等的 那么这个式子是不成立的 就会跳过大括号里的语句 接着执行括号外的语句 在比较两个数相等或者不相等的时候 如果两个数当中有一个数是常量 我们通常是把常量放在运算符的左边 比如比较A和五是否相等 如果我们一不小心丢掉了一个等于号 就变成了将五赋值给A 那么无论A里面是一个什么样的纸 都会被覆盖掉 这个式子得到的结果就是五 我们说一个数据非零就是真的 那么将五赋值给A得到的结果是五 这个式子永远是真的

### **[04:01]**
那么这里的语句一每次都会被执行 就失去了这里条件判断的意义 问题是 如果说你少写了一个等于号 那编译器如果编译之后能够查出来这个问题 你再去修改就可以了 但是这里在Q当中 将五赋值给A是一个合法的语句 那么这部分的语句编译之后就是零警告零 错误的先忽略一下 这里的汉字表达就是你写错了 但是编译器没有查出来 你自己也没有发现 那怎么办呢 所以一般会将常量放在符号的左边 这样如果你少写了一个等于号 就相当于将A赋值给五 我们知道赋值语句的左边 需要的是一个存储空间 它用来存储数据 但是这里的数字量五 它没办法提供一个存储空间 所以说这条语句是错误的

### **[05:01]**
编译之后就会有一个错误 那么你根据这个错误再去查找 去修改就可以了 同样不等于运算符 如果我们丢掉了感叹号 也会变成一条赋值语句 比如这里判断B不等于一 如果我们丢掉了感叹号 就变成了将一赋值给B 那么这条语句永远是真的 语句一就会一直会被执行 而且编译器查不出来这样的问题 所以说一般会将常量放在符号的左边 那么如果你少写了一个感叹号 变成了B赋值给一 那么这条语句就是错误的 编译器就会报这个错误 然后你根据错误去修改就可以了 还有一个需要注意 还有一个需要注意的地方 就是判断两个数相等或者不相等 这两个数不能使用浮点类型 为什么呢 我们来看一下 因为浮点数它存储的是最优近似值

### **[06:03]**
什么是最优近似值 比如这的0.1 0.2 0.3 我们都按照float类型来存储 0.1存储的时候 前面是精确值 后面是一些近似值 0.2存储的时候 前面是精确值 后面是近似值 同样1.6存储的时候也是这样子的 浮点数存储某个数的时候 它是足够的靠近接近这个数 而不是数学意义上的绝对相等 这一点要注意一下 比如将这里float类型的0.2相加相加八次 得到的结果是1.6 这里是得到的结果 1.6前面是金阙指 后面是近似纸 这个是1.6直接存储的一个结果 这是1.6直接存储的一个结果 那么这两个结果是不相等的 但他们有一个特点就是近似相等

### **[07:02]**
那么近似相等就不能用这里的等语来判断 为什么不能用这里的等语来判断呢 因为这里的等于是判断 二进制的每一位是绝对相等的 不适用于判断近似相等 那如何判断两个近似值相等呢 可以通过忽略它们之间的微小误差来判断 比如有一个浮点数是A 有一个浮点数B 假如这是A 这是B 那么A减B的绝对值 也就是A减B差的绝对值小于一个极小值 当小于这个极小值的时候 我们就说这两个数相等了 我们来举一个例子 定义一个浮点类型数X 然后再来判断零等于X吗 也就是X等于零吗 这里的X除非你直接给它赋值零

### **[08:03]**
然后它的每一位都是零 其他的情况X都是一个近似值 你要判断它等不等于零 也是一个近似等于零的值 所以说我们一般取X的绝对值小于一个极小值 如果小于这个极小值的话 我们就判断X是零 否则的话S就不是零 对于浮点类型的数 我们除了记住它是一个小数之外 我们还要记住它是一个它 我们还要记住它存储的是一个最优近似值 前面的几位是金阙指后面的是近似只 那么在计算的时候 这个近似值也会参与计算 所以说我们要注意一下

