---
status: todo
created: 2026-06-30
tags:
  - c/advanced
  - eth/prerequisite
  - todo
references:
  - "[[../../代码段与对应关键字/代码段与对应关键字综合笔记.md|代码段与对应关键字综合笔记]]"
  - "[[../../叶宇单片机教程字幕md/【叶宇单片机】—74 结构体指针在函数接口处的频繁调用—【2026-04-24】-中文.md|结构体指针高频调用]]"
---

# 回调函数与函数指针的事件驱动模型

在以太网协议栈（如 **LwIP**）的无操作系统（NO_SYS=1）移植中，**事件驱动模型**是其核心架构。这种模型完全依赖于 C 语言的**函数指针**和**回调函数（Callback）**机制。

---

## 1. 为什么需要函数指针？

传统的单片机编程通常是“顺序执行”或“轮询（Polling）”模型。例如，主循环不断检查是否有数据到来。然而，在网络通信中，数据的到来是随机且异步的。为了高效处理网络事件，LwIP 采用了类似中断的思想：**你把一个处理函数的地址交给我，当事件发生时，我来调用这个函数。**

### 1.1 函数指针的定义与传递
在 C 语言中，函数名本身就是一个指向该函数代码段首地址的指针。
```c
// 1. 定义一个符合特定格式的函数
err_t my_recv_callback(void *arg, struct tcp_pcb *tpcb, struct pbuf *p, err_t err) {
    // 处理接收到的数据
    return ERR_OK;
}

// 2. 传递函数指针给底层注册接口
tcp_recv(tpcb, my_recv_callback); 
```

> [!TIP]
> 这里的 `my_recv_callback` 被作为参数传递给了 `tcp_recv` 函数。底层协议栈会将这个地址保存起来。这种被作为参数传递、并在特定事件发生时被第三方（底层库）调用的函数，就称为**回调函数**。

---

## 2. LwIP 中的回调函数实战

在 LwIP 的 **RAW API** 中，几乎所有的业务逻辑都是通过注册回调函数来完成的。

### 2.1 接收数据回调：tcp_recv()
当你建立一个 TCP 连接后，你不能用 `while(1)` 去死等数据。你需要告诉协议栈：“如果这个连接有数据来了，请调用我指定的函数”。
```c
// 注册接收回调
tcp_recv(pcb, http_recv_callback);
```
当网卡（MAC）收到数据包并层层解析，确认是发给这个 TCP 连接的负载数据后，LwIP 内部会自动触发调用 `http_recv_callback`。

### 2.2 连接接入回调：tcp_accept()
如果你写的是一个 TCP 服务端（Server），你需要监听某个端口。当有客户端尝试连接时，你需要一个回调来处理新连接。
```c
// 注册接受连接回调
tcp_accept(pcb, my_accept_callback);
```
在 `my_accept_callback` 中，你可以为这个新连接分配资源，并继续为它注册单独的 `tcp_recv` 回调。

---

## 3. 回调函数的上下文安全性

在编写回调函数时，必须时刻警惕其**执行上下文（Context）**：
1. **中断上下文还是线程上下文？** 
   如果在网卡的 RX 中断服务函数中直接调用 LwIP 的解析代码，那么你的回调函数也是在**中断中执行的**。此时回调函数内**绝对禁止使用任何阻塞操作（如延时、等待信号量）**，否则会导致整个系统崩溃。
2. **全局变量的安全性：**
   回调函数通常是异步执行的，如果回调函数修改了主循环也在使用的全局变量，必须加锁或使用 `volatile` 关键字防止编译器优化，这与中断函数的防错机制一致。

---

## 4. 深度补充：LwIP 中的 `void *arg` 参数魔法

在前面 `my_recv_callback` 的定义中，你可能会注意到第一个参数是 `void *arg`。这是 C 语言面向对象编程（OOP）思想的经典体现，也是回调机制中极为重要的一环。

**痛点场景**：
假设你的单片机上同时跑了两个 TCP 客户端（一个连服务器 A，一个连服务器 B），它们复用同一个回调函数 `my_recv_callback`。当回调触发时，你怎么知道是哪一个客户端收到了数据？

**解决方案**：
当你使用 `tcp_arg(pcb, my_custom_struct_ptr)` 注册参数时，LwIP 会把这个自定义的指针保存下来。
当 `my_recv_callback` 被底层调用时，LwIP 会将这个指针原封不动地通过 `void *arg` 传回给你。
你只需要在回调函数内部将其强转回你的结构体类型：

```c
err_t my_recv_callback(void *arg, struct tcp_pcb *tpcb, struct pbuf *p, err_t err) {
    // 1. 将 void* 强转回我们自定义的控制块结构体
    struct my_app_state *state = (struct my_app_state *)arg;
    
    // 2. 现在你可以根据 state->client_id 来区分是哪个客户端了
    if (state->client_id == 1) { /* 处理 A */ }
    
    return ERR_OK;
}
```
这种机制完美地实现了**状态的上下文保持**，使得底层的通用回调函数能够处理上层复杂的独立业务逻辑。

---

## 总结速查

- **函数指针**是 C 语言实现事件驱动模型的核心机制——本质是"把函数地址交给底层，事件触发时由底层回调"
- **LwIP RAW API** 的所有业务逻辑通过注册回调函数完成（tcp_recv、tcp_accept 等）
- **回调上下文**必须明确：中断上下文严禁阻塞操作；全局变量需 volatile 或锁保护（详见第 3 节）
- **回调注册模式**使协议栈无需轮询，实现了"被动响应、主动通知"的高效架构

---

## 待深入 / 遗留疑问

- [ ] LwIP NO_SYS=1 模式下，主循环如何配合回调函数实现完整的网络业务流程？
- [ ] 多个回调函数的优先级与执行顺序是否有潜在冲突？
- [ ] RTOS 模式（NO_SYS=0）下 LwIP 的线程安全回调机制有何不同？

---

## 关联笔记

- [[../../代码段与对应关键字/代码段与对应关键字综合笔记.md#回调函数|代码段与对应关键字综合笔记]] — 深入理解函数指针作为参数的高级用法
- [[结构体内存对齐与消除填充#实战：数据包还原与强制类型转换|结构体内存对齐与消除填充]] — 回调解析中 packed 结构体强转映射的实现细节
- [[大小端无缝转换机制#核心转换函数与宏|大小端无缝转换机制]] — 回调解析包头时 ntohs/htons 的转换配合
- [[零碎片内存池与动态数据包缓存机制#pbuf：以太网的灵魂数据结构|零碎片内存池与动态数据包缓存]] — pbuf 链表在回调函数中的分配与释放生命周期
