---
status: todo
created: 2026-06-30
tags:
  - c/advanced
  - eth/prerequisite
  - todo
references:
  - "[[../../叶宇单片机教程字幕md/【叶宇单片机】—73  结构体数据的传输与还原—【2026-04-20】-中文.md|结构体数据的传输与还原]]"
---

# 结构体内存对齐与消除填充

在网络通信底层开发中，由于我们需要将一段连续的内存字节流“强转”为特定协议包的结构体进行解析，**结构体内存对齐（Memory Alignment）**和**字节填充（Padding）**成为了最容易踩坑的地方。

---

## 1. 什么是内存对齐与填充？

32 位单片机（如 ARM Cortex-M, RISC-V）的 CPU 在从内存读取数据时，通常希望数据的地址是 4 的整数倍（即 4 字节对齐）。如果数据不对齐，CPU 可能需要执行多次读取指令来拼接数据，导致性能下降，甚至在某些架构下会直接触发**硬件异常（HardFault）**。

因此，C 语言编译器（如 GCC）为了提升运行效率，会**自动在结构体的成员之间插入空字节（Padding）**，以保证下一个成员的地址能够对齐。

### 1.1 以太网 MAC 首部惨案
我们来看一个标准的以太网 MAC 层帧头（14 字节）：
```c
struct eth_hdr {
    uint8_t  dest[6];  // 目标 MAC 地址，6字节
    uint8_t  src[6];   // 源 MAC 地址，6字节
    uint16_t type;     // 协议类型，2字节
};
```
理论上 `6 + 6 + 2 = 14` 字节。
但如果我们使用 `sizeof(struct eth_hdr)` 打印大小，在某些编译器下，你可能会惊讶地发现它是 **16 字节**！
原因：编译器为了让 `eth_hdr` 整体能够被 4 字节对齐，在 `type` 后面自动填充了 2 个空字节。

> [!WARNING] 
> 在网络传输中，网线上发来的数据是严丝合缝的连续字节流。如果你用含有“填充字节”的结构体指针去映射接收缓存，**后续所有的字段都会发生错位**，解析出来的 IP 和端口将全是乱码！

---

## 2. 如何消除内存填充？

为了保证结构体与物理网络包 100% 映射，我们必须强迫编译器**取消内存对齐填充**，使其变为“紧凑型（Packed）”结构体。

### 2.1 GCC / Clang / MounRiver Studio 语法
使用 `__attribute__((packed))` 关键字：
```c
struct __attribute__((packed)) eth_hdr {
    uint8_t  dest[6];  
    uint8_t  src[6];   
    uint16_t type;     
};
```
此时 `sizeof(struct eth_hdr)` 将严格等于 14 字节。

### 2.2 Keil MDK (ARMCC) 语法
在老版本的 Keil 中，通常使用 `#pragma pack` 指令：
```c
#pragma pack(1)  // 设置 1 字节对齐（取消填充）
struct eth_hdr {
    uint8_t  dest[6];  
    uint8_t  src[6];   
    uint16_t type;     
};
#pragma pack()   // 恢复默认对齐方式
```

> [!TIP]
> 很多跨平台协议栈（如 LwIP）为了兼容不同的编译器，会定义一套包装宏，如 `PACK_STRUCT_BEGIN` 和 `PACK_STRUCT_END`，开发者只需将结构体包裹在这些宏之中即可保证可移植性。

---

## 3. 实战：数据包还原与强制类型转换

当我们通过以太网 DMA 接收到了一串数据缓存（存放于 `uint8_t buffer[1500]`），我们如何提取其中的数据？
利用紧凑型结构体配合**指针强转**（化零为整）：

```c
// buffer 开头的前 14 个字节必然是 MAC 头
struct eth_hdr *mac_header = (struct eth_hdr *)buffer;

// 判断协议类型是否为 IPv4 (0x0800)
// 注意：网络包是网络字节序，这里需要用到 ntohs 转换（详见大小端专题）
if (ntohs(mac_header->type) == 0x0800) {
    // 处理 IP 包
}
```

---

## 4. 深度补充：CPU 为什么天生讨厌“非对齐数据”？

很多初学者不理解，为什么编译器非要自作主张去“填充”内存？这其实是由硬件总线决定的。

假设 32 位单片机的内存数据总线是 32 根线，它每次去内存读取数据，只能“对齐”着读 4 个字节（即地址 0x00, 0x04, 0x08...）。
- **如果数据是对齐的**（比如一个 `uint32_t` 放在 0x04），CPU 发起 **1 次** 总线读取，直接拿到 4 个字节。
- **如果数据是不对齐的**（比如一个 `uint32_t` 放在 0x01，跨越了 0x00~0x03 和 0x04~0x07 两个区块），CPU 必须发起 **2 次** 内存读取，先把前一个区块读出来，再把后一个区块读出来，最后通过内部移位器拼接起来。

这种操作不仅**极其耗时（消耗额外的时钟周期）**，而且在某些严格的处理器架构（如 Cortex-M0）上，硬件干脆不支持非对齐访问，一碰到就会直接触发硬件异常（HardFault）死机。

因此，使用 `__attribute__((packed))` 消除填充后，虽然解决了网络包解析的错位问题，但也**牺牲了 CPU 的局部读取性能**。在高性能网络开发中，这是一个必须权衡的妥协。

## 5. 实战避坑：#pragma pack() 忘记恢复的灾难

如果你使用的是 Keil 的 `#pragma pack`，一定要记住它是成对出现的！

```c
#pragma pack(1)  // 开始 1 字节紧凑对齐
struct eth_hdr { ... };
#pragma pack()   // 【极其重要】恢复编译器的默认对齐
```

如果你忘记写最后一句 `#pragma pack()`，编译器会把你后面所有包含的头文件、全局变量、甚至 RTOS 的核心控制块全部变成 1 字节对齐！这会导致整个单片机的运行效率断崖式下跌，甚至莫名其妙崩溃，查 Bug 会让你痛不欲生。

---

## 总结速查

- 32 位 CPU 期望数据 4 字节对齐，编译器自动在结构体成员间插入填充字节
- 含填充字节的结构体与网络字节流映射会发生字段错位，导致解析全乱（详见第 1 节 WARNING）
- GCC 用 `__attribute__((packed))`，Keil 用 `#pragma pack(1)` 消除填充
- 跨平台协议栈通过 **PACK_STRUCT** 宏封装，保证可移植性
- 紧凑型结构体配合指针强转，可直接映射接收缓存实现零拷贝解析

---

## 待深入 / 遗留疑问

- [ ] packed 结构体在非对齐访问时是否会触发 HardFault？取决于 CPU 架构是否支持非对齐存取
- [ ] LwIP 内部 PACK_STRUCT 宏的具体实现与跨平台兼容策略
- [ ] DMA 接收缓存区本身的对齐要求与 packed 结构体的交互影响

---

## 关联笔记

- [[../../叶宇单片机教程字幕md/【叶宇单片机】—73  结构体数据的传输与还原—【2026-04-20】-中文.md|结构体数据的传输与还原]] — 理解数据包接收后如何通过结构体指针强转进行无损还原
- [[回调函数与函数指针的事件驱动模型#回调函数的上下文安全性|回调函数与函数指针的事件驱动模型]] — packed 结构体解析回调中的上下文安全注意事项
- [[大小端无缝转换机制#核心转换函数与宏|大小端无缝转换机制]] — packed 仅解决对齐问题，多字节字段的大小端转换仍需 ntohs
- [[零碎片内存池与动态数据包缓存机制#pbuf：以太网的灵魂数据结构|零碎片内存池与动态数据包缓存]] — pbuf payload 指针强转为 packed 结构体的实际用法
