---
status: done
created: 2026-09-11
tags:
  - embedded/uart-protocol
  - c/parsing
  - rocketpi/base_samples
references:
  - "[[Embedded_Code]] 01待处理/rocketpi/base_samples/08_rocketpi_uart_control_led/main.c"
---

# 串口文本协议谱系：从 08 到 AT 框架

> 回答一个问题：08/09 对串口学习的意义是否就是"协议相关"？——是，但不止。这两例练的是**串口文本协议的通用解析技术**，本文把这套技术放到整个协议谱系里定位，为 10（Ymodem 二进制）、11（shell）、13（雷达帧）铺路。

## 串口协议的两大门派

串口只是一根字节管道，本身不规定内容含义。往管道里放什么、怎么解读，全是自定义空间——这就是"协议"。所有串口协议分两大门派：

| | 文本协议 | 二进制协议 |
|---|---|---|
| 代表 | AT、NMEA、shell 命令、JSON、Modbus-ASCII | Modbus-RTU、Ymodem、雷达帧、Mavlink |
| 人可读 | 是（串口助手直接看） | 否（需 hexdump，[[06_rocketpi_uart_printf_3|06_3]] 的价值所在） |
| 帧边界 | 换行/分隔符（天然易辨） | 帧头+长度字段（需状态机） |
| 带宽效率 | 低（1 字节只传 1 个数字字符） | 高（1 字节 256 种值） |
| 错误韧性 | 差（错 1 字节整行乱，靠校验和补救） | 可设计（帧头魔数+校验快速重同步） |
| 解析难度 | 低（逐字段推进） | 高（字节序、位域、对齐、状态机） |

08/09 属于文本门派，而且选了 JSON 这种**自描述结构**——字段名和数据在一起，上位机脚本不需要文档就能扩展字段。代价是三颗灯的命令 `{"led":["B","G"],"state":[1,0]}` 要 33 字节，等价二进制只要 5 字节。**带宽换开发效率，调试期绝对划算**——这也是为什么调试交互几乎全用文本，产品内量产通信才用二进制。

## 文本协议解析的四层递进（以 42 例为序）

42 例序列实际上是一部「文本协议解析」渐进教程：

1. **无结构命令**（08/09）：固定字段、固定语义，解析器为字段定制。规模小、最快上手，但加一个字段就要改解析链
2. **分隔符字段流**（13 雷达）：`$帧头,字段,字段*校验`——NMEA 风格，字段数量可变，需要字段遍历+异或校验。对照 [[08_rocketpi_uart_control_led_1|交互终端笔记]] 里 NMEA 的格式，可以看清 13 要学什么
3. **完整 shell**（11 microrl）：空格分词 + 参数任意 + 命令查表 + 行编辑。解析升级为「词法分析」，命令注册机制是架构重点
4. **结构化数据交换**（09 cJSON）：完全体语法树，嵌套/类型/转义全支持，代价是堆和 Flash

学完 09 回头看这个序列：**在 08 手写的每个函数（find_json_value、token 解析、查表）都是某个成熟方案的简化版**——这不是巧合，是刻意设计的教材脉络。

## AT 命令解析器：08 技术的第二个就业方向

对接 WiFi/蓝牙/蜂窝模组时的 AT 框架，本质是 08 的镜像应用（主控当客户端、模组当服务器）。一个最小 AT 框架的部件清单，全部在 08 里见过：

- **发送侧**：`snprintf("AT+CIPSEND=%d\r\n", len)` + 等待应答——console_send_string 的变体
- **接收侧**：行缓冲 + `\r\n` 结算——handle_input_byte 原样复用
- **应答匹配**：strstr 找 `"OK"`/`"ERROR"`——find_json_value 的锚定思想（但 AT 应答无引号锚，需按行首匹配，更严格）
- **URC 处理**：模组主动上报（如 `+IPD,` 来数据了）会插在应答之间——行结算后先判 URC 前缀再判应答，多一个分派分支
- **超时重试**：send 后 HAL_GetTick 计时等应答——05 的时间戳技术

业界成熟 AT 框架（如 ESP-AT、RT-Thread at_device）骨架不过如此。**学完 08+13，就具备了手写 AT 框架的全部前置知识**。

## 二进制协议预告（10 的门口）

10 的 Ymodem 是 42 例里第一个二进制协议，它的帧结构 `SOH | 序号 | ~序号 | 数据128 | CRC16` 用的是完全不同的技术栈：帧头魔数检测、序号连续性校验、CRC 查表。文本协议的"逐字符解析"在二进制世界里变成"状态机字节流"——**同一个缓冲区，两种世界观**。07 留下的 Normal vs Circular DMA 对比坐标也会在那里兑现（大块连续数据用 Circular 最顺手）。

## 与 I2C/SPI 对比：串口把智能从硬件挤到了软件

熟悉 I2C/SPI 的人初学串口协议常感无从下手，根源在物理层差异：

| 能力 | I2C/SPI | UART |
|---|---|---|
| 时序 | 时钟线同步（SCL/SCLK） | 无时钟线，双方约定波特率，不协商 |
| 寻址 | 从机地址/片选信号 | 无地址，点到点独占 |
| 硬件应答 | ACK 机制 | 无，靠协议层补 |
| 错误检测 | 部分（I2C NACK） | 无，全靠协议层校验 |

串口把时钟、寻址、应答、校验全部从硬件层撤掉，代价是**协议层必须自己把这一切补回来**：波特率错了=乱码（I2C 永不乱）、多设备要靠协议字段区分、可靠传输要自己做校验和重发（10 的 Ymodem 会完整展示）。理解了"串口把智能从硬件挤到软件"，就理解了为什么串口协议的设计空间和解析技术如此丰富——这正是本系列后半段（10/11/13/37/38）反复练习的内容。

## 关联笔记

- [[08_rocketpi_uart_control_led]] — 文本协议解析的手写完整实现
- [[09_rocketpi_uart_control_led_cjson]] — 同款需求的库实现
- [[08_rocketpi_uart_control_led_1|串口交互终端的意义]] — shell/AT 终端的背景与动机
- [[08_rocketpi_uart_control_led_5|函数协作全景]] — 文本解析骨架的运行细节
- [[07_rocketpi_uart_echo]] — DMA 接收：二进制大块数据的接收侧基础
