视频标题：stm32教程，STM32单片机高级篇（CAN、以太网、Wi-Fi、蓝牙、LoRa、RTC、电源控制） - P66【066_Wi-Fi案例二_TCP通讯_TCP服务器启动和数据收发】
视频链接：https://www.bilibili.com/video/BV1TsPWeUEkX?p=66
视频作者：尚硅谷
发布日期：2025-02-25
视频标签：#嵌入式 #单片机 #科技 #编程 #32单片 #STM32 #物联网 #开发板 #尚硅谷 #iot
视频简介：【配套资料】关注公众号：尚硅谷教育，回复“视频资料”免费获取
【课程简介】本教程为STM32教程高级篇，核心内容：物联网开发实战、电源管理（低功耗）、RTC、BKP、看门狗。

---
字幕正文
---

前面我们已经实现了两种工作模式的设置
那现在我们这个无线WIFI网络
就已经搭建起来了
已经联网成功了啊
那所以现在该ping的话当然
都能够ping通
关键点就在于我们接下来基于这个网络
还要再创建一个TCP的服务器
然后接下来呢还要通过建立连接
TCP服务器和客户端之间
要进行数据的收发
要把这个完成
这才算我们完成了这个案例
所以接下来我们还要做其他的一些工作啊
呃那首先我们看一下点C文件里边
接下来就是一个TCP server
start
启动一个TCP服务器
那这里边的这个流程大家应该也想到了啊
本质上来讲
就按照我们之前这个手册
里边给我们写好的
这个过程额
那大家其实发现了
在这个建立TCP服务器之前
告诉我们了
必须通过这样一个命令使能多连接
因为服务器必须要由多个客户端同时访问
支持这种功能才行
所以我们首先还要做这样的一个配置啊
当然了啊
这个配置先后顺序无所谓啊
我们可以放在前面
也可以放在后面
这是没问题的啊
按照这个流程的话
我们就先先把这个先写一下吧
第一步那就是设置使能多连接
这是第一步操作
然后第二步操作
第二步操作
其实就是下边我们直接调这里的c
IP server
让它等于一表示把这个服务
器打开就可以了
所以这个其实是非常简单的一个命令啊
直接发送这个命令打开就完事
我们就是开启TCP服务器啊
那当然了
第三步呢我们还准备再进行
一些其他的配置诶
大家发现了
后面如果我们想要正常的做那个
数据收发的话
是不是还涉及到那个数据格式的解析啊
接收到什么类型的数据
我们还得清楚
那后边我们准备是让他的那个
接收的数据啊
里边包含的信息应该是最多的那种场景啊
那大家看到了
在这个AT消息里边加IPD
这就是我们接收到的数据格式
我们希望它包含的信息最多
那是不是应该CIPMUX
这是多连接
把它置一
这个没问题
我们前面已经配了
然后呢是c IP r e CV
receive mode
接收模式至零志零
意思就是说这是一个主动模式
它只要有数据主动就给我们上报
就不用我们再去发命令去读它了
哎所以这个就会简单很多啊
我们就用这种模式把它置零
当然了
这步其实不用做也行
因为前面我们说是不是默认它就是零啊
所以你看他的那个示例流程里边
是不是根本没有在用接收数据的那个命令
再去读取啊
直接读就完了
哎那所以这个我们也不设了啊
默认它就是零吧
那下面这个
如果我们想看到这个IP和端
口号信息的话
是不是得把这个质疑啊
c IP d info把这个置一
接下来我们就可以拿到两个新的信息了
IP和端口号
因为大家看到前面我们这个案例里边啊
他拿到的数据格式
是不是他就没有那个IP和端口号
哎这就是默认情况下啊
这一位是零啊
前面我们的那个接收模式也是零
是主动模式
但是这个信息呢不显示IP和端口号
所以我现在就把那个d
info把那个命令啊
让它等于一做一个配置就可以了
所以接下来我们应该还有一步操作
那就是设置IP地数据格式
我们相当于是一个显示详情啊
显示详情的一个数据格式啊
那这样的话我们还是把上面
这个流程抄一下
还是发送命令吧啊
那首先我们这个是设置使能多连接
然后当前我们要给的这个命令
看一下这边的流程
前面那个命令当然就是CIPMUX
等于一直接抄下就可以了
就是这条命令把它直接放在这里
然后把它发出去
哎非常简单
大家看到其实就是这样的一条一条命令
你知道发什么
然后把参数写好
写对就照着这个流程啊
然后调一次send
common就可以了啊
那同样下面还是这是开启TCP服务器
开启TCP服务器
前面这个类型就不需要再去声明了
那这里边的这个定义呢是c IP
server等于一
这里大家需要注意啊
后面我们其实是可以加上
其他的一些参数的
因为大家会发现这里边没有
指明我们的端口号
所以它其实是用的默认端口号333
如果我们想要指明端口号的话
是不是后面可以加逗号跟第二个参数啊
哎那所以我们在这可以直接逗号
比方说我们的端口号还是8080
那跟在后面就可以了
这个参数个数和类型大家都要注意啊
一定要跟他的手册里边
每一条命令要求的完全一样
完全一致
才可以有一些命令
它后面那个参数呢是可变的啊
就是有一些可以有可以没有
那些参数可以省略
但是如果是必须要有的那些参数
就是它没有放在中括号里面的那些参数
我们必须给出来
这里的话这个是可有可无的啊
没有的话就是333
接下来还有最后一句
那就是设置IP地的数据格式
我们把这个还是复制在这里
接下来下边的这一条指令啊
那这条指令我们在这个案例里面
他并没有给我们写出来啊
那我们去搜一下吧
大家还记得后面那一部分是不是就
叫做d info啊
就是这样的几个字符
所以其实就是c IP d info呃
大家知道这里的这个IP
其实不是单独是IP地址的意思啊
其实就是IP地合在一起
IP地指的就是我们所
说的incoming
packet的data
就是接收进来的打包好的数据
这个意思
问号的话是
查询当前它到底设置的是什么样的啊
是true or false啊
两种类型
那如果要是设置的话
等于后面当然就是要不等于零
要不等于一
大家可以看到
如果等于零
这是默认情况啊
在加IP地消息里边就不提示
对端IP和端口信息
那如果把它置为一
就会提示对端的IP和端口信息
我们要的是不是就把它制成一啊
就这样一个命令啊
所以直接把这个做一个复制
把它放在这里就可以了啊
那后边这部分删掉c
IP d info
等于一设置好就可以了
所以这个TCP服务器的启动本身
还是很简单的啊
核心就是这样一条命令
那我们这儿是又增加了两个设置啊
啊这两个设置一个是必须的
你设置服务器的时候必须要使能多连接
另外一个主要是考虑到
我们后面接收数据的时候
想让它显示详情
把这个建立好了之后
那剩下的当然就是收发数据了
首先我们定义的这个函数是发送数据
发送数据的话
这个其实是比较简单的
大家还记得我们在这边处理的流程里边
发送数据是不是直接就发了一个
特殊的命令
叫做加c IP send
当然了
这个说是比较特殊
本身这个命令没什么特殊的啊
他就传两个参数就完了
前面这个是一个当前的连接id
相当于是socket编号
后面这个呢呃就是当前要发送的数据长度
比较奇怪的是
你是要发送数据是散的
但是数据没有跟在后边
其实大家也大概能够想到啊
因为你要发送数据的话
有可能数据很长
那这个你通过串口一直发送
那我那边接收是不是要
很大的一个缓冲区啊
然后他那边你不发送完
他还没办法做这个数据解析
那就相当于一直卡在那了吗
而你后边如果进入数据接收模式的话
进入数据模式去传送数据的话
他是不是可以传一个就读一个
传一个就读一个呀
那我们的代码就不会卡死在这儿啊
哎整个这个效率就会更高
所以我们不会把这个数据
作为命令的一部分
直接传过去
而是先发送当前的连接id和数据长度
进入数据模式
所以大家需要注意
接下来我们其实分成了两步走
首先第一步是要发送CIIP3的命令
先设置好我们当前的数据长度
然后呢进入数据模式
第二步才是直接发送我们所有的数据
那发送数据这个怎么发呢
很简单
不用管别的
这就不用调用命令了
现在是数据模式吗
那是不是就是直接通过串口发什么
他就收什么呀
就是这样的一种方式啊
大家想到前面我们不是介绍了
有一个概念叫做透传模式吗
尽管现在并不是直接进透传模式
不是说后面就不发命令了
就全是数据了
但是暂时我进入到数据模式
大家是不是可以认为它就是一个透
传的状态呀
哎就相当于他是个小透明啊
直接给到串口的数据直接发送
出去就可以了啊
那所以这里我们整体就分成了两步走
第一步那就是需要去设置连接id
就是发送数据的连接id和数据长度
那第二步就是进入数据模式
那么我们就直接通过串口发送数据
整体来讲就是这样的两步
前面这一步我们是要发送命令的
C i p send
那后边的这一步操作呢
那就不用发命令了
直接串口做一个transmit就
可以了啊
那所以首先我们先看一下前面这个命令啊
还是把上面这个直接复制一下
啊那首先我们这里就是要
给对方发送数据了
我直接说一下
给对方发送数据
当前的这个命令呢
那就是c IP send啊
当然大家如果怕写错的话
你也可以直接做一个复制啊
我们可以跟这边做一个比对
就是AT加c IP send
然后后面等于零逗号四
那这里大家需要注意一下
我这儿的这个零和四是直接写死的吗
哎大家发现这不是写死的啊
我们现在这调的是一个函数
每一次发送是不是都有可能
有不同的连接id
和不同的数据长度啊
当然数据也是不同的啊
这不是写死的
所以现在我们的这个命令呢
那你也不能直接写死
那现在我们这个命令
是不是相当于就得做一个拼接呀
诶那所以我们在这儿啊做一个命令拼接吧
我们定义一个UINT8杠T
我们把这个就叫做三的CMD
定义一个数组
这个长度的话应该不会太长
我给个30
或者大家不放心的话
给个50肯定够了啊
啊那初始直接都定义成零
然后接下来我们其实就是要
把它来做一个拼接
那就不要再定义这个CMD了
大家应该能够想到
是不是接下来要做这个字符串拼接的话
我可以直接调s print f呀
按照我们的格式直接把它做一个拼接啊
当然了
最后拼接起来的这个格式放在send
command里面
我们可以把它强转成叉芯
这是我们最后要拼接的内容放在这里吧
具体要拼接的格式
是不是就靠后边的这一个格式化的字符串
来做一个表示啊
哎那当然了
这个字符串里边我们看到了
前边就是AT加c IP send
这都是没问题的
那后边是不是应该是两个参数呀
分别都是整数类型
所以就是百分号D逗号
百分号D后边杠R杠N完全一样
那这里边传进来的是不是
就应该是id和lance
id l e n放在这就可以了
那同样如果是把它改成叫这个名的话
那下面我们就同样得把它叫做send
command
你如果不改的话
上面那个我们可以把这个临时的数组啊
直接定义成CMD也是可以的
主要是为了跟上面做一个区分
这是做了一个拼接
然后发送这样一个命令啊
好吧
把它发出去之后
那接下来我们就进入到数据模式了
通过串口发送数据
那就help your art
transmit
我们现在指定的串口当然是UART2了
Hu at2
那对应的数据数据当然就是data了
也是参数里边直接指定好的
还有size size
当然就是长度了
L e n length
最后还有一个超时时间
随便给一个1100秒钟的超时时间
这样就完成了
所以这个发送的流程还是比较简单的啊
但是关键点就在于我们得考虑清楚
他是先发一条命令进入到数据模式
然后接下来呢再通过串口进行
真正数据的发送
好发送已经完成了
最后就是接收
接收的话
大家感觉好像是最复杂的一部分内容了啊
因为它需要去解析我们当前的数据格式
那首先我们应该能想到
我得知道当前的数据长什么样
然后才能做相应的一个接收
我们看一下那个数据到底长什么样呢
还是找到at消息
这里加IPD
我们直接就找到最后这一行吧
我们现在的配置不就是这个样子吗
多连接CIPMUX等于一
然后默认是主动接收模式
IP receive
mode等于零啊
我们不需要去发送任何的命令
不需要发接收命令
自动它有数据就会通过串口给我们发过来
所以大家应该能想到啊
我们在这个代码里边上来之后
是不是就不发命令了呀
是不是直接比方说我们设置一个缓冲区
然后给它读取这个当前串口的内容
直接做一个接收就可以了呀
直接从串口接收数据啊
那当然本来我们这个参数里面传进来的
就是有一个接收缓冲区的
但是呢我不想用这个
因为大家知道这个
我最后是不是要把接收到的真正的数据
做一个返回啊
而我们现在接收到的是什么呢
是不是应该是这个加IPD开头的
这一长串东西
前面还有一堆参数啊
唉所以我是希望后面还要做一个截取的
但是这有两种做法啊
一种就是你就先把它完整的都放在
我们的这个技术缓冲区这里的这个rx
Buff里面
然后后边是不是我截完了之后
再做一个调整啊
那还有一种方法是
我是不是可以在临时定义
一个临时的缓冲区
先把数据拿过来先截
截
完了之后复制到我们真正的这个
数据缓冲区里面啊
这也是一种操作啊
啊这是我们能够想到的第一步
然后第二步的话是不是就要解析数据了
从接收到的IP地数据中解析数据
那当然了
解析数据的话对应它的长度
Id
IP还有端口号
这些参数其实就都能填进去了
而最后的数据呢我们可能再做一个截取
塞到rx Buff里面就完成了
这就是我们想要做的操作啊啊
所以要说简单
这个好像也简单啊啊他就
不需要发任何的指令
从窗口做一个接收
接收到之后再做一个字符串
解析就完了好呃
那至于怎么解析
我们当然是得对着这个模板来啊
我们先把这个拿到
到时候我们的数据就长这个样子
接下来我们可以看一下
首先是从串口接收数据
我们用一个比较简单的实现吧
就是还是定义一个临时缓冲区
先把它缓存下来
然后我们经过处理之后
最后复制到这个rx Buff里面
就可以了啊
所以这里边我们用一个用临时缓冲区
接收IPD数据啊
那所以这里边我们就直接在这定义好吧
UINT8杠t tep Buff
当然了
我现在是直接把它定义成了一个局部变量
局部变量的话
大家可能会想到就是我当前的这个数据啊
那最好你就不能太大
大家知道本身我们定义那个数据的时候
之前在里边大家还记得吧
我们当时如果做那个发送和接收的话
他是不是都有要求啊
发送的时候是4096嘛
有这样一个限制
那接收的时候
其实对应的也是有相应的一个限制的
因为你的缓冲区总是有大小限制的嘛
但是它的那个大小我们在这
一个临时变量里边
能直接给个像
比方说4096吗
不用说4096
2048也不行
大家知道临时变量跟全局变量可不一样
全局变量是可以直接统一
做那个内存分配的
而局部变量的话是不是要
占用我们的占空间啊
我们当前的占用空间是多大
大家忘记的话
可以看一下我们的启动文件
这个点S汇编文件里边
一开始除了后边大家知道有
那个中断向量表啊
一开始是不是就有相对应的
占大小和堆大小的一个定义啊
大家看到占大小定义
初始值给的是0X4004
零零的意思就是这个四不就是0100吗
这是16进制数后面的一个
零就是四个零吧
那所以总共是一
后面十个零就是二的十次方
正好1K1024
所以我们现在的占空间就只有1024
那大家应该就能想到
我这是不是最大就1024啊
你超出去的话
那肯定就有问题了啊
这代码有可能就直接会出现异常
还有可能直接卡死就跑不起来了
所以大家注意一下
就是这里我们最大
它的这个数组大小只能给1024
而且这个前提是
后边我不再去定义其他的临时变量了
你如果定义了其他的临时变量
大家知道你如果还是在占空间去定义的话
是不是就会有问题啊
呃那所以我们在这可以这么定义啊
那如果大家觉得这个有点麻烦的话
你也可以直接把它扔到扔到外边去
定义一个全局变量
是不是就不会有这个问题啊
啊那比方说我们直接放到外边
定义一个全局变量啊
用一个临时的缓冲区来接收IP地数据
那如果要是全局变量的话
那另外是不是还应该有一个
它的数据长度呀
这个就叫temp l e n
直接把它先定义好
接下来从串口接收数据
直接调函数
U art receive
大家知道是不是还得receive
to idol啊
因为它是变长数据嘛
我并不知道当前你传过来的数据是多少
所以我们依然是receive
to idol
而且不开中断
我就是直接用轮询的方式
一直等等到他当前的这个数据接收完为止
把他所有的数据都读出来啊
那所以如果说我们用了这样的一种
方式的话啊
那里边传的参数
当然就是还是读HUR的二串口二
然后接下来啊那就是tap Buff
这是我们的接收缓冲区
另外大家注意后边给的这个参数啊
这里给的这个参数其实应该是它的总长度
或者说超出这个长度
它自动就返回了
就不再等我们的那个爱豆空闲帧了啊
那所以这里边
我们一般给的是它的数组总长度啊
1024给到这
然后后边这才是我们真正意义上的
那个数据的长度啊
其实大家知道这个数据长度没什么用
我们现在这不是真实的数据长度吗
真实的数据长度是不是在信息里边
我们可以提取出来呀
哎所以这个其实没啥用啊
但这里的参数它必须要有这么个东西
所以我还是把它定义出来吧
传的是对应的地址
Templength
最后还有一个超时时间
那我们随便给一个吧
给一个1000
这就是我们从串口接收数据的一个过程
然后接下来那接收到的数据
就要做一个解析了
那大家其实还是能想到
跟之前我们接收响应是一样的啊
现在你想接收这个数据
接收到的一定就是我们真正要的数据吗
那不一定啊
那是不是有可能接收到的
又是他的一个状态改变的上报的消息啊
那所以对于那种上报的消息
我们是要把它过滤掉的
我现在只要以IPD开头的
这样的一个数据信息啊
所以这里边我们应该首先要做一个筛选啊
做一个过滤
首先判断是否已IPD开头啊
当然前面他还有一个加号
再看到他的这个数据格式
本身在我们这里边啊
收到的这个消息它是加IPD开头的哎
那所以我们可以判断
这就是它的起始的这个数据信息啊
我们就判断有没有这样的几个字符
进行一个过滤
那这当然就是一个if判断了
那判断这个有没有这个子串
大家都非常熟悉了
S str
string string做一个
提取不就完了吗
那首先基于的这个字符串
当然就是贪Buff
还是给大家做一个强转吧
差芯儿
那后边我们要查找的字符串
就是加IPD
就这样的一个字符串
判断它有没有这个结果呢
呃如果它不等于now
说明查到了
只要有它
接下来我们就可以进行对应的解析了
如果要没有的话
没有大家知道
那就相当于不是我们想要的数据啊
那就应该跳过
然后继续做下一次的读取
继续读下一个数据啊
那接下来具体的解析呢
大家应该能想到还是是不是用一个
之前我们在做拼接的时候是s
print f
现在的话是不是反其道而行之
反向操作
那就应该是一个s scan f啊
直接从固定格式的字符串里边
做一个信息的提取啊
那所以额我们提取的话
那就得相当于把那个对应的位置啊
都要填写好
但这里边又会有一个比较麻烦的事啊
呃首先别的还好啊
别的都是整数吗
你这里边有一个id
有一个这个长度
还有一个端口号
这都是整数
那后面的data data我们
准备单独截啊
最后再做一个截取就完了
那你前面的这个IP
它是引号引起来的一个字符串
这个字符串的话
你直接截用百分号S去截
能不能截出来啊
那是不行的
2%s是不是要找最后的杠铃
作为结束标志啊
如果直接用百分号S的话
它后面不是杠铃
是不是后面还有一大堆东西啊
他会把后面所有的东西都给我们截出来
那就不对了
所以到时候我用什么方法去做一个处理呢
我们可以利用在这个S干F里边
可以添加的正则表达式来做一个处理
之前应该我们也做过一些简单的应用啊
所以在这我们就又派上用场了啊
那这里边我们所用到的这个正则表达式
其实也比较简单
那就是首先我开头找到一个引号
末尾找到一个引号
接下来呢我要查找的就是两个引号
中间的那一部分
中间的所有字符作为我们的一个字符串
把它截取出来就可以了
这就是截取IP的一个基本的想法
那除此之外另外还有一个点就是钱变
我们接收到这个返回数据的时候呢
它最开始应该是加IPD
但是有时候呢它前面会有回车换行
也就是说前面有可能会有杠R杠N
这个就比较尴尬
我们前面如果你要是规定
它就有一个杠R杠N的话
万一它没有它就不匹配了是吧
哎那或者说他有这个回收换行
我也不知道他有几个呀
哎那所以这个我们又是前面加一个正则
这个正则不是要做一个匹配信息的提取
而是要做一个过滤呃
就是满足什么条件
我就不要它直接把所有的这些都给跳过
直接把它忽略过去
我要后边的那些信息做一个提取
所以接下来我们可以看一下
怎么样写这样的一个正则啊
我们在这里可以利用s scan
f和正则表达式
提取所有的信息
那所以这里边我们的核心
那也就是一个s sky f了
我们先把这个整体的架构先写出来啊
SGF呃
当然还是从time Buff里面
去做提取啊
他还是做一个强喘差芯儿
后边就是关键了
就是我们定义的这个格式
大家应该能想到
首先它就是加IPD嘛
但是我们说了
前面还有可能有杠R杠N
有回车换行
这个怎么办呢
这个我们需要跳过任意的杠R杠N
大家知道我们在SKF或者是s
sky f里边
百分号本身表示的是一个占位符
他要用后边的对应的那个变量做一个填充
或者是提取出来相应的信息
给到变量地址指向的那个变量里面去
哎那所以我们现在不是要做提取
我们现在是不是相当于就是要跳过呀
跳过大家注意就不是百分号了
是百分号芯儿
百分号芯是不是表示他是要跳过了
然后后边呢跟着的是一个正则表达式
这个正则表达式它就表示我要跳过
符合这种条件的所有的字符
我们现在是不是就是杠R杠N都跳过
那当然了
杠R杠N都需要跳过
这就相当于他要组成一个集合
就是这一组
这相当于是我们的黑名单
对不对
列到黑名单里边的都不要诶
所以这个黑名单是不是直接用一个中括号
括起来就可以了呀
杠R杠N中括号括起来的
就是表示在这个里边的所有字符
都可以直接匹配上
我们现在如果用后面这一个正则
来做一个匹配的话
是不是匹配到的就是
任意的杠R任意的杠N啊
前面呢又是百分号芯儿
表示不要
它表示跳过
最后的效果就是跳过了前面
所有的杠二杠二啊
所以这个语法比较特殊啊
大家知道就可以了
然后后边当然就是逐字匹配了
加IPD
然后逗号这也是逐字匹配
一点都不能变
在后边是一个连接id
连接id的话
我们知道它其实就是一个整数嘛
哎那我们可以在这直接写一个百分号D
但大家注意啊
这个其实是有问题的
因为如果是百分号D的话
我们后边要匹配的应该是什么呢
本来是不是应该要匹配的就是那个id啊
然后要他那个变量的地址
我们这个本来就是一个指针嘛
所以这个就不用取地址了啊
直接把id放在这就完了
大家看一下这个id对应的指针
类型是什么
是UINT8杠T芯
而我们这里如果百分号D的话
它对应的是不是应该是一个32位的数啊
所以它本质上来讲啊
提取出来之后
他是要塞在一个32位整数
这样四个字节的存储空间里面
当然我们现在都是所谓的这个小端存储嘛
本质上来讲
你如果要是不够四个字节的话
存一个字节
相当于存放的还是我们最低位的那个字节
那是从数据上来看是没问题的
但是你多占了几个字节
是不是就有可能会影响到别的变量呀
有可能会影响到其他的东西
对不对
所以这里啊类型最好是要严格匹配
不要直接就统一都用百分号D
那所以我们现在对这个unit8杠T啊
要做一个设置
unit8杠T它类似什么呢
UNIN发动机是不是就类似
于叉呀一个字节嘛
那CHA对应的那个占位符应该是什么
大家知道如果你要是一个哨子的话
两个字节是哨子的话
是不是对应的是一个H啊
HD对不对
哎那如果要是差的话
它相当于是比short还要short
所以它对应的是HHD
这就是所谓的差对应的一个占位符
当然我们现在呢还不光是差
因为差本身是有符号的嘛
我们现在是u INT8杠T
是不是相当于是on sin的差呀
无符号的一个字节的整数
所以我们这儿应该是HHU
U是不是表示无符号数啊
标准的一个匹配应该这样去匹配啊
HHU这个就对了啊
然后再往后再往后注意还得是逗号
而且不能有任何的空格
必须逐字匹配
逗号后边的话又是一个length
这个length的话
我们定义的它本身是
一个UINT16杠体芯
它是一个两个字节的
那我们现在的占位符
是不是就应该给一个百分号HU啊
无符号的两个字节的整数
是不是类似于unsigned
short
对应的占位符就是百分号HU哎
所以id后边当然跟着的就是rx
length
这个也不需要加取地址
因为我们前面也是一个指针
再往后看又是一个逗号
关键的就来了
接下来是一个字符串
截取一个IP
那这个字符串我们现在是不是
前面有一个引号
后面有一个引号啊
所以先得把这个引号要打出来
那大家知道当前本来这就是一个字符串吗
你这个引号是不是还要做一个转移啊
先把这个两个引号全部做一个转移
然后在这两个引号之间的所有内容
是不是要做一个匹配啊
百分号做一个匹配
这个时候注意跟前面相比啊
都是正则
但是不要加这个芯儿了
加芯儿表示是跳过
我们现在是就按照这个来匹配
那我匹配的其实就是任意字符
唯一有一个限制
是不是就是不能是引号呀
换句话说就是我从前一个引
号开始往后匹配
匹配到哪里为止呢
我一开始是逐个字符匹配的
他并不知道后面还有一个引号对吧
所以我在这儿做这个匹配的时候呢
就是任意字符除了引号
那遇到后面那个引号
是不是相当于我就结束匹配啊
然后后面再跟上对应的这个引号就可以了
他是这样一个意思啊
好所以前面这个任意字符不是引号的
任意字符怎么写
那大家其实知道肯定还是跟引号有关吧
先把引号打出来
加一个反斜杠做一个转移
这是引号的表示
那不是引号
就在前面加一个小上间号
就像我们那个异或的表达式一样啊
按位异或那个表达式
所以这个语法意思就是说就是不要引号
相当于也是一个取反的意思
大家注意啊
这个跟前面的表达不一样是吧
前面这个表达是什么呢
中括号里边是我们正则匹配上
我是要匹配到杠R杠N
然后前面我是说了
你匹配上了
我把它跳过
我不要杠R杠N
而我们现在呢我们现在是说中
括号里边是正则
需要匹配的东西
匹配什么呢
匹配不是引号的
那不是引号的
所有字符都可以匹配
就是这个意思
所以就是把所有的字符都提取出来
然后百分号这是一个占位符
真正要把它提取出来
提取出来
是不是就给到我们后边的那个字符串
啊啊那后面的字符串当然就是IP了
给到这儿就可以了
好那把这一个提取完了之后
后边又一个引号
这样的话我们前面这一段就完成了
最后又是逗号
后边是不是还有一个remote
port
我们的端口号呀
端口号的话就跟前面一样了
端口号不也是一个16位的整数吗
也是一个无符号
16位整数啊
所以也是百分号HU放在这啊
那再往后的话
是不是就相当于把对应的port
填写在这里就可以了
port本身也是一个指针啊
那不用再转地址了
这就是我们想要的这个所有的东西
这写起来稍微的有一点绕啊
我们可以换行
大家看得清楚一点
这里边的最关键最麻烦的点可能就是前面
这里边我们跳过了所有的杠R杠N
然后后边呢又使用这种方式
匹配了两个引号之间的所有字符
用到了一些正则表达式里面的知识啊
这个大家可以下来之后
自己稍微的看一看正则表达式
如果不是特别了解的话
那就记住啊
当前我们可以这么用就可以了
本质上就是一个字符串的处理啊
你不用这种方式
用别的方式挨个字符啊
一个一个去找
一个一个去匹配
也是可以的
主要是因为我们这里的那个IP地址
它还不是固定的位数
大家应该能想到
如果要是固定位数的话
就简单了
我是不是就按照当前那个字节的个数
挨个去截就可以了呀
哎但是你现在如果不是固定位数的话
就比较麻烦一点
我们比较简单的方法就是这样的一种方式
好把它都已经提取出来了
大家发现了
最后是不是还有一个data呀
data还没截完呢啊
就真正意义上来讲
我们在这一个port后边
还要加一个冒号
冒号后边才是我们真正的data
当然了
你可以把它一起都放在前面这个s
can f里面
但是感觉这个有点前面太长了啊
我单独做一个截取吧
这个data就比较简单了啊
我们将真正的数据复制到传
入的缓冲区参数中
哎这是我们接下来要做的一个事情啊
哎那所以首先我们要判断一下
后边那个数据是从哪开始的
那大家其实知道这个也很简单
是不是我可以直接找那个冒号的位置啊
这里边它只有一个冒号
前面没有任何地方有冒号嘛
所以我就找冒号的位置
找到冒号的位置
指针再加一
后边是不是就是我作为字符串的
这个数据的起始位置啊
所以这个其实非常简单啊
那就直接还是用STR
s t r string
string去查找一下冒号吧
同样还是查找差希儿
当前的time Buff
现在我们查找的就是冒号
查找到的这个位置
我们还是单独的保存一下吧
就定义成叉芯吧
叫做p data哦
那大家其实知道查找到这个位置
其实不是data
起始位置
是冒号的起始位置
我们现在是不是还要再加一啊
再加一就完了
接下来把它做一个复制
那就是memory copy m
e MCP y复制过来就可以了啊
那复制的目标是rx Buff
复制的源头是p data长度长度
那当然就是前面我们提取出来的rx
length了
注意本身它是一个指针
所以这里边我们还要取它的内容
对应的长度
把后面所有的数据复制过来
就完成了我们当前接收数据的这样
一个流程好
所以大家可以看到这个过程
看起来有一点麻烦啊
其实本身的这个步骤还是很少的
主要就是做一个字符串的处理
做一个提取就可以了
这就是TCP server的启动
以及数据的收发代码
我们就都写完了
